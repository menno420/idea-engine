# idea-engine — ownership

> **Status:** `binding`
>
> Generated by substrate-kit. Which area / service / pipeline owns each
> table, event, and write path. **NOT SOURCE OF TRUTH** for code — source
> files always win.

> **Steady state:** this doc's table is **generated** from store / manifest
> specs where those exist — a projection, not hand-prose. This skeleton is
> the interim hand-maintained form until that projection lands.

## Ownership model

Sole-writer per file. control/status.md: coordinator only. control/inbox.md: fleet manager only. control/outbox.md: append-only, this repo's sessions. ideas/<section>/: partitioned by claim - parallel workers claim disjoint sections so they never collide.

## Ownership table

| Area | Owner (module / service) | Writes it owns | Notes |
|---|---|---|---|
| (one row per owned area) | | | |

## New areas

A new section appears only when a new active lane row appears in superbot docs/eap/fleet-manifest.md; the wake that spots the row creates the section with its README stub. Sections are never invented ad hoc.
