# Merge-automation verification probe

> **Status:** `historical`

This file was added on 2026-07-15 as a merge-automation verification probe for a
merge-on-green CI audit session. It is a plain content change (no workflow files
touched) intended to confirm that ordinary code/doc PRs in this repo land on
green CI with zero human click, via the existing `auto-merge-enabler` workflow.

Safe to delete once the audit that added it is closed out.
