# ideas/substrate-kit

> **Target lane:** `menno420/substrate-kit` (manifest-derived section — see README.md § Sections).

Ideas FOR this lane: files per the idea grammar, harvested lane-born ideas indexed by
link below. Claim this section (`control/claims/`) before working it.

## Index

- [`guard-fires-dedupe-regime-cliff-2026-07-16.md`](guard-fires-dedupe-regime-cliff-2026-07-16.md) — sim-ready · process · drafted+probed 2026-07-16 (the ORDER 003 FLEET-BACKLOGS rotation slot, round 17 OPENER — PROPOSAL 081; the slot's first deliberate RE-TAP: no untapped fleet source remains, substrate-kit stalest by tap history, harvested firsthand from the locally vendored dist @ kit v1.17.0): a pre-registered exact replay of the guard-fires tail-scan dedupe (`GUARD_FIRES_DEDUPE_WINDOW_S = 600`, `_GUARD_FIRES_DEDUPE_SCAN = 200`, the verdict-row exemption) pricing the source's own "a burst larger than this simply dedupes less" comfort sentence — the scan bound is a CLIFF (leak exactly max(0, F−200)/run, leaked identity rotating period F/gcd(F, F−200)), exempt verdict rows COMPOUND (registered orbits to 13× the one-for-one intuition; the drafter's own first law falsified live), the record rate is NON-MONOTONE in check cadence (p(c) = c·(⌊600/c⌋+1): a 601 s checker writes exactly 1200/601 ≈ 2× a 600 s one), and the bounded scan bounds parses while whole-file reads grow quadratically; REJECT-first bands, deterministic decision legs (joint pass probability 1), three repair arms priced (raise-S relocation / keyed last-append index / drop-the-cap), seeds 20261718–721 (aux never read) — sim-ready · process · drafted+probed 2026-07-13 (the ORDER 003 FLEET-BACKLOGS rotation slot, round 4 — PROPOSAL 029; the direct successor to round 3's own verdict: V027 REJECTED silence-keyed expiry and routed the lease-renewal `renewed:` slice while its LIMITS line left the mechanism undesigned): a pre-registered dual-arm renewal-race sim pricing the routed mechanism's three unpinned constants — renewal cadence p_w ∈ {2,12,24} h × compliance p_f ∈ {0.02,0.10,0.25} (the forget axis NO prior grid carries) × expiry horizon θ_r ∈ {6..72} h — on V027's inherited bands (T ≤ 0.05, O95 ≤ 120 h) with worst-case-per-metric contender tempos from V027's own grid; chained anchors: the silence-baseline leg must reproduce V027's committed Feas map exactly, the C48 identity restated by citation (contender-side latency — no signal design fixes it); the V027-named zero-tooling git-log probe RUN at drafting on this repo (11 claim lifetimes, 19 heartbeat gaps, multisets quoted into the fixture); pre-registered REJECT-first (descope the slice before build spend) / APPROVE (ship with pinned row θ_r† + measured compliance floor) / NULL (conditional per-cadence rule; warn-first deployment makes p_f measurable); fully hermetic, Arm A exact on all 45 decision points, seeds 20260756–59
- [`claim-expiry-horizon-lane-death-2026-07-13.md`](claim-expiry-horizon-lane-death-2026-07-13.md) — sim-ready · process · drafted+probed 2026-07-13 (the ORDER 004 rule-3 FLEET-BACKLOGS rotation slot, round 3 — PROPOSAL 025): a pre-registered dual-arm silence-expiry race sim pricing the kit's two planted claim-expiry constants (`check_claims.py:95` `CLAIM_STALE_HOURS = 24`, `:102` `WORK_CLAIM_STALE_HOURS = 72` @ kit HEAD `917261b3`, whose own comment reads "Seeded at 72h; revisable by data like every horizon constant" — no data exists) against both REALIZED failure modes: twin execution (kit PRs #50/#51) vs dead-lane deadlock (the doctrine's own "a dead lane must never deadlock an order"); measures false-takeover rate T vs p95 orphan latency O95 across 3 gap-tail regimes × 3 contender check rates, θ swept {6,12,24,48,72,168} h with both planted constants in-grid; pre-registered APPROVE (pin one fleet-wide horizon) / REJECT (silence is the wrong signal — route the lease-renewal `renewed:` stamp slice) / NULL (per-regime conditional rule + the zero-tooling git-log probe locating every real lane's cell); fully hermetic, Arm A exact closed forms on the whole decision surface, seeds 20260744–47
- [`kit-line-self-drift-local-check-2026-07-10.md`](kit-line-self-drift-local-check-2026-07-10.md) — parked(routed) · product: zero-network `check --strict` finding comparing a repo's OWN three version artifacts (vendored dist header · config pin · heartbeat `kit:` line) — probed 2026-07-11: the class re-verified LIVE at v1.9.0 (fresh 05:33:10Z fleet scan @ kit HEAD `7122aca`: the same 5-repo self-report-lag class regenerated by today's release wave, confirmed at live HEADs 06:25Z; NO local check in v1.8.0 or v1.9.0 `cmd_check` — `_check_one_status` never parses the `kit:` version), routed to the kit lane's own `cmd_check` seam as a near-composition of already-vendored pieces (`parse_kit_line` + `KIT_VERSION` + config pin + `heartbeat_files` loop; `RepoCurrency.drifts()` is already the pure comparison), advisory-first because consumer #0 is born-red (the kit's own config pin 1.0.0 vs dist 1.9.0), plus `upgrade` printing the paste-ready line via the existing call-site-less `kit_line_example()`; no sim-lab proposal (no simulator question — a contract check is proven by its own red/green)
- [`parallel-session-heartbeat-reconcile-2026-07-10.md`](parallel-session-heartbeat-reconcile-2026-07-10.md) — parked(routed) · process · probed 2026-07-11 (batched with the behind-stall sibling below, two full batteries, one verify-first sweep at live kit HEAD `be72c09`): the kit-planted control/README template carries NO same-lane reconcile guidance — its collision model is prevention only (one-writer-per-file, per-lane heartbeats, claims) — so the routed build is a "parallel sessions, same lane" template section + two mechanical checker advisories (non-monotonic `updated:` stamp · reconcile-dropped-facts), with this repo's README § Landing conventions recipe (proven PRs #10–#17, renumber rule PR #52) as the working reference implementation; no sim-lab proposal (no simulator question)
- [`behind-stall-auto-updater-2026-07-10.md`](behind-stall-auto-updater-2026-07-10.md) — parked(routed) · product · probed 2026-07-11 (batched with the reconcile sibling above; scope-sharpened): verified un-adopted at live kit HEAD `be72c09` (no update-branch machinery in any workflow or the enabler generator; ⚑ OWNER-ACTION 11 still open @`5d0f59b`) BUT the capture's mechanism is wrong as written — updates pushed with plain `GITHUB_TOKEN` do not trigger workflow runs, so the naïve update-branch call trades a behind-stall for a checks-never-report stall; the routed build must pick an owner PAT/App token or an explicit gate re-dispatch, and OWNER-ACTION 11's one checkbox stays the cleanest full close (this workflow is the agent-side fallback); no sim-lab proposal (no simulator question)
- [`host-checkers-one-gate-2026-07-10.md`](host-checkers-one-gate-2026-07-10.md) — parked(routed) · product · probed 2026-07-11 (the section's LAST captured head — section 6/6 probed-or-routed): host checkers under the one gate, LAYER-CORRECTED at live kit HEAD `489e763` — no `extra_checks` seam exists (`_extra_check_findings` @ `cli.py:606` is a false-friend internal helper; `Config.from_dict` drops unknown keys, schema closed) and the capture's `cmd_check`-subprocess mechanism collides with the kit's §3.2 no-shell-out check invariant, so the routed build is the gate GENERATOR seam: a host-owned config key (`gate_host_checks`-shaped, empty default byte-stable) rendered as named non-control-lane steps — retiring this repo's five-times-caught PR #18 re-apply treadmill and absorbing the kit's own `adopt-plants-pytest-gate-step` backlog head as one default entry; consolidates with the enabler-guard head's "gate-preflight config seam" item (same landing zone, build once); no sim-lab proposal (no simulator question)
- [`enabler-card-status-guard-upstream-2026-07-11.md`](enabler-card-status-guard-upstream-2026-07-11.md) — parked(routed) · process · probed 2026-07-11 (batched with the needles sibling below, two full batteries, one verify-first sweep at kit HEAD `09837de`): un-adopted upstream (card-less enabler template, no gate config seam, `[Unreleased]` empty) but SCOPE-SHARPENED — v1.10.0's `session-card-hold` (live-fired on this repo's #125) absorbed the arm guard's primary race at the required check, so the routed core is the gate-preflight config seam + badge-grammar single-sourcing (capture correction: `grammar.py` exists at HEAD but `_BADGE_RE` lives in `checks/check_docs.py:40`), the guard demoted to opt-in defense-in-depth; no sim-lab proposal (no simulator question)
- [`carveout-needles-config-2026-07-11.md`](carveout-needles-config-2026-07-11.md) — parked(routed) · process · probed 2026-07-11 (batched with the guard sibling above; manager-ORDER-worthy): host-declared `carveout_needles` in `substrate.config.json` verified UN-ADOPTED at live kit HEAD `09837de` (no v1.11.0 tag, `[Unreleased]` empty, zero needle hits, `gate_carveouts` still step-granular in code at `adopt.py:882` — "edits of kit content are NOT reported" verbatim) — the two-hop-evidenced step-blindness class (`f417a1f` · `b11fe68`) closes for every adopter with one config key + a ~15-line loop riding the existing report/bank machinery; this repo's `--enabler-anchors` advisory is migration #1 and retires on adoption; kit-lane fan-in now THREE routed heads (this pair + kit-line-self-drift) — one ORDER could carry all three; no sim-lab proposal (no simulator question)

## Cross-links

Sibling-section ideas indexed **by link** (README § Idea file grammar — never duplicated;
rule source: PR #17 card):

- [`../superbot/seat-boot-verification-harness-2026-07-10.md`](../superbot/seat-boot-verification-harness-2026-07-10.md) — parked(routed) · probed 2026-07-10: the seat-boot `check_seat.py` harness parses the KIT-OWNED heartbeat grammar, so its probe's Q7 puts the parsing core on this lane (a remote-tree/heartbeat-parse seam — one grammar, one parser, kit-versioned; rhymes with `host-checkers-one-gate`) with only a thin operator wrapper in superbot `scripts/`; routing relayed to the manager via the idea-engine heartbeat notes — no sim-lab proposal (no simulator question)
- [`../superbot/project-capability-self-awareness-2026-07-10.md`](../superbot/project-capability-self-awareness-2026-07-10.md) — sim-ready · probed 2026-07-10 (batched with `session-start-capability-self-probe`, its agent-plane half): the fleet-internal mitigation is a kit-owned `bootstrap.py capabilities --probe` — this lane already plants the ledger it would regenerate (`adopt.py:66` CAPABILITIES.md.tmpl @ kit HEAD `415c37e`) and its `session-start` injection is the natural carrier for the agent-plane checklist (same one-grammar/one-parser logic as the seat-boot routing above); build gated on PROPOSAL 005's sim-lab verdict (is probe-regenerated capability truth honest across seat types → decides regenerate-whole-file vs annotate-per-seat schema)
- [`../websites/open-pr-awareness-at-wake-2026-07-10.md`](../websites/open-pr-awareness-at-wake-2026-07-10.md) — parked(build-direct) · probed 2026-07-10: the wake-time open-work sweep (open PRs + PR-less unmerged `claude/*` branches, three-state classifier) is a websites build-direct doc edit, but its fleet-generic home is the KIT-PLANTED `control/README.md` wake ritual — which already carries the order-claim re-read and the `landing:` line; the open-work sweep is the missing third leg (orders → sections → branches), one template paragraph every kit-adopted lane inherits at upgrade; NOT the `session-start` hook (its evidence sources are deliberately zero-network/zero-subprocess — the sweep is a documented live read, never a hook or a committed ledger); no sim-lab proposal (no simulator question)
- [`../websites/own-heartbeat-parse-self-check-2026-07-11.md`](../websites/own-heartbeat-parse-self-check-2026-07-11.md) — parked(overtaken-by-events) · probed 2026-07-11: websites shipped its own lane-local slice (PR #79 `0f2cd17`, the repo's real `control/status.md` through its `/fleet` parsers at PR time), but the fleet-generic residue is THIS lane's seam — a kit-owned heartbeat-grammar self-check finding in the existing per-repo `check` (which already reads the repo's own heartbeat for staleness + the `kit:` line), plus an extension-key declaration story in the planted `control/README.md` grammar BEFORE strict enforcement is honest: the probe measured undeclared lane keys (idea-engine's `mode:`/`BACKPRESSURE:`) folding into `phase` as continuations through websites' hand-kept `KNOWN_KEYS` parser copy — the same one-grammar/one-parser logic as the seat-boot routing above, and the heartbeat-side sibling of `kit-line-self-drift-local-check`; no sim-lab proposal (no simulator question — a contract check is proven by its own red/green)
- [`../fleet/branch-prefix-drift-tripwire-2026-07-11.md`](../fleet/branch-prefix-drift-tripwire-2026-07-11.md) — historical(#62) · probed + built 2026-07-11 (`scripts/preflight.py --branch-prefix-drift`): report-only advisory that fnmatches the branch names a repo ACTUALLY merges (local `git log --merges` PR-merge subjects) against `substrate.config.json::automerge.branch_patterns` and reports recurring unmatched prefixes — the silent-disarm class the kit's own `claude/*` default shipped (PR #54/#55: zero real branches matched, the staged enabler was a no-op until a hand survey fixed it); the fleet-generic home is THIS lane's seam — the kit plants both the enabler and the config key, so the drift check belongs in the kit's own per-repo `check` (same config-vs-reality family as the kit-line and heartbeat-grammar checks above), with idea-engine's preflight entry as the working reference implementation; no sim-lab proposal (no simulator question — a deterministic string comparison is proven by running it; replay proof in the idea's Q3)
- [`../fleet/stop-hook-telemetry-loop-exemption-2026-07-11.md`](../fleet/stop-hook-telemetry-loop-exemption-2026-07-11.md) — park(built-here) · built 2026-07-11 (`scripts/patch-stop-hook-git-check.sh`, SessionStart-wired): the kit's own hooks rewrite tracked state anchors at every turn/session end (guard-fires on any warning, the sessionstart anchor in `state.json`, `reflect --mine` in `reflections.json`), so any harness/CI that asserts clean-at-rest fights the kit forever — this repo measured 12+ one-line telemetry PRs (#58..#100) before exempting the anchors from the CCR harness stop hook's dirty nag; the fleet-generic home is THIS lane's seam: publish the state anchors as a machine-readable rewritten-at-rest list (config or `check` output) that host tooling and harnesses can honor natively, instead of every adopter hand-listing `.substrate/` paths; no sim-lab proposal (no simulator question — the loop and its fix are both directly observable)
- [`../websites/review-queue-row-auto-check-2026-07-11.md`](../websites/review-queue-row-auto-check-2026-07-11.md) — parked(build-direct) · probed 2026-07-11: the owed-row detector (filtered `git diff --numstat` vs the fleet review-queue's binding 50-line rule) is a websites build-direct advisory step, but the fleet-generic floor is THIS lane's seam — every merge-anyway lane has the identical memory-only enforcement gap (the documented 116-merged-PRs/zero-rows failure state; idea-engine's own PR #47 measurably crosses a naive threshold at 205 script lines), and the honest carrier is a kit-planted ritual paragraph / advisory `check` finding whose threshold + exclusion list are read from ONE pinned source (fleet-manager `docs/review-queue.md` @ its SHA), never N hand copies — the same one-source/no-copy-drift family as the kit-line and heartbeat-grammar checks above; no sim-lab proposal (no simulator question — a diff-count is proven by running it)
- [`../superbot-mineverse/heartbeat-ladder-field-2026-07-11.md`](../superbot-mineverse/heartbeat-ladder-field-2026-07-11.md) — parked(routed) · probed 2026-07-11: a machine-parseable `ladder:`/`progress:` heartbeat field for staged lanes — the DECLARATION is THIS lane's seam (no such token in `src/engine/grammar.py` @ `941be2e`; the fold-in-over-declare rule (idea-engine PR #59) bars lanes from writing the key undeclared, and consumers already disagree on unknown keys: fleet-manager's `gen_roster.py` ignores them, websites' parser folds them into `phase` — the exact extension-key declaration story the own-heartbeat entry above names), with fleet-manager's now-machine-generated roster (gen #5, `scripts/gen_roster.py`) as the motivating first consumer (gen-over-gen stage diff, the roster's self-acknowledged "NOT auto-derived" half); vocabulary design open — one lane runs a rung ladder, others run bands/rounds; no sim-lab proposal (no simulator question — a grammar field is proven by its parser test)
