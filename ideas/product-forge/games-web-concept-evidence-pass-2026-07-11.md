# The owner's deferred evidence pass on the games-web concept — an unrouted head

> **State:** sim-ready
> **Class:** product · **Target:** `menno420/product-forge`
> **Grounding:** https://raw.githubusercontent.com/menno420/product-forge/0a6efe96ef8021679b9f8a6ee63a617cd9d61ffc/control/inbox.md@0a6efe9 · fetched 2026-07-11T03:31:46Z
> **Sequence:** after the games-web↔mineverse scope-seam probe — its finding shapes the ONE simulator question

## Problem

product-forge ORDER 001's `why:` line records an owner decision that currently lives
nowhere else: *"the full concept heads to sim-lab for an evidence pass later ('could
be simulated later' — owner), so phase 1 stays mock-data-first and cheap to
redirect."* The deferral is deliberate and correct — but as ORDER prose it is an
orphan-risk head: no idea file, no proposal, nothing that ever comes due. This repo's
mandate is that ideas end evidence-checked, parked, or rejected — never orphaned —
and routing sim-ready questions to sim-lab is exactly its outbox surface (Q-0264).

## Idea

Hold the deferral here as a probeable head. When probed, shape the **ONE** simulator
question for the full games-web concept — a Shakes & Fidget-style comic browser-RPG
presentation over superbot's existing games — and route a PROPOSAL via
`control/outbox.md`. The scope-seam capture sharpens what that question is: with
superbot-mineverse already shipping direct utility views over the same miner state,
the concept's genuinely open question is the **value of the presentation layer
itself** (does the comic-RPG skin earn a second frontend over data players can
already see?) — which is precisely the redirect decision phase 1's mock-first
cheapness was bought to protect. The probe must also check the trivial exit: if the
seam probe ends in fold/specialize, the evidence pass may collapse into that ruling
and this head parks with it.

**Why now (and honestly, why not sooner):** not expiring — this head can wait behind
expiring ones, and it is sequenced after the seam probe by design. It is captured now
because the redirect window is what decays: the evidence pass is worth most *before*
phase-2 investment makes redirecting expensive, and the batched read-only-API ORDER
(which unblocks phase 2) is heading to the manager's routing table.

## Probe report (v0, 2026-07-11 — batched with `games-web-live-preview-review-surface-2026-07-11.md`, one shared verify-first sweep)

> **Grounding:** https://raw.githubusercontent.com/menno420/product-forge/43563dccc874c58946576444fbba38600bb45f86/control/inbox.md@43563dc · fetched 2026-07-11T09:10Z
> **Grounding:** https://raw.githubusercontent.com/menno420/sim-lab/d89303eeb68c89e2f69a35396582693a5e90224d/control/inbox.md@d89303e · fetched 2026-07-11T09:11Z
> **Grounding:** https://raw.githubusercontent.com/menno420/superbot/58040c6cfde814e9a8eba3de049c2e78a1b13f1d/control/inbox.md@58040c6 · fetched 2026-07-11T09:11Z
> **Grounding:** https://raw.githubusercontent.com/menno420/fleet-manager/3150f0e2b8096de7b497778189d3d85676e69ceb/control/status.md@3150f0e · fetched 2026-07-11T09:11Z

*Verify-first, three findings. (1) The `Sequence:` gate has FIRED: the scope-seam
probe is done (this repo PR #87 — parked(routed), ranking (A) specialize > (B)
reconcile-artifacts-kept > (C) fold, decided-and-flagged to the manager). The
capture's own trivial exit checked honestly: only a fold ruling would collapse this
head into the seam ruling, and the ranking is (A) — under specialize, games-web IS
the presentation layer, and whether that layer earns phase-2 investment is exactly
the question left open. (2) The deferral is still routed NOWHERE: ORDER 001's `why:`
prose is verbatim unchanged at pf inbox `43563dc`; sim-lab's intake @ `d89303e` is
INTAKE 001–006 only (zero games-web/concept entries, INTAKE 006 = the idle-economy
relay, VERDICT 006 finalized 05:09:53Z — queue otherwise EMPTY); nothing in fm's
inbox/status @ `3150f0e` orders an evidence pass. (3) The redirect window is OPEN
but on a visible clock: phase-2 is still BLOCKED (pf status @ `43563dc`), the
providing ORDER is still UNROUTED — superbot's `control/inbox.md` now EXISTS
(created by superbot PR #1977, 04:41Z; the #87 probe's missing-bus blocker is GONE)
but carries only ORDER 001 @ `58040c6` — while fm ORDER 012/013 already DECIDED
placement ("API placement: superbot lane, contracted committed-JSON feed per #1920
… feed slice NOW"). The moment that order files and phase-2 spends, this pass loses
most of its value. Expiry-aware: route now.*

**1. What is this really?** The owner's own deferred decision coming due. ORDER
001's `why:` line deferred the FULL games-web concept to a sim-lab evidence pass
("could be simulated later" — owner) and bought mock-first cheapness precisely to
keep the redirect option open. This head is that pass's routing trigger: the asset
being protected is the redirect decision, and its value decays with phase-2
investment, not with time.

**2. What is the possibility space?** (a) Route the ONE question now — the seam
probe already sharpened it (the concept's genuinely open question is the value of
the presentation layer ITSELF, given mineverse ships a direct utility view over the
same miner state); (b) wait for the manager's seam ruling first — but the ruling
picks WHO builds, not WHETHER the layer is worth building, and the providing ORDER
could route in the same sweep, starting the spend before evidence lands; (c)
collapse into the seam ruling — valid only under (C) fold, which is not the
ranking; (d) never route — the deferral orphans as ORDER prose, the exact failure
mode this capture exists to prevent (Q-0264: never orphaned).

**3. Most advanced capability reachable by the simplest implementation?** One
outbox append: PROPOSAL 007 relaying the owner's own deferral as a single sharpened
question with a stated-criteria done-when. All the sharpening work is already done
and committed (#87's battery); sim-lab's judgment-capable battery class (VERDICT
002's stated-criteria quality judgments; INTAKE 002's JUDGMENT-ONLY leg) fits an
evidence pass on concept value. The verdict lands BEFORE phase-2 spend and feeds
the manager's seam ruling its missing axis — #87 ranked A>B>C on cost/ownership
grounds and explicitly could not measure presentation-layer value.

**4. What breaks it?** (i) Concept value is judgment-heavy — a verdict without
stated criteria and named evidence would be vibes; the done-when demands both, and
the repo's honesty norm ("not measured" beats invention) rides it. (ii) The manager
rules the seam before the verdict — acceptable: the ruling and the pass answer
different questions (WHO vs WHETHER/WHAT), stated explicitly in the done-when so
neither waits on the other. (iii) The providing ORDER routes and phase-2 spends
first — the real race; mitigation is routing NOW while sim-lab idles on an empty
queue. (iv) Duplicate intake if the manager later relays the deferral
independently — reconciled by source citation at sim-lab intake, the PROPOSAL 006
precedent clause.

**5. What does it unlock?** The phase-2 go/redirect/stop decision gets evidence
before spend; the manager's seam ruling gets the value axis it lacks; the owner's
deferral stops being orphan-risk prose and enters the verdicted pipeline; sim-lab's
empty queue gets its next pull without a nudge (its declared wake behavior is
pulling this outbox — Q-0264.6).

**6. What does it depend on?** sim-lab (executing seat — idle, queue empty at
`d89303e`); product-forge phase-1 artifacts as evidence inputs (contract v1.0.1,
two committed fixtures, a11y pass — all at `43563dc`); the providing ORDER's
routing clock (fm ORDER 012/013 decided, superbot-side order un-filed at
`58040c6`); the manager's seam ruling as the verdict's primary consumer.

**7. Which lane should build it?** sim-lab runs the pass — that is the owner's
stated destination, verbatim in the deferral. This repo routes it (the outbox is
exactly its Q-0264 surface); the manager owns everything post-verdict. No build
lane exists or is needed yet — the verdict decides whether one spends.

**8. What is the smallest shippable slice?** PROPOSAL 007 in `control/outbox.md`
(this PR): one question, stated-criteria done-when, `depends:` carrying the
sequencing (providing-ORDER clock) and the co-consumers (seam ruling, phase-2
scoping).

**Recommendation: sim-ready** — the owner's own deferral come due: the sequencing
gate fired (#87, ranking A not C), the pass is verified routed nowhere (pf
`43563dc` · sim-lab `d89303e` · fm `3150f0e`), the redirect window is open but
clocked by the still-unrouted providing ORDER (superbot `58040c6`), and the
executing seat idles on an empty queue — PROPOSAL 007 appended this PR.

## Sim verdict (2026-07-11)

sim-lab **VERDICT 007 · finalized 2026-07-11T09:56:46Z · needs-more-evidence**
(= this repo's PROPOSAL 007 — numbering cross recorded on
`.sessions/2026-07-10-sim-verdicts-fanin.md`'s lineage (V001=P003 · V002=P001 ·
V003=P002 · V004=P004 · V005=P005 · V006=P006) and this session's card:
**V007 = P007**; all seven verdicted intakes carry their cross). Unlike V006,
VERDICT 007 HAS a `ruling:` field — the verdict token is a plain
`verdict: needs-more-evidence` and the operative ruling is the `ruling:` field,
quoted verbatim from the pin: "redirect — NOT proceed-as-scoped (blocked by a
measured conformance failure), NOT stop-at-phase-1 (value hypothesis plausible +
phase-1 cheap-to-redirect). Named changes before phase-2 spend: (1) re-contract
games-web as a projection of `mining_snapshot.v1` — map the 9 real gear slots,
derive stat lines from real fields or cut the invented ones, derive rarity/power
from real `gear_wear` or drop (no fabricated numbers shipped as real); (2) consume
the committed-JSON `mining_snapshot.v1` feed (fm ORDER 012/013), retire the
competing self-API; (3) presentation-VALUE is a phase-2 HYPOTHESIS to MEASURE (A/B
vs the mineverse direct view) — the "more evidence" named, unreachable from
sim-lab." SETTLED: the phase-2 go/redirect/stop question ends in REDIRECT — not
proceed-as-scoped (blocked by a measured conformance failure: disjoint 8-vs-9 gear
taxonomy, invented stats, and a transport conflict with the already-decided
committed-JSON feed), not stop-at-phase-1 (the value hypothesis stays plausible
and phase 1 was bought cheap to redirect); phase-1's dependency-free inline-SVG
paper-doll + rarity chips + a11y scaffolding survive as a reusable presentation
SHELL (the verdict's own recommendation line). NAMED CHANGES before phase-2 spend:
(1) re-contract `games-web.character-sheet` as a projection of real
`mining_snapshot.v1` (map the 9 real gear slots; derive stat lines from real
fields or cut the invented ones; derive rarity/power from real `gear_wear` or
drop); (2) consume the committed-JSON `mining_snapshot.v1` feed (fm ORDER
012/013) and retire the self-authored `GET /v1/games-web/character-sheet` API;
(3) phase 2 MUST ship the A/B hook (comic-RPG presentation vs the mineverse
direct view over the same data) so presentation-VALUE is finally MEASURED, not
assumed — that A/B is the "more evidence" the token names, unreachable from
sim-lab. WHO builds the games-web↔mineverse seam (A/B/C, this repo's #87) stays
the manager's call, OUT OF SCOPE — the verdict's own target line says so.
Evidence caveat: `evidence: JUDGMENT-ONLY` (the value axis has no ground truth
reachable from sim-lab) — but the redirect RESTS ON a measured, reproducible
rung-2 CONFORMANCE CHECK (40/40 self-checks, byte-identical, exit 0); mixed
label, weakest-for-the-question travels with the verdict (the V005 precedent).
Source:
[sim-lab control/outbox.md VERDICT 007 @ 015e28e](https://github.com/menno420/sim-lab/blob/015e28e/control/outbox.md)
(gate PASS — the conformance core is measured/reproducible: 40/40 self-checks,
byte-identical, exit 0; the disjoint 8-vs-9 gear taxonomy + invented stats +
transport conflict are robust; the verdict is needs-more-evidence NOT approve
because the value axis is unmeasured JUDGMENT-ONLY and the concept is
non-conformant as-scoped). State stays sim-ready — no post-verdict state in the
grammar.
