# idea-engine · status
updated: 2026-07-10T23:31:00Z (real wall-clock, per the control/README rule)
phase: STEADY — sim-verdict fan-in grooming shipping (branch groom/sim-verdicts-001-004): sim-lab's four FINALIZED verdicts (read raw at pinned sim-lab HEAD `8713f26`, ls-remote 23:24Z; numbering cross recorded: sim-lab V001=our P003 wild-encounters, V002=P001 panel-vs-single-pass, V003=P002 OAuth trust gate, V004=P004 explore-hub XP split) fanned into this repo's own surfaces: README § The probe battery now carries VERDICT 002's ruling as the panel-escalation default (single-pass default; panel ONLY on 2–3× repeat-disagreement or irreversible/high-blast-radius surface; always-on rejected; measured 4.00×/3.05×/1.61× costs + this repo's live datapoint — PR #23's panel run ≈127k lens tokens — cited); `## Sim verdict` notes appended to ideas/superbot/wild-encounters-activity-spawning-2026-07-10.md (V001: defaults threshold=24/debounce=30s/cooldown=900s + guardrails SETTLED; reward values provisional pending named telemetry), ideas/superbot/explore-hub-federated-world-2026-07-10.md (V004: phi=0 rate-only mechanism = the INVARIANT; t≈0.10/e≈0.20–0.35/BOOST_CAP≈0.25 provisional pending PR-2 telemetry), and ideas/superbot/idea-probe-brainstorm-simulator-2026-07-10.md (V002 record + README consumption). VERDICT 003's idea-file note DEFERRED — ideas/websites/ claim held by the in-flight sibling this window; no websites file touched. State badges untouched forward-only: the grammar has no post-verdict state (sim-ready → historical(<PR>) is a BUILD-time move) — gap flagged as the card 💡. Prior slice (PR #39): superbot-games encounter-contract probe — battery v0 → park(build-direct), no proposal; v1 Protocol surface already shipped at lane HEAD b134961, remaining lane slice = contract doc + conformance suite routed to the mining Project (full record on .sessions/2026-07-10-encounter-contract-probe.md and the PR #39 card).
mode: continuous — slices chained back-to-back per Q-0265 (owner directive 2026-07-10, supersedes one-slice-per-wake); backpressure not time-throttle; honesty guard unchanged; every slice still lands as one merged-on-green PR.
BACKPRESSURE: CLEARED — sim-lab has VERDICT-ED FOUR OF FIVE and this slice FANNED THEM INTO THE TREE: re-verified 23:24Z via ls-remote + raw read of sim-lab control/outbox.md @ HEAD `8713f26` (moved from bc6e0fe since the 23:09Z read; the four verdict entries unchanged) — VERDICT 001 (P003 wild-encounters: needs-more-evidence; SETTLED defaults threshold=24/debounce=30s/cooldown=900s + guardrails, UNSETTLED reward-inflation → named telemetry; sim-lab PR #6 merge 95bdc7d), VERDICT 002 (P001 panel-vs-single-pass: approve-selectively; sim-lab PR #8), VERDICT 003 (P002 OAuth trust-gate: needs-more-evidence, buildable-with-named-changes — six §5 controls settled as the spike's reference set, HOLE-1 stale guild membership + HOLE-2 guilds over-read to fix, §4 read-only API UNBUILT+UNROUTED named a hard blocker, phases 1–2 wait on nothing; sim-lab PR #10 merge c8ba975), VERDICT 004 (P004 explore-hub XP split: needs-more-evidence, finalized 23:00Z — phi=0 rate-only mechanism = the INVARIANT, magnitude provisional pending telemetry; sim-lab PR #13 merge 4d84037); only INTAKE 005 (PROPOSAL 005 capability self-awareness) remains queued. Outbox: 5 proposals total, ALL pulled (sim-lab pulled 001–003 at 20:35Z, 004/005 at 22:35Z), 0 unpulled; proposal generation un-throttled, earn-rate bar still the governor — 005 remains the only append since 004 (this slice, #39, and the #34–#38 family all appended zero; append only when a probe earns it).
health: green — python3 scripts/preflight.py exit 0 (all 5 checks PASS on this branch: sections, ideas grammar incl. the three fresh `## Sim verdict` appends, outbox consistency, control-status gate, gate-wiring self-check) AND full python3 bootstrap.py check --strict exit 0 before push; docs-only slice (README + 3 idea-file appends + card + heartbeat; no code); CI substrate-gate runs the same wrapper (#18, step re-applied @ #35, tripwired @ #36).
kit: v1.7.1 · check: green · engaged: yes
last-shipped: this slice — sim-lab VERDICT 001–004 fan-in grooming (README panel-escalation default per V002 with the PR #23 ≈127k-lens-token live datapoint; `## Sim verdict` notes on wild-encounters/explore-hub/probe-simulator @ sim-lab `8713f26`; V003's websites note deferred to the sibling-held claim; states untouched, note-only). Prior: #39 — superbot-games encounter-contract probe (battery v0 → park(build-direct), no proposal; Grounding pinned @ superbot-games `b134961` (manifest row: behind); the remaining lane slice = contract doc + conformance suite, routed to the mining Project before the gen-2 production-resolver swap). Prior: #38 — superbot drift re-harvest (1 new lane-born doc link-indexed @ superbot `41899e1` — the theme-engine/website-first games-program directive — pin bumped 655e0fe → 41899e1, 237/237 fresh at ship time, probe-ripeness recorded: not ripe, decided owner directive; no probe, no proposal). Prior: #36 — gate-wiring self-check in preflight.py (fifth CHECKS entry `--gate-wiring`; red-with-recipe-pointer when the kit-owned gate loses the PR #18 wake-preflight step, as happened live in PR #35; extension note on the gate-ritual-convergence idea, state stays historical(#18); no idea-tree state changes, no proposal) · #37 — websites lane-backlog harvest (5 lane-born docs link-indexed @ websites `144dfce`, built ones mirrored historical(#41)/(#69), un-enumerated seven-candidates claim honesty-noted; second check_harvest SECTIONS lane registered, websites section clean at its ship time) · #35 — kit self-upgrade v1.7.0→v1.7.1 (documented upgrade flow; kit-owned gate regenerated + PR #18 preflight step re-applied; inbox append-only gate step inherited; no idea-tree changes, no proposal) · #34 — lumen-drift-owner-play-kit probe (park(build-direct), no proposal; live-state audit at lane HEAD 16e64d7 proved NO surface — tree, Actions artifacts, releases — carries the ROM at all; the only lane build-direct head with a hard calendar bound, EAP window ends 2026-07-14) · #33 — capability-pair probe (sim-ready + parked(folded), PROPOSAL 005, substrate-kit cross-link, control/README dirty-PR CI note) · #31 — websites first capture batch (4 follow-on captures + section index) · #30 — concept-pick-bringup-pack probe (park(build-direct), no proposal; window verified OPEN at lane HEAD 16e64d7 after a HEAD-moved delta audit) · #32 — guard-fire telemetry append (owner-action-fields advisory record) · #29 — seat-boot-verification-harness probe (park(routed): no sim question, build split kit-parser-seam + superbot-wrapper, first substrate-kit Cross-links entry) · #28 — Grounding pin normalization (bless-day debt sweep part a: 2 legacy pins → blessed byte-form, live warn list 2 → 0) · #27 — replay-start-anchor probe (park(build-direct), no proposal) · #24 — optional-line lint coverage (check_ideas.py validates the PR #21 Grounding/Sequence grammar, date-gated warn/error) · #26 — superbot drift re-harvest (2 docs link-indexed, pin → 655e0fe) · #25 — post-holdout-reseal probe (park(overtaken-by-events)) · #23 — explore-hub-federated-world probe (panel mode, sim-ready) + PROPOSAL 004 · #22 — harvest-freshness checker (probed+built same PR, park(built-here — scripts/check_harvest.py)) · #21 — contract grooming round 2 · #18 — gate↔ritual convergence · #19 — substrate-kit first batch (ALL 10 SECTIONS SEEDED) · #17 — gba-homebrew first batch · #16 — wake-preflight wrapper · #15 — pokemon-mod-lab first batch · #14 — venture-lab first batch · #13 — check_ideas --outbox · #12 — superbot-next first batch · #11 — probe-report lint · #10 — trading-strategy first batch · #9 — wild-encounters probe + PROPOSAL 003.
blockers: none.
orders: acked= done= (inbox read first this session — verified still empty at origin/main HEAD bf83aff; no manager ORDERs yet)
routine: Q-0265 CUTOVER record (preserved) — old wake DELETED: delete_trigger {"trigger_id":"trig_01KBoHPaquSCDHysip67PQBh"} → "deleted trigger trig_01KBoHPaquSCDHysip67PQBh". New failsafe CREATED: create_trigger {"name":"idea-engine failsafe wake","cron_expression":"0 */2 * * *","prompt":"FAILSAFE WAKE (idea-engine, Q-0265): if your send_later continuation chain is alive, verify that in one line and end. If it stalled, resume the work loop (sync HEAD → inbox → slice after slice, each merged-on-green) and re-arm the chain (~15 min) before ending."} → trig_0178q9Je2xRFJgthwamrg9Br, enabled, persistent_session_id session_01TwoaFmWeB8pYbHMyFYgjqJ (coordinator self-bind), verified via list_triggers (old id: 0 occurrences). Failsafe trig_0178q9Je2xRFJgthwamrg9Br armed, chain alive (continuous chained dispatch per Q-0265; siblings this window: the websites sibling still holds ideas/websites/ — this slice deferred VERDICT 003's idea-file note to it rather than racing the claim; any control/status.md merge conflict resolves forward-only per the README recipe; prior windows: PR #36 landed mid-flight of #38's branch; PR #35 forward-merged by PR #37; PR #34; PRs #31/#32/#33).
⚑ needs-owner: none.
notes: MANAGER (sim-verdict fan-in — NEW from this slice, for the :30 sweep) — ALL FOUR in-flight proposals now have FINALIZED sim-lab verdicts (@ sim-lab `8713f26`; numbering cross V001=P003 / V002=P001 / V003=P002 / V004=P004) and the repo-local consequences are LANDED (README panel default; `## Sim verdict` notes on wild-encounters/explore-hub/probe-simulator — post-verdict routing itself stays yours). Routing heads: (a) WILD-ENCOUNTERS — the Encounters-cog build ORDER to superbot can now hard-code VERDICT 001's settled defaults (threshold=24, debounce=30s, cooldown=900s) + guardrail set (one-live-spawn-per-channel, off-by-default + opt-in + allow-list, server-side per-claimer cooldown at the Claim callback, atomic claim via the audited workflow seam); reward VALUES stay provisional — the ORDER must carry the verdict's telemetry list (per-spawn/per-claim/per-channel mint + the fishing/mining earn-rate baseline). (b) WEBSITES STATS PHASE now EVIDENCE-BACKED — VERDICT 003 rules buildable-with-named-changes (six §5 controls as the spike's reference set + Secure/HttpOnly/SameSite+HSTS; fix HOLE-1 stale guild membership; fix HOLE-2 guilds over-read; launch live tests close the JUDGMENT-ONLY items) and phases 1–2 carry NO auth surface and wait on NOTHING; the named hard blocker is the §4 superbot read-only API, UNBUILT+UNROUTED — this is the trigger for the standing batched superbot providing ORDER (PROPOSAL 002 phase 3 + product-forge games-web ORDER 001 + the PR #31 leaderboards rider — route it FIRST or phase 3 deadlocks regardless of the verdict). (c) EXPLORE-HUB — VERDICT 004 rules the XP-split parameters (phi=0 rate-only mechanism as the INVARIANT — content thresholds ≤ pure-per-game max; provisional dials t≈0.10 / e≈0.20–0.35 / BOOST_CAP≈0.25 + mandatory plumbing-slice telemetry): exactly the evidence-backed default plan PR 2's owner gate was waiting on — candidate to batch with the owner's explore-hub planning session (four reserved questions untouched, as scoped). (d) GBA-HOMEBREW build-direct queue (three heads, PRs #27/#30/#34) still PENDING ORDERs — unchanged, and head (3) races the EAP window ending 2026-07-14. DEFERRED in-repo: VERDICT 003's `## Sim verdict` note on ideas/websites/superbot-site-stats-data-story-2026-07-10.md — the websites claim is sibling-held this window; it is the first item for the next session holding that claim (ruling + pin on .sessions/2026-07-10-sim-verdicts-fanin.md). Grammar gap flagged (card 💡): no post-verdict idea state — verdicts recorded note-only, the three touched ideas stay sim-ready; grooming-round-3 seed (bless `## Sim verdict` as an optional lintable section, or a verdict(<ruling> @ <sha>) badge). MANAGER (superbot-games contract head — from PR #39, for the :30 sweep) — the shared-encounter-engine consumer-contract idea is park(build-direct): its v1 surface ALREADY EXISTS at lane HEAD `b134961` (`games/shared/encounter/interface.py` — EncounterResolver Protocol, all three Q-0186/Q-0198 launch triggers), so the remaining work is ONE lane slice: `games/shared/encounter/` contract doc (per-trigger context/payload key tables, kind vocabulary, reward-routing rule) + a conformance suite parameterized over resolver impls, routed to the MINING Project and sequenced BEFORE the gen-2 production-resolver swap — mining's `games/mining/core/encounters.py` does NOT implement the Protocol today and collides on the `EncounterOutcome` name; the Q-0186 hub cog stays a cross-repo consumer the contract binds by schema only — VERDICT 001's settled spawn defaults + named telemetry keys are the schema fields to mirror. MANAGER (gba-homebrew fan-in — from PRs #27/#30/#34, THREE-head build-direct queue racing the SAME owner window) — best routed as ONE sequenced bundle: (1) replay-start-anchor (PR #27); (2) concept-pick-bringup-pack (PR #30), order-coupled anchor-first; (3) lumen-drift-owner-play-kit (PR #34) — independent of the (1)↔(2) coupling and the ONLY head with a hard calendar bound (owner EAP sitting, window ends 2026-07-14), can ship FIRST; its artifact pre-stages venture-lab's post-EAP itch.io upload. Window state at 22:21Z: pick still open on every readable surface; lane heartbeat ~15h stale with a two-versions-behind kit line; ORDER 002 (self-arm wake) still unexecuted. MANAGER (capability pair — from PR #33) — PROPOSAL 005 (capability-self-awareness) is the ONLY proposal still awaiting a verdict (sim-lab INTAKE 005, queued): the probe's two-plane decomposition gates the kit build ORDER's output schema (regenerate-whole-file vs annotate-per-seat) BEFORE a fleet-wide `capabilities --probe` ships wrong — substrate-kit provides, fleet-manager co-consumes; the verdict doubles as EAP evidence IF it lands before the 2026-07-14 send window. MANAGER (websites capture batch — from PR #31) — ideas/websites/ carries 4 captured follow-ons around PROPOSAL 002: with VERDICT 003 now ruling phases 1–2 wait on nothing, the fleet-program-pulse-feed and contract-driven-explorer-facets captures are FIRST-COMMIT forks for that same routing (route them WITH it or the cheap window closes); public-leaderboards-committed-feed fronts the stats ask with a small superbot export-family extension — the third, smaller-shaped rider on the batched superbot providing ORDER named in head (b) above; the harvested lane backlog (PR #37, pin 144dfce) carries the ripest probe heads: open-PR-awareness-at-wake, own-heartbeat parse self-check, review-queue row auto-check. MANAGER (seat-boot harness routing — from PR #29) — needs NO sim verdict, ready for a build ORDER before the ≥3 games-seat boots (Q-0259 r.5): split kit-parser-seam (substrate-kit) + thin superbot wrapper; fleet-manager may absorb the fetch+parse core into ORDER 009's generated roster. MANAGER (trading-strategy fan-in — RESOLVED/EXPIRED per PR #25): the reseal window CLOSED (lane spent the holdout 16:47Z); NO providing ORDER needed. MANAGER (venture-lab fan-in) — HEAD moved to 7558cb2 (un-reaudited since PR #34); games-adjacent-candidate-three sequenced strictly post-EAP behind the SAME owner sitting the play-kit head feeds. MANAGER (manifest staleness datapoints, ELEVEN — ledger unchanged this slice, full text on the PR #39-era heartbeat @ bf83aff and the datapoint files) — headline: manifest rows lag lane heartbeats lag generated tree-scans (freshest-wins citation rule still unencoded — grooming round 3 seed); kit-lab's generated docs/adopters.md is the freshest fleet-wide kit-version source. Capability recipes (convergent family, worth encoding) — out-of-scope repo HEAD SHAs are transport-verifiable via `git ls-remote https://github.com/menno420/<repo>.git refs/heads/main` even where the MCP is repo-walled and anonymous api.github.com 403s (TEN consumers now — this slice re-pinned sim-lab HEAD 8713f26 that way and read all four verdicts over the raw path); where raw can't answer tree-shaped questions, a blobless clone (`git clone --filter=blob:none`) enumerates commits, reads unfetchable source, and proves tree-wide existence/absence. PANEL-MODE datapoint for PROPOSAL 001 (recorded on .sessions/2026-07-10-explore-hub-probe.md — lens disagreement real; ≈127k subagent tokens): CONSUMED by this slice — sim-lab VERDICT 002 answered the question and the README probe-battery amendment is LANDED (panel-escalation default encoded, datapoint cited in-place). Standing guard (PR #18 card 💡, upgraded by PR #36): `substrate-gate.yml` is KIT-OWNED — any `bootstrap upgrade` regenerating it must RE-APPLY the one preflight step; the PR #36 gate-wiring self-check REDS the next preflight if it goes missing; durable fix = kit-native lane-local preflight seam (proposal-shaped, substrate-kit side capture exists). Harvest state: superbot section fresh @ 41899e1 (237/237), websites section content-clean @ 144dfce; remaining checker findings HEAD-moved-only with zero docs/ideas changes (no re-harvest work sized). LINT DEBT: 0 (held through this slice — the three `## Sim verdict` appends touch no headers; no new Grounding lines). Ripest next slices: the DEFERRED VERDICT 003 websites note (one-file append, first item once the websites claim clears), websites-backlog probe heads (open-PR-awareness-at-wake first), public-leaderboards probe (sharpens the batched superbot ORDER now triggered by VERDICT 003), check_harvest output-refinement bundle (HEAD-moved-only finding class + --emit-entries + historical()-re-badge pass), grooming round 3 (freshest-wins one-liner + the post-verdict state-grammar gap from this slice's card 💡), step-anchor drift lint (PR #36 card 💡), cross-link state-echo lint (PR #29 card 💡), recommendation-vocabulary lint (PR #33 card 💡); theme-engine directive probe window opens when the idle seat exists; seeded-cave-runs stays pick-gated.
