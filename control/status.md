# idea-engine · status
updated: 2026-07-11T10:15:14Z (real wall-clock via date -u, per the control/README rule — monotonic past the #154 sibling's 10:09:09Z overwrite, forward-merged into this branch and reconciled here)
phase: STEADY — SUPERBOT INDEX RECHECKED HONEST AT LIVE HEAD (this slice, branch groom/superbot-recheck; claim rode fast-lane PR #152, merged 09:57:57Z squash 62541f7; claims dir re-read at post-claim HEAD — only sibling claim was harvest/websites-round-3, disjoint section, since landed as #154 and cleared): the full checker suite (plain + --bullet-drift, --re-badge, --states) ran green against live superbot HEAD and came back PIN-BUMP-ONLY — 237 indexed · 237 live · 0 new · 0 deleted · 0 changed · 0 re-badge · 0 state-drift. superbot HEAD moved TWICE during the pass (58040c6 → e0fd8ef at first check, → 227c220 at the pre-pin ls-remote re-verify); reconciled per the write-pins rule — checker content-verified e0fd8ef against the pin record, then the e0fd8ef→227c220 delta was blob-verified directly (blobless clone, ls-tree of docs/ideas diffed against .harvest-pin.json: all 237 recorded blob shas identical). README re-pinned @ 227c220; --write-pins re-recorded ideas/superbot/.harvest-pin.json (harvest-pin/v1, 237 docs @ 227c220). Zero index-entry edits needed; per-entry @ <sha> pins untouched per the never-retrofit rule. SHARED-FILE RECONCILE (forward-merge, never rebase): the #154 sibling independently pin-bumped superbot 58040c6 → e0fd8ef in its round-3 run (superbot#1980 delta fact) — both facts now live in ONE pin-history paragraph, resolved to the newer verified pin 227c220; sibling's .harvest-pin.json @ e0fd8ef superseded by this slice's record @ 227c220 (identical 237-blob docs map, later pin). Divergences report-only: NONE in ideas/superbot; the two websites findings this slice's suite surfaced (backlog.md +101/-27; open-pr-awareness parked→built) were consumed by #154's own round-3 re-pin + deliberate-divergence ACK before this branch shipped.
health: green — python3 scripts/preflight.py ALL TEN checks PASS + full python3 bootstrap.py check --strict green on this final merged tree immediately before push, run AFTER this heartbeat reconcile (the last content change on the branch, per the heartbeat-last rule). This slice: docs-only groom (README pin paragraph + .harvest-pin.json re-record + card + claim add via #152 / deleted here + this heartbeat) — no code, no workflow edits, outbox untouched.
kit: v1.10.0 · check: green · engaged: yes
last-shipped: this slice (branch groom/superbot-recheck; PR number written only after the PR exists, per the never-guess rule — #152 (claim) and the #154 sibling are the only numbers stateable as merged fact at write time).
blockers: none.
orders: acked=001-002 done=001 (inbox re-read this session at post-claim HEAD 62541f7 — ORDER 001 only at that read; ORDER 002 (P1 owner-requested self-review) landed mid-flight via #153 and was acked by the #154 sibling's heartbeat — that ack is PRESERVED here, not regressed; ORDER 002 is outside this slice's claimed scope (ideas/superbot grooming) and stays due within the lane's next two wakes per its done-when. ORDER 001's standing per-session rule re-satisfied by this slice's card: 📊 Model: fable-5).
⚑ needs-owner: SHARED-WINDOW NOTE UPDATE (this slice, same fewer-clearer-asks hygiene): the gba-homebrew CONCEPT PICK (lane ⚑ @ c7592d6 — Lumen-deepening / Clockwork Courier / Shoal, full click path on the lane's own heartbeat; seeded-cave-runs is now the costed 'more Lumen' option, this slice's park) is the THIRD item landing in the SAME ≤2026-07-14 EAP sitting — deliberately NOT a new ⚑ here, the ask lives on the LANE's own heartbeat (one ask, one owner surface); the sweep should read the sitting as carrying THREE bundled decisions: (1) the Lumen Drift itch.io go/no-go standing entry below, (2) fm docs/owner-queue.md item 3 (pokemon playtest verdicts), (3) the gba concept pick. The prior note follows verbatim: SHARED-WINDOW NOTE (this slice, hygiene per fewer-clearer-asks): the pokemon-mod-lab playtest-kit park (this slice) is deliberately NOT a new ⚑ here — its owner ask already lives as fm docs/owner-queue.md item 3 @ 1afca50 (one ask, one owner surface, the manager's); what the sweep must see is that BOTH owner-sitting items — the Lumen Drift ⚑ OWNER-ACTION below AND that queue item — land in the SAME EAP sitting, window ends 2026-07-14; the kit-preparation routing (fm order or lane self-serve) is the manager-side item flagged in notes. The standing entry follows verbatim: ⚑ OWNER-ACTION (from this slice, decision-adjacent — EAP window ends 2026-07-14): WHAT: post-EAP go/no-go + one itch.io sitting to publish Lumen Drift as a PWYW listing. WHERE: itch.io (account) + gba-homebrew dist/lumen-drift.gba v1.3 (sha256 195a867…, provenance dist/README.md) + the parked idea ideas/venture-lab/games-adjacent-candidate-three-2026-07-10.md. HOW: play Lumen Drift during the EAP sitting; on go: create itch.io account → New project → set pay-what-you-want → upload dist/lumen-drift.gba → publish (venture-lab pre-drafts the full listing copy so this is paste-and-click). WHY-IT-MATTERS: first second-channel distribution datapoint + a publish pipeline every finished fleet game inherits, at zero build cost. UNBLOCKS: the parked candidate #3, venture-lab's listing-kit prep, and gives revenue-ingestion-owner-relay its first non-marketplace source. Sequencing note (this slice's probe): the click need NOT wait on the revenue-ingestion relay — itch.io/marketplace sales exports are retro-downloadable, so pre-relay sales stay recoverable; the lane lands the ledger convention independently (fan-in below, flagged ORDER-worthy). VERIFIED-NEEDED: account creation/external publishing is rail-banned for agents on both lanes' READMEs (venture-lab @ 0ad0ea4, gba-homebrew @ 31c8672) — owner-only by rule, not by missing capability. ALSO note for the :30 sweep: the wake-resilience verdict (PR #56) is gated on an owner click that already lives as a six-field OWNER-ACTION entry on the TRADING-STRATEGY lane's own heartbeat @ `d0d789e` (⚑ (b): paste environments/setup-universal.sh into that project's environment setup-script field); deliberately NOT duplicated here per the fewer-clearer-asks hygiene — one ask, one owner surface, the lane's. Routed via the trading-strategy fan-in note below. SAME hygiene for venture-lab's ⚑B/⚑D publish clicks (launch-ready, UNFROZEN @ `9f1b616`) — they live as owner-action entries on the LANE's own docs/launch/membership-kit/owner-actions.md, deliberately NOT duplicated here; routed via the venture-lab fan-in note below.
notes: SECTION LEDGER UNCHANGED BY DESIGN (this slice, for the :30 sweep): 10-of-13 sections fully probed-or-parked; superbot, websites, and fleet are the three remaining-by-design — superbot and websites are reference/link-index sections groomed against fast-moving canonical repos (superbot is now honest at 227c220 with a fresh blob record, so its next slice should be a PROBE from the card's shortlist — games-theme-engine-website-first, rebuild-websites-cutover-role, claude-code-projects-for-the-rebuild — not another groom; websites is honest at c81ce76 as of #154 with its divergence ACKed), fleet is the cross-cutting lane. ORDER 002 (self-review, P1, due within two wakes) is the ripest routed head for the next wake — it wants a coordinator-context seat, not a section-scoped worker. Method note for the sweep: the superbot pin-race hit twice in one pass this slice AND two parallel slices independently pin-bumped the same section README within 20 minutes (#154's e0fd8ef vs this branch's 227c220, reconciled by forward-merge) — the card's 💡 proposes a checker --re-pin mode (ls-remote → blob-verify → pin rewrite → record write, atomic) that would mechanize the raciest step of every grooming slice and shrink exactly this collision window.
