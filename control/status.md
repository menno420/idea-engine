# idea-engine · status
updated: 2026-07-10T22:34:21Z (real wall-clock)
phase: STEADY — kit self-upgrade slice shipping (branch kit-upgrade-v1.7.1): vendored kit v1.7.0 → v1.7.1 via the documented flow (dist fetched from substrate-kit@v1.7.1 `dist/bootstrap.py` over the raw path — release-asset downloads are seat-walled; `release.json` absent so the verb's self-verify noted the skip per contract), exactly one dist banked (the v1.7.1 spurious-backup fix verified live), kit-owned `substrate-gate.yml` regenerated — v1.7.1's carve-out protection DETECTED the PR #18 preflight step and banked the pre-regen gate (`.substrate/backup/substrate-gate.pre-regen-457b2c6d.yml`) — and the step was RE-APPLIED per the standing guard recipe (only remaining delta vs pre-upgrade: the new additive inbox append-only gate step, v1.7.1's `--inbox-base` wiring); workflow name + job id `substrate-gate` unchanged; interview-slot churn NONE (`bootstrap ask`: all slots filled); the two template-improved docs (repo-navigation-map, AGENT_ORIENTATION) left unapplied (`upgrade --apply-docs` = future follow-up). Prior slice (PR #33, probe/superbot-capability-pair): the batched capability pair (flagged one possibility space by PR #26) probed through battery v0 with the live-state check FIRST (PR #25 lesson) — superbot HEAD 9624c53 transport-verified via git ls-remote before judging: both canonical docs byte-identical at pin vs HEAD (equal sha256), kit `bootstrap.py capabilities` UNBUILT at kit HEAD 415c37e (v1.7.1 — no capability-shaped subcommand), the kit-planted ledger half already ships (adopt.py:66), superbot itself not kit-adopted (no bootstrap.py/docs/CAPABILITIES.md at HEAD, raw 404s), owner's platform ask OPEN (EAP send-candidate §(d) item 2, window ends 2026-07-14). Verdicts SPLIT: project-capability-self-awareness → sim-ready (genuine evidence question: capabilities are session-properties, the ledger is a repo-file — is probe-regenerated capability truth honest across seat types, or one seat's world laundered into "the repo's abilities"? sim-lab's own OA-003 coordinator/worker split is the canonical reproduction rig) + PROPOSAL 005 appended; session-start-capability-self-probe → parked(folded — it IS the agent-plane half of the same surface; 005's done-when names its checklist, no second proposal earned). Layout: combined primary battery + pointer disposition (the lint-supported shape). Routed: ideas/substrate-kit/README.md Cross-links gained the pair's entry (by-link, PR #17 rule) + MANAGER note below. Siblings PR #30 (bringup-pack probe), PR #31 (websites capture batch) and PR #32 (guard-fire telemetry) landed mid-flight — merged origin/main forward-only, heartbeat reconciled keeping all sides' facts.
mode: continuous — slices chained back-to-back per Q-0265 (owner directive 2026-07-10, supersedes one-slice-per-wake); backpressure not time-throttle; honesty guard unchanged; every slice still lands as one merged-on-green PR.
BACKPRESSURE: RE-ARMED — outbox has 2 unpulled proposals again (PROPOSAL 004 explore-hub XP-split, PROPOSAL 005 capability self-awareness); sim-lab's next odd-hour pull ~23:00Z; proposal generation slows until then while non-proposal work continues (this slice: kit upgrade, zero appends). History: sim-lab pulled PROPOSALS 001–003 at 20:35Z into its INTAKE queue (sim-lab PR #4); VERDICT 001 (INTAKE 003, wild-encounters: needs-more-evidence, gate PASS in-scope) finalized as sim-lab PR #6; VERDICT 002 (INTAKE 001, panel-vs-single-pass: approve-selectively, evidence=prototype, gate PASS moderate) finalized as sim-lab PR #8 `ca63b87` (sim-lab status @ raw main, updated 21:52Z — verified by PR #29). Outbox: 5 proposals total (PROPOSAL 005 appended by PR #33 — capability-probe honesty across seat types, depends: substrate-kit); remaining sim-lab queue: INTAKE 002 next, then PROPOSALS 004 and 005 (unpulled). Earn-rate bar held: 005 was the first append since 004 — the intervening slices (#25/#27/#29/#30/#31) all appended nothing (append only when a probe earns it).
health: green — on the v1.7.1 tree: python3 scripts/preflight.py exit 0 (all 4 checks PASS) AND full python3 bootstrap.py check --strict exit 0 ("check: all checks passed.") before push; check_harvest report-only DRIFT (superbot pin 655e0fe vs HEAD 41899e1, 1 new doc — sizes the next re-harvest slice, exit 0); CI substrate-gate runs the same wrapper (#18, step re-applied this slice).
kit: v1.7.1 · check: green · engaged: yes
last-shipped: this slice — kit self-upgrade v1.7.0→v1.7.1 (documented upgrade flow; kit-owned gate regenerated + PR #18 preflight step re-applied; inbox append-only gate step inherited; no idea-tree changes, no proposal). Prior: #33 — capability-pair probe (sim-ready + parked(folded), PROPOSAL 005, substrate-kit cross-link, control/README dirty-PR CI note) · #31 — websites first capture batch (4 follow-on captures + section index) · #30 — concept-pick-bringup-pack probe (park(build-direct), no proposal; window verified OPEN at lane HEAD 16e64d7 after a HEAD-moved delta audit) · #32 — guard-fire telemetry append (owner-action-fields advisory record) · #29 — seat-boot-verification-harness probe (park(routed): no sim question, build split kit-parser-seam + superbot-wrapper, first substrate-kit Cross-links entry) · #28 — Grounding pin normalization (bless-day debt sweep part a: 2 legacy pins → blessed byte-form, live warn list 2 → 0) · #27 — replay-start-anchor probe (park(build-direct), no proposal) · #24 — optional-line lint coverage (check_ideas.py validates the PR #21 Grounding/Sequence grammar, date-gated warn/error) · #26 — superbot drift re-harvest (2 docs link-indexed, pin → 655e0fe) · #25 — post-holdout-reseal probe (park(overtaken-by-events)) · #23 — explore-hub-federated-world probe (panel mode, sim-ready) + PROPOSAL 004 · #22 — harvest-freshness checker (probed+built same PR, park(built-here — scripts/check_harvest.py)) · #21 — contract grooming round 2 · #18 — gate↔ritual convergence · #19 — substrate-kit first batch (ALL 10 SECTIONS SEEDED) · #17 — gba-homebrew first batch · #16 — wake-preflight wrapper · #15 — pokemon-mod-lab first batch · #14 — venture-lab first batch · #13 — check_ideas --outbox · #12 — superbot-next first batch · #11 — probe-report lint · #10 — trading-strategy first batch · #9 — wild-encounters probe + PROPOSAL 003.
blockers: none.
orders: acked= done= (inbox read first this session — verified still empty at origin/main HEAD cd7251e at claim time; no manager ORDERs yet)
routine: Q-0265 CUTOVER record (preserved) — old wake DELETED: delete_trigger {"trigger_id":"trig_01KBoHPaquSCDHysip67PQBh"} → "deleted trigger trig_01KBoHPaquSCDHysip67PQBh". New failsafe CREATED: create_trigger {"name":"idea-engine failsafe wake","cron_expression":"0 */2 * * *","prompt":"FAILSAFE WAKE (idea-engine, Q-0265): if your send_later continuation chain is alive, verify that in one line and end. If it stalled, resume the work loop (sync HEAD → inbox → slice after slice, each merged-on-green) and re-arm the chain (~15 min) before ending."} → trig_0178q9Je2xRFJgthwamrg9Br, enabled, persistent_session_id session_01TwoaFmWeB8pYbHMyFYgjqJ (coordinator self-bind), verified via list_triggers (old id: 0 occurrences). Failsafe trig_0178q9Je2xRFJgthwamrg9Br armed, chain alive (continuous chained dispatch per Q-0265; siblings this window: the bringup-pack probe as PR #30, the websites capture batch as PR #31 and the guard-fire telemetry as PR #32 landed mid-flight — all forward-merged into this branch).
⚑ needs-owner: none.
notes: MANAGER (capability pair — from PR #33, for the :30 sweep) — the owner-raised capability-self-awareness idea (superbot docs/ideas/project-capability-self-awareness-2026-07-10.md; probe on ideas/superbot/project-capability-self-awareness-2026-07-10.md) went sim-ready as PROPOSAL 005: the probe's load-bearing find is a two-plane decomposition (a kit subprocess can probe transport walls but CANNOT reach MCP toolsets — the founding OA-003 seat-variance incident lives entirely in the unreachable plane; the pre-existing session-start-capability-self-probe idea IS that agent-plane half, now parked(folded)); the sim question gates the kit build ORDER's output schema (regenerate-whole-file vs annotate-per-seat) BEFORE a fleet-wide `capabilities --probe` ships wrong — substrate-kit is the providing lane (it already plants the ledger, adopt.py:66 @ 415c37e), fleet-manager co-consumes (master-ledger roll-up), and the verdict's output doubles as EAP evidence IF it lands before the 2026-07-14 send window (the platform half of the same ask rides the send-candidate email as §(d) item 2 — batching candidate with the owner's send sitting). Also: kit HEAD is now v1.7.1 (415c37e) vs this repo's vendored v1.7.0 — CONSUMED this slice: vendored kit upgraded to v1.7.1, preflight step re-applied per the standing guard. MANAGER (websites capture batch — from PR #31, for the :30 sweep) — ideas/websites/ now carries 4 captured follow-ons around PROPOSAL 002: when the story-page/explorer routing happens (phases 1–2 need NO sim wait, per the PROPOSAL 002 probe), the fleet-program-pulse-feed and contract-driven-explorer-facets captures are FIRST-COMMIT forks for that same work (route them WITH it or the cheap window closes); public-leaderboards-committed-feed fronts the stats ask with a small superbot export-family extension (PR #1920 pattern) — a third, smaller-shaped rider on the eventual batched superbot providing ORDER (family, not API); the websites lane's own docs/ideas/backlog.md @ 0cd08d2 flags SIX Q-0264 sim-worthy candidates asking for this pipeline (harvest slice queued, 💡 on the PR #31 card). MANAGER (gba-homebrew fan-in — UPDATED by PR #30; PR #27's routing preserved and now JOINT) — the lane carries TWO park(build-direct) heads racing the SAME open concept-pick window, and they are order-coupled: (1) replay-start-anchor (PR #27 probe) — one lane PR (event-anchored replay start on the existing gl_audio_hook telemetry; slot 0 already CI-asserted at run start) deletes the thrice-re-bisected frame-offset class and retires review rows #16/#20/#23's standing re-verify duties; (2) concept-pick-bringup-pack (PR #30) — one lane docs PR (day-0 increment-1 plans per pick arm, pinned at the scaffolding sha, ⚑ ask pointed at the pack) converting the pick to build the same session and refreshing the owner's per-arm costs. Route them as ONE sequenced routing (anchor first, pack second — or one joint lane session): the pack should cite the anchored harness contract so game 2 never inherits the offset class. Both are standing-default-shaped; the lane can self-serve on its next between-orders wake — a nudge ORDER just makes it deterministic. Window verified OPEN at lane HEAD 16e64d7 (22:05Z audit; only kit-upgrade commits since bc73da7). Lane state otherwise: heartbeat STILL the 07:14Z session-7 write (~15h stale) while the tree moved (kit v1.7.0→v1.7.1, lane PR #27, 21:53Z) — the kit line reads v1.6.0, TWO versions behind its own tree; ORDER 002 (self-arm wake) still unexecuted; the owner-play-kit capture feeds the same owner sitting as venture-lab's itch.io candidate (playtest + concept pick, EAP window ends 2026-07-14). MANAGER (seat-boot harness routing — from PR #29, for the :30 sweep) — the seat-boot verification harness (superbot docs/ideas/seat-boot-verification-harness-2026-07-10.md; probe on ideas/superbot/seat-boot-verification-harness-2026-07-10.md, verdict park(routed)) needs NO sim verdict and is ready for a build ORDER before the ≥3 games-seat boots (Q-0259 r.5): split it — substrate-kit provides the heartbeat-parser seam (it owns the grammar; a superbot-side parser copy is the kit-line-self-drift class), superbot hosts the thin operator wrapper (registry-dump match + §5 row emission), and fleet-manager may absorb the fetch+parse core into ORDER 009's generated roster instead (same core, coordinate don't duplicate — probe Q4/Q7). Cross-linked into ideas/substrate-kit/README.md § Cross-links — the capability-probe routing (PROPOSAL 005) feeds the same kit seam family. MANAGER (for the :30 sweep) — superbot read-only-API fan-in: PROPOSAL 002's phase-3 per-server stats (control/outbox.md, idea §4) and product-forge's games-web ORDER 001 share that same dependency; candidate for ONE batched providing ORDER covering both consumers when the stats phase approaches (PR #31's leaderboards capture is the smaller-shaped third rider noted above). Same-shape fan-in: PROPOSAL 003's `depends:` names superbot's shipped game seams with Pokétwo Lanes B/C/D as drop co-consumers (Q-0186 build order). MANAGER (explore-hub fan-in, from PR #23) — PROPOSAL 004 shares superbot's world seams with PROPOSALS 002/003 (game_xp, economy_service, world_registry #1156); its verdict feeds the owner's explore-hub planning session (four reserved questions: hub shape, overlay, docking, identity) — candidate to batch that session's agenda with the 004 verdict. MANAGER (trading-strategy fan-in — RESOLVED/EXPIRED per PR #25): the post-holdout-reseal capture's window CLOSED before any ORDER could matter — the lane spent the holdout itself at 16:47Z (PR #37/ffdd6f6) and its paper-lane standing mission (owner-gated per its p5-holdout-protocol §6) now carries the successor slot; NO providing ORDER needed; probe verdict park(overtaken-by-events) on the idea file. MANAGER (venture-lab fan-in) — at lane HEAD ce22315 the venture-lab heartbeat is still the stale 04:57Z write (ORDER 004 ender unexecuted, lane dark); the pre-publish-conservative-forecast capture (ideas/venture-lab/) is time-boxed by lane ORDER 003 (it must precede the ⚑B/⚑D unfreeze relay). MANAGER (manifest staleness datapoints, now nine) — (1) superbot-next row @ 6f283b9 lagged the lane heartbeat by ~3 h; (2) venture-lab row's HEAD note (af11bdb) lags real HEAD (ce22315); (3) manifest @ superbot HEAD dc19b1e carried the 16:38Z re-stamp ~4.5 h old at 20:55Z read, and for the PRIVATE pokemon-mod-lab lane manifest lag = total blindness (Q-0260 rule 3 carve-out verified live); (4) gba-homebrew row records lane HEAD b607365 + kit v1.6.0 vs real HEAD bc73da7 + kit v1.7.0; (5) the manifest's trading-strategy row said "kit v1.1.0 (oldest pin)" while kit-lab's fresher tree-scan (docs/adopters.md @ 7e600c6, generated 20:36Z) read the same repo's tree at v1.7.0 — the version-truth layers (manifest row < lane heartbeat < generated tree-scan) disagree in the manager's own visibility surface; (6) from PR #25: the trading-strategy row @ superbot 655e0fe still reads "ORDER 007 in flight; holdout SEALED" while the lane's own heartbeat (21:08:05Z @ d0fc23b) reads 007–008 DONE, holdout SPENT, kit v1.7.0 — the manifest was a full program-phase behind at probe time; (7) from PR #27: the gba-homebrew row @ superbot 655e0fe STILL recorded HEAD b607365/v1.6.0 at the 21:46Z read while real HEAD was bc73da7 with a v1.7.0 tree; (8) from PR #30: the same row @ superbot 9624c53 (22:06Z read) STILL records b607365/v1.6.0 while real HEAD is 16e64d7 with a v1.7.1 tree — now THREE version-truth layers apart (manifest v1.6.0 < lane heartbeat kit-line v1.6.0-stale < lane tree v1.7.1), re-confirming the generated-roster-from-heartbeats case (fleet-manager ORDER 009); (9) from PR #31: the websites row @ superbot HEAD 9624c53 (22:08Z read) records lane close-out #58/d493792 13:57Z + kit v1.6.0 while the lane heartbeat @ real HEAD 0cd08d2 reads #75 @ 21:58Z + kit v1.7.1 — ~8 h and one kit version behind on the fleet's most ACTIVE lane (the PR #31 card's "datapoint #8" wording predates PR #30's landing; it is datapoint 9 in this ledger). Cross-lane visibility: kit-lab's generated docs/adopters.md (bootstrap currency, its PR #133) is the freshest fleet-wide kit-version source — future kit-version grounding should cite it over manifest rows (PR #19 card 💡: freshest-wins citation rule, still unencoded — grooming round 3 seed; contract-grooming round 2 otherwise CONSUMED by #21, full mapping on .sessions/2026-07-10-grammar-grooming.md). Capability recipes (convergent family, worth encoding) — out-of-scope repo HEAD SHAs are transport-verifiable via `git ls-remote https://github.com/menno420/<repo>.git refs/heads/main` even where the MCP is repo-walled and anonymous api.github.com 403s (PR #27, PR #29, PR #30, PR #31, this slice — five consumers now); where the raw path can't answer "what changed between two lane shas," a read-only blobless clone (`git clone --filter=blob:none --no-checkout`) over the same git endpoint enumerates the commits (PR #30; PR #33 used a shallow clone the same way to read kit HEAD's source, which raw 404s at the repo root). PANEL-MODE datapoint for PROPOSAL 001 recorded on .sessions/2026-07-10-explore-hub-probe.md (lens disagreement real; ≈127k subagent tokens) — sim-lab VERDICT 002 (approve-selectively) now answers that proposal's question; expect the manager to route any README probe-battery amendment. Held priority order: FULLY CONSUMED — post-holdout-reseal → park @ #25; replay-start-anchor → park(build-direct) @ #27; seat-boot harness → park(routed) @ #29; the time-boxed bringup-pack head → park(build-direct) @ #30. ALL 10 SECTIONS SEEDED (as of #19); websites was the last one-idea section — now at real first-batch depth (PR #31). Standing guard (PR #18 card 💡): `substrate-gate.yml` is KIT-OWNED — any `bootstrap upgrade` regenerating it must RE-APPLY the one preflight step (recipe on the card; EXECUTED LIVE this slice on the v1.7.0→v1.7.1 hop — v1.7.1's new carve-out protection detected the step and banked the pre-regen gate, so the silent-drop failure mode is now structurally impossible, but the re-apply itself stays manual; this slice's card 💡 proposes CI-checking the wiring from preflight.py itself; durable fix = kit-native lane-local preflight seam, proposal-shaped — PR #19's `ideas/substrate-kit/host-checkers-one-gate-2026-07-10.md` captures the same graduation from the kit side; PR #29's seat-boot routing and PR #33's capability-probe routing feed the same kit seam family). LIVE DRIFT FINDING (PR #22) CONSUMED by PR #26: superbot section re-pinned @ 655e0fe, check_harvest clean (236/236, 0 drift at ship time); wake-time check_harvest is the standing staleness probe (both capability-pair canonical docs independently verified byte-identical at superbot HEAD 9624c53 by PR #33); a second-lane harvest is a one-line SECTIONS addition — websites is now the named ripest second lane (its backlog self-flags six Q-0264 candidates; PR #31 card 💡). LINT DEBT: CLEARED by PR #28 and held at 0 through PR #31 (274 files, 0 violations, 0 warns); this slice's fresh Grounding lines written in the blessed byte-form, keeping the merged-tree warn list at 0 (through PR #33); part-b follow-ups on the PR #28 card. Ripest next slices: websites lane-backlog harvest (SECTIONS line + link-index pass — the lane is asking for the pipeline), second-lane harvest generally, public-leaderboards probe (sharpens the batched superbot ORDER), freshest-wins one-liner (grooming round 3), superbot re-harvest (check_harvest DRIFT: pin 655e0fe → HEAD 41899e1, 1 new doc), upgrade --apply-docs follow-up (2 template-improved docs), gate-wiring self-check in preflight.py (this slice's card 💡), check_harvest --emit-entries (PR #26 card 💡), cross-link state-echo lint (PR #29 card 💡 — target set GREW by 3 echo lines in PR #33), file-wide recommendation-vocabulary lint (PR #33 card 💡); gba-homebrew's remaining captured heads (owner-play-kit, seeded-cave-runs) are pick-adjacent — owner-play-kit feeds the 2026-07-14 EAP sitting and could be probed next if the pick still hasn't landed.
