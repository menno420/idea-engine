# idea-engine · status
updated: 2026-07-11T10:09:56Z (real wall-clock via date -u, per the control/README rule — monotonic past the gba slice's 09:43:05Z stamp this heartbeat overwrites)
phase: STEADY — SUPERBOT INDEX RECHECKED HONEST AT LIVE HEAD (this slice, branch groom/superbot-recheck; claim rode fast-lane PR #152, merged 09:57:57Z squash 62541f7; claims dir re-read at post-claim HEAD — only sibling claim is harvest/websites-round-3, disjoint section, its scope's superbot touch is a read-only pin check): the full checker suite (plain + --bullet-drift, --re-badge, --states) ran green against live superbot HEAD and came back PIN-BUMP-ONLY — 237 indexed · 237 live · 0 new · 0 deleted · 0 changed · 0 re-badge · 0 state-drift. superbot HEAD moved TWICE during the pass (58040c6 → e0fd8ef at first check, → 227c220 at the pre-pin ls-remote re-verify); reconciled per the write-pins rule — checker content-verified e0fd8ef against the pin record, then the e0fd8ef→227c220 delta was blob-verified directly (blobless clone, ls-tree of docs/ideas diffed against .harvest-pin.json: all 237 recorded blob shas identical). README re-pinned @ 227c220 with the recheck sentence appended to the pin-history paragraph; --write-pins re-recorded ideas/superbot/.harvest-pin.json (harvest-pin/v1, 237 docs @ 227c220). Zero index-entry edits needed; per-entry @ <sha> pins untouched per the never-retrofit rule. Divergences report-only: NONE in ideas/superbot; the two cross-section findings the suite surfaced (websites backlog.md blob f1d93e3→0897a6f +101/-27; open-pr-awareness-at-wake parked→built state-drift) belong to ideas/websites inside the in-flight websites-round-3 claim and were deliberately not touched (--write-pins correctly refused that section: README pin d862364 != live f7f07e7; wrote nothing there).
health: green — python3 scripts/preflight.py ALL TEN checks PASS + full python3 bootstrap.py check --strict green on this final tree immediately before push, run AFTER this heartbeat overwrite (the last content change on the branch, per the heartbeat-last rule). This slice: docs-only groom (README pin paragraph + .harvest-pin.json re-record + card + claim add via #152 / deleted here + this heartbeat) — no code, no workflow edits, outbox untouched.
kit: v1.10.0 · check: green · engaged: yes
last-shipped: this slice (branch groom/superbot-recheck; PR number written only after the PR exists, per the never-guess rule — the claim's fast-lane PR #152 is the only number this slice can state as merged fact at write time).
blockers: none.
orders: acked=001 done=001 (inbox re-read this session at post-claim HEAD 62541f7 — ORDER 001 remains the only order; its standing per-session rule re-satisfied by this slice's card: 📊 Model: fable-5).
⚑ needs-owner: none new from this slice (a pin-bump groom raises no owner decision). The gba-homebrew shared-window note from the 09:43Z heartbeat stands as recorded — retrieve it verbatim from this file's git history @ 62541f7 if the :30 sweep hasn't consumed it yet.
notes: SECTION LEDGER UNCHANGED BY DESIGN (this slice, for the :30 sweep): 10-of-13 sections fully probed-or-parked; superbot, websites, and fleet are the three remaining-by-design — superbot and websites are reference/link-index sections groomed against fast-moving canonical repos (superbot is now honest at 227c220 with a fresh blob record, so its next slice should be a PROBE from the card's shortlist — games-theme-engine-website-first, rebuild-websites-cutover-role, claude-code-projects-for-the-rebuild — not another groom), fleet is the cross-cutting lane. Websites is the section actually carrying drift (1 CHANGED +101/-27, 1 state-drift parked→built) and has its own in-flight claim (harvest/websites-round-3) — route nothing there until that claim clears. Method note for the sweep: the pin-race hit twice in one pass this slice; the card's 💡 proposes a checker --re-pin mode (ls-remote → blob-verify → pin rewrite → record write, atomic) that would mechanize the raciest step of every grooming slice.
