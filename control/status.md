# idea-engine · status
updated: 2026-07-11T10:34:30Z (real wall-clock via date -u, per the control/README rule — monotonic past the #156 sibling's 10:22:39Z claim edit, forward-merged into this branch and reconciled here)
phase: STEADY — GAMES-THEME-ENGINE DIRECTIVE PROBED → parked(routed) (this slice, branch probe/games-theme-engine; section claim committed as the branch's first commit and cleared at close; claims dir re-read at origin/main HEAD 0c70f33 — no competing section claim, the only sibling claim is ORDER 002's heartbeat annotation, disjoint scope): the #155 shortlist's first recommended probe executed. Verify-first at NINE live pins (ls-remote batch 10:22:09Z) found the owner directive CONSUMED, not open — per-§ ledger appended to ideas/superbot/games-theme-engine-website-first-2026-07-10.md: §1 seam built (superbot-idle 1e2c9f6: schema v1 + theme-gate + 12 packs) and adopted (superbot-games e62818a theme-leaks R2, audit 19✅·9⚠️·2❌); §2 exceeded (12 themes vs "2–3"); §3 write path FROZEN (SETUP-CODE FORMAT v1 binding, 150 vectors) with the consumer/UI open-by-sequencing (websites f0e7710 clean; fm mapping §4 sequences the selector LAST); §5 both seats live + first shippables shipped (fm roster gen #5 @ 3150f0e); §6 idle-economy kernel consumed by PROPOSAL 006 → sim-lab VERDICT 006 approve @ d89303e; §7 all four Q-0240 flags executed or tracked. No outbox entry (nothing sim-ready). Canonical doc byte-identical 41899e1 → live superbot c77ee0d (diffed) — the #155 groom's 0-drift verdict extended one HEAD further.
health: green — python3 scripts/preflight.py ALL TEN checks PASS + full python3 bootstrap.py check --strict green on this final merged tree immediately before push, run AFTER this heartbeat reconcile (the last content change on the branch, per the heartbeat-last rule). This slice: docs-only probe (probe report append + state flip + 2 index-echo re-badges (superbot + superbot-idle cross-link) + card + claim add/clear + this heartbeat) — no code, no workflow edits, outbox untouched.
kit: v1.10.0 · check: green · engaged: yes
last-shipped: this slice (branch probe/games-theme-engine; PR number written only after the PR exists, per the never-guess rule — #156 (the ORDER-002 claim edit) is the only new number stateable as merged fact at write time).
blockers: none.
orders: acked=001-002 done=001 claimed-by: 002 orders/self-review-2026-07-11 2026-07-11T10:22:39Z (claim annotation PRESERVED verbatim from the #156 sibling edit — ORDER 002 is that session's, not this slice's; it stays due within the lane's next two wakes per its done-when and its Self-review section had NOT landed on main at this overwrite's re-fetch (origin/main 0c70f33). ORDER 001's standing per-session rule re-satisfied by this slice's card: 📊 Model: fable-5).
⚑ needs-owner: SHARED-WINDOW NOTE UPDATE (this slice, same fewer-clearer-asks hygiene): the gba-homebrew CONCEPT PICK (lane ⚑ @ c7592d6 — Lumen-deepening / Clockwork Courier / Shoal, full click path on the lane's own heartbeat; seeded-cave-runs is now the costed 'more Lumen' option, this slice's park) is the THIRD item landing in the SAME ≤2026-07-14 EAP sitting — deliberately NOT a new ⚑ here, the ask lives on the LANE's own heartbeat (one ask, one owner surface); the sweep should read the sitting as carrying THREE bundled decisions: (1) the Lumen Drift itch.io go/no-go standing entry below, (2) fm docs/owner-queue.md item 3 (pokemon playtest verdicts), (3) the gba concept pick. The prior note follows verbatim: SHARED-WINDOW NOTE (this slice, hygiene per fewer-clearer-asks): the pokemon-mod-lab playtest-kit park (this slice) is deliberately NOT a new ⚑ here — its owner ask already lives as fm docs/owner-queue.md item 3 @ 1afca50 (one ask, one owner surface, the manager's); what the sweep must see is that BOTH owner-sitting items — the Lumen Drift ⚑ OWNER-ACTION below AND that queue item — land in the SAME EAP sitting, window ends 2026-07-14; the kit-preparation routing (fm order or lane self-serve) is the manager-side item flagged in notes. The standing entry follows verbatim: ⚑ OWNER-ACTION (from this slice, decision-adjacent — EAP window ends 2026-07-14): WHAT: post-EAP go/no-go + one itch.io sitting to publish Lumen Drift as a PWYW listing. WHERE: itch.io (account) + gba-homebrew dist/lumen-drift.gba v1.3 (sha256 195a867…, provenance dist/README.md) + the parked idea ideas/venture-lab/games-adjacent-candidate-three-2026-07-10.md. HOW: play Lumen Drift during the EAP sitting; on go: create itch.io account → New project → set pay-what-you-want → upload dist/lumen-drift.gba → publish (venture-lab pre-drafts the full listing copy so this is paste-and-click). WHY-IT-MATTERS: first second-channel distribution datapoint + a publish pipeline every finished fleet game inherits, at zero build cost. UNBLOCKS: the parked candidate #3, venture-lab's listing-kit prep, and gives revenue-ingestion-owner-relay its first non-marketplace source. Sequencing note (this slice's probe): the click need NOT wait on the revenue-ingestion relay — itch.io/marketplace sales exports are retro-downloadable, so pre-relay sales stay recoverable; the lane lands the ledger convention independently (fan-in below, flagged ORDER-worthy). VERIFIED-NEEDED: account creation/external publishing is rail-banned for agents on both lanes' READMEs (venture-lab @ 0ad0ea4, gba-homebrew @ 31c8672) — owner-only by rule, not by missing capability. ALSO note for the :30 sweep: the wake-resilience verdict (PR #56) is gated on an owner click that already lives as a six-field OWNER-ACTION entry on the TRADING-STRATEGY lane's own heartbeat @ `d0d789e` (⚑ (b): paste environments/setup-universal.sh into that project's environment setup-script field); deliberately NOT duplicated here per the fewer-clearer-asks hygiene — one ask, one owner surface, the lane's. Routed via the trading-strategy fan-in note below. SAME hygiene for venture-lab's ⚑B/⚑D publish clicks (launch-ready, UNFROZEN @ `9f1b616`) — they live as owner-action entries on the LANE's own docs/launch/membership-kit/owner-actions.md, deliberately NOT duplicated here; routed via the venture-lab fan-in note below.
notes: MANAGER SWEEP FLAG (this slice's find, relay-worthy): the theme-schema promotion park (ideas/superbot-idle/theme-schema-plugin-contract-promotion-2026-07-11.md, parked(awaiting-arming-event)) has its arming condition HALF-FIRED — its un-park's first disjunct ("a plugin/manifest contract doc becomes fetchable anywhere superbot-next publishes") reads SATISFIED: superbot-next docs/game-plugin-contract.md (D-0056, binding) returns 200 at live HEAD 8b55d95 AND at the historical pins 4a32f61 + 4c8c5b0 — the park's grounding ("all contract-doc paths 404") was a path-keyed false negative, the exact failure its own Q4 named; second disjunct NOT fired (ORDER 002 still status: new at 8b55d95; superbot-plugin-hello still zero refs). Per the park's own prescription the move is a MANAGER-routed adopt-with-evidence relay (this repo writes no lane files, Q-0260) — the sweep should collect it. SECOND WATCH ITEM: superbot-idle had NOT consumed the VERDICT 006 lift at its 10:11:04Z heartbeat (blockers still list SIM-001); when its tuning PR lands it must carry the verdict's two named guardrails (growth-ratio near-floor 1.15, base-rate integer-floor fix + re-sweep). SECTION LEDGER: superbot's games-program head is now parked(routed) — the section's next probes stay the #155 shortlist remainder (rebuild-websites-cutover-role, claude-code-projects-for-the-rebuild). Deliberate non-actions this slice, both recorded on the card: no un-park edit to the sibling superbot-idle head (outside claim; manager's move), no Seat-A world-themes pre-capture (lane self-serve prior). ORDER 002 self-review remains the sibling's — its claim annotation preserved on the orders line above.
