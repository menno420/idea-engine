# idea-engine · status
updated: 2026-07-10T22:20:46Z (real wall-clock)
phase: STEADY — probe slice shipping (branch probe/superbot-capability-pair): the batched capability pair (flagged one possibility space by PR #26) probed through battery v0 with the live-state check FIRST (PR #25 lesson) — superbot HEAD 9624c53 transport-verified via git ls-remote before judging: both canonical docs byte-identical at pin vs HEAD (equal sha256), kit `bootstrap.py capabilities` UNBUILT at kit HEAD 415c37e (v1.7.1 — no capability-shaped subcommand), the kit-planted ledger half already ships (adopt.py:66), superbot itself not kit-adopted (no bootstrap.py/docs/CAPABILITIES.md at HEAD, raw 404s), owner's platform ask OPEN (EAP send-candidate §(d) item 2, window ends 2026-07-14). Verdicts SPLIT: project-capability-self-awareness → sim-ready (genuine evidence question: capabilities are session-properties, the ledger is a repo-file — is probe-regenerated capability truth honest across seat types, or one seat's world laundered into "the repo's abilities"? sim-lab's own OA-003 coordinator/worker split is the canonical reproduction rig) + PROPOSAL 005 appended; session-start-capability-self-probe → parked(folded — it IS the agent-plane half of the same surface; 005's done-when names its checklist, no second proposal earned). Layout: combined primary battery + pointer disposition (the lint-supported shape). Routed: ideas/substrate-kit/README.md Cross-links gained the pair's entry (by-link, PR #17 rule) + MANAGER note below.
mode: continuous — slices chained back-to-back per Q-0265 (owner directive 2026-07-10, supersedes one-slice-per-wake); backpressure not time-throttle; honesty guard unchanged; every slice still lands as one merged-on-green PR.
BACKPRESSURE: LIFTED — sim-lab pulled PROPOSALS 001–003 at 20:35Z into its INTAKE queue (sim-lab PR #4); VERDICT 001 (INTAKE 003, wild-encounters: needs-more-evidence, gate PASS in-scope) finalized as sim-lab PR #6; VERDICT 002 (INTAKE 001, panel-vs-single-pass: approve-selectively, evidence=prototype, gate PASS moderate) finalized as sim-lab PR #8 `ca63b87` (sim-lab status @ raw main, updated 21:52Z — verified by the PR #29 slice). Outbox: 5 proposals total (PROPOSAL 005 NEW this slice — capability-probe honesty across seat types, depends: substrate-kit); remaining sim-lab queue: INTAKE 002 next, then PROPOSALS 004 and 005 (unpulled). Earn-rate bar held: 005 is the first append since 004 across four probe slices (PRs #25/#27/#29 all parked).
health: green — python3 scripts/preflight.py exit 0 (all 4 checks PASS) on this branch before push; CI substrate-gate runs the same wrapper (#18).
kit: v1.7.0 · check: green · engaged: yes
last-shipped: this slice — capability-pair probe (sim-ready + parked(folded), PROPOSAL 005, substrate-kit cross-link, control/README dirty-PR CI note). Prior: #29 — seat-boot-verification-harness probe (park(routed): no sim question, build split kit-parser-seam + superbot-wrapper, first substrate-kit Cross-links entry) · #28 — Grounding pin normalization (bless-day debt sweep part a: 2 legacy pins → blessed byte-form, live warn list 2 → 0) · #27 — replay-start-anchor probe (park(build-direct), no proposal) · #24 — optional-line lint coverage (check_ideas.py validates the PR #21 Grounding/Sequence grammar, date-gated warn/error) · #26 — superbot drift re-harvest (2 docs link-indexed, pin → 655e0fe) · #25 — post-holdout-reseal probe (park(overtaken-by-events)) · #23 — explore-hub-federated-world probe (panel mode, sim-ready) + PROPOSAL 004 · #22 — harvest-freshness checker (probed+built same PR, park(built-here — scripts/check_harvest.py)) · #21 — contract grooming round 2 · #18 — gate↔ritual convergence · #19 — substrate-kit first batch (ALL 10 SECTIONS SEEDED) · #17 — gba-homebrew first batch · #16 — wake-preflight wrapper · #15 — pokemon-mod-lab first batch · #14 — venture-lab first batch · #13 — check_ideas --outbox · #12 — superbot-next first batch · #11 — probe-report lint · #10 — trading-strategy first batch · #9 — wild-encounters probe + PROPOSAL 003.
blockers: none.
orders: acked= done= (inbox read first this session — verified still empty at origin/main HEAD 698fd93 at claim time; no manager ORDERs yet)
routine: Q-0265 CUTOVER record (preserved) — old wake DELETED: delete_trigger {"trigger_id":"trig_01KBoHPaquSCDHysip67PQBh"} → "deleted trigger trig_01KBoHPaquSCDHysip67PQBh". New failsafe CREATED: create_trigger {"name":"idea-engine failsafe wake","cron_expression":"0 */2 * * *","prompt":"FAILSAFE WAKE (idea-engine, Q-0265): if your send_later continuation chain is alive, verify that in one line and end. If it stalled, resume the work loop (sync HEAD → inbox → slice after slice, each merged-on-green) and re-arm the chain (~15 min) before ending."} → trig_0178q9Je2xRFJgthwamrg9Br, enabled, persistent_session_id session_01TwoaFmWeB8pYbHMyFYgjqJ (coordinator self-bind), verified via list_triggers (old id: 0 occurrences). Failsafe trig_0178q9Je2xRFJgthwamrg9Br armed, chain alive (continuous chained dispatch per Q-0265; siblings reported in flight this window: gba-homebrew probe + websites seed — expect forward merges on landing).
⚑ needs-owner: none.
notes: MANAGER (capability pair — NEW, from this slice, for the :30 sweep) — the owner-raised capability-self-awareness idea (superbot docs/ideas/project-capability-self-awareness-2026-07-10.md; probe on ideas/superbot/project-capability-self-awareness-2026-07-10.md) went sim-ready as PROPOSAL 005: the probe's load-bearing find is a two-plane decomposition (a kit subprocess can probe transport walls but CANNOT reach MCP toolsets — the founding OA-003 seat-variance incident lives entirely in the unreachable plane; the pre-existing session-start-capability-self-probe idea IS that agent-plane half, now parked(folded)); the sim question gates the kit build ORDER's output schema (regenerate-whole-file vs annotate-per-seat) BEFORE a fleet-wide `capabilities --probe` ships wrong — substrate-kit is the providing lane (it already plants the ledger, adopt.py:66 @ 415c37e), fleet-manager co-consumes (master-ledger roll-up), and the verdict's output doubles as EAP evidence IF it lands before the 2026-07-14 send window (the platform half of the same ask rides the send-candidate email as §(d) item 2 — batching candidate with the owner's send sitting). Also: kit HEAD is now v1.7.1 (415c37e) vs this repo's vendored v1.7.0 — upgrade candidate for a future slice (re-apply the PR #18 preflight step per the standing guard below). MANAGER (seat-boot harness routing, from PR #29) — the seat-boot verification harness (probe verdict park(routed)) needs NO sim verdict and is ready for a build ORDER before the ≥3 games-seat boots (Q-0259 r.5): split it — substrate-kit provides the heartbeat-parser seam (it owns the grammar; a superbot-side parser copy is the kit-line-self-drift class), superbot hosts the thin operator wrapper (registry-dump match + §5 row emission), and fleet-manager may absorb the fetch+parse core into ORDER 009's generated roster instead (same core, coordinate don't duplicate — probe Q4/Q7). Cross-linked into ideas/substrate-kit/README.md § Cross-links. MANAGER (for the :30 sweep) — superbot read-only-API fan-in: PROPOSAL 002's phase-3 per-server stats (control/outbox.md, idea §4) and product-forge's games-web ORDER 001 share that same dependency; candidate for ONE batched providing ORDER covering both consumers when the stats phase approaches. Same-shape fan-in: PROPOSAL 003's `depends:` names superbot's shipped game seams with Pokétwo Lanes B/C/D as drop co-consumers (Q-0186 build order). MANAGER (explore-hub fan-in, from PR #23) — PROPOSAL 004 shares superbot's world seams with PROPOSALS 002/003 (game_xp, economy_service, world_registry #1156); its verdict feeds the owner's explore-hub planning session (four reserved questions: hub shape, overlay, docking, identity) — candidate to batch that session's agenda with the 004 verdict. MANAGER (trading-strategy fan-in — RESOLVED/EXPIRED per PR #25): the post-holdout-reseal capture's window CLOSED before any ORDER could matter — the lane spent the holdout itself at 16:47Z (PR #37/ffdd6f6) and its paper-lane standing mission (owner-gated per its p5-holdout-protocol §6) now carries the successor slot; NO providing ORDER needed; probe verdict park(overtaken-by-events) on the idea file. MANAGER (venture-lab fan-in) — at lane HEAD ce22315 the venture-lab heartbeat is still the stale 04:57Z write (ORDER 004 ender unexecuted, lane dark); the pre-publish-conservative-forecast capture (ideas/venture-lab/) is time-boxed by lane ORDER 003 (it must precede the ⚑B/⚑D unfreeze relay). MANAGER (gba-homebrew fan-in — UPDATED by PR #27) — replay-start-anchor probed → park(build-direct): a routing candidate for the LANE, not sim-lab — one lane PR (event-anchored replay start on the existing gl_audio_hook telemetry; slot 0 already CI-asserted at run start) deletes the thrice-re-bisected frame-offset class and retires review rows #16/#20/#23's standing re-verify duties; best sequenced BEFORE the concept-pick harness transfer (window verified open at lane HEAD bc73da7, ls-remote 21:45Z). Lane state otherwise unchanged: heartbeat still the 07:14Z session-7 write, ORDER 002 (self-arm wake) unexecuted, kit line stale (v1.6.0 vs v1.7.0 tree); the owner-play-kit capture feeds the same owner sitting as venture-lab's itch.io candidate (playtest + concept pick, EAP window ends 2026-07-14) — and the concept-pick-bringup-pack capture is Sequence-tied to the SAME open pick window (probe it before the pick lands or it decays like PR #25's head). MANAGER (manifest staleness datapoints, now seven) — (1) superbot-next row @ 6f283b9 lagged the lane heartbeat by ~3 h; (2) venture-lab row's HEAD note (af11bdb) lags real HEAD (ce22315); (3) manifest @ superbot HEAD dc19b1e carried the 16:38Z re-stamp ~4.5 h old at 20:55Z read, and for the PRIVATE pokemon-mod-lab lane manifest lag = total blindness (Q-0260 rule 3 carve-out verified live); (4) gba-homebrew row records lane HEAD b607365 + kit v1.6.0 vs real HEAD bc73da7 + kit v1.7.0; (5) the manifest's trading-strategy row said "kit v1.1.0 (oldest pin)" while kit-lab's fresher tree-scan (docs/adopters.md @ 7e600c6, generated 20:36Z) read the same repo's tree at v1.7.0 — the version-truth layers (manifest row < lane heartbeat < generated tree-scan) disagree in the manager's own visibility surface; (6) from PR #25: the trading-strategy row @ superbot 655e0fe still reads "ORDER 007 in flight; holdout SEALED" while the lane's own heartbeat (21:08:05Z @ d0fc23b) reads 007–008 DONE, holdout SPENT, kit v1.7.0 — the manifest was a full program-phase behind at probe time; (7) from PR #27: the gba-homebrew row @ superbot 655e0fe STILL records HEAD b607365/v1.6.0 at the 21:46Z read while real HEAD has been bc73da7 with a v1.7.0 tree — the row is two layers behind (manifest < lane heartbeat < lane tree), re-confirming the generated-roster-from-heartbeats case (fleet-manager ORDER 009). Cross-lane visibility: kit-lab's generated docs/adopters.md (bootstrap currency, its PR #133) is the freshest fleet-wide kit-version source — future kit-version grounding should cite it over manifest rows (PR #19 card 💡: freshest-wins citation rule, still unencoded — grooming round 3 seed; contract-grooming round 2 otherwise CONSUMED by #21, full mapping on .sessions/2026-07-10-grammar-grooming.md). Capability recipe (PR #27/#29, reused verbatim this slice — held): out-of-scope repo HEAD SHAs are transport-verifiable via `git ls-remote https://github.com/menno420/<repo>.git refs/heads/main` even where the MCP is repo-walled and anonymous api.github.com 403s — cheaper and stronger than raw-URL+date grounding. PANEL-MODE datapoint for PROPOSAL 001 recorded on .sessions/2026-07-10-explore-hub-probe.md (lens disagreement real; ≈127k subagent tokens) — sim-lab VERDICT 002 (approve-selectively) now answers that proposal's question; expect the manager to route any README probe-battery amendment. ALL 10 SECTIONS SEEDED (as of #19) — steady-state work is probes, harvests, grooming, and repo-internal tooling. Standing guard (PR #18 card 💡): `substrate-gate.yml` is KIT-OWNED — any `bootstrap upgrade` regenerating it must RE-APPLY the one preflight step (recipe on the card; durable fix = kit-native lane-local preflight seam, proposal-shaped — PR #19's `ideas/substrate-kit/host-checkers-one-gate-2026-07-10.md` captures the same graduation from the kit side; the seat-boot routing and this slice's capability-probe routing feed the same kit seam family). LIVE DRIFT FINDING (PR #22) CONSUMED by PR #26: superbot section re-pinned @ 655e0fe, check_harvest clean at PR #26 ship time; wake-time check_harvest is the standing staleness probe (both capability-pair canonical docs independently verified byte-identical at superbot HEAD 9624c53 this slice); a second-lane harvest is a one-line SECTIONS addition. LINT DEBT: CLEARED by PR #28 (bless-day sweep part a — live warn list 2 → 0; part-b follow-ups on the PR #28 card); this slice's fresh Grounding lines written in the blessed byte-form, keeping the merged-tree warn list at 0. Ripest next slices: concept-pick-bringup-pack probe (Sequence-tied to the open pick window — time-boxed like PR #25's head was), second-lane harvest, freshest-wins one-liner (grooming round 3), kit v1.7.0→v1.7.1 upgrade (re-apply the gate preflight step), check_harvest --emit-entries (PR #26 card 💡), cross-link state-echo lint (PR #29 card 💡 — target set GREW by 3 echo lines this slice), file-wide recommendation-vocabulary lint (this slice's card 💡).
