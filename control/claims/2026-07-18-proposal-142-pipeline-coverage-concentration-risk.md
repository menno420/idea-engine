# CLAIM — proposal-142-pipeline-coverage-concentration-risk (round-33 VENTURE slot, P142 → V155, +13)

- `claude/proposal-142-pipeline-coverage-concentration-risk` · **scope** — PROPOSAL 142 — round-33 VENTURE slot — pipeline coverage concentration risk (rotation fleet→venture→game→unrelated; P141 OPENED round-33 at the fleet slot, THIS takes the venture slot): the sales "pipeline coverage ratio" (expected pipeline value ÷ quota) is NOT a sufficient statistic for quota attainment. At a FIXED 3× coverage ratio and a FIXED per-deal win probability p, the probability of MISSING quota rises sharply with deal-size concentration. Two pipelines with identical coverage and identical win-rate — one granular (many small deals), one concentrated (a few whales) — have identical EXPECTED bookings, yet the concentrated pipeline misses quota far more often, because bookings variance is exactly proportional to the Herfindahl-Hirschman concentration index (HHI) of deal values: Var(B)=p(1−p)·V²·HHI, so at fixed p and V the variance ratio between two pipelines equals their HHI ratio (p(1−p) and V cancel). "3× coverage" says nothing about miss risk; concentration does. Idea doc + committed stdlib verifier (SEED=20260717, one random.seed(SEED) single stream, batch-means; three ordered gates G1 anchor-variance-law → G2 coverage-control → G3 head-miss-gap; G1/G2 |z|≤3 two-sided, G3 z≥3 one-sided; WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY sha256, pretty indent=2 stdout ≠ compact hashed preimage) + dry-run + outbox block (P142 → V155, +13) + heartbeat. Anchored to the Herfindahl–Hirschman concentration index + the Bernoulli/Binomial variance p(1−p) (portfolio / market-concentration + second-moment law). Distinct from every prior venture head — referral-bonus-value-trap, growth-cash-trough, retention-survivorship-mirage, discount-breakeven, freemium-support-cost-inversion, sales-ramp-capacity-drag, nrr-composition-mirage, partner-channel-margin-stacking, annual-prepay-financing-trap, blended-churn-ltv, usage-based-billing-variance-shock; this is about pipeline-coverage SUFFICIENCY and deal-CONCENTRATION miss-risk (coverage held equal, HHI is the differentiator), touched by none. VERDICT 155 (P142 → V155, +13) excluded — separate future slice; files: `ideas/venture-lab/pipeline-coverage-concentration-risk-2026-07-18.md` + `ideas/venture-lab/pipeline_coverage_concentration_risk.py` + `control/{outbox,status}.md` + `.sessions/2026-07-18-proposal-142-pipeline-coverage-concentration-risk.md` + this claim · 2026-07-18
