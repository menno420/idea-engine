# CLAIM — proposal-141-erlang-b-trunking-efficiency (round-33 FLEET-slot OPENER, P141 → V154, +13)

- `claude/proposal-141-erlang-b-trunking-efficiency` · **scope** — PROPOSAL 141 — round-33 FLEET slot — Erlang-B trunking efficiency (rotation fleet→venture→game→unrelated; OPENS round-33 on the proposal side after P137 fleet / P138 venture / P139 game / P140 unrelated CLOSED round-32): fragmenting a loss-system fabric into small independent pools at the SAME offered-load-per-server MULTIPLIES blocking. Model each pool as an M/M/c/c loss system (Poisson arrivals rate λ, exponential service rate μ, c servers, NO queue, blocked-calls-cleared); offered load a=λ/μ Erlangs; by PASTA the blocking probability is the exact Erlang-B loss formula B(c,a)=(a^c/c!)/Σ_{k=0}^{c}(a^k/k!), computed by the stable recursion B(0,a)=1, B(k,a)=a·B(k−1,a)/(k+a·B(k−1,a)). Counterintuitive headline: at a FIXED offered-load-per-server ρ=a/c, B(c,cρ) FALLS steeply as c grows (trunking efficiency) — at ρ=0.8, B(2,1.6)≈0.330, B(10,8)≈0.122, B(100,80)≈0.004 (~90× less blocking from 2 to 100 servers), so fragmenting one pooled 100-server fabric into 50 independent 2-server pools at the same ρ lifts the drop rate from ~0.4% to ~33%. Idea doc + committed stdlib verifier (SEED=20260717, one random.Random(SEED) drawn sequentially in the fixed config order, three ordered ≥3σ /se gates CFG_SMALL verifier-correctness → CFG_MED verifier-correctness → CFG_HEAD counterintuitive headline z≥3, whole-dict sha256) + dry-sim + outbox entry + heartbeat. Anchored to the classical Erlang-B loss formula for an M/M/c/c blocked-calls-cleared system (teletraffic / queueing). Distinct from every prior queueing head — service-variance-wait-tax (P137, M/G/1 Pollaczek-Khinchine WAIT inflation from service-time VARIANCE, single-server DELAY), checkout-pooling-single-line (M/M/c Erlang-C DELAY/queue, expected WAIT), variance-blind-provisioning-trap (provisioning under demand variance), two-choices-routing-cliff (P113, load-balancing max-load / balls-in-bins placement imbalance); this is a multi-server LOSS system (no queue, calls DROPPED), the object is the BLOCKING probability B(c,a) and its economy-of-scale in pool size. VERDICT 154 (P141 → V154, +13) excluded — separate future slice; files: `ideas/fleet/erlang-b-trunking-efficiency-2026-07-18.md` + `ideas/fleet/erlang_b_trunking_efficiency.py` + `control/{outbox,status}.md` + `.sessions/2026-07-18-proposal-141-erlang-b-trunking-efficiency.md` + this claim · 2026-07-18
