# CLAIM — proposal-117-checkpoint-interval-optimum (round-27 FLEET-slot opener, P117 → V130, +13)

- `claude/proposal-117-checkpoint-interval-optimum` · **scope** — round-27 FLEET-domain PROPOSAL 117 (the checkpoint-interval optimum — fault-tolerant computing / long-running-job checkpoint scheduling · the Young–Daly optimal checkpoint interval: a long fleet job that can crash and restart from its last checkpoint faces two competing wastes — checkpoint OVERHEAD (~C/τ per unit work) and lost RECOMPUTE work on a crash (~τ/2M per unit work at MTBF M) — so total wasted time is U-SHAPED in the checkpoint interval τ with a unique INTERIOR optimum τ*=√(2·C·M) (Young 1974 first-order / Daly 2006 higher-order) and minimum overhead √(2C/M); "checkpoint as often as you can afford" is WRONG — over-checkpointing wastes strictly MORE, exactly as wastefully (leading order) as the symmetric under-checkpointing, and the optimal interval grows only as the SQUARE ROOT of the MTBF (a 4× more reliable machine warrants only a 2× longer interval and halves the achievable overhead); verified by a stdlib Monte Carlo (segment-by-segment Poisson-failure checkpoint/restart) with ≥3σ gates on the /se margin against the EXACT Daly closed form O(τ)=(M/τ)(e^{(τ+C)/M}−1)−1, plus an over-checkpointing headline (τ*/8 wastes more by ≥3σ), a U-shape control (τ*·8 also wastes more by ≥3σ), and a square-root-law control (quadrupling the MTBF at the rescaled optimum τ*(4M)=2τ* halves the optimum overhead by ≥3σ) — proving the U-shaped optimum and its √M scaling, not a monotone "more is safer"; FLEET-slot pure-mechanism head, distinct from the prior fleet heads P113 two-choices routing cliff (balanced-allocations max load), P109 correlated-fleet variance floor (error aggregation), P105 delegation loop collapse, P101 winner's-curse auction, and the metastable-retry-storm-collapse head — no prior head models the checkpoint-frequency vs wasted-work tradeoff): idea doc + committed stdlib verifier + dry-sim + outbox entry + heartbeat. VERDICT 130 (P117 → V130, +13) excluded — separate future slice; files: `ideas/fleet/checkpoint-interval-optimum-2026-07-18.md` + `ideas/fleet/checkpoint_interval_optimum.py` + `control/{outbox,status}.md` + `.sessions/2026-07-18-proposal-117-checkpoint-interval-optimum.md` + this claim · 2026-07-18
