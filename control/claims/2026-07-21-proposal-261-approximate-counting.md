# claim — PROPOSAL 261 · Morris's approximate counting (base-2 Morris counter): an exactly unbiased streaming counter with exact variance

- `claude/proposal-261-approximate-counting` · **scope** — author fleet PROPOSAL 261 (Morris's approximate counting algorithm: a base-2 Morris counter `C` starts at 0; to process one increment draw `u ~ Uniform(0,1)` and set `C := C+1` iff `u < 2^{−C}`, else leave `C` unchanged; after `n` increments the estimator is `N̂ = 2^{C_n} − 1`. It is EXACTLY unbiased — `E[2^{C_n}] = n+1`, hence `E[N̂] = n` — with EXACT variance `Var[N̂] = Var[2^{C_n}] = n(n−1)/2`, from `E[4^{C_n}] = 1 + 3n(n+1)/2`) → append `## PROPOSAL 261` block to control/outbox.md; files: ideas/fleet/approximate-counting-2026-07-21.md, ideas/fleet/verify_261_approximate_counting.py, control/outbox.md, .sessions/2026-07-21-approximate-counting.md, control/claims/2026-07-21-proposal-261-approximate-counting.md · 2026-07-21

Notes: born-red card (Status: in-progress) holds the PR red behind the substrate-gate until the deliberate flip; SEED=20260721, results_sha256=ade24bf035bcc213d144494660998ed207ddae62b00e0a36879643b5a98239e6 (in-process double-run + --selfcheck + separate re-invocation byte-identical); verifier file sha256 fdc06e07bbce31a877e56e00ddc78cb5232142656878a7fd1b1a09b82765f71e; all four gates PASS each in its own direction (G1 EXACT fractions.Fraction — full-pmf rational DP over n=0..80, per-n assert E[2^C]==n+1, E[N̂]==n, E[4^C]==1+3n(n+1)/2, Var[N̂]==n(n−1)/2, and Σ pmf==1, checked=405, mismatches=0, z="exact"; G2 MC AGREEMENT — n=63, M=300000 iid replicas, mean=62.945547, honest iid SE=0.080218, z=−0.6788, sample variance 1930.5012 within 0.9885× the exact 1953; G3 INVARIANCE — martingale increment E[2^{C_{n+1}}]−E[2^{C_n}]==1 over n=0..79, martingale_violations=0, plus a second scale n=31, M=300000, z2=−1.0545; G4 FALSIFIABILITY — same n=63 sample, primary foil "forgot the −1" est=2^C rejected at z_foil=11.7871, secondary foil raw counter est=C rejected at z=−36771.5913, true estimator accepted at z=−0.6788). all_gates_pass=true, first_failing_gate=null, decision=PASS. Grounding pinned to Wikipedia "Approximate counting algorithm" oldid 1356953469 (raw-wikitext sha1 f2de0cd8da297f8e039b8d8f57128382529490a3, 5705 bytes; API revisions.sha1 == self-computed hashlib.sha1, exact match; the Morris-counter page, exists, not a redirect). Quoted-vs-derived split accurate both directions: QUOTED (grep>0) the increment probability 2^{−c}, the 2^c estimator, the qualitative unbiasedness, and the Morris/Bell Labs 1977/Flajolet history; DERIVED (grep-0) the exact E[2^C]=n+1, the −1 correction N̂=2^C−1 (the page's "Approximation" column uses raw 2^c and calls it "unbiased" loosely — disclosed as the G4 primary foil), the exact Var=n(n−1)/2, the rational-DP pmf, the martingale invariance, all z-values, SEED, and the digest. Disclosed token seam: the only `morris` token elsewhere in the repos is the unrelated Efron-Morris shrinkage estimator (P128); the head is anchored on "approximate counting" (grep-0 in both repos). P261 → V274 (+13 offset). Does not touch control/status.md; authors no VERDICT block and no high-water advance from this worker. Distinct from every shipped math/probabilistic head — Wilson P260, Stirling P256, Gaussian √π P252, Cantor set P248, open-addressing hashing P245, Buffon P244, coprime density 6/π² P232 — a randomized streaming/sketch log-counter and its exact first two moments, not a limit/count/game object.
