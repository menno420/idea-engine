# CLAIM — proposal-137-service-variance-wait-tax (round-32 FLEET-slot proposal, P137 → V150, +13)

- `claude/proposal-137-service-variance-wait-tax` · **scope** — P137 round-32 FLEET-slot OPENER (rotation unrelated→fleet, P137 opens round-32 at the fleet slot): the service-variance wait tax — for an M/G/1 FIFO queue the Pollaczek–Khinchine mean-value law gives mean queue wait W_q = (ρ/(1−ρ))·E[S]·(1+C²)/2, where C²=Var(S)/E[S]² is the squared coefficient of variation of SERVICE time, so at FIXED utilization ρ=0.8 and FIXED mean service E[S]=1.0 the wait depends ONLY on service-time variance: W_q = 2.0 (M/D/1, C²=0) / 4.0 (M/M/1, C²=1) / 10.0 (M/H2/1, C²=4) — a 1:2:5 spread, 5× the queue wait at the same load and same average job, invisible to any utilization/throughput dashboard because the (1+C²)/2 tax is a factor capacity math never sees. Idea doc + committed stdlib verifier (SEED=20260717, exact Lindley recursion + method of independent replications, three ordered ≥3σ /se gates G1→G2→G3, whole-dict sha256) + dry-sim + outbox entry + heartbeat. Distinct from P089 variance-blind-provisioning-trap (that prices SLA-violation rate + the (1+CV²) CAPACITY-provisioning correction across two lanes; this prices the pure P-K queue-WAIT LAW/tax itself across D/M/H2 at fixed ρ & mean), from inspection-paradox-wait-inflation (observer/size-biased sampling), correlated-fleet-variance-floor (N_eff aggregation), and checkout-pooling-single-line (M/M/c pooling). VERDICT 150 (P137 → V150, +13) excluded — separate future slice; files: `ideas/fleet/service-variance-wait-tax-2026-07-18.md` + `ideas/fleet/service_variance_wait_tax.py` + `control/{outbox,status}.md` + `.sessions/2026-07-18-proposal-137-service-variance-wait-tax.md` + this claim · 2026-07-18T15:07:28Z
