# CLAIM — proposal-121-hedged-request-tail-cure (round-28 FLEET-slot opener, P121 → V134, +13)

- `claude/proposal-121-hedged-request-tail-cure` · **scope** — round-28 FLEET-domain PROPOSAL 121 (the hedged-request tail cure — distributed-systems tail-latency theory / the "tail at scale": a request served by one replica has a heavy-tailed latency whose 99th percentile is dominated by occasional stragglers. The folk fix is "to kill the tail you must duplicate every request (2× load)"; the counterintuitive result is that DEFERRING the second (hedged) request to the p95 delay — firing it only on the ~5% of requests whose primary exceeds the 95th percentile — recovers the large majority of full duplication's tail collapse at ≈5% extra load, not 100%. The tail cure is almost free: response = min(primary, d + secondary) with d = p95; hedge-fire load = P(S > p95) ≈ 0.05; the last sliver of tail benefit that full duplication adds beyond the p95 hedge costs ~20× the load. Verified by a stdlib Monte Carlo (SEED=20260717, service time a two-component straggler mixture MEAN_FAST=10 / MEAN_SLOW=200 / P_STRAGGLER=0.05, N_REQUESTS=20000 × TRIALS=200) with the /se z convention and ≥3σ gates: G1 hedge collapses the tail (mean p99_base − p99_hedge > 0 by ≥3σ), G2 the knee (capture ratio (p99_base−p99_hedge)/(p99_base−p99_dup) ≥ 0.80 by ≥3σ AND hedge-fire load ≤ 0.08 while full-dup costs +100%), G3 almost-free efficiency (tail-reduction-per-unit-extra-load of the p95 hedge strictly exceeds full duplication's by ≥3σ); distinct from the prior fleet load/queueing heads P113 two-choices routing cliff (balanced-allocations max-load), P109 correlated-fleet variance floor, variance-blind-provisioning-trap (M/M/1 utilization), metastable-retry-storm-collapse (bistable retry overload), long-chain-flexibility, and checkpoint-interval-optimum — a tail-latency redundancy-vs-load object, not a max-load / utilization / retry-stability / flexibility / checkpoint object): idea doc + committed stdlib verifier + dry-sim + outbox entry + heartbeat. VERDICT 134 (P121 → V134, +13) excluded — separate future slice; files: `ideas/fleet/hedged-request-tail-cure-2026-07-18.md` + `ideas/fleet/hedged_request_tail_cure.py` + `control/{outbox,status}.md` + `.sessions/2026-07-18-proposal-121-hedged-request-tail-cure.md` + this claim · 2026-07-18
