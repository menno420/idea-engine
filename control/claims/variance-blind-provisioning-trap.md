# Claim — PROPOSAL 089 draft + land (round-19 fleet-backlogs opener: variance-blind provisioning trap) (2026-07-16)

- `claude/variance-blind-provisioning-trap` · **draft+land PROPOSAL 089 — round-19 fleet-backlogs OPENER (variance-blind provisioning trap)** — the round-19 fleet-backlogs OPENER slot (standing ORDER 003 · rotation per ORDER 004 rule 3), a fleet-ops M/G/1 queueing head: provisioning fleet-lane capacity from MEAN task cost systematically under-serves HIGH-VARIANCE lanes — two single-server M/G/1 lanes with IDENTICAL arrival rate λ=0.8, IDENTICAL mean service E[S]=1.0, and therefore IDENTICAL nominal utilization ρ=0.8 have SLA-violation rates differing by 3.67× purely from service-time variance (lane A exponential CV=1, lane B hyperexponential CV=3), because mean queue wait scales with the squared coefficient of variation (Pollaczek–Khinchine, Wq=ρ/(1−ρ)·(1+CV²)/2·E[S]); the gap closes only when the lane is provisioned by (1+CV²), not the mean. Four pre-registered gates: R1 the trap (viol_B ≥ 2.5× viol_A, ≥3σ), R2 world sanity (measured Wq within ±5% of the P–K closed form both lanes — failure ⇒ INVALID), R3 dose-response (SLA-viol ratio B/A monotone in CV_B, crosses 2× at CV≈1.51), R4 provisioning correction (re-provision B to the P–K-guided SLA-parity ρ_B*=0.512 closes the gap within 3σ AND requires ρ_B* ≪ ρ_A). Calibrated on a self-contained stdlib dry-sim (exact single-server Lindley recursion, N=200000/lane/rep, warmup=20000, R=12 seeds 1001..1012; run twice → byte-identical; NOT committed per the P084/P086/P087/P088 disclose-inline precedent). SEEDLESS — seeds 1001..1012 are in-file reporting constants, next-free block 20261730 untouched. This repo edits ONLY itself (Q-0260); the sim is drafted here and re-derived sim-lab-side → VERDICT 102 (+13, P089 → V102). Distinct from P044 (checkout-pooling: server POOLING at LOW variance on a discrete frame where P–K does NOT bind) and P085 (RR-vs-LQF: scheduling POLICY, not provisioning). Expected files: `ideas/fleet/variance-blind-provisioning-trap-2026-07-16.md`, `control/outbox.md` (PROPOSAL 089 append), `control/status.md` (heartbeat), `.sessions/2026-07-16-variance-blind-provisioning-trap.md` (card), `control/claims/variance-blind-provisioning-trap.md` (this claim) · 2026-07-16T22:04:51Z
