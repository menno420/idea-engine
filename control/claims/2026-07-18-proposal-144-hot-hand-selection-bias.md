# CLAIM — proposal-144-hot-hand-selection-bias (round-33 UNRELATED slot, P144 → V157, +13)

- `claude/proposal-144-hot-hand-selection-bias` · **scope** — PROPOSAL 144 — round-33 UNRELATED-domain slot (rotation fleet→venture→game→unrelated; P141 opened round-33 at the fleet slot, P142 took venture, P143 took game, THIS takes the unrelated slot): the hot-hand streak-selection bias (Miller & Sanjurjo, Econometrica 2018). In a FINITE sequence of N fair coin flips, select every flip that immediately follows a run of k consecutive heads and measure the fraction of those selected flips that are heads; the EXPECTED value of that per-sequence fraction is strictly BELOW 0.5 — not 0.5 — even though the coin is fair and memoryless, because conditioning on "the previous k flips were heads" and averaging the RATIO across finite sequences induces a selection/sampling bias (a within-sequence negative dependence between selecting a flip and that flip being heads). The counterintuitive part: a memoryless fair process, measured with the natural "what happens after a streak?" estimator, looks anti-streaky. Idea doc + committed stdlib-only verifier (SEED=20260717, N fair-coin flips × M Monte-Carlo sequences, streak length K; EXACT finite-sequence expectation computed by exhaustive enumeration of all 2^N sequences as the closed-form reference; three ordered gates G1 estimator-agreement (two independent halves of the M per-sequence fractions do NOT significantly disagree, |z|<3) → G2 closed-form agreement (the MC mean is NOT significantly different from the exact enumerated Miller–Sanjurjo expectation, |z|<3) → G3 head (the MC mean is SIGNIFICANTLY below 0.5, z=(0.5−mean)/se ≥ 3); WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY sha256, every float rounded to 6 dp, pretty indent=2 stdout dump distinct from the compact canonical hashed preimage — the P127+ TWIST) + deterministic double-run + outbox block (P144 → V157, +13) + heartbeat. Anchored to Miller & Sanjurjo, "Surprised by the Hot Hand Fallacy? A Truth in the Law of Small Numbers", Econometrica 86(6):2019-2047, 2018 (https://en.wikipedia.org/wiki/Hot_hand). Distinct from every prior fleet/unrelated head — a finite-sample selection-bias result on i.i.d. fair coin flips, NOT any queueing, order-statistics, optimal-stopping, congestion, or estimator-shrinkage head; the hot-hand was a named runner-up dropped on merit in the secretary-rule-cardinal-regret card and has NEVER been proposed. VERDICT 157 (P144 → V157, +13) excluded — separate future slice; files: `ideas/fleet/hot-hand-streak-selection-bias-2026-07-18.md` + `ideas/fleet/hot_hand_streak_selection_bias.py` + `control/{outbox,status}.md` + `.sessions/2026-07-18-proposal-144-hot-hand-selection-bias.md` + this claim · 2026-07-18
