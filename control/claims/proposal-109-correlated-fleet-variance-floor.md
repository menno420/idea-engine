# CLAIM — proposal-109-correlated-fleet-variance-floor (round-25 FLEET-slot opener, P109 → V122, +13)

- `claude/proposal-109-correlated-fleet-variance-floor` · **scope** — round-25 FLEET-domain PROPOSAL 109 (the correlated-fleet variance floor — multi-agent estimation / wisdom-of-crowds economics: a fleet of N agents each estimating a common truth with a shared common-mode error component (equicorrelation rho) has an aggregate mean-squared error that FLOORS at rho·sigma^2 regardless of N, so the effective independent count saturates at N_eff = sigma^2/MSE → 1/rho — a fleet of 400 correlated estimators is worth only ~4 independent ones, and ~99% of the variance reduction independence predicts never materializes; verified by a stdlib Gaussian Monte Carlo with >=3σ gates on the /se margin against the exact equicorrelation variance rho·sigma^2+(1−rho)·sigma^2/N, with a rho=0 control that collapses the floor to sigma^2/N; FLEET-domain opener, distinct from prior fleet slots P101 (common-value auction winner's curse), P105 (liquid-democracy random mapping), P064 (sequential information cascades)): idea doc + committed stdlib verifier + dry-sim + outbox entry + heartbeat. VERDICT 122 (P109 → V122, +13) excluded — separate future slice; files: `ideas/fleet/correlated-fleet-variance-floor-2026-07-18.md` + `ideas/fleet/correlated_fleet_variance_floor.py` + `control/{outbox,status}.md` + `.sessions/2026-07-18-proposal-109-correlated-fleet-variance-floor.md` + this claim · 2026-07-18
