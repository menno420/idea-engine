# CLAIM — proposal-120-german-tank-mvue (round-27 UNRELATED-slot closer, P120 → V133, +13)

- `claude/proposal-120-german-tank-mvue` · **scope** — round-27 UNRELATED-domain PROPOSAL 120 (the German-tank estimator — statistical estimation theory / discrete-uniform population-size inference: you observe k distinct serial numbers drawn without replacement from an unknown fleet numbered 1..N and must estimate N. The naive answer "N = the largest serial you have seen" (the sample maximum m) is systematically too LOW — E[m] = k(N+1)/(k+1) < N — because you almost never sample the very top of the range. The minimum-variance unbiased estimator EXTRAPOLATES ABOVE the largest observed serial: N̂ = m(1 + 1/k) − 1 = m + (m−k)/k = "sample max + the average gap between samples", which is unbiased (E[N̂] = N exactly) and has the exact variance Var(N̂) = (N−k)(N+1)/(k(k+2)); the alternative unbiased "twice-the-mean" estimator 2·x̄ − 1 is also unbiased but strictly LESS efficient — its variance is (k+2)/3× larger. Verified by a stdlib Monte Carlo (SEED=20260717, sampling k=5 distinct serials from N=1000 without replacement across 200 replications × 4000 samples) with the /se z convention and ≥3σ gates: G1 MVUE unbiased (measured mean N̂ within 3σ of 1000), G2 sample-max biased low + MVUE lands ABOVE the max (mean m ≈ 834 below N by ≥3σ, mean N̂ − mean m ≈ 166 above the observed max by ≥3σ), G3 closed-form variance anchor MATCH (empirical Var(N̂) reproduces (N−k)(N+1)/(k(k+2)) within 3σ) + efficiency (Var(2x̄−1) − Var(N̂) > 0 by ≥3σ); distinct from ALL prior unrelated closers P100 Kelly-overbet / P104 epidemic overshoot / P108 regression-to-mean / P112 series-MTBF collapse / P116 arcsine lead illusion, and from the coupon-collector-tail / variance-blind-provisioning / inspection-paradox / secretary-rule heads — a point-estimation bias-and-efficiency object, not a sojourn distribution, completion-cost, waiting-time, or optimal-stopping object): idea doc + committed stdlib verifier + dry-sim + outbox entry + heartbeat. VERDICT 133 (P120 → V133, +13) excluded — separate future slice; files: `ideas/fleet/german-tank-mvue-2026-07-18.md` + `ideas/fleet/german_tank_mvue.py` + `control/{outbox,status}.md` + `.sessions/2026-07-18-proposal-120-german-tank-mvue.md` + this claim · 2026-07-18
