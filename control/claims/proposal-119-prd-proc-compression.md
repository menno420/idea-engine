# CLAIM — proposal-119-prd-proc-compression (round-27 GAME-slot proposal, P119 → V132, +13)

- `claude/proposal-119-prd-proc-compression` · **scope** — round-27 GAME-domain PROPOSAL 119 (Pseudo-Random Distribution (PRD) proc-rate compression — the Warcraft III / Dota 2 anti-streak proc system: proc chance starts low and rises by a constant increment C on each consecutive failure (P(proc on the n-th attempt since last proc)=min(1,C·n)), resetting on a success — bounding bad-luck streaks. The counterintuitive trap: the increment C is NOT the proc rate; a designer who wants "25% crit" and naively sets C=0.25 gets an EFFECTIVE long-run rate ~0.45 — nearly DOUBLE intended — because the escalating chance forces frequent early procs (effective rate=1/E[N], E[N]=2.21875 for C=0.25). The fix: SOLVE C for the target rate (C≈0.0847 yields effective 0.25), never set C=nominal. Verified by a stdlib Monte Carlo (2M-attempt seeded streams, SEED=20260717) with ≥3σ gates on the binomial-proportion se: G1 naive-C overshoot (measured effective 0.450684 ≥0.40, z_vs_null=655σ), G2 solve-C-fixes-it (solved C≈0.084744 yields measured 0.24998 within 3σ of 0.25), G3 closed-form anchor 1/E[N]=0.450704 matched + anti-streak bound (PRD max miss-streak 3 vs true-random 51); distinct from prior game slots P099 shop-reroll optimal-stopping / P103 streak-shield regressivity / P107 energy-cap overflow / P111 matchmaking win-rate mirage / P115 compounding-reward inequality / P091 gacha-pity / P095 rubber-band PID): idea doc + committed stdlib verifier + dry-sim + outbox entry + heartbeat. VERDICT 132 (P119 → V132, +13) excluded — separate future slice; files: `ideas/superbot-games/prd-proc-compression-2026-07-18.md` + `ideas/superbot-games/prd_proc_compression.py` + `control/{outbox,status}.md` + `.sessions/2026-07-18-proposal-119-prd-proc-compression.md` + this claim · 2026-07-18
