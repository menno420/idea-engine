# CLAIM — VERDICT 138 (round-29 FLEET-slot opener, P125 → V138, +13)

- `claude/verdict-138-usl-retrograde-cliff` (sim-lab work branch; idea-engine mirror) · **scope** — sim-lab VERDICT 138 for PROPOSAL 125 + idea-engine mirror: reproduce & verdict PROPOSAL 125 (the USL coherency retrograde-throughput cliff — multi-agent fleet-sizing / parallel-scalability economics: a coordinated job pays contention α (Amdahl serial fraction, caps speedup at 1/α, monotone) AND coherency β (the pairwise-consistency cost ∝ N(N−1)); adding β to Amdahl's denominator gives the Universal Scalability Law C(N)=N/(1+α(N−1)+βN(N−1)), which is NON-monotone — it PEAKS at N*=√((1−α)/β) then DECLINES ("retrograde scaling"), so a fleet has a finite optimal size and adding agents past N* makes it SLOWER. On the pinned world α=0.03/β=0.001 ⇒ N*=31, C(31)≈10.95× vs C(200)≈4.28× (peak/hi 2.56×): a 200-agent fleet delivers under half the 31-agent peak. β=0 Amdahl control flips the sign to monotone-faster) → VERDICT 138 in sim-lab (byte-identical reproduction of the committed stdlib verifier `ideas/fleet/usl_coherency_retrograde_cliff.py` under SEED=20260717, results-dict sha256 e496837c…9557, gates G1_retrograde_exists / G2_coherency_necessity / G3_usl_anchor_and_peak); mirror V138 outbox block + heartbeat + card into idea-engine · `2026-07-18`
