# CLAIM — proposal-123-snipe-clearing-leak (round-28 GAME-slot proposal, P123 → V136, +13)

- `claude/proposal-123-snipe-clearing-leak` · **scope** — round-28 GAME-domain PROPOSAL 123 (the snipe clearing leak — game auction-house economics / market microstructure: a hard-close (fixed-deadline) game auction clears at a second-price over only the bids that TRANSMIT, but last-instant sealed snipes fail to transmit with probability q (server-tick / latency / rate limits), thinning the effective field so the second-price clear lands BELOW the true competitive second-highest valuation v_(2); a soft-close (anti-snipe / auto-extend) auction lets the full field respond and clears at v_(2), so the anti-snipe timer is an ECONOMIC mechanism (a seller→best-connection value transfer + an AH-cut gold-sink leak that scales with q), not UX politeness. On the pinned world K=8 bidders, valuations iid Uniform(0,1), the soft-close clears at E[v_(2)]=(K−1)/(K+1) while the hard-close clears at a transmitted-subset second price, so leak=P_soft−P_hard≥0, rising in q. Verified by a stdlib paired common-random-numbers Monte Carlo with ≥3σ gates on the /se margin — G1 existence leak>0 @q*=0.15 z≥3, G2 dose-response leak(0.25)>leak(0.05) z≥3 (paired coupled draws), G3 specificity max|placebo leak @q=0|==0.0 exactly, plus a binomial-mixture closed-form E_hard anchor; game-slot, distinct from prior game cards P099 shop-reroll-ruin / P103 streak-shield / P107 energy-cap / P111 matchmaking-winrate-mirage / P115 compounding-reward-inequality / P119 prd-proc-compression / P091 gacha-pity / P095 rubber-band — the FIRST auction / market-microstructure game card): idea doc + committed stdlib verifier + dry-sim + outbox entry + heartbeat. VERDICT 136 (P123 → V136, +13) excluded — separate future slice; files: `ideas/superbot-games/snipe-clearing-leak-2026-07-18.md` + `ideas/superbot-games/snipe_clearing_leak.py` + `control/{outbox,status}.md` + `.sessions/2026-07-18-proposal-123-snipe-clearing-leak.md` + this claim · 2026-07-18
