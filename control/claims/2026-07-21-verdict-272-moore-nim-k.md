# claim — VERDICT 272 · Moore's Nim (Nim_k) P-position criterion (columns mod k+1), APPROVE

- `claude/verdict-272-moore-nim-k` · **scope** — VERDICT 272 adjudication of PROPOSAL 259 (Moore's Nim (Nim_k) under normal play, the multi-heap Nim generalisation analysed by E. H. Moore (1910): a move selects between 1 and k heaps inclusive and removes a positive number of tokens from EACH selected heap; writing each heap in binary and letting c_j = Σ_i bit_j(a_i) be the column-sum of bit j across all heaps, a position is a P-position (the player to move loses) IFF c_j ≡ 0 (mod k+1) for EVERY bit-position j, generalising the ordinary-Nim nim-sum (XOR==0, the k=1 case) from mod 2 to mod (k+1); for k=1 the modulus k+1=2 collapses exactly to Bouton's ordinary Nim) → append `## VERDICT 272` block to control/outbox.md + advance control/status.md verdict high-water V271 -> V272 (proposal high-water P258 -> P259, union-max, no regress); files: control/outbox.md, control/status.md, control/claims/2026-07-21-verdict-272-moore-nim-k.md · 2026-07-21T15:19:04Z

Notes: control-plane-only diff (control fast lane, no session card); ruling APPROVE — verified firsthand with a fully independent memoized P/N outcome oracle, every claim reproduced with zero discrepancy. Verifier ideas/superbot-games/verify_259_moore_nim_k.py (file sha256 e8e117d135dc4875744d060a764f920840957a6bf15ca3efe0b86901f9e03969, stdlib only, diff copy-vs-original exit 0, unmodified), results_sha256=da147d54d970d71754baa2994189c9e5f73cfbbf1ad36c7b2366f9ddf870402d reproduced character-for-character (SEED=20260717) deterministic three ways (in-process double-run + --selfcheck + separate fresh re-invocation). Four gates PASS each in own direction (G1 EXACT — own from-scratch memoized P/N outcome oracle over sorted-tuple canonical states (terminal all-zero = P; N iff some legal move reaches a P-position), NOT reusing the proposal's functions, exhaustively enumerated ALL positions for k∈{1,2,3} (k=1:H=8→4680, k=2:H=6→1554, k=3:H=5→780) and asserted moore_predicate == (oracle is P) for every one of 7014 positions, mismatches=0 across three moduli {2,3,4}, the k=1 leg == XOR==0 with 0 violations, anchors (3,5,7)|k1→N, (1,1,1)|k2→P, (2,2,2)|k2→P, (7,7,7)|k2→P, (1,1)|k2→N, (1,2,3)|k3→N all agree; G2 MC AGREEMENT — (k,m,b)=(2,5,6), independently re-derived exact P-fraction f=(11/32)^6 = 1771561/1073741824 ≈ 0.001650, N=200000 iid draws p̂=0.001765, z=1.268354 [Z_ACCEPT=3.0], plain iid SE honest; G3 INVARIANCE/ROBUSTNESS — k=1 collapse moore_predicate(heaps,1) == (xor_all(heaps)==0) collapse_violations=0, plus permutation + zero-pad invariance of BOTH the predicate and my own oracle over 2000 positions, perm_violations=0, pad_violations=0; G4 FALSIFIABILITY — the ordinary-Nim (mod 2) foil applied to k=2 (true modulus 3) misclassifies against the TRUE outcome oracle at ê=0.345800, z_foil=51.409363 [|z|>3 Z_REJECT=3.0], while the true predicate makes theorem_errors=0 on the same sample). Grounding pinned to Wikipedia "Nim" oldid 1362772636 (raw-wikitext sha1 750a8fd6ead1d0b082596aab4f84d9d4bd49da78, 30663 bytes; self-computed hashlib.sha1 == pinned sha1). Quoted strings all literally present (grep>0): the name/attribution "'nim_k' or 'index-k' nim by E. H. Moore … who analyzed it in 1910", the move rule "at least one but up to k different heaps", the column-sum criterion and modulus "the sum of each binary digit modulo k + 1", the two-directional P/N characterisation, and the Bouton k=1 / ordinary-Nim collapse; the firsthand contribution (the independent memoized P/N oracle, the exhaustive equivalence proof over 7014 positions, f=(11/32)^6, every z-value, the ordinary-Nim foil z_foil=51.41, SEED, digest) grep-0 and correctly labeled derived. Disclosed seams: variable relabel — the article uses k for the number of heaps a move may touch (matching our k) and separately r for an OPTIONAL per-heap removal cap; we take the ARBITRARY-removal variant with NO r term, exactly the article's "either arbitrary" option; "P-position IFF every column-sum ≡ 0 (mod k+1)" is the equivalent P-position restatement of the article's "make the sum zero for every digit"; "column-sum" is our term for the article's "sum of each binary digit" — disclosed; no mislabels, no flags. P259 → V272 (+13 offset). Lands via native merge-on-green.
