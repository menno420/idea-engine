# Claim — enabler claude/ prefix allowlist fix

- `fix/enabler-claude-prefix-allowlist` · **enabler-allowlist fix** — add `claude/` to the auto-merge-enabler head-branch allowlist (PR #271 jam, enabler runs 264-266 skipped) · `.github/workflows/auto-merge-enabler.yml` + `.sessions/` card · 2026-07-12
