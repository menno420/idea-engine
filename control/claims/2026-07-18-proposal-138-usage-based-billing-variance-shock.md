# CLAIM — proposal-138-usage-based-billing-variance-shock (round-32 VENTURE-slot proposal, P138 → V151, +13)

- `claude/proposal-138-usage-based-billing-variance-shock` · **scope** — P138 round-32 VENTURE slot (rotation fleet→venture after P137 opened round-32 at the fleet slot): the usage-based billing variance shock — under consumption pricing monthly revenue R = Σ Uᵢ is a sum over N independent accounts, so the law-of-large-numbers folk model predicts CV(R) = CV_account/√N. FALSE under heterogeneous account SIZES: independent-but-unequal accounts add in variance weighted by size², giving CV(R) = CV_account·√HHI with HHI = Σwᵢ² (wᵢ = mᵢ/Σm) the Herfindahl concentration index and N_eff = 1/HHI the EFFECTIVE account count, NOT N. With Zipf sizes (mᵢ = 1/i, i=1..400) a 400-account book has N_eff ≈ 26 — revenue ~4× more volatile than CV_account/√N predicts, a shock the LLN hides because size concentration (Cauchy–Schwarz forces HHI ≥ 1/N) not customer count sets the variance floor. Idea doc + committed stdlib verifier (SEED=20260717, deterministic size vectors, batch-means SE, three ordered ≥3σ /se gates G1→G2→G3, whole-dict sha256) + dry-sim + outbox entry + heartbeat. Distinct from P109 correlated-fleet-variance-floor (that floor is CORRELATION-driven: ρσ² as N→∞ on identical units; this is SIZE HETEROGENEITY on independent accounts), P134 blended-churn LTV understatement (Jensen convexity of 1/c in a POINT estimate; this is a second-moment RISK/variance result whose point mean is unbiased), and P122 NRR composition mirage. VERDICT 151 (P138 → V151, +13) excluded — separate future slice; files: `ideas/venture-lab/usage-based-billing-variance-shock-2026-07-18.md` + `ideas/venture-lab/usage_based_billing_variance_shock.py` + `control/{outbox,status}.md` + `.sessions/2026-07-18-proposal-138-usage-based-billing-variance-shock.md` + this claim · 2026-07-18
