# Claim — PROPOSAL 112 (round-25 UNRELATED-slot closer, P112 -> V125, +13)

- `claude/proposal-112-series-reliability-collapse` (idea-engine work branch) · **scope** — draft & land PROPOSAL 112 (the series-reliability MTBF collapse: a series system fails at its first component failure, so its lifetime is the min of N per-component lifetimes and its MTBF collapses to T/N while parallel redundancy buys only logarithmic H_N) → fan to sim-lab as VERDICT 125 (+13); reliability-engineering pure-mechanism head, fleet-external · `2026-07-18`
