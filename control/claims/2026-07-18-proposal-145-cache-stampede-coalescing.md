# CLAIM — proposal-145-cache-stampede-coalescing (round-34 FLEET slot, P145 → V158, +13)

- `claude/proposal-145-cache-stampede-coalescing` · **scope** — PROPOSAL 145 — round-34 FLEET-domain slot OPENER (rotation fleet→venture→game→unrelated; round-33 closed with P141 fleet / P142 venture / P143 game / P144 unrelated, so round-34 opens at the FLEET slot): the cache-stampede / request-coalescing herd law. When a HOT cached key expires it is stale for one recompute window T, and under a plain TTL cache WITHOUT coordination every request arriving in [0,T) independently recomputes it at the origin, so the per-expiry origin herd is exactly Poisson(λT) — expected size λT, which GROWS LINEARLY with request rate (a hotter key stampedes harder, not softer; the Wikipedia example verbatim: 10 req/s × 3 s render = 30 simultaneous recomputes). Request coalescing (single-flight / dogpile lock) lets only the FIRST in-window request recompute and makes the rest wait, collapsing the herd to exactly 1 (E = 1−e^{−λT} ≈ 1) — INDEPENDENT of λ; the stampede cure is coalescing, not a longer TTL (which leaves herd size λT untouched). Idea doc + committed stdlib-only verifier (SEED=20260717, T_RECOMPUTE=1.0, SIGMA_GATE=3.0, three configs CFG_LOW λ=5 / CFG_HIGH λ=30 / CFG_HEAD λ=30 coalesced, R=4000 each; per-expiry Poisson(λT) arrival draw via successive Exp(λ); three ordered gates G1 CFG_LOW naive herd matches exact Poisson mean λT=5 |z|<3 → G2 CFG_HIGH naive herd matches exact Poisson mean λT=30 |z|<3 (herd scales 6× with 6× rate) → G3 head coalescing paired amplification (naive−coalesced) z=(mean−0)/se ≥ 3; WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY sha256, every float rounded to 6 dp, pretty indent=2 stdout dump distinct from the compact canonical hashed preimage — the P127+ TWIST) + deterministic double-run (exit 0, byte-identical) + outbox block (P145 → V158, +13) + heartbeat. Dry-run digest 81929730f04afec22fcacffd0d44d81609c6d2d2e395dd0803ec77ee29f19422; gates G1 z=-2.522567 / G2 z=+0.963546 / G3 z=+341.643962, all PASS, first_failing_gate null. Anchored to cache stampede / dogpile / thundering-herd + probabilistic early expiration (Vattani, Chierichetti & Lowenstein, "Optimal Probabilistic Cache Stampede Prevention", VLDB 2015; https://en.wikipedia.org/wiki/Cache_stampede, verified reachable 2026-07-18 via WebFetch). Distinct from every prior fleet head — a per-EXPIRY concurrency/coordination herd result (Poisson(λT) recomputes and its O(1) collapse under single-flight), NOT a retry congestion-collapse (metastable-retry-storm), a tail-latency cure (hedged-request), a queueing loss/delay system (erlang-b, checkout-pooling), a fan-out/placement object (fan-out-tail, two-choices), or an SIR contagion (epidemic-overshoot). VERDICT 158 (P145 → V158, +13) excluded — separate future slice; files: `ideas/fleet/cache-stampede-coalescing-2026-07-18.md` + `ideas/fleet/cache_stampede_coalescing.py` + `control/{outbox,status}.md` + `.sessions/2026-07-18-proposal-145-cache-stampede-coalescing.md` + this claim · 2026-07-18
