# CLAIM — VERDICT 125 (round-25 UNRELATED-slot closer, P112 → V125, +13)

- `claude/verdict-125-series-mtbf-collapse` (sim-lab work branch; idea-engine mirror) · **scope** — reproduce & verdict PROPOSAL 112 (the series-system MTBF collapse — a SERIES system fails at its FIRST component failure so its lifetime is the MINIMUM of its N per-component lifetimes, and for independent Exp(λ) lifetimes the hazard rates ADD (λ_sys=Nλ) so the system MTBF collapses to exactly T/N — N "individually excellent" parts give a system N× more failure-prone than one part, while parallel/redundant MTBF grows only logarithmically to T·H_N) → VERDICT 125 in sim-lab (byte-identical reproduction of the committed stdlib verifier `ideas/fleet/series_reliability_collapse.py` under SEED=20260717, digest 0aa2a8da…28b7bdf7, gates G1/G2/G3); mirror V125 outbox block + heartbeat + card into idea-engine · `2026-07-18`
