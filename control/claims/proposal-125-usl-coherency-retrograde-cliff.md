# CLAIM — proposal-125-usl-coherency-retrograde-cliff (round-29 FLEET-slot opener, P125 → V138, +13)

- `claude/proposal-125-usl-coherency-retrograde-cliff` · **scope** — round-29 FLEET-domain PROPOSAL 125 (the coordination-overhead retrograde-throughput cliff — multi-agent fleet-sizing / parallel-scalability economics: a fleet of N agents working one coordinated job does NOT get monotonically faster as N grows. Amdahl already caps speedup at 1/α from a serial/contention fraction α, but with ANY pairwise coordination (coherency) cost β>0 the Universal Scalability Law C(N)=N/(1+α(N−1)+βN(N−1)) makes throughput PEAK at N\*=√((1−α)/β) and then DECLINE — "retrograde scaling": past N\* every extra agent REDUCES total throughput. On the pinned world (α=0.03, β=0.001 ⇒ N\*=31) a 200-agent fleet delivers ~4.28× throughput, LESS than half the ~10.95× peak of a 31-agent fleet — adding 169 agents makes the fleet slower. Verified by a stdlib Monte Carlo (SEED=20260717) that MEASURES the fleet completion time as a sum of stochastic components — a (1−α)/N parallel term whose 1/N emerges by averaging N per-agent draws, an (α−β) serial/contention term, and a βN coherency term that emerges by summing N pairwise-coordination draws — so the retrograde peak EMERGES from the competing 1/N-vs-βN terms, not asserted; ≥3σ gates on the /se margin: G1 retrograde existence (time at N=200 > time at N\*=31 ⇒ throughput drops), G2 coherency-necessity control (β=0 Amdahl-only: the SAME fleet is monotone-faster, retrograde vanishes), G3 closed-form USL anchor MATCH (measured time-curve reproduces (1−α)/N+(α−β)+βN and its argmin = N\*=31). FLEET-domain opener, distinct from prior fleet slots P101 (winner's curse), P105 (delegation collapse), P109 (correlated-fleet VARIANCE floor — a variance-of-a-mean-estimator object, NOT a throughput-vs-concurrency curve), P113 (two-choices routing), P117 (Young–Daly checkpointing), P121 (hedged-request tail cure), and from variance-blind-provisioning (M/M/1 queueing tail), metastable-retry-storm-collapse (congestion feedback), and cascade-independence-quota (sequential herding) — a scalability/throughput-curve object with a coherency-driven retrograde peak, not a variance floor / queueing tail / feedback-collapse / herding object): idea doc + committed stdlib verifier + dry-sim + outbox entry + heartbeat. VERDICT 138 (P125 → V138, +13) excluded — separate future slice; files: `ideas/fleet/usl-coherency-retrograde-cliff-2026-07-18.md` + `ideas/fleet/usl_coherency_retrograde_cliff.py` + `control/{outbox,status}.md` + `.sessions/2026-07-18-proposal-125-usl-coherency-retrograde-cliff.md` + this claim · 2026-07-18
