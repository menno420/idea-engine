# CLAIM — proposal-105-delegation-loop-collapse (round-24 FLEET-slot opener, P105 → V118, +13)

- `claude/proposal-105-delegation-loop-collapse` · **scope** — round-24 FLEET-domain PROPOSAL 105 (delegation loop collapse — multi-agent governance / liquid-democracy coordination economics: N agents each delegate their vote to one uniformly-random peer (self = voting directly), so the delegation graph is a random mapping (Flajolet–Odlyzko 1990) — only cyclic nodes decide and their count is ~√(πN/2)=O(√N) (effective electorate collapses below 10%), one accidental weakly-connected component captures a Θ(N) share of all voting weight (asymptotic constant ≈0.758), and the cycle controlling that bloc is only O(√N) agents (sub-5% clique); uniform one-hop delegation silently concentrates ≥30% of voting weight into a sub-5% clique, verified by a stdlib functional-graph Monte Carlo with ≥3σ gates; fleet-domain opener): idea doc + committed stdlib verifier + dry-sim + outbox entry + heartbeat. VERDICT 118 (P105 → V118, +13) excluded — separate future slice; files: `ideas/fleet/delegation_loop_collapse.md` + `ideas/fleet/delegation_loop_collapse.py` + `control/{outbox,status}.md` + `.sessions/2026-07-18-proposal-105-delegation-loop-collapse.md` + this claim · 2026-07-18
