# CLAIM — VERDICT 134 (round-28 FLEET-slot closer, P121 → V134, +13)

- `claude/verdict-134-hedged-request-tail-cure` (sim-lab work branch; idea-engine mirror) · **scope** — reproduce & verdict PROPOSAL 121 (the hedged-request tail cure — distributed-systems tail-latency theory / the "tail at scale": a request served by one replica has a heavy-tailed latency whose 99th percentile is dominated by occasional stragglers; the counterintuitive result is that DEFERRING the second (hedged) request to the p95 delay — firing it only on the ~5% of requests whose primary exceeds the 95th percentile — recovers the large majority of full duplication's tail collapse at ≈5% extra load, not 100%: response = min(primary, d + secondary) with d = p95, hedge-fire load = P(S > p95) ≈ 0.05, the last sliver of tail benefit full duplication adds beyond the p95 hedge costs ~20× the load) → VERDICT 134 in sim-lab (byte-identical reproduction of the committed stdlib verifier `ideas/fleet/hedged_request_tail_cure.py` under SEED=20260717, results-dict sha256 gate, gates G1/G2/G3); mirror V134 outbox block + heartbeat + card into idea-engine · `2026-07-18`
