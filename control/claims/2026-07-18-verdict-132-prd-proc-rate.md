# CLAIM — VERDICT 132 (round-27 GAME-slot, P119 → V132, +13)

- `claude/verdict-132-prd-proc-rate` (sim-lab work branch; idea-engine mirror) · **scope** — reproduce & verdict PROPOSAL 119 (Pseudo-Random Distribution (PRD) proc-rate compression — the Warcraft III / Dota 2 anti-streak proc system: proc chance starts low and rises by a constant increment C on each consecutive failure (P(proc on the n-th attempt since last proc)=min(1,C·n)), resetting on a success, bounding bad-luck streaks — the counterintuitive trap being that the increment C is NOT the proc rate: a designer who naively sets C=0.25 for "25% crit" gets an EFFECTIVE long-run rate ≈0.45 (nearly DOUBLE intended) because the escalating chance forces frequent early procs, effective rate=1/E[N] with E[N]=2.21875 for C=0.25; the fix is to SOLVE C for the target rate (C≈0.0847 yields effective 0.25), never set C=nominal) → VERDICT 132 in sim-lab (byte-identical reproduction of the committed stdlib verifier `ideas/superbot-games/prd_proc_compression.py` under SEED=20260717, results-dict sha256 ce121bd9…248911, gates G1/G2/G3); mirror V132 outbox block + heartbeat + card into idea-engine · `2026-07-18`
