# CLAIM — proposal-129-condorcet-jury-correlation-floor (round-30 FLEET-slot proposal, P129 → V142, +13)

- `claude/proposal-129-condorcet-jury-correlation-floor` · **scope** — round-30 FLEET-domain PROPOSAL 129 (the correlated-vote Condorcet jury ceiling — multi-agent binary-decision aggregation / social-choice reliability: N agents each independently correct with probability p>1/2 decide a binary question by MAJORITY VOTE; the Condorcet Jury Theorem promises accuracy→1 as N→∞ under INDEPENDENCE, but with a shared common-factor correlation ρ>0 among the agents' errors the majority-vote accuracy does NOT converge to certainty — it plateaus at a closed-form CEILING A_∞=Φ(Φ⁻¹(p)/√ρ)<1 set by ρ and p, so adding agents past a saturation scale ≈1/ρ buys essentially nothing and 500 correlated 60%-competent voters are only ~68% accurate, not ~100%. Verified by a stdlib latent-Gaussian one-factor probit Monte Carlo with paired common-random-numbers and ≥3σ /se gates: G1 correlation-ceiling existence (independent minus correlated majority accuracy at N_HI > 0 by ≥3σ — the persistent floor), G2 Condorcet-convergence control (the SAME voters made independent climb N_LO→N_HI to near-certainty by ≥3σ, isolating ρ not p as the cause), G3 closed-form ceiling anchor (measured A_corr(N_HI) matches A_∞=Φ(Φ⁻¹(p)/√ρ) and the marginal A(1)=p within 3σ, correlated grid monotone-nondecreasing and plateaued); fleet multi-agent-aggregation slot, distinct from every prior fleet head P101 winner's-curse / P105 delegation-collapse / P109 correlated-fleet VARIANCE-floor (a continuous-MSE ρσ² object) / P113 two-choices-routing / P117 Young–Daly / P121 hedged-request-tail / P125 USL-retrograde-cliff, and distinct from cascade-independence-quota (sequential Bayesian herding, not simultaneous majority vote) — a BINARY-DECISION correctness-PROBABILITY object (a Gaussian-orthant threshold ceiling), NOT P109's continuous L2/variance floor: idea doc + committed stdlib verifier + dry-sim + outbox entry + heartbeat. VERDICT 142 (P129 → V142, +13) excluded — separate future slice; files: `ideas/fleet/condorcet-jury-correlation-floor-2026-07-18.md` + `ideas/fleet/condorcet_jury_correlation_floor.py` + `control/{outbox,status}.md` + `.sessions/2026-07-18-proposal-129-condorcet-jury-correlation-floor.md` + this claim · 2026-07-18
