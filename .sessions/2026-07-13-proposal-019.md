# Session — PROPOSAL 019: backlog low-water signal tuning (fleet-backlogs rotation)

> **Status:** `complete`
> **Model/time:** fable · 2026-07-13T01:31:20Z (Ideas Lab worker slice — harvest the
> ORDER 004 rule-3 fleet-backlogs slot from the websites lane backlog + PROPOSAL 019
> outbox append; card born in-progress as the designed gate hold, flips complete in
> this PR's final commit)

## Scope

Draft a genuinely new idea harvested from the FLEET-BACKLOG lanes, under standing
owner ORDER 003 (continuous pipeline) + ORDER 004 night-run rotation rule 3 ("fleet
backlogs → venture's book/product space → game mechanics → COMPLETELY UNRELATED
domains", verified open at HEAD `d0dca70`): PROPOSALs 015/016 were game mechanics,
017 the unrelated-domain slot, 018 the venture slot — this slice restarts the cycle
at fleet backlogs. Harvest source: the websites lane's own backlog (canonical
`docs/ideas/backlog.md` @ websites `8f97654`, blob `e14bb15` — re-fetched raw this
slice and byte-verified by `git hash-object` against the PR #244 groom's recorded
blob), whose captured bullet "Backlog low-water signal in the heartbeat" carries an
invented threshold constant ("below ~3") and an untested mechanism claim ("routing
latency beats idle wakes"). The head pre-registers the threshold question — signal
threshold N* under swept arrival regimes (incl. one anchored to the backlog's own
four measured re-pin intervals), consumption rates, and manager routing latencies,
APPROVE/REJECT/NULL bands registered before any code — written up as
`ideas/fleet/backlog-low-water-signal-tuning-2026-07-13.md` (probed single-pass this
slice, sim-ready) and appended to `control/outbox.md` as PROPOSAL 019. Fully
hermetic sim (the PROPOSAL 017/018 precedent): every fixture is a pinned constant in
the idea file, zero repo/network reads in the verdict session — the real-trace
anchor constants are quoted into the fixture from the pinned blob at drafting time.
Dedup swept vs PROPOSALs 001–018, sim-lab VERDICTs 001–018 (local clone @ `a3b921b`),
all `ideas/fleet/` + `ideas/websites/` heads, and tree-wide greps (low-water /
upkeep-dry / dry-wake / replenish / base-stock, bootstrap.py/.substrate excluded) —
only the harvested lane-backlog index itself carries the bullet; nearest prior art
is PROPOSAL 012 → VERDICT 014 (same fleet-ops policy-grid family, different control
variable, objective, and consumer — cited by source numbers verbatim per the offset
map, never derived).

**📊 Model:** fable · content + outbox proposal only (idea file, card, index row,
claim file, outbox append; no product code, no control/status.md or control/inbox.md
writes)

## 💡 Session idea

Make `check_harvest --bullet-drift` emit the per-pin state tallies it already
computes as a machine-readable time series: this slice had to hand-derive the
websites backlog's trajectory (per-interval births {2,5,4,11} and consumptions
{5,9,4,3}) by archaeology over four prose re-pin sections of
`ideas/websites/lane-backlog-2026-07-10.md` — numbers the harvest tooling touched
at every re-pin and then let evaporate into narrative. Kit/local fix: when
`--write-pins` records `.harvest-pin.json`, also append one
`{pin_sha, fetched, captured, built, retired, born, consumed}` row per pin (the
groom's reproducible count rule — state token OR em-dash outcome prose — already
exists as prose; encode it), so any future sim or manager sweep reads a lane's
backlog trajectory as committed data instead of re-deriving it from re-pin prose.
Deduped this slice: rg over ideas/ + .sessions/ + docs/ + scripts/ for
trajectory/time-series/per-pin-tally — zero prior capture; nearest neighbors are
the PR #244 groom's count-rule paragraph (prose, not machinery) and the
2026-07-12 harvest card's 💡 (bullet-title-set diff for `--bullet-drift` — detects
WHICH bullets moved, not the depth series; different fact, same tool seam, natural
one-PR bundle).

## ⟲ Previous-session review

Newest predecessor card (`.sessions/2026-07-13-proposal-018.md`, worker slice
~01:12Z): closed clean — its PR #283 merged at `d0dca70` with the PROPOSAL 018 outbox
entry grammar-clean and the card flipped complete in its final commit as designed;
notable craft: its dual-arm discipline (seedless analytic quadrature pinning the MC
arm to a 1.5% agreement tolerance on the f=1 slice) gives the verdict session a
built-in self-check no single-arm sim gets, and its dedup honestly recorded the
NEAR-miss that mattered (venture's own price-elasticity routing) instead of only
convenient distant neighbors. Its 💡 (kit `adopt` plants a headed empty
`control/outbox.md` + bus-completeness warning, born from its raw-404 undecidable
dedup read against venture-lab's missing outbox) is still unconsumed and NOT
re-minted here — checked before writing this card's own 💡 (rg for
outbox-plant/bus-completeness — only its own card carries it). No reconciliation
debt found.
