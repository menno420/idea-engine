# PROPOSAL 143 — Rank-decay churn cliff: under loss aversion (λ≈2.25) aggressive rank decay BACKFIRES — instead of pulling idle players back it pushes marginal players to quit, so beyond a small optimal decay rate raising the decay rate REDUCES total ladder activity; the SIGN of decay's effect on activity depends on the loss-aversion coefficient (round-33 GAME slot)

> **Status:** `in-progress`
> 📊 Model: Claude Opus · high · idea/planning

Born-red HOLD: this card holds the PR red from its first commit until the slice is complete and verified; flipping to `complete` (the deliberate LAST commit, after the heartbeat) releases the landing workflow. No gate is bypassed; a red gate AFTER the flip is a real defect, not the HOLD.

## Objective

Draft and land round-33 GAME-slot PROPOSAL 143 — rank-decay churn cliff. Folk belief: "rank decay keeps a ladder healthy — if players lose rank when they stop playing, decay pressures them to keep playing, so more decay = more activity." Counterintuitive claim: under loss aversion (a rank loss is felt λ≈2.25× as strongly as an equal rank gain — the empirical Kahneman & Tversky prospect-theory coefficient), aggressive rank decay BACKFIRES — instead of pulling idle players back to play it pushes marginal players to quit the game entirely, so beyond a small optimal decay rate raising the decay rate REDUCES total ladder activity. The SIGN of decay's effect on activity depends on λ: with no loss aversion (λ=1) decay helps (folk belief holds), with realistic loss aversion (λ=2.25) decay hurts. A model-dependence result (the P024 discipline). Ship a markdown-first card, a committed stdlib agent-based verifier (SEED=20260717, per-trial seed offset from SEED, N players × T periods × R trials, three ordered gates G1 estimator-agreement → G2 mechanism-control → G3 head, whole-dict sha256), a real dry-run, an outbox block (P143 → V156, +13), and a heartbeat update. VERDICT 156 (P143 → V156, +13) is the next independent slice, NOT written here.

## Constraints honored

[[fill: constraints on close-out]]

## What happened

[[fill: what happened on close-out]]

## ⟲ Previous-session review

[[fill: previous-session review on close-out]]

## 💡 Session idea

[[fill: session idea on close-out]]

## GROUNDING

[[fill: grounding url@sha on close-out]]
