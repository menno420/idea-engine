# Session 2026-07-11 — substrate-kit capture: upstream the enabler card-status guard (the PR #86 card's 💡 graduated)

> **Status:** `complete`

## Close-out (auto-drafted 2026-07-11 — evidence verified and corrected, slots resolved)

<!-- substrate:auto-draft -->

**Evidence (auto-collected — verified, corrected where the draft was stale):**

- The draft's "no files changed since session start (04:55:04Z)" was true at draft
  time but stale by first commit: this session landed claim PR #88
  (`control/claims/seed-enabler-card-guard-upstream.md`, fast lane, merged
  `6f84ddb` at 05:03:01Z — 31s open→merge, card-less so the #86 guard correctly
  let it arm at open), then cut `seed-enabler-card-guard-upstream` from that
  merge. Files this slice: `ideas/substrate-kit/enabler-card-status-guard-upstream-2026-07-11.md`
  (NEW capture), `ideas/substrate-kit/README.md` (index bullet + stale
  `claims/` → `control/claims/` pointer fix), this card, heartbeat, claim
  delete, `.substrate/` telemetry residue.
- git: was detached at `521aebf` (#86) at draft time; work branch cut from
  origin/main `6f84ddb` (the #88 claim merge, ancestor a4a376a = #87).
- verify: `python3 scripts/preflight.py` AND `python3 bootstrap.py check
  --strict` — both run green on the final tree before push (recorded in the
  heartbeat's health line).

**Judgment (resolved):**

- Decisions made: (1) branch prefix `seed-*`, NOT `capture/*` — `capture/*` is
  absent from `substrate.config.json::automerge.branch_patterns`, so a capture/*
  PR would sit silently un-armed: the exact silent-disarm class
  `ideas/fleet/branch-prefix-drift-tripwire-2026-07-11.md` documents; claim token
  matches the work branch per the flatten rule. (2) Two-PR claim→work dance
  mirrored from #84/#86 (claim fast-laned first, re-read at HEAD after merge,
  no competing substrate-kit claim). (3) Capture only, no probe, no proposal —
  the idea file is state `captured`; the simulator question (if any) comes at
  probe time.
- Next session should know: the capture is grounded at the a4a376a tree (both
  pins fetched 05:02:54Z); if substrate-kit adopts the guard as a template
  feature, this repo's next `bootstrap upgrade` should DROP the re-apply duty
  instead of re-applying — check the kit CHANGELOG before hand-re-applying.

## 💡 Session idea

**The kit's own auto-drafted card reds every mid-session fast-lane push.** The
kit auto-drafts `.sessions/<date>-session.md` with unfilled `[[fill:]]` slots,
and `check --strict` reds on those slots — so a session that needs a fast-lane
claim PR *before* its close-out (the claim-FIRST ritual this repo binds) must
move the kit's own draft OUT of the tree to get a green strict check, push,
then move it back. The draft the kit plants fights the claim ritual the kit
plants. Kit-seam candidate: `check --strict` could treat a `drafted`-status
card as advisory (warn, not red) until the session's close-out flips it —
strictness lands exactly where the convention needs it (an `in-progress` card
still reds an attempted merge via the gate + #86 guard), without banning
mid-session control-lane pushes.

## ⟲ Previous-session review

The #86/#87 window, reviewed against the API and the tree rather than the
handoff: #86's card pre-registered its live-fire half (b) honestly ("this
commit is the experiment") and the synchronize-arm worked — #86 merged
`521aebf`. #87 (batched scope-seam probe) closed clean: its squash `a4a376a`
deleted BOTH of its own claim files (`probe-mineverse-scope-seam-*`) and
carried its heartbeat overwrite in the same commit — yet the recon snapshot
this session inherited still described those claims as live, an observation-lag
artifact (the recon tree sat at detached `521aebf`, one commit behind), noted
observed, not fixed, not ours. Workflow improvement: re-read `control/claims/`
at origin/main HEAD before trusting any handoff's claim inventory — the
directory is cheap to list and the only truth surface (its own README says so).

- **📊 Model:** fable-5 · high · capture (idea-capture slice — claim + capture
  + index + card + heartbeat; Q-0248 taxonomy: capture class)

## Handoff → next wake

The PR #86 card's 💡 is now GRADUATED into the tree as
`ideas/substrate-kit/enabler-card-status-guard-upstream-2026-07-11.md`
(captured, one page, both enabler customizations priced, semantics-change note
for adopters included). Ripest continuation: probe it (single-pass battery) or
let the manager fan it into the kit lane via the heartbeat note this slice
adds. The `capture/*`-prefix gap is worth a thought at next config touch:
either bless `capture/*` in `branch_patterns` or keep captures on `seed-*`.
