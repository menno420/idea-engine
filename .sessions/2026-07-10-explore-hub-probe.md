# Session — explore-hub probe (superbot): panel mode → sim-ready + PROPOSAL 004

> **Status:** `complete`
> **Model/time:** fable-5 · 2026-07-10 (dispatched by the continuous-mode coordinator per Q-0265)

## What this session did
Probed `ideas/superbot/explore-hub-federated-world-2026-07-10.md` — harvest-flagged BIG ("one shared-world decision homes four gated game lanes"). Mode: **PANEL** — first live use of the README's big-or-contested panel default: 4 parallel lenses (design/possibility-space, engineering/simplicity, adversarial/risk, dependencies/routing) + coordinator as synthesizer. Verdict: **sim-ready** — narrowed to the one evidence-serviceable gate (superbot explore-hub plan PR 2's progression-balance model); the four owner-reserved questions (hub shape, survival overlay, docking topology, cross-game identity) stay with the owner. PROPOSAL 004 appended to `control/outbox.md` — first post-lift append (sim-lab pulled 001–003 at 20:35Z, sim-lab PR #4); it queues behind sim-lab's declared order 003 → 001 → 002. Section claimed first (`claims/probe-explore-hub-federated-world.md`), claim deleted in the branch's final commit.

**📊 Model:** fable-5 · high · docs-only (probe report + outbox append + card + heartbeat; no code)

**Live datapoint for PROPOSAL 001 (mode-1 vs mode-2 question):** lens disagreement was real — the risk lens returned needs-more-grooming (doc staleness) and the deps lens overturned the harvest framing ("four gated lanes" are superbot-internal planning docs, not fleet-manifest rows); a single-pass probe grounded only in the canonical doc would likely have missed that the idea is partially BUILT (plan PR 1 = world_registry #1156 merged, PR 3 built, PR 2 owner-gated). Measured panel cost: 4 lens agents ≈ 127k subagent tokens.

**Deviations:** risk lens's needs-more-grooming consumed in-report (reality-check section) rather than as a separate grooming slice — state advances forward-only in one hop captured → sim-ready (wild-encounters precedent).

## 💡 Session idea
Harvest-time framing can be stale by probe time even same-day — a probe's first move on a harvested idea should be a **shipped-reality diff** (lane plan docs + merged PRs), not the idea text alone; candidate for probe-battery v1 as a "Q0 reality check" step.

## ⟲ Previous-session review
Contract-grooming round 2 card handoff consumed: the grooming slice held proposal-generating probes under Q-0265 backpressure and named explore-hub-federated-world as the probe to take once sim-lab pulled — sim-lab pulled 001–003 at 20:35Z (sim-lab PR #4), so this slice took it. The probe report carries the grooming round's new grammar (Grounding pin with fetch time). Standing unbuilt 💡s (harvest freshness checker, earn-rate budget, optional-line lint coverage, freshest-wins one-liner) remain unbuilt.

## Handoff → next wake
Inbox first (verified empty at origin/main HEAD `aaff871`). Backpressure LIFTED — outbox depth 1 (PROPOSAL 004 only unpulled); proposal-generating probes may resume per the earn-rate bar. Ripest next per heartbeat: post-holdout-reseal-protocol (time-boxed by trading-strategy ORDER 008), then gba-homebrew replay-start-anchor.
