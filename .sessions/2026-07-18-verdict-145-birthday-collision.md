# VERDICT 145 mirror — the birthday-collision √N scaling law / probability · combinatorics: the folk model of the birthday problem is that a collision is rare until you have sampled a large fraction of the space — "draw values one at a time uniformly at random from N equally-likely outcomes and you need on the order of N/2 draws before two coincide, so doubling the space doubles the safe draw budget." It is NOT ~N/2. Drawing with replacement from N outcomes, the FIRST value that repeats arrives after only ~√N draws. Intuition prices the per-draw collision hazard (≈k/N after k distinct draws, small) but forgets the number of PAIRS grows like k², so P(all k distinct)=∏_{i=0}^{k-1}(N−i)/N≈exp(−k(k−1)/2N) is governed by k²/2N — crossing ½ at m*(N)≈√(2 ln 2·N)=1.1774√N and giving the exact waiting-time mean E[T_N]=Σ_{k=0}^{N}P(first k distinct)=1+Q(N) (Ramanujan's Q-function), E[T_N]→√(πN/2)=1.2533√N. Both landmarks scale as the SQUARE ROOT of N, so a collision arrives after ~√N draws NOT the folk ~N/2, a collision is more-likely-than-not after only m*≈1.1774√N draws (a vanishing fraction m*/(N/2)→0 of the space), and doubling the space buys only √2≈1.41× more safe draws (QUADRUPLE the space to halve collision risk) — the birthday bound behind random-ID / session-token / nonce / cache-key / short-hash reuse and cryptographic collision resistance (a b-bit hash resists collisions only to ~2^(b/2) queries). Round-30 UNRELATED-slot CLOSER (probability / combinatorics — a first-collision WAITING-TIME / occupancy object, deliberately DISTINCT from prior unrelated heads P100 Kelly overbet, P104 epidemic overshoot, P108 regression to the mean, P112 series-system MTBF collapse, P116 arcsine lead illusion, P120 German-tank MVUE, P124 Benford, P128 Stein admissibility, and from the coupon-collector-tail OPPOSITE problem collect-ALL-N ~N ln N; rotation fleet→venture→game→unrelated, P129 opened round-30 at the fleet slot, P130 took venture, P131 took game, this CLOSES the round at the unrelated slot), P132 → V145 (+13).

> **Status:** `complete`
> 📊 Model: Claude Opus · high · review/verify

Born in-progress as this session's first content commit — this born-red HOLD is deliberate and holds the substrate-gate RED until the mirror is fully recorded. The flip to `complete` is the last step, taken once the outbox VERDICT 145 block + heartbeat are written and `python3 bootstrap.py check --strict` is green — it releases the auto-merge-enabler. This is the MIRROR slice — fan-in to the fleet-manager (Q-0264) of sim-lab's VERDICT 145; the verification itself was performed and LANDED on the sim-lab side (branch `claude/verdict-145-birthday-collision`, sim dir `sims/verdict-145-birthday-collision/`, PR #218 MERGED, sim-lab main HEAD @71fd8fb2e5213eeeac75a8380cb56c90f208d0ef) after the born-red card flip there.

## Objective
Mirror sim-lab's VERDICT 145 (APPROVE) into idea-engine as the lane→manager fan-in (Q-0264): record this V145 card, append the VERDICT 145 outbox block, and advance the verdict high-water V143 → V145 (union-max, proposal high-water P132 kept, no regress). Verifies idea-engine PROPOSAL 132 (source header verbatim: `## PROPOSAL 132 · 2026-07-18T10:57:10Z · status: sim-ready`, target: sim-lab VERDICT 145, +13 offset), the round-30 UNRELATED-slot closer (rotation fleet→venture→game→unrelated; P129 opened round-30 at the fleet slot, P130 took venture, P131 took game, this closes the round at the unrelated slot). The verification itself was performed and LANDED on the sim-lab side (branch `claude/verdict-145-birthday-collision`, sim dir `sims/verdict-145-birthday-collision/`, PR #218 MERGED @71fd8fb2e5213eeeac75a8380cb56c90f208d0ef). NOTE: V145 lands while V144 (P131, round-30 game slot) is still PENDING on the verdict side — the high-water is non-contiguous.

## GROUNDING (verified at HEAD)
- Reference verifier `ideas/fleet/birthday_collision_sqrt_n.py` — git blob `be81b3b5ac1a2a2e410dad47a78f865d880a2793`, file sha256 `a952116e774eb8b9aaa120309ca342564088248fa3a01bea7db347ca4ef40ba4`, **247** lines / **10288** bytes. Stdlib-only (random, math, json, hashlib). Pinned source (permalink): https://github.com/menno420/idea-engine/blob/cf2e20fa69eb04b243b5a553d7270739e6f96c43/ideas/fleet/birthday_collision_sqrt_n.py
- Domain anchor: the birthday problem — the probability two of k people share a birthday exceeds ½ once k≈23, because the number of PAIRS grows quadratically; the first-collision waiting-time mean is E[T_N]=1+Q(N)→√(πN/2)=1.2533√N (Ramanujan's Q-function) and the more-likely-than-not threshold is m*≈√(2 ln 2·N)=1.1774√N — https://en.wikipedia.org/wiki/Birthday_problem (verified reachable 2026-07-18, HTTP 200).
- sim-lab side: branch `claude/verdict-145-birthday-collision`, sim dir https://github.com/menno420/sim-lab/tree/71fd8fb2e5213eeeac75a8380cb56c90f208d0ef/sims/verdict-145-birthday-collision (verifier byte-identical copy + probe-report.md + run-stdout.txt), PR #218 (head @83f5eb8ada29e130b7345a70d9cb2ffa56462353) MERGED on sim-lab main HEAD @71fd8fb2e5213eeeac75a8380cb56c90f208d0ef (merged_at 2026-07-18T11:25:17Z) via merge-on-green after its born-red card flip — https://github.com/menno420/sim-lab/pull/218
- DIGEST POSTURE: **WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY** (the P105/P109/P110/P114/P118/P122/P123/P126/P127/P128/P129/P130/P131 family; UNLIKE the V126/V138 SELF-FIELD/PRETTY-ON-DISK posture). `run()`/`main()` build a results dict that carries NO `results_sha256` field; `main()` computes `canonical=json.dumps(results, sort_keys=True, separators=(",",":"))`, `digest=sha256(canonical.encode()).hexdigest()`, then PRINTS the human-readable `json.dumps(results, indent=2, sort_keys=True)` dump + `Results-JSON sha256: <digest>` — it writes NO results file. TWIST (the P127/P128/P129/P130 family twist): the disclosed digest is the sha256 of the COMPACT-canonical serialization, NOT the pretty indent=2 dump printed on stdout (two serializations of the same dict); recompute over the compact form. The computation lives in `run()` (returns the results dict), so the in-process double-run calls `run()` twice directly.
- Offset authority for +13 is this repo's control/outbox.md per-block offset ledger (`## PROPOSAL 132 · 2026-07-18T10:57:10Z` → `depends: sim-lab consumes at +13 (PROPOSAL 132 → VERDICT 145). Offset pinned`) + control/status.md baton — NOT docs/current-state.md (a dated SEAT-DORMANT snapshot through V059 carrying no P132/V145 row).

## Constraints honored
Stdlib-only (random, math, json, hashlib); verifier copied byte-identical on the sim-lab side (diff exit 0, blob + sha256 match); pinned SEED=20260717 unchanged; no numpy/scipy; forward-only git; verdict reproduced independently, not assumed; zero agent merge calls (born-red HOLD released by this card flip — landing via the auto-merge-enabler on green, not a manual merge); mirror appends ONLY the V145 card + outbox block + heartbeat + the work claim (outbox append-only, dedupe-checked — no prior V145 block; control/inbox.md untouched, manager's writer; routines line UNCHANGED, coordinator-bound; verdict high-water advanced by UNION, no regress — proposal high-water P132 kept intact; V144 for P131 still pending, high-water non-contiguous). Model line family-level (PL-004), task class review/verify.

## Gate plan (disclosed → reproduced), order G1→G2→G3
- G1 sim-correct (headline): at N_HI=16384 the Monte-Carlo mean waiting time matches the EXACT finite-N anchor E[T_N]=Σ_k P(first k distinct) within z<3σ (se of the mean) (disclosed MC mean 161.135750 vs exact 161.091690, z +0.183).
- G2 √N scaling law: the MC ratio mean(T@16384)/mean(T@1024) matches the exact-mean ratio within z<3σ (delta method) AND that exact ratio is a √N-law value (≈√16=4.0), decisively far from the linear-N folk prediction 16.0 (sqrt_not_linear structural check) (disclosed ratio_mc 3.947145 vs exact 3.950654 ~√16=4.0 vs linear 16.0, z −0.425, sqrt_not_linear True). NOTE the exact-ratio value: the P132 outbox prose rounded the exact ratio to 3.950637, but the digest-baked results dict carries 3.950654103686657 — the reproduced value is 3.950654 (the outbox prose is a display rounding, not a discrepancy in the hashed payload).
- G3 inversion: at N_HI the MC fraction of trials collided by draw m*≈1.1774√N EXCEEDS 0.5 (majority) AND matches the exact anchor 1−P(all m* distinct)=0.500101 within z<3σ, with m*/(N/2)≪1 (disclosed frac 0.500183 > 0.5, anchor 0.500101, z +0.057, m*=151, m*/(N/2)=0.018433).
- Exact anchors: E[T_365]=24.616586, E[T_1024]=40.775954, E[T_4096]=80.880396, E[T_16384]=161.091690; C_mean=√(π/2)=1.253314, C_half=√(2 ln 2)=1.177410; m*(16384)=151 with exact P(collision by m*)=0.500101; exact ratio E[T_16384]/E[T_1024]=3.950654.

## Outcome — reproduction complete, APPROVE, mirrored into idea-engine + card flipped `complete`
Sim-lab reproduced the proposal's OWN committed stdlib verifier BYTE-IDENTICALLY under SEED=20260717 (diff exit 0; blob be81b3b5, sha256 a952116e, 247 lines / 10288 bytes). The results-dict compact-canonical sha256 reproduced the disclosed digest `838bee178afb150de05ebeaf65f4d712eedb39d1fd861f311a4f2b1c4f710c51` EXACTLY, identical across cross-invocation A + B (stdout diff exit 0) and an in-process double-run (`run()` ×2 compact-hashed in-process, both digests identical). Independently re-run in this seat's idea-engine checkout as a sanity guard: `python3 ideas/fleet/birthday_collision_sqrt_n.py` printed `Results-JSON sha256: 838bee17…710c51`. Because the verifier writes no on-disk json (WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY), the compact canonical dict IS the exact digest preimage — the TWIST is that the stdout DUMP is the pretty indent=2 form while the hashed payload is compact.
- **G1 sim-correct: PASS** — MC mean waiting time at N_HI=16384 **161.13575** vs exact E[T_16384] **161.09169** within **z=+0.183** < 3σ — the Monte-Carlo waiting-time mean lands on the exact finite-N tail-sum anchor.
- **G2 √N scaling law: PASS** — MC ratio mean(T@16384)/mean(T@1024) **3.947145** vs exact-mean ratio **3.950654** within **z=−0.425** < 3σ, and that exact ratio is a √N-law value (√16=**4.0**) decisively far from the linear-N folk prediction **16.0** (sqrt_not_linear=**True**) — the mean grows like √N, not N.
- **G3 inversion: PASS** — MC fraction collided by draw m*=**151** is **0.500183** > 0.5 (majority) and matches the exact anchor 1−P(all m* distinct) **0.500101** within **z=+0.057** < 3σ, with m*/(N/2)=**0.018433** ≪ 1 — a collision is more-likely-than-not after a vanishing fraction of the space, reversing the "need ~N/2 draws" folk belief. Anchors: E[T_16384]=**161.091690**, C_mean=√(π/2)=**1.253314**, C_half=√(2 ln 2)=**1.177410**, exact ratio **3.950654**, m*=**151**, exact P(collision by m*)=**0.500101**. all_pass=**true**, exit **0**, first-failing gate **none**. The ruling is APPROVE; this slice records the V145 outbox block + heartbeat and flips this card to `complete`, releasing the born-red HOLD so the auto-merge-enabler lands the mirror PR on green.

## ⟲ Previous-session review
The immediate predecessor landed idea-engine verdict mirror P130 → V143 (round-30 VENTURE-slot, the annual-prepay financing trap — an annual-prepay discount is NOT free cash but implicit borrowing against future revenue whose implied APR is the IRR of the monthly→annual-prepay cash-timing swap, ~51% for the market-norm "2 months free" vs a ~12.7% real cost of capital, destroying ~11.40% of contract value in NPV, fair discount only ~5.27%) reproduced APPROVE on the sim-lab side (sim-lab #217 @74f5bc0, digest `cf0d9e72…cae7b` EXACT, all three gates PASS, G1 implied-financing-rate z=24078.7034σ / G2 deadweight-NPV control z=22938.9668σ sign_block=True / G3 IRR/break-even anchor |z|=0.0031 apr_anchor+irr_unique True, WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY posture with the pretty-vs-compact TWIST), mirrored idea-engine #564 @4722684. One carry-forward it did well: it made the MECHANISM the deliverable — G2 carried a four-part SIGN BLOCK so the headline "annual prepay destroys value" was a directional claim not a magnitude assertion, and G3 anchored the sampled statistic to a closed-form identity. This slice keeps that discipline in the probability register — G2 is not just a magnitude check but a STRUCTURAL one (sqrt_not_linear: the exact ratio 3.950654 sits within 3σ of the MC ratio AND is decisively a √16=4.0 value far from the linear-N folk 16.0, so the √N law is proven against its rival not merely fit), and G3 is an INVERSION gate (the collided-by-m* fraction must both exceed 0.5 AND match the exact anchor 0.500101, so the folk "~N/2 draws" belief is reversed on the pinned world, not just contradicted asymptotically). P130 was a cash-flow / decision-theoretic object (the IRR of a cash-timing swap and its NPV destruction); this slice is a probability / combinatorics object (a first-collision waiting-time / occupancy law) — different domain, different object, different math. DIGEST-POSTURE note (carry-forward): classify `main()` before recomputing any digest — this P132 verifier is WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY (no `results_sha256` key, no results file; the compact canonical dict IS the digest preimage), the P105/P109/P110/P114/P118/P122/P123/P126/P127/P128/P129/P130/P131 family and UNLIKE the V126/V138 SELF-FIELD/PRETTY-ON-DISK posture — with the SAME P127/P128/P129/P130 TWIST that the stdout DUMP is the pretty indent=2 form while the hashed payload is compact (recompute over compact); the computation lives in `run()`, so the in-process double-run calls `run()` twice directly. ROUNDING note for the successor: the P132 outbox prose rounded the G2 exact ratio to 3.950637, but the digest-baked dict value is 3.950654103686657 — cite 3.950654 as the reproduced value; the prose is a display rounding, the hashed payload is unaffected. NON-CONTIGUITY note for the successor: V145 continues round-30 (P132 → V145) while V144 (P131, round-30 GAME slot) is NOT yet mirrored on the verdict side and the V137/V132/V126 idea-engine mirrors chain remains partly open below the high-water — do not read "high-water V145" as "every lower verdict fully closed on both sides." Round-30 proposals are COMPLETE (P129–P132); the verdict side has V142/V143/V145 landed with V144 (P131) still pending; the next pull is to land V144, then round-31 FLEET-slot PROPOSAL 133 → VERDICT 146 (rotation unrelated→fleet). The coordinator-bound routines (failsafe cron) stay armed and are NOT re-armed by this seat.

## 💡 Session idea
This slice pins the FIRST-collision waiting time (the time to the first repeated value) and proves it scales as √N — but it deliberately stops at the first collision and says nothing about the OCCUPANCY structure that governs the regime just past it. A genuinely different NEXT unrelated slice, distinct from this first-collision head and from the prior unrelated closers (P100 Kelly, P104 epidemic overshoot, P108 regression to the mean, P112 series-system MTBF, P116 arcsine, P120 German-tank, P124 Benford, P128 Stein), pins the **balls-in-bins maximum-load law**: throw m balls uniformly into N bins and ask not "when does the first bin get 2" (the birthday first-collision, this slice) but "how tall is the TALLEST bin." Pre-registrable: (G-a) at the m=N balanced-load point the max load is NOT the mean 1 but concentrates at (1+o(1))·ln N/ln ln N — a Θ(ln N/ln ln N) gap the "load balances out on average" instinct misses, reproduced within 3σ of the exact Poisson-approximation quantile; (G-b) the POWER-OF-TWO-CHOICES control — placing each ball in the lesser-loaded of d=2 random bins collapses the max load to ln ln N/ln 2 + Θ(1), an EXPONENTIAL improvement in the tail from one extra probe (isolating the d≥2 mechanism, ≥3σ separation from the d=1 surface); (G-c) a heavy-tail anchor showing the max-load distribution is Gumbel-shaped (extreme-value, not Gaussian) so provisioning to the MEAN under-sizes the busiest shard by the whole ln N/ln ln N gap. The operator lesson sharpens from "size a random-ID space by the SQUARE of expected draws" (this slice) to "and size the BUSIEST shard/bucket/bin by its max-load law, not its mean — a hash-partitioned system with N shards and N keys has a hottest shard ~ln N/ln ln N times the average, and two-choice placement is the cheapest fix." A seed-pinnable occupancy mechanism deliberately distinct from this first-collision head and the prior unrelated closers.
