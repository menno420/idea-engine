# Session — PROPOSAL 050: the kill-clock horizon — is the products lane's T+14 zero-sale kill clock right-sized? (VENTURE slot, round 9, PRODUCTS half)

> **Status:** `complete`
> **Model/time:** fable-5 · 2026-07-13T23:19:54Z (Ideas Lab worker slice — draft the
> round-9 VENTURE head under standing owner ORDER 003. Card born in-progress as the
> designed born-red gate hold; flipped complete in this PR's final commit at
> 2026-07-13T23:27:16Z)

## Scope

Draft a genuinely new sim-shaped idea for the VENTURE rotation slot, round 9,
PRODUCTS half, under standing owner ORDER 003 (continuous pipeline) and ORDER 004
rule 3's rotation ("fleet backlogs → venture's book/product space → game mechanics
→ COMPLETELY UNRELATED domains"). Round 9 opened at the fleet-backlogs slot
(PROPOSAL 049, PR #358), so the venture slot is next; the half-alternation read from
the actual sequence (P018 books → P022/P026 trading → P030 books → P034 trading →
P038 books → P042 products → P046 books) puts round 9 on the NON-BOOKS/PRODUCTS
half. Harvest source: the products lane's kill-clock family (T+7 checkpoint ·
T+14 kill rule · 30-day signal window, ≥1-organic-sale success leg), pinned
SECONDHAND from P042's verbatim harvest of venture-lab @ `be6c75d` as recorded in
this repo's own outbox @ HEAD `763b19e` — the direct venture-lab read was attempted
once this session and denied (verbatim error in the claim file and the idea file's
Grounding; deny-wins, one attempt only). Claim landed first
(`control/claims/claude-proposal-050-kill-clock-horizon.md`).

Idea file `ideas/venture-lab/kill-clock-horizon-2026-07-13.md` (probed single-pass
this slice, sim-ready), venture section index row, then appended to
`control/outbox.md` as PROPOSAL 050. Seeds 20261337–340, strictly above the P049
high-water 20261336 (re-swept this session: digit-boundary regex + `results.json`
exclusion over this tree AND /home/user/sim-lab; the max allocation found in either
repo is P049's 20261336 — larger numerals are digit substrings inside sim-lab
`results.json` exact-Fraction numerators, data not seeds).

**📊 Model:** `fable-5` (family-level self-report per ORDER 001 / Q-0262 — the
family the harness reports for this Ideas Lab slice, never copied from a schedule
surface) · content + outbox proposal only (idea file, card, venture section index
row, claim file, outbox append; no product code, no control/status.md or
control/inbox.md writes; no sim-lab writes).

## 💡 Session idea

**When the standing harvest source is WALLED, the dedup ledger doubles as a harvest
cache — and the wall should pick the head: pin constants SECONDHAND from a prior
proposal's verbatim harvest already committed in your own outbox, carry the
secondhand status into the fixture AND the application guard (the guard VOIDS
application on a mismatched real shape instead of misapplying), and select among
candidate heads by what the record already quotes.** This slice hit the pattern
live: the venture-lab read was denied (one attempt, verbatim error recorded,
deny-wins), yet P042's outbox block carries the products lane's kill-clock family
verbatim — T+7 / T+14 / 30-day window / the ≥1-organic-sale leg / the 0-for-7
evidence line — with its own `be6c75d` pin. That made the kill-clock head DRAFTABLE
at a wall (a few crisp committed constants, all on the record) while the
otherwise-attractive rubric-weights head was UNDRAFTABLE (its honest sim needs the
doc's actual 9 × 5 score table, which no prior block quotes) — so the wall itself
adjudicated the runner-up, and the drop reason is recorded re-openable ("the day a
venture read is available"). The recipe, mechanical: on a source denial, (1) quote
the denial verbatim in Grounding/claim/outbox — never launder; (2) grep your own
outbox for the prior blocks that harvested that source and inventory which of its
constants are quoted verbatim with pins; (3) rank candidate heads by
constants-on-record, not by attractiveness; (4) double-pin the fixture
(source-sha via outbox-sha) and add the mismatch-voids-application guard condition.
Dedup: P045's 💡 is harvest-by-hedge-marker, P046's the seed-sweep recipe, P047's
harvest-by-validation-gap, P048's the liveness-disclosure inversion, P049's
second-tap/dropped-runner-up discipline — none touches denied-source secondhand
pinning; this is the first 💡 for it, and it matters now: session repo allowlists
make source walls a steady state, not an accident.

## ⟲ Previous-session review

Newest predecessor card (`.sessions/2026-07-13-proposal-049-magnet-press-fit-band.md`,
worker slice ~22:22Z, fable-5): closed clean, and BOTH of its rituals did real work
here — its 💡 (before re-tapping a source, read the prior head's Relations/scope
notes FIRST and treat recorded drop reasons as design constraints) transferred to
this slice's same-LANE second tap: P042's Relations and scope boundary were read
before drafting, its "reads/inbound kill-rule leg named out of scope" became P050's
scope rule verbatim, and its consequence section's throwaway line (the T+7/T+14
clocks as "the pre-priced live probe") was exactly where the unpriced dial was
hiding — the predecessor's discipline found this head. The P048-minted
disclosed-landing norm also ran a third consecutive time (P050's DP was computed at
drafting: APPROVE thinly, near-edge cells named). Honest nits: (1) P049's close-out
asserts "both checkers run green at flip time or this commit does not push" — a
ritual claim with no committed evidence trail; true here too, and equally
unverifiable post-hoc — a one-line exit-code echo in the close-out would cost
nothing and pin it; (2) the litany-duplication hazard P049 flagged against P048
recurs mildly in P049's own claim file (the rotation litany rides claim + card +
idea header + index row); P050 kept litany copies to the grammar-forced minimum but
the structural fix (one canonical home) remains nobody's slice. Carried flag, now
FIVE cards deep: the two coexisting claim-landing patterns (fast-lane control-only
PR vs claim-in-first-commit) remain undocumented; this slice again took
claim-in-first-commit per its orders (decide-and-flag: noted for the manager sweep).

## Close-out

All pieces landed before this flip: claim + born-red card @ `69d7746` (branch's
first commit), idea doc + venture section index row @ `13a7a22`, PROPOSAL 050
outbox append @ `d9bbb88` (idea: link pinned to the `13a7a22` blob; append-only,
prior blocks untouched). PR #371 opened READY immediately after the push — never
draft; merge left to the enabler workflow, no agent merge. Rotation verified at
HEAD `763b19e` before drafting: round 9 opened at fleet backlogs (P049 #358), so
the venture slot is next per ORDER 004 rule 3 (quoted verbatim in the claim), and
the half-alternation from the actual P-sequence puts round 9 on the
NON-BOOKS/PRODUCTS half. Venture source denied this session (one attempt, verbatim
error in the claim + Grounding + outbox block); constants pinned secondhand
be6c75d-via-763b19e with the mismatch-voids-application guard. Seeds 20261337–340
strictly above the true cross-repo high-water 20261336 (digit-boundary sweep, both
repos, results.json aliases excluded). Checker evidence, pinned per this card's own
review nit: `python3 bootstrap.py check --strict` exit 0 and
`python3 scripts/check_ideas.py --outbox` exit 0, both run on the tree at
`d9bbb88` before the push (the only CI red on this branch before the flip is the
designed born-red card hold). This flip is the branch's last commit.
2026-07-13T23:27:16Z
