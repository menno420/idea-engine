# PROPOSAL 148 — The Will Rogers phenomenon (stage migration): reclassifying members across a group boundary can raise the mean of BOTH groups at once while the population mean is EXACTLY unchanged and no member's value changes — moving a member x from the higher-mean group HIGH into the lower-mean group LOW where LOW_mean < x < HIGH_mean sheds a below-its-mean member from HIGH and adds an above-its-mean member to LOW, so both subgroup means rise on a pooled population that is only RELABELLED; "every subgroup improved" and "the population improved" are DIFFERENT claims that coincide only when group membership is fixed (round-34 UNRELATED slot)

> **Status:** `in-progress`
> 📊 Model: Claude Opus · high · idea/planning

Born-red HOLD: this card is the session's FIRST content commit (alongside the claim) and is born `in-progress` to hold the `substrate-gate` RED so the PR cannot merge before the deliberate `complete` flip. Flipping to `complete` (the deliberate LAST commit, after the verifier + idea doc + outbox P148 block + heartbeat) releases the merge-on-green backstop. The flip is taken once the verifier is committed, the digest `79b7443f3c993d830de4cd9d63c2ce826111e45ec426c3fd8bf17a8c33306470` reproduces EXACTLY across a deterministic double-run, the PROPOSAL 148 outbox block + heartbeat are written, and `python3 bootstrap.py check --strict` is green apart from this born-red card HOLD itself. A red gate AFTER the flip is a real defect, not the HOLD.

## Objective

Draft and land round-34 UNRELATED-slot PROPOSAL 148 — the Will Rogers phenomenon (stage migration), a NEW unrelated-domain mechanism (classification / measurement artifact) outside the fleet, venture, and game domains. Folk belief: "if the average outcome improves within EVERY subgroup, the population as a whole must have improved — you cannot make every part better without making the whole better." Counterintuitive claim (Will Rogers' Okies quip; the medical stage-migration artifact of Feinstein, Sosin & Wells, NEJM 1985): reclassifying a member x from the higher-mean group HIGH into the lower-mean group LOW, where LOW_mean < x < HIGH_mean, RAISES both group means at once — HIGH sheds a below-its-mean member, LOW gains an above-its-mean member — while the pooled population mean is IDENTICALLY conserved, because the member's value was never touched, only its LABEL. Every subgroup can improve while the whole does not move a hair. Ship a markdown-first card, a committed stdlib-only verifier (SEED=20260717, N members × R paired trials, three ordered gates), a deterministic double-run, an outbox block (P148 → V161, +13), and a heartbeat update. VERDICT 161 (P148 → V161, +13) is the next independent slice, NOT authored here.

## Constraints honored

- Round-34 UNRELATED slot (rotation fleet→venture→game→unrelated; round-34 opened at the FLEET slot with P145, VENTURE slot took P146, GAME slot took P147, so the unrelated slot is P148). Offset +13 (P148 → V161) cited from the P147 depends-ledger + control/status.md; the verdict is NOT authored here.
- Digest posture: WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY (the P105…P147 family) with the P127+ TWIST — run() builds a results dict with NO `results_sha256` key (every float rounded to 6 dp), main() computes `payload=json.dumps(results, sort_keys=True, separators=(",",":"))` then `digest=sha256(payload)`, PRINTS the pretty `json.dumps(results, indent=2, sort_keys=True)` dump + `Results-JSON sha256:`, and writes NO on-disk JSON — so the disclosed digest IS the sha256 of the compact canonical dict, and the pretty stdout dump is a different serialization.
- Stdlib-only verifier (hashlib, json, math, random — no numpy/scipy); pinned SEED=20260717; /se z convention (z = mean/se); three ORDERED gates APPROVE iff ALL (G1 estimator-agreement half-vs-half |z|<3 → G2 conservation-control cons_max_rel≤1e-9 and pooled change not-significantly-positive z<3 → G3 head both subgroup means rise, min(z_low,z_high)≥3); deterministic double-run asserted across two fresh CLI invocations (byte-identical, exit 0).
- Outbox `## PROPOSAL 148 · … · status: sim-ready` block appended once (append-only, deduped) matching the P145/P146/P147 skeleton (target / idea / question / done-when / depends / loop).
- Status heartbeat: proposal high-water P147 → P148 (union-max, no regress); verdict high-water kept (this is a PROPOSAL slice and does not author a verdict); routines line UNCHANGED (coordinator-bound); baton → round-34 proposals COMPLETE, next: V161 for P148, then round-35 fleet-slot P149. control/inbox.md left untouched (manager-written).
- Born-red first commit (card in-progress + claim) → verifier + idea doc → outbox + status → flip complete LAST.

## GROUNDING

Round-34 UNRELATED-domain slot of the fleet/cross-cutting proposal→verdict pipeline (ORDER 003 continuous-pipeline standing-active; ORDER 018 overnight loop). Idea source (firsthand, this repo): https://github.com/menno420/idea-engine/blob/main/ideas/fleet/will-rogers-stage-migration-2026-07-18.md · verifier (ground truth): https://github.com/menno420/idea-engine/blob/main/ideas/fleet/will_rogers_stage_migration.py — stdlib-only (hashlib, json, math, random). Grounding commit (branch point): idea-engine main `b948e5c0a068c94361686480eb4f91d32599aee3` (fetched 2026-07-18), the branch point for `claude/proposal-148-will-rogers-stage-migration`. Pinned world: SEED=20260717, N=4000, R=40, T_COARSE=0.5, T_FINE=0.6, CONS_TOL=1e-9, SIGMA_GATE=3.0; per member v~Uniform[0,1] FIXED, coarse split at T_COARSE (LOW v<0.5, HIGH v≥0.5), the bottom slice of HIGH (T_COARSE≤v<T_FINE) migrates into LOW (the finer classifier), and per-trial before/after group means are computed (paired). Closed form (Uniform[0,1]): LOW 0.25→0.30, HIGH 0.75→0.80, pooled 0.5→0.5 (improvements +0.05 each, pooled conserved). External reference (reachable, verified 2026-07-18 via WebFetch): the Will Rogers phenomenon — https://en.wikipedia.org/wiki/Will_Rogers_phenomenon (the medical stage-migration artifact; "moving an element from one set to another can raise the average of both sets"). Offset authority for +13 is this repo's control/outbox.md per-block ledger (P147 depends-line) + control/status.md heartbeat.

## Probe questions

**1. Is the mechanism genuinely new against the unrelated backlog?** Yes. The known-taken unrelated heads — Kelly, SIR/epidemic-overshoot, regression-to-mean, series-MTBF, arcsine, German-tank, Benford, Stein, birthday-collision, PPV/base-rate collapse, gambler's-ruin, hot-hand streak-selection, variance-blind-provisioning, coupon-collector-tail, inspection-paradox, secretary, Parrondo, Simpson's aggregation-reversal, Braess, friendship-paradox — were each checked at HEAD; none is the Will Rogers / stage-migration reclassification artifact (both subgroup means rise on a conserved population). The closest rhyme is Simpson's paradox (aggregation reversal on FIXED groups); this is the opposite move — the GROUPS change (a member is relabelled) and both group means rise while the pooled mean is fixed. A grep of `ideas/` and `control/outbox.md` at HEAD for "will rogers" / "stage migration" returned zero hits.

**2. Do all three pre-registered gates PASS in order on the /se margin?** Yes. G1 AGREEMENT — the two halves of the R low-group improvements agree (half_a +0.049696 vs half_b +0.049445, z=+0.361594, |z|<3) PASS; G2 CONTROL — the pooled population mean does NOT improve (pooled_impr +0.000000, cons_max_rel 0.0 ≤ 1e-9 with count conservation, z=+0.010159 not-significantly-positive z<3) PASS; G3 HEAD — BOTH subgroup means rise significantly (low +0.049571 z=+144.191642, high +0.049516 z=+133.926813, min z=+133.926813 ≥3) PASS. all_pass=true, first_failing_gate=null, exit 0.

**3. Does the digest reproduce EXACTLY and deterministically?** Yes — results-dict sha256 `79b7443f3c993d830de4cd9d63c2ce826111e45ec426c3fd8bf17a8c33306470`, byte-identical across two fresh CLI invocations; WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY (the dict carries no results_sha256 key, no on-disk JSON, floats rounded 6 dp), with the P127+ twist (pretty indent=2 stdout dump, compact canonical hashed preimage).

**4. Does the proposal advance the proposal high-water without regressing anything?** Yes — proposal high-water P147 → P148 (union-max, round-34 UNRELATED slot completes the round-34 rotation above the round-34 GAME-slot P147); verdict high-water kept (this is a PROPOSAL slice, no verdict authored). Round-34 is now COMPLETE on the proposal side (FLEET P145, VENTURE P146, GAME P147, UNRELATED P148); the next pull is V161 for P148, then round-35 fleet-slot P149. Concurrent sibling PRs mirroring V159/V160 (for P146/P147) may land at branch time; if so, merge origin/main IN and UNION the ledgers (highest P/V high-water per line, every append-only outbox block kept).

## Outcome

PROPOSAL 148 drafted and staged sim-ready. On the pinned world (SEED=20260717, N=4000, R=40, T_COARSE=0.5, T_FINE=0.6, the P104…P147 /se convention) a stdlib paired Monte-Carlo confirms the Will Rogers stage-migration law: migrating the bottom slice of the HIGH group into the LOW group raises BOTH subgroup means (LOW +0.0496 z=+144.19; HIGH +0.0495 z=+133.93) while the pooled population mean is EXACTLY conserved (cons_max_rel 0.0, pooled_impr +0.000000, z=+0.01) — every subgroup improves ≥3σ on a population that did not change at all, matching the closed form (LOW 0.25→0.30, HIGH 0.75→0.80, pooled 0.5→0.5). all_pass=true, exit 0, first_failing_gate None; digest `79b7443f3c993d830de4cd9d63c2ce826111e45ec426c3fd8bf17a8c33306470` reproduced EXACT across a deterministic double-run. Transferable rule: when a segmented metric improves in EVERY segment, the population is NOT proven to have improved unless the segment boundaries are fixed — a moving boundary (stage migration, cohort re-bucketing, tier reclassification, changed inclusion criteria) can manufacture across-the-board subgroup gains from pure relabelling; audit any "every cohort is up" story for boundary movement and check the conserved pooled total. The verifier + idea doc + outbox P148 block + heartbeat are written; the flip to `complete` releases the merge-on-green backstop. VERDICT 161 (P148 → V161, +13) is the next independent slice, not authored here.

## ⟲ Previous-session review

The round-34 GAME-slot P147 (Gresham currency hoarding, PR #597, results-dict sha256 90995ccd…f7b969, three gates PASS G1 z=−0.70 / G2 z=−1442.35 / G3 z=+1701.03) took the game slot; its baton named "round-34 unrelated-slot P148" as the successor's pull, and this slice executes exactly that pull, completing the round-34 rotation with zero re-derivation. Two carry-forwards honored: (i) the DIGEST posture — P147 and this card are both the WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY family with the P127+ TWIST (compact canonical hashed payload, pretty indent=2 stdout dump, floats rounded 6 dp, no on-disk JSON), reconciled against the verifier text, not assumed; (ii) the exemplar STRUCTURE — the fleet/unrelated exemplar shape (a humblest-object simulation with a closed-form reference and three ordered /se z-gates: an estimator-agreement stabiliser, a not-significant control, and a significant head) from the hot-hand P144 card is mirrored here, with the head asserting BOTH subgroup means rise and the control asserting the pooled mean does not. Continuity carried forward: concurrent sibling PRs mirroring VERDICT 159/160 (for P146/P147) may be open at branch time and, if they land first, are reconciled by an append-only UNION of the outbox/status ledgers (highest P/V high-water per line, every block kept). The coordinator-bound routines (failsafe trig_01MzoLVHMDuL28UPkKnbmHCm, pacemaker chain) stay armed and are NOT re-armed by this proposal slice.

## 💡 Session idea

The verifier pins a SINGLE migration step (one boundary move, HIGH→LOW) and shows both subgroup means rise once. The load-bearing axis the gates do NOT touch is the DIRECTION-and-magnitude law of the effect: which way each subgroup mean moves, and by how much, is a deterministic function of WHERE the migrated slice sits relative to the two group means. A genuinely different next UNRELATED slice pins that axis: hold the population fixed and sweep the migrated slice's position s (the finer threshold T_FINE) across the whole HIGH range, then pre-register (G-a) a both-rise band — for s in the interior band (migrated members above LOW_mean and below HIGH_mean) BOTH subgroup means rise at ≥3σ (this card is one point of that band); (G-b) a sign-flip gate — once T_FINE passes HIGH_mean the migrated slice is ABOVE HIGH's mean, so moving it DOWN now LOWERS the HIGH mean (one subgroup falls while the other still rises), a ≥3σ-resolved crossover at s* = HIGH_mean; and (G-c) a conservation invariant — the pooled mean stays conserved across the ENTIRE sweep (the artifact is always relabelling, never real change). That converts "a stage migration can raise both subgroup means" into "here is the migrated-slice-position knob and the exact band where both rise vs where one falls" — a seed-pinnable classification-artifact mechanism distinct from this slice's single-step model and from every prior unrelated head. Guard recipe for the follow-up: anchor the sweep in `run()` of `ideas/fleet/will_rogers_stage_migration.py` (parametrise `T_FINE` into a grid, reuse `simulate`/`_mean_se`/`_z`), and gate the crossover on `min`/`max` of the per-slice subgroup z's.
