# Session 2026-07-20 — P215 Stackelberg commitment / first-mover advantage (round-51 GAME slot)

> **Status:** complete

## 💡 Session idea
Land PROPOSAL 215: the Stackelberg commitment / first-mover advantage. In a linear-demand quantity duopoly (inverse demand P(Q)=A−Q, symmetric marginal cost C, m=A−C>0), a leader who PUBLICLY COMMITS to an output before the rival moves earns strictly MORE than it gets in the simultaneous-move Cournot (Nash) equilibrium — π_leader = m²/8 > π_cournot = m²/9, a commitment advantage of exactly m²/72 > 0 for every market size m. The surprise: the value comes precisely from the INABILITY to revise — throwing away flexibility and even leaking your quantity is worth money because credible commitment bends the rival's best response in your favour. Folk intuition ("keep your options open; moving first only shows your hand") is exactly inverted. A deterministic stdlib verifier proves it four ways: G1 a ≥3σ Monte-Carlo significance leg (advantage survives an ε-noisy follower), G2 an exact fractions.Fraction gate where exhaustive integer-grid enumeration with the follower's exact backward-induction reaction matches the closed forms m²/8, m²/9, m²/16 EXACTLY with leader>cournot, G3 robustness across a scaled world (m=24) and a cost-shifted world (same m via A=15,C=3), and G4 a falsifiability leg where the naive static-follower accounting is correctly rejected (and would flip the sign). Pairs to VERDICT 228 (+13). Disclosed results_sha256 f6fdd85e2c22a1d49be9af6bd7479ab2e59869be818e1c451d1ca40caba6fb7b.

## ⟲ Previous-session review
Prior GAME slot was P207 (complete rent dissipation in the all-pay auction → VERDICT 220). Boot outbox high-water: P214 → V227 (round-51 VENTURE slot, Nash-bargaining BATNA ½ pass-through, landed idea-engine #787). Dedup: `grep -rn` across ALL lanes in `ideas/` for stackelberg / leader-follower / commitment-advantage / first-mover / minimax / risk-dominance — `stackelberg`, `leader-follower`, `commitment advantage`, `risk-dominance` all return ZERO shipped heads; the only `first-mover`/`commit first` hits are the OPPOSITE head (Penney's game and Efron dice, where committing first is a LOSING role via second-mover advantage). Stackelberg is the exact inverse — committing first is a WINNING role — so no collision; disclosed as a deliberate contrast in the doc's Dedup. No pivot to the minimax / risk-dominance fallbacks needed. Advancing outbox high-water P214 → P215 (union-max). This card is born in-progress to HOLD the PR until authoring + preflight are green; flipping it to complete releases merge-on-green.

## 🫀 Heartbeat
Heartbeat (`control/status.md`) left to the coordinator per single-writer rule; this card is the session trail. Claim `control/claims/claude-p215-stackelberg-commitment-advantage.md` bundled into this first commit; released after landing.

> 📊 Model: Claude Opus 4.8 · high · idea/planning

## Decisions made
- Primary head landed (no pivot): Stackelberg commitment / first-mover advantage. π_leader = m²/8 > π_cournot = m²/9 is EXACT for every m>0; the follower's exact backward-induction reaction q_F=(m−q_L)/2 makes the whole check exact-rational (fractions.Fraction, no floats in the exact gates).
- Model choice: the follower plays its EXACT (Fraction) best response while the leader is enumerated exhaustively over an integer quantity grid — this is the correct backward-induction/SPNE object and makes the grid optimum land exactly on the closed form (an integer-argmax follower instead lets the leader exploit rounding, e.g. profit 21 at q_L=7, which is a discretisation artefact, not the Stackelberg value; caught and corrected during authoring).
- Four pre-registered gates, run first then thresholds pinned to what passed: G1 significance z_vs_0=105.045621 ≥ 3σ (mean_gap 1.99223 ≈ m²/72=2); G2 exact (leader 18=144/8, cournot 16=144/9, follower 9=144/16, leader>cournot); G3 robustness (scaled m=24: 72/64/36 exact; cost-shift m=12: 18/16 exact; MC scaled z=209.424945); G4 falsifiability (static-follower accounting gives 12 ≠ true 18, and 12<16<18 so the wrong model flips the sign). Determinism verified across three separate process invocations (byte-identical).
- Grounding byte-pinned to Wikipedia "Stackelberg competition" rev 1365066831 (raw-wikitext sha1 36004d1bfd7d09b149e3a78e4ea3f6bea0da4f06, self-computed over 29228 raw bytes, full-40 match to the API-reported revision sha1). The page firsthand documents the leader-follower SPNE, backward induction, "higher profits than under Cournot," a worked example (leader 2,000,000 vs Cournot (16/9)·10⁶ ≈ 1.78M), and "first-mover advantage"/"credible commitment is a tool for competition." Honest caveat: the exhaustive enumeration, the exact-Fraction check, the ε-noisy-follower significance test, the shifted-world robustness sweep, and the static-follower falsifiability rejection are the verifier's own firsthand computations.
- Verifier lives beside the doc in `ideas/superbot-games/` (single-repo PROPOSAL convention, per P175/P199/P207); sim-lab reproduces at VERDICT 228 time.

## Next session should know
- P215 → VERDICT 228 (+13) is now awaiting sim-lab reproduction of digest f6fdd85e…a6fb7b; outbox high-water advanced P214 → P215. Heartbeat (`control/status.md`) left to the coordinator.
- A reusable stdlib backward-induction harness (exact Fraction follower reaction + closed-form/enumeration exact gate + seeded Monte-Carlo significance/robustness gates + a static-follower falsifier) now lives in `ideas/superbot-games/stackelberg_commitment_advantage.py`.
