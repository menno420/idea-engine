# Session — PROPOSAL 025: claim-expiry horizons vs lane death (fleet-backlogs rotation, round 3)

> **Status:** `complete`
> **Model/time:** fable · 2026-07-13T04:45:36Z (Ideas Lab worker slice — draft the
> ORDER 004 rule-3 FLEET-BACKLOGS round-3 head: round 1 harvested the websites
> backlog (P019 → V021 approve), round 2 the superbot backlog (P021 → V023 reject);
> round 3 rotates the harvest SOURCE again, into the kit-planted claims doctrine —
> the fleet-backlog slot itself demonstrably spans backlog sources; card born
> in-progress as the designed gate hold, flips complete in this PR's final commit
> once the 💡 slot resolves)

## Scope

Draft a genuinely new sim-shaped idea under standing owner ORDER 003 (continuous
pipeline) + ORDER 004 rule 3's rotation — the round-3 FLEET-BACKLOGS slot. Survey
this slice deliberately weighted the sections fleet-slot rotation has NOT yet
tapped: `ideas/substrate-kit/` (6/6 heads probed-or-routed, every one marked "no
sim-lab proposal — no simulator question"), `ideas/superbot-next/` (all heads
"nothing sim-shaped" or consumed — P011 → V013), `ideas/superbot-mineverse/`
(SECTION COMPLETE, one head explicitly "no sim question until a real endpoint
exists"), `ideas/product-forge/` (4/4 disposed; its one sim-shaped head became
P007). The genuinely sim-shaped material in that territory is where kit-planted
doctrine carries INVENTED CONSTANTS: the claim-expiry horizons. The kit ships
`CLAIM_STALE_HOURS = 24` and `WORK_CLAIM_STALE_HOURS = 72`
(`src/engine/checks/check_claims.py:95`/`:102` @ kit HEAD `917261b3`, anonymous
blobless clone this slice), and the kit's own source comment says the quiet part
out loud: "Seeded at 72h; revisable by data like every horizon constant" — no data
exists. Both failure modes the horizons trade off are REALIZED, not hypothetical:
twin execution (substrate-kit PRs #50/#51 — two lanes executed the same ORDER 005
the same day; the claim ritual exists because of it) and dead-lane deadlock (the
doctrine's own "a dead lane must never deadlock an order"; product-forge archived
with triggers disarmed; the 2026-07-11 coordinator archive event). The head: a
pre-registered dual-arm silence-horizon sim — alive lanes emit visible activity
separated by heavy-tailed gaps, dead lanes go silent forever, contenders check at
Poisson rate and steal any claim silent past θ — measuring the false-takeover rate
T (twin work) vs the orphan latency O95 (deadlock) across gap regimes × check
rates, θ swept over {6,12,24,48,72,168} with BOTH planted constants in-grid.
Written up as `ideas/substrate-kit/claim-expiry-horizon-lane-death-2026-07-13.md`
(probed single-pass this slice, sim-ready; homed in the substrate-kit section —
the verdict consumer owns the constants) and appended to `control/outbox.md` as
PROPOSAL 025. Fresh PRNG seeds 20260744–47 allocated strictly above P024's
high-water 20260743 (seed-registry hygiene, sixth consecutive slice). Dedup swept
tree-wide (`rg -i 'lease|ttl|abandonment horizon|claim-expir|claims-stale'`,
bootstrap.py and .substrate excluded) — hits only the claims doctrine files
themselves; no proposal, idea file, or card asks the expiry question, and no
sim-lab verdict V001–V024 touches claim lifecycle (the layout question — per-file
vs shared-append — was measured by superbot's `claim_layout_sim.py` and is cited
BY the doctrine; the expiry question is untouched by it).

**📊 Model:** fable · content + outbox proposal only (idea file, card, index row,
outbox append; no control/status.md or control/inbox.md writes; no checker or
script changes)

## 💡 Session idea

A **`claims-merged` deterministic pruning rule** — retire completed claims by git
FACT, not by silence horizon. Observed live this slice (the proposal's own anchor
data, seen from the other side): `control/claims/claude-proposal-018.md`–`023.md`
sit undeleted at HEAD `b11e2584` HOURS after their work branches merged, because
the lived convention rides the claim file on the work PR itself and nothing ever
deletes it — the doctrine's "delete your own claim file at session close" has no
enforcement except the ~72h `claims-stale` horizon, which is a STATISTICAL
instrument being spent on a case that is DETERMINISTIC: a claim whose
branch/scope token names a branch that has already MERGED is completed by
definition — no waiting, no false-positive risk, one `git log --merges` lookup.
The slice: `check_claims.py` gains a `claims-merged` advisory (same
always-exit-0 family as `claims-stale`) that fnmatches each claim's backticked
token against merged-PR branch names in local history and flags "completed —
prune on sight, no horizon needed" (mechanism precedent: the branch-prefix-drift
tripwire already mines `git log --merges` subjects locally, `scripts/preflight.py`
— same data source, new consumer). Complementary to PROPOSAL 025, not a slice of
it: 025 tunes (or retires) the statistical horizon for the ALIVE-vs-DEAD
ambiguity; this rule REMOVES the completed-claim class from that population
entirely, so the horizon polices only genuinely ambiguous silence — and tonight's
n=6 litter is its measured day-one catch. **Dedup** (this slice, `rg -i
'claims-merged|merged branch.*claim|self-consum|delete.*claim'` over ideas/ +
.sessions/ + doctrine): the doctrine states the delete-at-close duty and one card
notes a session deleting its own files by hand; NOTHING keys claim pruning off
merged-branch facts, and no checker in the kit's claims family reads git history.
Nearest neighbors: the claims-stale checker itself (statistical, this is
deterministic), the branch-prefix-drift tripwire (same git-log source, entirely
different consumer), and P025 (statistical horizon tuning — the class this rule
subtracts from).

## ⟲ Previous-session review

Newest predecessor card (`.sessions/2026-07-13-proposal-024.md`, worker slice
~04:12Z): closed clean — the round-2 COMPLETELY-UNRELATED-domain head (Braess's
paradox added-edge frequency), which rotated the unrelated-domain lane into a
SECOND fleet-external domain rather than re-mining P017's social choice. Two of
its habits are consumed here. First, **seed-registry hygiene**: P024 drew
20260740–43 above P023's high-water; this slice extends the sweep to 20260744–47,
strictly above 20260743 — sixth consecutive card on the discipline, still one-file
readable for the next head. Second, its 💡 — the **`model-basis:` declaration**
for sims whose verdict rides an embedded behavioral model — is DOGFOODED here for
the second time: this head's number rides on (gap-tail mixture × Poisson contender
checks), so the idea file carries a `## Model basis` note naming both and the
single most-likely-to-flip alternative (empirically measured gap distributions
from live claim history — which is exactly the NULL-case live probe). That makes
n=2 worked instances of the field with zero grammar spend, strengthening P024's
case for the optional outbox line. One treadmill P024 tracked and this card must
honestly extend: the **rotation-position treadmill** is now n=5 — this card again
re-derives its rotation position (round 3: 025 fleet-backlogs, after 021→022→023→024
closed round 2) from predecessor prose rather than reading a machine ledger;
P021's 💡 (the rotation ledger) remains measured-and-unbuilt. No reconciliation
debt found in P024's wake; its PR landed clean at `b11e2584`.
