# Session — PROPOSAL 034: drift-regime observability — is the arm of V024's conditional trust rule readable from the basket stream in time to act? (venture rotation, round 5)

> **Status:** `complete`
> **Model/time:** fable · 2026-07-13T09:06:14Z (Ideas Lab worker slice — draft the
> VENTURE rotation round-5 head under standing owner ORDER 003. Card born
> in-progress as the designed gate hold; flips complete in this PR's final commit
> once the 💡 slot resolves)

**📊 Model:** fable · content + outbox proposal only (idea file, card, index row,
outbox append; no control/status.md or control/inbox.md writes; no checker or
script changes)

## Scope

Draft a genuinely new sim-shaped idea for the VENTURE rotation slot, round 5,
under standing owner ORDER 003 (continuous pipeline — verified open/standing at
HEAD `11c5a1f` before any work: priority P1 · standing, "continue coming up with
new ideas, that is your main purpose"). Rounds 1–4 of the slot: P018 (books →
V020 null, mode-conditional rule), P022 (trading → V024 null, drift-conditional
rule), P026 (trading → V028 approve, breadth pays under its own bar), P030
(books → V032 reject, early-signal adaptation buys nothing — V020's conditional
rule stands). Candidates weighed on merit after reading the actual verdict texts
at the local sim-lab clone `cd47c06`:

- (a) **V024's own open thread — WINNER.** V024's "What
  it did NOT settle" opens with "Which null arm the lane's real data lives in",
  and its NULL rule is drift-conditional: the top KEEP margins clear best-of-6
  selection noise ONLY in the drift-matched arm (P ≤ 0.004; matched q99(G6)
  0.398324 < 0.484) and are selection-compatible in the zero-drift arm (P up to
  0.169). The named locator probe targets the VOL axis, which V024 itself
  measured INERT (spread 0.000) — the binding DRIFT axis is located only by the
  full-window basket Sharpe 1.147, a static average. Whether the drift ARM is
  readable from the basket stream at actionable windows — i.e. whether a
  drift-conditional trust rule is even implementable — is un-simmed, and V028's
  own LIMITS line points straight at it: "a bar registered for a
  regime-switching world should be recomputed under it." Written up as
  `ideas/trading-strategy/xsec-drift-regime-observability-2026-07-13.md`
  (probed single-pass this slice, sim-ready) and appended to
  `control/outbox.md` as PROPOSAL 034 (tail verified at HEAD before append:
  PROPOSAL 033 · 2026-07-13T08:43:05Z). Seed registry: seeds 20260772–75
  allocated strictly above the P033 high-water 20260771. One design lesson
  recorded in the 💡 below: the first-draft APPROVE band (BA ≥ 0.80) was
  killed by drafting arithmetic before registration — the pure-window ceiling
  tops out at 0.717 at w = 252 — and the decision metric was re-derived to
  ΔE_oracle before any band froze.
- (b) the books mode fork from a new angle (portfolio-level allocation across
  families vs per-family mode choices) — V032 killed within-family adaptation
  only, so the fork is live; but V032's REJECT is one wake old, the books half
  has had two of four venture rounds, and its "richer families only if the lane
  measures a real cell where the conditional rule is unavailable" clause argues
  for letting the lane's V020 s-probe land first.
- (c) an un-simmed venture-lab product backlog item — the section survey shows
  the committed anchors there are pricing/elasticity heads the lane itself
  already routed (P018's dedup clause), and the maker/ebook rows carry no
  committed numeric anchor to chain a hermetic sim to tonight.

## 💡 Session idea

**Band-liveness arithmetic as a required probe-battery row — kill dead decision
bands at drafting time, before pre-registration freezes them.** This slice's
first-draft decision metric was balanced accuracy with an APPROVE band at
BA ≥ 0.80 — and a five-line drafting-side calculation (SE of a trailing w-bar
Sharpe ≈ √(252/w) → pure-window ceiling Φ(1.15·√(w/252)/2) = 0.613/0.658/0.717
across the swept windows) showed that band was ARITHMETICALLY UNREACHABLE:
a pre-registered sim whose APPROVE can never fire is theater, and once
registered the band cannot be honestly moved. The fix here was to re-derive the
decision metric (ΔE_oracle vs the best static prior) BEFORE any registration —
but the check only happened because the ceiling had a closed form and the
drafter thought to compute it. The slice: one more row in the probe battery
(README § probe battery) — "band liveness: for EACH of APPROVE/REJECT/NULL,
one or two lines of arithmetic or citation showing a reachable world" — the
same pre-emption the P033-family "frontier is genuinely open, not rhetorical"
paragraphs already do ad hoc (P021's drafting arithmetic, P033's lam04·V2
open-frontier check), promoted from habit to checklist so the dead-band
hazard is caught by form, not luck. Dedup (this slice, `rg -i
"band.?liveness|dead.?band|reachab.*band|all three.*live"` over ideas/ +
.sessions/ + control/): the "REJECT, APPROVE, and NULL are all live" phrasing
appears as free prose in P020/P021/P024/P028/P032/P033 with supporting
arithmetic in some and bare assertion in others — no committed convention,
checker row, or battery question requires it; distinct from P033's 💡 (an
outbox digest index — read-cost, not band validity) and from the kit's
check_ideas PROBE rule (which counts the 8 questions but cannot see whether a
band is reachable).

## ⟲ Previous-session review

Newest predecessor card (`.sessions/2026-07-13-proposal-033.md`, worker slice
~08:43Z): closed clean — the fleet-backlogs round-5 head (assign-at-merge
merge-queue tax, PROPOSAL 033), with the candidate kill list recorded honestly
and the seed registry advanced to high-water 20260771. Two habits consumed
here: its seed-registry note makes this slice's 20260772+ allocation mechanical;
and its practice of reading the parent verdict's committed results.json direct
(not prose) is repeated here — V024's per-cell q99(G6) anchors below are
machine-read from `sims/verdict-024-keep-margins-noise/results.json` at the
local clone, not transcribed from the REPORT. Round-4 venture context carried:
P030 → V032 REJECT (early-signal adaptation buys nothing; the costly-error arm
was checked first and hit) — that verdict's discipline (an adaptive rule must
beat the best STATIC rule, not just a strawman) is imported into this head's
baseline design as the oracle-static comparison bar.
