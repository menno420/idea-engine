# Session — PROPOSAL 020: casino house-edge fairness envelope (game-mechanics rotation)

> **Status:** `in-progress`
> **Model/time:** fable · 2026-07-13T02:02:25Z (Ideas Lab worker slice — draft the
> ORDER 004 game-mechanics head pre-pinning tonight's minigame/casino consolidation
> odds + PROPOSAL 020 outbox append; card born in-progress as the designed gate
> hold, flips complete in this PR's final commit)

## Scope

Draft a genuinely new game-mechanics idea under standing owner ORDER 003
(continuous pipeline) + ORDER 004's seat cycle (item 1: "SIM-REQUESTs first — …
World's balance pins … will arrive; same-wake turnaround"; rule 3's rotation,
game-mechanics slot taken early and deliberately because its consumer window —
tonight's minigame/casino consolidation — closes before a strict rotation reaches
it). The head: a pre-registered fairness-envelope sweep for house-banked wager
minigames — (house-edge grid × max-bet cap × payout shape) against three
registered bands (FUN casual P_ahead ≥ 0.25 / SAFE max-policy P_wipe ≤ 0.05 with
capped martingale swept / SINK grinder best-policy P_double ≤ 0.10), dual-arm
(exact `math.comb` binomial tails + gambler's-ruin closed forms + an e=0 exact
control pin the seeded MC at a 1.0 pp gate), fully hermetic (the PROPOSAL
017/018/019 precedent: every fixture a pinned constant, zero repo/network reads
in the verdict session) — written up as
`ideas/superbot/casino-house-edge-fairness-envelope-2026-07-13.md` (probed
single-pass this slice, sim-ready) and appended to `control/outbox.md` as
PROPOSAL 020. Premise grounding is entirely in-tree (no new network reads):
superbot's wager surface via the wager-flow-map probe's pinned seam facts
(@ superbot `2c7d2de`) + PROPOSAL 009's game corpus; the consolidation window via
inbox ORDER 004's verbatim "World's balance pins". Dedup swept vs PROPOSALs
001–019 (outbox re-read at HEAD `f7906e5`), sim-lab VERDICTs 001–019 (local clone
@ `e5e1bee`, offset map read — V017/V018's settled game-mechanics questions NOT
re-asked), the superbot section's 237-doc harvest index (wager-flow-map = tooling
tracer; faucet-sink diagnostic = historical + observational), and tree-wide greps
(casino / house edge / martingale / gambler / ruin / RTP / max bet / bankroll,
bootstrap.py/.substrate excluded) — the word "casino" did not occur in this
repository before this head; nearest prior art is PROPOSAL 016 → VERDICT 018 by
consumer (same pin-before-the-build move, different mechanic: encounter pacing vs
wager sink) and PROPOSALs 003/008 by family (faucet rate-fencing vs sink
ruin-fencing, threat inverted).

**📊 Model:** fable · content + outbox proposal only (idea file, card, index row,
claim file, outbox append; no product code, no control/status.md or
control/inbox.md writes)

## 💡 Session idea

(placeholder — filled in this PR's final commit as designed)

## ⟲ Previous-session review

Newest predecessor card (`.sessions/2026-07-13-proposal-019.md`, worker slice
~01:31Z): closed clean — its PR #284 merged at `f7906e5` with the PROPOSAL 019
outbox entry grammar-clean and the card flipped complete in its final commit as
designed; notable craft: it anchored one arrival regime to REAL data it
hand-derived from four re-pin prose sections of the websites lane backlog
(births {2,5,4,11} / consumptions {5,9,4,3}, byte-verified at blob `e14bb15` by
`git hash-object`) instead of inventing all three regimes — and then honestly
demoted that anchor to a bracketing, reporting-only role because n=4 cannot fit
a distribution. Its pre-registered EVALUATION ORDER (REJECT-a checked FIRST so
"organic refill suffices" cannot be shadowed by an eager APPROVE) is a discipline
this card's proposal reuses. Its 💡 (make `check_harvest --write-pins` append a
per-pin {born, consumed, …} tally row so backlog trajectories become committed
data instead of re-pin-prose archaeology) is still unconsumed and NOT re-minted
here — checked before writing this card's own 💡 (rg for per-pin tally /
trajectory capture — only its own card carries it). No reconciliation debt found.
