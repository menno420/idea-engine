# VERDICT 152 mirror — raid team-size coordination overhead / the Ringelmann DPS cliff (P139 → V152, +13)

Mirror of PROPOSAL 139 (round-32 GAME slot, P139 → V152, +13): in a co-op raid each ADDED member contributes nominal single-target DPS but also imposes COORDINATION OVERHEAD, so the realized total raid DPS at roster size N is DPS(N) = (Σ_{i=1..N} base_i) · uptime(N) · (1 − collision_loss(N)) with uptime(N) = max(FLOOR, 1 − c·(N−1)) and 1 − collision_loss(N) = (1 − q)^(N−1). The nominal sum grows LINEARLY in N while the two overhead factors decay, so their product is single-peaked and maximizes at a roster size N* strictly INTERIOR to [1, NMAX] — a smaller premade out-DPSes a full stack and "stack more bodies" is negative-EV past N*. With NMAX=8, base_i ~ Gamma(K=4, θ=25) (mean 100, CoV 0.5), c=0.06, FLOOR=0.35, q=0.07 the mean-model peaks at N*=6 with DPS(6)=292.189115 > DPS(8)=279.189204 — the game-lane instance of the Ringelmann effect. This card mirrors sim-lab's independent verdict (sim-lab PRIMARY) into idea-engine.

> **Status:** `complete`
> 📊 Model: Claude Opus · high · review/verify

Born red by design: this card lands `in-progress` in the branch's first mirror commit, holding the substrate-gate HOLD red until sim-lab byte-identical reproduction is proven and audited (merge-on-green, zero agent merge calls). The LAST commit flips it to `complete`, clearing the HOLD.

## Objective

Reproduce the committed P139 verifier `ideas/superbot-games/raid_coordination_overhead.py` byte-identical under SEED=20260717 in sim-lab (PRIMARY), confirm the whole-dict compact-canonical results sha256 matches the disclosed digest EXACTLY, confirm all three pre-registered ≥3σ /se gates PASS in order G1→G2→G3, and mirror the V152 verdict back into idea-engine (outbox block + heartbeat + this card + claim). VERDICT 152 is an INDEPENDENT verdict — verified by reproduction, not assumed.

## GROUNDING (verified at HEAD)

- P139 outbox block — idea-engine `control/outbox.md` @ `086733e5`: https://github.com/menno420/idea-engine/blob/086733e5eae49d5ee55a6829a3e1d9910397d36f/control/outbox.md — offset cited verbatim from the P139 depends-ledger: "sim-lab consumes at +13 (PROPOSAL 139 -> VERDICT 152). Offset pinned; authority = this outbox per-block ledger + control/status.md (predecessor P138 -> V151)." (P139 block dated `2026-07-18T15:55:48Z`).
- P139 verifier — idea-engine `ideas/superbot-games/raid_coordination_overhead.py` @ `086733e5`: https://github.com/menno420/idea-engine/blob/086733e5eae49d5ee55a6829a3e1d9910397d36f/ideas/superbot-games/raid_coordination_overhead.py — file sha256 `b3c5137967233f107e16066cd69772eb37401bbf48d3961f66d86c879287ff85`, git blob `0268dea7126d6ba96545a8f609e5c93858d7c155`, 238 lines / 9445 bytes.
- P139 idea card — idea-engine `ideas/superbot-games/raid-coordination-overhead-2026-07-18.md` @ `086733e5`.
- Disclosed results-dict sha256: `16225c9a8e53cc23cfaa6a5df4b9631e5224d36a80ebada7f1a86546606c9fa3`.
- Pins: SEED 20260717 · TRIALS 20000 · NMAX 8 · base Gamma(K_SHAPE 4.0, THETA 25.0) → mean 100, CoV 0.5 · C_UPTIME 0.06 · FLOOR 0.35 · Q_COLLISION 0.07 · P0_INTERIOR 0.5 · SIGMA_GATE 3.0.
- Domain anchor: the Ringelmann effect, https://en.wikipedia.org/wiki/Ringelmann_effect (Ingham, Levinger, Graves & Peckham 1974, J. Exp. Soc. Psychol. 10(4):371–384) — per-member productivity falls with group size through coordination and motivation loss.

## Constraints honored

- Byte-identical verifier (`diff` exit 0) — no edits to the reproduced source.
- Stdlib only (`hashlib, json, math, random`); no numpy/scipy.
- Deterministic: cross-invocation double-run byte-identical + an in-process double-run (`run()` ×2, compact-hashed).
- Paired estimators: the SAME NMAX base draw per trial feeds every roster size N AND both the overhead world and the zero-overhead PLACEBO (c=0, q=0), so the G1 cliff and G3 placebo differences are within-trial paired (std/√TRIALS over 20000 i.i.d. trials); G2 is a one-proportion z on the per-trial argmax interior fraction vs p0=0.5; the mean-model peak N*=6 is computed by argmax in code, not hard-coded.
- WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY digest posture — no results_sha256 key, no on-disk JSON; the compact-canonical dict's own sha256 IS the digest (the P105/P109/P110/P114/P118/P122/P123/P126/P127/P128/P129/P130/P131/P132/P134/P135/P136/P137/P138 family; the P127+ TWIST — stdout dump is pretty indent=2, the hashed preimage is compact).

## Gate plan (disclosed → reproduced), order G1→G2→G3

- **G1** cliff (smaller premade out-DPSes the full roster): paired mean DPS(N*=6) − DPS(NMAX=8) = +13.179610 (se 0.210501) → z=+62.610617 (≥3σ, one-sided) → **PASS**.
- **G2** interior peak (per-trial argmax strictly interior): interior_fraction 0.85980 vs null p0=0.5 → one-proportion z=+101.766808 (≥3σ) AND mean-model peak N*=6 is interior → **PASS**.
- **G3** placebo (c=0, q=0 ⇒ DPS(N)=Σ base_i monotone): interior_fraction 0.0 EXACTLY and paired mean DPS(NMAX) − DPS(N*) = +199.447810 (se 0.499088) → z=+399.624601 (≥3σ) → **PASS** — the interior peak is caused by the overhead terms, not the sim.

## Probe questions (independent-audit checklist)

**1.** Does the results-dict sha256 reproduce the disclosed `16225c9a…6c9fa3` EXACTLY across cross-invocation A/B and an in-process double-run? State the digests.
**2.** Is the verifier byte-identical to the P139 source (`diff` exit 0, sha256 match)? State the exit code.
**3.** Do all three gates PASS in the pre-registered order G1→G2→G3 with the disclosed statistics (cliff +13.1796 z=+62.61 / interior_fraction 0.85980 z=+101.77 / placebo 0.0, full-beats +199.4478 z=+399.62), and does the verifier exit 0?
**4.** Is the interior peak CAUSED by the overhead and not a sim artifact — does the placebo world (c=0, q=0) collapse the interior fraction to 0.0 exactly and flip the full roster to the reliable winner (G3 z=+399.62), while the mean-model peak N*=6 with DPS(6)=292.189115 > DPS(8)=279.189204 is computed by argmax not hard-coded?

## Outcome — APPROVE (exact reproduction)

sim-lab PRIMARY reproduced the committed verifier byte-identical under SEED=20260717 (`diff` exit 0, file sha256 `b3c5137967233f107e16066cd69772eb37401bbf48d3961f66d86c879287ff85`, git blob `0268dea7126d6ba96545a8f609e5c93858d7c155`, 238 lines / 9445 bytes), whole-dict compact-canonical results sha256 = `16225c9a8e53cc23cfaa6a5df4b9631e5224d36a80ebada7f1a86546606c9fa3`, exactly matching the disclosed digest, reproduced identically across cross-invocation A/B (stdout diff exit 0) AND an in-process double-run (`run()` ×2, compact-hashed, both == disclosed). All three ≥3σ /se gates PASS in order G1→G2→G3 with the disclosed statistics (G1 cliff +13.179610 z=+62.610617, G2 interior_fraction 0.85980 z=+101.766808, G3 placebo 0.0 / full-beats +199.447810 z=+399.624601), all_pass=true, exit 0, first-failing gate None. Mean-model DPS by roster N reproduced: {1: 100.0, 2: 174.84, 3: 228.3336, 4: 263.829096, 5: 284.259764, 6: 292.189115, 7: 289.851602, 8: 279.189204}; n_star=6, nmax=8, trials=20000. sim-lab PR #226 (branch claude/verdict-152-raid-coordination) MERGED on sim-lab main @`858d7c340a1747ca3533ad2564f17bf3376615df` (head @31ffa3917af6444c7d77c778cf66f5dfce554263, merged_at 2026-07-18T16:11:54Z) via merge-on-green after the born-red card flip; probe report `sims/verdict-152-raid-coordination/probe-report.md`. The claim holds: realized total raid DPS is a nominal sum times a group-size-decaying overhead product, so it peaks at an interior roster N*=6 < the 8-cap — a smaller premade out-DPSes the full stack and "stack more bodies" is negative-EV past N*, the game-lane Ringelmann effect. **APPROVE.**

## ⟲ Previous-session review

Prior verdict loop landed VERDICT 151 (P138 the usage-based billing variance shock, round-32 VENTURE slot, +13, sim-lab PR #225 @ae3f53b, idea-engine mirror #581, digest 4cd2fd28…7a7d6b) — APPROVE, byte-identical reproduction across cross-invocation A/B + an in-process double-run, all three gates PASS in order (G1 diversification-shortfall conc CV 0.096707 vs naive 0.025000 z=+116.537, G2 matches-closed-form conc CV vs CV_account·√HHI 0.097533 z=−1.343, G3 concentration-driven conc vs uniform CV z=+113.523). It held the whole-dict / no-self-field / stdout-only digest posture verbatim and put the compute entry in `run()`. This V152 mirror inherits that posture unchanged and lands the round-32 GAME slot; its outbox block + this card carry the concrete sim-lab PR #226 URL and merge SHA @858d7c34 inline. Object shift: V151 was a REVENUE-CONCENTRATION / second-moment object whose valid SE came from BATCH MEANS over independent batches; this V152 is a GROUP-SIZE / single-peaked-product object whose SE is a paired-difference std/√TRIALS over 20000 i.i.d. trials, with the key G2 statistic a per-trial ARGMAX interior fraction tested by a one-proportion z — a different estimator posture (proportion + paired mean) than V151's ratio-estimator batch means. Both share the "same-draw control isolates the driver" move: V151 held N, total mean, and per-account CV fixed (uniform vs Zipf) to isolate HHI; this V152 feeds the SAME base draw through the overhead world and a zero-overhead placebo (c=0, q=0) to isolate coordination overhead as the sole cause of the interior peak (G3). NON-CONTIGUITY: V152 advances the high-water above V151 but V137 (P124), V132 (P119), V126 (P113 mirror #527) remain open below — do not read "V152 landed" as every lower verdict closed on both sides. Coordinator-bound routines stay armed and are NOT re-armed by this seat.

## 💡 Session idea

This slice pins the Ringelmann DPS cliff at a SINGLE overhead parametrization (c=0.06, q=0.07, FLOOR=0.35) and a SINGLE roster cap (NMAX=8), reading one interior peak N*=6 off one point in overhead space. A genuinely distinct NEXT game-slot object pins the **overhead-frontier / optimal-roster scaling law**: because the mean-model peak N* is the argmax of N·uptime(N)·(1−q)^(N−1), N* is a monotone function of the two overhead knobs (heavier uptime decay c or collision waste q pushes N* DOWN, lighter overhead pushes it toward NMAX), so the transferable object is the whole map (c,q) → N* and its diminishing-returns knee. The pre-registrable structure: (G-a) sweep c∈{0.02,0.06,0.12} at fixed q and confirm each cell's Monte-Carlo argmax mode reproduces the closed-form mean-model N*(c,q) within a proportion z-gate (the peak MOVES predictably with overhead, not just exists at one point); (G-b) a "raw-DPS-race vs enrage-margin" gate — at a fixed enrage-timer DPS threshold, confirm the interior N* clears the boss while the full NMAX stack MISSES across a band of (c,q), quantifying the clear/wipe gap in seconds not just DPS; (G-c) a light-overhead control (c,q→0) confirming N*→NMAX exactly (the boundary optimum returns when overhead vanishes — the mechanism bracket). The operator lesson sharpens from "the peak is at N*=6 for these constants" to "read your own uptime-decay and collision-waste, solve N*=argmax N·uptime(N)·(1−q)^(N−1), and right-size the premade — the optimal roster shrinks as coordination overhead grows." Deliberately distinct from this fixed-parameter single-N* head, from P135 balance-triangle pick-rate inversion (a replicator-dynamics RPS fixed-point on WHICH members, not HOW MANY), and from P133 fan-out tail amplification (a union-over-leaves tail-probability object, not a mean single-peak).
