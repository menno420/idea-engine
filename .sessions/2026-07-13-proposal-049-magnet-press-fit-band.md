# Session — PROPOSAL 049: the magnet press-fit band — does the shipped 0.15 mm interference default land the PRESS band? (FLEET-BACKLOGS slot, round 9 opener)

> **Status:** `complete`
> **Model/time:** fable-5 · 2026-07-13T22:22:40Z (Ideas Lab worker slice — draft the
> round-9 FLEET-BACKLOGS head under standing owner ORDER 003. Card born in-progress
> as the designed born-red gate hold; flipped complete in this PR's final commit at
> 2026-07-13T22:28:48Z)

## Scope

Draft a genuinely new sim-shaped idea for the FLEET-BACKLOGS rotation slot, round 9,
under standing owner ORDER 003 (continuous pipeline) and ORDER 004 rule 3's rotation
("fleet backlogs → venture's book/product space → game mechanics → COMPLETELY
UNRELATED domains"). Round 8 closed fully served (fleet backlogs → P045 #343, venture
→ P046 #348, game mechanics → P047 #352, COMPLETELY UNRELATED → P048 #353), so round
9 reopens at the FLEET-BACKLOGS slot and this slice serves it. Harvest source:
curious-research @ `a9fd5faa6a10b4d1364d205dbeac7a8678e1bd73` (ls-remote re-verified
this session) — the repo's SECOND tap: P041 (round 7) harvested the spool-weight-scale
project only; this head harvests a different, untapped project subtree
(`projects/effector-mount/` + the `projects/tolerance-test-coin/` band semantics as
cross-instrument calibration), disclosed in the idea file's dedup section. Claim
landed first (`control/claims/claude-proposal-049-magnet-press-fit-band.md`).

Idea file `ideas/fleet/magnet-press-fit-band-2026-07-13.md` (probed single-pass this
slice, sim-ready), fleet README index row, then appended to `control/outbox.md` as
PROPOSAL 049. Seeds 20261333–336, strictly above the P048 high-water 20261332
(re-swept this session: digit-boundary regex + range-notation companion +
`results.json` exclusion over this tree AND /home/user/sim-lab; the max allocation
found in either repo is P048's 20261332 — larger numerals are digit substrings inside
sim-lab `results.json` exact-Fraction numerators, data not seeds, the P046 💡
sweep-recipe trap re-confirmed live).

**📊 Model:** `fable-5` (family-level self-report per ORDER 001 / Q-0262 — the family
the harness reports for this Ideas Lab slice, never copied from a schedule surface) ·
content + outbox proposal only (idea file, card, fleet section index row, claim file,
outbox append; no product code, no control/status.md or control/inbox.md writes; no
sim-lab writes — the V059 verdict session runs parallel there and its claim
`control/claims/claude-verdict-059-parrondo-lattice.md` is untouched).

## 💡 Session idea

**When the rotation re-harvests an already-tapped repo, the dedup burden INVERTS: read
the PRIOR head's own Relations/runners-up section FIRST, because a runner-up it weighed
and DROPPED is both a landmine and a gift — it looks like fresh material but was already
adjudicated, and its recorded DROP REASON names exactly the structural problem your new
model must dissolve to harvest there honestly.** This slice hit the pattern live: P041's
Relations explicitly dropped "curious-research tolerance-test-coin clearances" as a
standalone head ("the doc's own point is that the answer is printer-specific — a sim
would launder bench variance it cannot know"). A naive second tap would have re-proposed
the coin and silently resurrected a dropped head; the recorded drop reason instead
became the DESIGN CONSTRAINT — P049 models printer-specificity as the population spread
`H` (the exact structure whose absence made the dropped head unpriceable), uses the coin
only as band semantics + one pinned exemplar, and tests a DIFFERENT artifact (the
effector-mount's shipped universal default). The recipe, mechanical: before a second tap
of repo X, `rg -n 'runners-up|weighed and dropped|Relations' ideas/` for X's prior
heads, quote the drop reason in your own dedup section, and show either (a) your head is
a different artifact, and (b) if it touches the dropped material at all, that your model
STRUCTURALLY answers the drop reason rather than ignoring it. Dedup (this slice, at HEAD
`dec6ec4`): P045's 💡 is harvest-by-hedge-marker, P046's is the seed-sweep recipe,
P047's is harvest-by-validation-gap, P048's is the liveness-disclosure inversion — none
touches second-tap discipline or dropped-runner-up adjudication; this is the first 💡
for it, and it becomes load-bearing exactly now: the fleet-backlogs slot has more rounds
than untapped repos, so second taps are the slot's steady state from round 9 on.

## ⟲ Previous-session review

Newest predecessor card
(`.sessions/2026-07-13-proposal-048-parrondo-losing-games-combine.md`, worker slice
~21:49Z, fable-5): closed clean, and its rituals transferred here — the blob-pinned
`idea:` link (P049's outbox append pins the `bbcb9f9` idea-doc commit) and, most
consequentially, its 💡 itself: the liveness-disclosure inversion for closed-form
decision arms got its FIRST downstream application this slice — P049's decision arm is
an exact enumeration, so per that 💡 the drafting-time landing was computed and
DISCLOSED (REJECT at FAIL(15) = 145/861) instead of pinning a fictitious "no expected
landing"; the norm earned its keep one card after it was minted. The P046 seed-sweep
recipe also ran a FOURTH consecutive time and again produced the true high-water
(20261332) cleanly while the `results.json` exclusion absorbed the predicted
Fraction-numerator aliases — four-for-four. Honest nit: P048 repeats its
seven-domain rotation litany essentially verbatim in FIVE places (claim file, card
scope, idea-doc Class line, idea-doc Origin, fleet index row) — a lockstep-rot hazard
where one copy gets edited and four silently stale; a single canonical litany home (the
index row) with pointers would age better, and P049 kept its own litany copies to the
minimum the grammars force (claim + doc header + index row). Carried flag, now FOUR
cards deep: the two coexisting claim-landing patterns (P046's fast-lane control-only PR
vs claim-in-first-commit) remain undocumented; this slice again took claim-in-first-
commit per its orders (decide-and-flag: noted for the manager sweep, not expanded here).

## Close-out

All pieces landed before this flip: claim + born-red card @ `1f7a51f` (branch's first
commit; PR #358 opened READY immediately after the push — never draft; merge left to
the enabler workflow, no agent merge), idea doc + fleet section index row @ `bbcb9f9`,
PROPOSAL 049 outbox append @ `7102e62` (idea: link pinned to the `bbcb9f9` blob;
`scripts/check_ideas.py --outbox` exit 0 verified after the append). Rotation verified
at HEAD before drafting: round 8 closed fully served (P045 #343 / P046 #348 / P047 #352
/ P048 #353), so round 9 reopens at FLEET-BACKLOGS per ORDER 004 rule 3 (quoted
verbatim in the claim). Harvest source curious-research @ `a9fd5fa` ls-remote
re-verified (unchanged since P041's pin); the second tap disclosed with the
dropped-runner-up distinction argued in the idea file's Relations. Seeds 20261333–336
strictly above the true cross-repo high-water 20261332. This flip is the branch's last
commit; both checkers (`python3 bootstrap.py check --strict`, `python3
scripts/check_ideas.py --outbox`) run green at flip time or this commit does not push.
