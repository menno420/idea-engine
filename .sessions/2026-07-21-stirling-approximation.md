# PROPOSAL 256 — Stirling's approximation `lim n!/(√(2πn)(n/e)^n) = 1`: a self-contained pure-math closed form (the "unrelated / pure-math" slot). The limit is trivially 1, so the EXACT teeth come from the correction series: writing `r_n = n!/(√(2πn)(n/e)^n)`, the log-ratio `ln(r_n) = Σ_{k≥1} c_k/n^{2k−1}` has exact rational coefficients `c_k = B_{2k}/(2k(2k−1))` (Bernoulli) and the ratio expands as `r_n ~ Σ a_k/n^k` with exact `a_k = 1, 1/12, 1/288, −139/51840, …`. The whole rational backbone (Bernoulli recurrence → c_k → a_k, exp-of-series and its inverse log-of-series agreeing) is verified with 0 mismatches in `fractions.Fraction`. A Monte-Carlo estimate of `n!` via the Gamma integral agrees with the closed form at |z|=1.30; Robbins' EXACT rational bracket `1/(12n+1) < ln(r_n) < 1/(12n)` holds with 0 violations across n=1..64 in 60-digit `Decimal`; and the naive `√(πn)`-prefactor foil (factor-2-under-root, predicting ratio 1/√2) is REJECTED on the same sample at |z_foil|=598.9.

> **Status:** complete

> **📊 Model:** Claude Opus · high · idea/planning

started: 2026-07-21T13:15:00Z

💓 Heartbeat: 2026-07-21T13:15:00Z — FLEET slice — new PROPOSAL 256 (pure-mathematics / analysis lane — the "unrelated / pure-math" slot) → sim-lab VERDICT 269 (+13 offset). Ships the sim-ready idea doc `ideas/fleet/stirling-approximation-2026-07-21.md` + firsthand verifier `ideas/fleet/verify_256_stirling_approximation.py` (stdlib only: json, hashlib, math, random, fractions, decimal, sys) + this born-red card + the `## PROPOSAL 256` outbox block in ONE first commit. Digest target `results_sha256 = f6511c51458be68eb564460ed1229456b0e7fd0aff2733e75c47e22f4066d267` (printed AND compared as a literal 64-char string, no truncation); reproduced byte-identical across an in-process double-run guard (`in_process_double_run: IDENTICAL`), a `--selfcheck` path, AND a separate re-invocation process. Verifier file sha256 `d2e5054bcc38b0367152832fd9d21bd50a9cd91db2a291bf5479ea875bdfd407`. SEED=20260717. Four pre-registered gates each read in its OWN direction: G1 EXACT (fractions.Fraction — Bernoulli recurrence B_0..B_14, log-series c_k=B_{2k}/(2k(2k−1)), ratio-series a_0..a_6 via exp-of-series with the inverse log-of-series recovering the c_k, checked=29, mismatches=0), G2 MC AGREEMENT (N=400000 Gamma-importance-sampling draws estimate n!=∫x^n e^{−x}dx at n=10, ratio r̂=1.008 vs closed-form Stirling prediction, z=1.3029<3), G3 ROBUSTNESS/INVARIANCE (Robbins' exact rational bracket 1/(12n+1)<ln(r_n)<1/(12n) holds with 0 violations across n=1..64 in 60-digit Decimal + a second Gamma proposal estimator agrees at z=−1.4413), G4 FALSIFIABILITY (opposite direction, SAME sample — the √(πn) factor-2-under-root prefactor foil predicting ratio 1/√2=0.707107 REJECTED at z_foil=598.9410, |z_foil|≫50, while the true ratio accepted at z=1.3029; secondary "drop 1/(12n)" foil rejected at z=17.8982). Grounding sha1-pinned to Wikipedia "Stirling's approximation" oldid 1360453793 (`5b7a91f2e93f54a950126ac06be9d35bcf4d8095`, 34026 bytes; API sha1 == self-computed hashlib.sha1), split HONEST with disclosed seams (the main asymptotic, the full ratio-series a_1..a_5, the c_k formula, and Robbins' bracket are all QUOTED verbatim — the article is rich — so the firsthand contribution is the exact-Fraction backbone end-to-end + the exp/log inverse check that extends a_6 beyond the page + the MC Gamma-integral agreement + the numeric Robbins sweep + the √(πn) falsification; a_6, the Bernoulli recurrence route, the MC estimator, the numeric sweep, the foil, SEED, and the digest are grep 0). Born-red HOLD armed on this first card commit (in-progress).

⏳ Flip note (born-red): this card ships `> **Status:** in-progress` on its FIRST commit so the substrate born-red gate holds the idea-engine PR RED until the slice is reviewed. The coordinator (owner) flips it to `complete` as the deliberate LAST step, only after confirming the outbox block + `python3 bootstrap.py check --strict` reds ONLY on this HOLD. That flip clears the HOLD and releases merge-on-green. NO merge API calls from this session; NO status.md heartbeat edit; NO `## VERDICT` block; NO verdict / proposal high-water advance from this worker. The gate battery is GREEN (all four gates PASS, determinism holds three ways) — the red is the born-red HOLD alone, not a failing gate.

## What this proposal does

Adds a fleet PROPOSAL establishing **Stirling's approximation** `lim_{n→∞} n!/(√(2πn)(n/e)^n) = 1` as a self-contained, exactly-verifiable pure-math closed form — the "unrelated / pure-math" slot, orthogonal to every fleet, venture, and game head. Because the limit is 1, the **exact** teeth come from the **correction series**: the log-ratio coefficients `c_k = B_{2k}/(2k(2k−1))` (exact rationals from Bernoulli numbers: `1/12, −1/360, 1/1260, −1/1680, …`) and the ratio-series coefficients `a_k` (`1, 1/12, 1/288, −139/51840, …`). The verifier builds the whole backbone end-to-end in exact `fractions.Fraction` (Bernoulli recurrence → `c_k` → `a_k` by exp-of-series, cross-checked against the inverse log-of-series), estimates `n!` by Monte-Carlo integration of the Gamma representation, confirms **Robbins' exact rational bracket** across a full `n`-sweep in 60-digit `Decimal`, and **falsifies** the single most plausible confusion — the `√(πn)` (factor-2-under-the-root) prefactor. **Distinctness:** a pure-analysis closed form and its exact rational correction structure — no lane, no cash-flow, no game value, no Markov chain. `stirling` / `robbins` / `12n+1` / `1/(12n)` are grep-0 as heads across both repos (the only nearby tokens are "Stirling's constant" as a `√(2π)` foil label inside the Gaussian head P252 and a "Stirling limit `2/√π`" machinery note in a Banach-matchbox head — neither is the factorial asymptotic); orthogonal to random-walk return (P228), coprime density → 6/π² (P232, which uses ζ(2) — this uses NO zeta), Burnside (P236), non-crossing Catalan (P240), Buffon (P244), Cantor set (P248), derangements (P229), Gaussian integral √π (P252).

## Method

- Firsthand verifier, stdlib-only (`json`, `hashlib`, `math`, `random`, `fractions`, `decimal`, `sys`).
- Digest posture: WHOLE-DICT — the canonical results dict's own sha256 IS the digest; `main()` builds the results twice in-process and asserts canonical-JSON byte-equality before printing the digest and exiting 0. Target `results_sha256` full-64 `f6511c51458be68eb564460ed1229456b0e7fd0aff2733e75c47e22f4066d267` (literal 64-char compare, no truncation); reproduced byte-identical across an in-process double-run guard, a `--selfcheck` path, AND a separate re-invocation; SEED=20260717. Verifier file sha256 `d2e5054bcc38b0367152832fd9d21bd50a9cd91db2a291bf5479ea875bdfd407`.
- Exact backbone (`fractions.Fraction`, no floats): `bernoulli(upto)` = the recurrence `Σ C(m+1,k)B_k = 0`; `log_series_coeffs` = `c_k = B_{2k}/(2k(2k−1))`; `ratio_series_coeffs` = `a_k` by the exp-of-series recurrence `a_k = (1/k)Σ j·l_j·a_{k−j}`; `recover_log_from_ratio` inverts it (log-of-series) and must return the `l_j`. Monte-Carlo: importance sampling from `Gamma(shape, 1)` via `random.gammavariate`, per-draw weight `Γ(shape)·x^{n−shape+1}` (for `shape = n`, estimator `(n−1)!·x` with `E = n!`; the normalizer `(n−1)!` is the proposal's own, NOT the answer `n!` — non-circular); the ratio is formed in log-space via `lgamma` so no huge integer/float materializes. `random.seed(SEED)` re-seeded at the START of each MC gate.
- Gates (each read in ITS OWN direction, against the proposal's OWN criteria):
  - **G1 EXACT** — Bernoulli `B_0..B_14`, `c_k` (k=1..7), `a_k` (k=0..6) + exp→log round-trip; `checked = 29`, `mismatches = 0`, `z = "exact"`. PASS.
  - **G2 MC AGREEMENT** — `N = 400000`, `n = 10`, ratio `r̂ = 1.008` vs closed-form Stirling prediction, `z = 1.3029 < 3` [Z_ACCEPT=3.0]. PASS.
  - **G3 ROBUSTNESS/INVARIANCE** — Robbins bracket `1/(12n+1) < ln(r_n) < 1/(12n)` for all `n = 1..64`, `robbins_violations = 0` (60-digit `Decimal`); second Gamma proposal (`shape = n+2`) agrees at `z = −1.4413 < 3`. PASS.
  - **G4 FALSIFIABILITY** — OPPOSITE direction, SAME sample: the `√(πn)` prefactor foil (predicted ratio `1/√2 = 0.707107`) REJECTED at `z_foil = 598.9410`, `|z_foil| ≫ 50` [Z_REJECT=50.0]; secondary "drop `1/(12n)`" foil rejected at `z = 17.8982`; while the true ratio accepted at `z = 1.3029`. PASS.
  - `all_pass = true`, `first_failing_gate = null`, decision `PASS`.
- Independent cross-check (own throwaway derivation): the leading correction `c_1 = B_2/(2·1) = (1/6)/2 = 1/12` and `a_1 = c_1 = 1/12`, `a_2 = c_1²/2 = 1/288`, `a_3 = c_2 + c_1³/6 = −1/360 + 1/10368 = −139/51840` — recomputed by hand from the exp-of-series, matching the verifier's `Fraction` values; and Robbins at `n = 1`: `ln(r_1) = −(½ln(2π) − 1) = 0.081061`, with `1/13 = 0.076923 < 0.081061 < 1/12 = 0.083333`, holds.
- Grounding byte-pinned to the MediaWiki API wikitext of the revision; local raw-bytes `hashlib.sha1` and the API revision sha1 match.

## Grounding — honest scope

External, sha1-pinned: Wikipedia "Stirling's approximation" oldid 1360453793, wikitext (34026 bytes) sha1 `5b7a91f2e93f54a950126ac06be9d35bcf4d8095` (raw local sha1 == API revision sha1), fetched 2026-07-21. **QUOTED** (grep > 0): the main asymptotic `n! \sim \sqrt{2\pi n}\left(\frac{n}{e}\right)^n` (grep `\sqrt{2\pi n}` = 9); the full ratio-series `1 + \frac{1}{12n} + \frac{1}{288n^2} − \frac{139}{51840n^3} − \frac{571}{2488320n^4} + \frac{163879}{209018880n^5} − ⋯` (so `a_1..a_5` quoted; grep `139` = 3, `571` = 1, `163879` = 1); the log-series coefficient formula `c_k = \tfrac{B_{2k}}{2k(2k-1)}` (grep `Bernoulli` = 3); Robbins' exact bracket `√(2πn)(n/e)^n e^{1/(12n+1)} < n! < √(2πn)(n/e)^n e^{1/(12n)}` (grep `frac{1}{12n}` = 14, `Robbins` = 2, cited "A Remark on Stirling's Formula", 1955); the names "Stirling's formula" (=15), "de Moivre" (=5). **DERIVED** firsthand (grep 0): the coefficient `a_6 = 5246819/75246796800` (grep `5246819` = 0 — the on-page series truncates at `a_5` then "⋯"); the Bernoulli recurrence route `Σ C(m+1,k)B_k = 0` (grep `C(m+1` = 0); the exp→log inverse-consistency check; the Monte-Carlo Gamma-importance-sampling estimator + every z-value (grep `Monte`/`importance`/`gammavariate` = 0); the `√(πn)` factor-2-under-root foil → ratio `1/√2` (grep `1/sqrt(2)`/`0.7071` = 0); the numeric Robbins sweep "0 violations across n=1..64" (the bracket is quoted; the numeric verification is mine); SEED; digest. **Disclosed seams:** the article is RICH (34026 bytes) — the main asymptotic, the full ratio-series, the `c_k` formula, and Robbins' bracket are QUOTED verbatim, so nothing is oversold as a novel theorem; the firsthand contribution is the exact-`Fraction` backbone built end-to-end + cross-checked against its own inverse (extending `a_6` beyond the page) + the MC agreement + the numeric Robbins sweep + the `√(πn)` falsification + every z-value + the digest. Nothing oversold as novel: Stirling's approximation is textbook (de Moivre 1733; Stirling; Robbins 1955; the pinned page), cited as such.

## Merge posture

CI substrate-gate + the `enable-auto-merge` landing automation squash-merge the green PR once the born-red HOLD is released; **NO merge API calls are made from this session**. This slice ships the sim-ready idea doc + the firsthand verifier + this born-red `.sessions/` card + the `## PROPOSAL 256` outbox block in ONE first commit. NO `control/status.md` edit, NO `## VERDICT` block, NO verdict / proposal high-water advance from this worker — the proposal high-water ADVANCE (P255 → P256) is the coordinator's call in a later isolated step. The PR opens READY; the in-progress card holds it RED until the owner reviews and flips. The gate battery itself is GREEN — the only red is the born-red HOLD.

## ⟲ Previous-session review

Previous-session review: PROPOSAL 255 (Mock Turtles coin-turning game — the odious single-coin Grundy closed form `g(n) = the odious member of {2n, 2n+1}`, with an independent mex-DP oracle validating the odious formula + the XOR-sum theorem together; naive foils `g(n)=n` and `g(n)=2n+1` rejected at the Grundy-value level; P255 → V268) landed sim-ready with a full-64 digest, four gates each in its own direction, and a clean quoted-vs-derived split. Its carry-forward is GATE-POLARITY discipline: read each gate in ITS OWN direction — here G1 is a self-certifying EXACT rational backbone (Bernoulli recurrence → `c_k` → `a_k`, cross-checked against the inverse log-of-series, 0 mismatches), G2 is an AGREEMENT gate against the closed-form Stirling prediction (|z|=1.30<3), G3 is a ROBUSTNESS gate read as an exact rational bracket (Robbins holds with 0 violations across 64 values of `n`) plus an estimator-invariance z, and G4 is a FALSIFIABILITY gate at the OPPOSITE polarity (the `√(πn)` prefactor confusion rejected at |z|=598.9 on the same sample). It also carries the grounding-accuracy + honest-seam discipline: like P252's Gaussian page, this Stirling page is RICH (the asymptotic + ratio-series + `c_k` formula + Robbins bracket are QUOTED verbatim), so the firsthand contribution is the exact-`Fraction` verification apparatus built end-to-end (extending `a_6` beyond the page via the exp/log round-trip), the MC Gamma-integral agreement, the numeric Robbins sweep, and the `√(πn)` falsification — and it discloses that `a_6`, the Bernoulli-recurrence route, the MC estimator, the numeric sweep, the foil, SEED, and the digest are grep 0. Standing non-contiguity persists: landing P256 does not imply every lower proposal/verdict below the high-water is closed — known gaps and open pulls remain the coordinator's ledger, not this worker's.

## 💡 Session idea

The verifier pins the classical factorial asymptotic and its exact rational correction series (Bernoulli → `c_k` → `a_k`) plus Robbins' bracket. A cheap orthogonal follow-on (P-next) would pin the **Wallis product** `∏_{k≥1} (2k)²/((2k−1)(2k+1)) = π/2`, already textbook-adjacent to this page (Stirling is classically derived from Wallis). That keeps the exact-rational-backbone recipe: G1 becomes an EXACT identity on the partial products `W_N = ∏_{k=1}^N (2k)²/((2k−1)(2k+1)) = (2^{2N} (N!)² )² / ((2N)! (2N+1)!)` wait — `W_N = 4^N (N!)⁴ · (2N+1)? …` verified as an exact `Fraction` closed form for each `N` (0 residual), G2 a Monte-Carlo / high-precision AGREEMENT that `2·W_N → π` from below (monotone), G3 an INVARIANCE / monotonicity leg (the partial products strictly increase and stay under `π/2`, exact where possible), and a FALSIFIABILITY leg that the naive "the product converges to `π`" (dropping the factor 2) is rejected. Grounding stays honestly on a fresh pinned Wikipedia "Wallis product" revision (which states the product + the Stirling connection), off-page for the exact rational partial-product `Fraction` closed form, SEED, and digest — with the "from-below monotone convergence to `π/2` not `π`" distinction as the headline honest seam, a clean contrast to this slice's two-sided Robbins bracket.
