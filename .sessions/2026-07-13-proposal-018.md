# Session — PROPOSAL 018: book breadth vs versioning depth (venture book/product rotation)

> **Status:** `complete`
> **Model/time:** fable · 2026-07-13T01:12:51Z (Ideas Lab worker slice — draft the
> ORDER 004 rule-3 venture book/product-space idea + PROPOSAL 018 outbox append; card
> born in-progress as the designed gate hold, flips complete in this PR's final commit)

## Scope

Draft a genuinely new idea serving venture-lab's book/product space, under standing
owner ORDER 003 (continuous pipeline) + ORDER 004 night-run rotation rule 3 ("fleet
backlogs → venture's book/product space → game mechanics → COMPLETELY UNRELATED
domains", verified open at HEAD `80baad5`): PROPOSALs 015/016 were game mechanics and
017 the unrelated-domain slot — this slice serves the venture slot. The owner's
venture night-run directive (venture-lab `control/inbox.md` ORDER 008 @ `81c47ec`,
fetched raw this slice: "BOOKS: multiple new book ideas AND multiple versions of each
(different angles, audiences, lengths) — versions are cheap once the research
exists") contains an untested fixed-budget allocation policy; the head pre-registers
the breadth-vs-depth question — versions-per-title K* under swept version cost,
version spread, discovery lottery, selection fidelity, and audience separation, two
publication modes, APPROVE/REJECT/NULL bands registered before any code — written up
as `ideas/venture-lab/book-versioning-breadth-depth-allocation-2026-07-13.md`
(probed single-pass this slice, sim-ready) and appended to `control/outbox.md` as
PROPOSAL 018. Fully hermetic sim (the PROPOSAL 017 precedent): every fixture is a
pinned constant in the idea file, zero repo/network reads in the verdict session.
Dedup swept vs PROPOSALs 001–017, all 13 `ideas/venture-lab/` files, sim-lab VERDICTs
001–018 (MCP read this slice), venture-lab's outbox (no file exists at `81c47ec` —
raw 404), and tree-wide greps (breadth/portfolio/best-of/versions-per/cannibaliz,
bootstrap.py/.substrate excluded) — only hits are trading asset portfolios and a
judge-panel workflow, no allocation prior art; the pricing shape was deliberately
avoided because the venture lane already routed price elasticity to sim-lab itself
(webhook-test-kit index row @ `389bb37`).

**📊 Model:** fable · content + outbox proposal only (idea file, card, index row,
claim file, outbox append; no product code, no control/status.md or control/inbox.md
writes)

## 💡 Session idea

Plant the outbox at adopt time so a missing bus file stops masquerading as an empty
ledger: this slice had to dedup PROPOSAL 018 against venture-lab's SIM-REQUESTs and
got raw 404 on `control/outbox.md` @ `81c47ec` — undecidable evidence ("no requests
ever" vs "surface never adopted"), even though that lane's own night-run ORDER 008
rule 4 commands "pricing/feasibility uncertainty → SIM-REQUEST via outbox →
CONTINUE", i.e. the owner ordered writes to a file that does not exist. Kit-side fix:
`bootstrap adopt` plants a headed, empty `control/outbox.md` (skip-if-exists)
alongside inbox/status exactly as the claims dir is planted, and the always-exit-0
status-checker family warns when a `control/` plane is missing any of the three bus
files — turning absence-of-file into affirmative absence-of-entries for every
cross-lane reader. Deduped this slice: rg over ideas/ + .sessions/ + control/ + docs/
for outbox-plant/bus-completeness — zero prior capture; nearest neighbors are
control/README.md § The two files (documents inbox+status only — outbox is a
Q-0264-era addition the plant list never caught up with) and PROPOSAL 013's
heartbeat-grammar family (same checker home, different fact: it lints content,
this plants existence).

## ⟲ Previous-session review

Newest predecessor card (`.sessions/2026-07-13-proposal-017.md`, worker slice
~00:57Z): closed clean — its PR #281 merged at `80baad5` with the PROPOSAL 017 outbox
entry grammar-clean and the card flipped complete in its final commit as designed;
notable craft: its decide-and-flag placement note (fleet-external head homed in
`ideas/fleet/` because ad-hoc sections red the ORPHAN gate) turned a checker
constraint into recorded evidence instead of silent squatting, and sim-lab consumed
the pipeline's neighbors within the hour (INTAKE 016/VERDICT 018 at 01:01Z) — the
overnight cadence the night-run ordered is demonstrably live. Its 💡 (bless a
roster-independent `ideas/offworld/` section via a `check_sections.py`
ALWAYS_EXPECTED carve-out + a `knowledge` class) is still unconsumed and NOT
re-minted here — checked before writing this card's own 💡 (rg for
offworld/ALWAYS_EXPECTED/knowledge-class — only its own card and idea file carry it).
No reconciliation debt found.
