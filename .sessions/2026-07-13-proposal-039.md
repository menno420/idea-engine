# Session — PROPOSAL 039: Gloamline plateau survival ceiling (game-mechanics rotation, round 6)

> **Status:** `complete`
> **Model/time:** fable · 2026-07-13T16:27:16Z (Ideas Lab worker slice — draft the
> GAME-MECHANICS rotation round-6 head under standing owner ORDER 003. Card born
> in-progress as the designed gate hold; flips complete in this PR's final commit
> once both checkers verify green and the 💡 slot resolves)

**📊 Model:** fable · content + outbox proposal only (idea file, card, index row,
outbox append, claim file; no control/status.md or control/inbox.md writes; no
checker or script changes; nothing in sim-lab)

## Scope

Draft a genuinely new sim-shaped idea for the GAME-MECHANICS rotation slot, round 6,
under standing owner ORDER 003 (continuous pipeline — verified open and standing at
HEAD `8e588f0` before any work) and ORDER 004 rule 3's rotation ("fleet backlogs →
venture's book/product space → game mechanics → COMPLETELY UNRELATED domains").
Round 6 so far: P037 = fleet-backlogs (fleet-manager review-queue N=50), P038 =
venture (KU exclusivity fork) — so this slice serves the game-mechanics slot.
Rounds 1–5 of the slot: P020 (casino odds → V022 reject), P023 (entry fee → V025
reject), P027 (comp/stipend → V029 null), P031 (explore pacing → V033 null), P035
(mining booster throttle seal). Verified before claiming: `control/claims/` carries
no P039 claim and the open-PR list is empty at HEAD.

Subject sourced per the fleet reading path (superbot `docs/fleet-reading-path.md`,
raw-fetched this slice): the Game Lab repo `menno420/gba-homebrew` @ live HEAD
`d87f9ad7d283465c5bc8d26ea47b8cbffb307cc1` (shallow clone; pokemon-mod-lab DARK,
skipped per the path's own rule). The head: the Gloamline horde-defense
survival-ceiling question — no host proof measures whether the committed night ramp
(waves 2n−1, cap 24 at night 13) actually pressures a MOVING player, while the
lane's save/best-nights/rematch slices (9 and 11) built the score system on
"nights survived". The committed stdlib mirror `tools/check-gloam.py` (sha256
`c86d9507…`) is the byte-reusable machinery — the first game-mechanics head from
the Game Lab lane in this outbox, and the first with a fully in-tree pure-Python
engine (the V042/V043 byte-reuse precedent applied to a sibling lane's own proof
mirror). Idea file `ideas/gba-homebrew/gloamline-survival-ceiling-2026-07-13.md`
(probed single-pass this slice, sim-ready), appended to `control/outbox.md` as
PROPOSAL 039. Seed registry: seeds 20261293–96 allocated strictly above the P038
high-water 20261292 (re-checked across all 38 prior blocks — nothing higher).

## 💡 Session idea

**Give the fleet's game repos a committed MACHINERY INDEX — one line per repo
naming its byte-reusable pure sim layers.** This slice's real cost was not the
drafting but the SOURCING: the fleet reading path routes to repos and heartbeats,
but says nothing about which game repos carry committed, hermetically byte-reusable
engines — finding Gloamline's `tools/check-gloam.py` mirror (the head's entire
machinery) took a full shallow clone plus a grep tour of `main.c`/`gl_sim.h`,
and P031's drafter earlier PASSED a browser-game difficulty head on "no committed
in-tree baseline to chain to tonight" when this baseline existed one repo over.
The slice: a short "Committed machinery" section in each game-repo section README
(or one table in `ideas/fleet/`) — repo · pure-layer path · what it mirrors ·
last-verified sha — appended by whichever drafter touches the repo (this session's
row: gba-homebrew · `tools/check-gloam.py` + `tools/check-cave.py` +
`tools/check-brine.py` · line-for-line stdlib mirrors of each game's pure sim
layer · @ `d87f9ad`). Dedup (this slice): distinct from the P036 💡 (domain
ledger — the unrelated slot's occupied-domain census), the P035 💡 (seed
registry — numeric high-water), and the P033 💡 (outbox digest — block read
cost); `rg -ni "machinery index|pure mirror index"` over the tree hits nothing.

## ⟲ Previous-session review

Newest predecessor card (`.sessions/2026-07-13-ideas-link-backfill.md`, repair
worker slice ~15:50Z): closed clean and honest — it healed the latent
outbox↔ideas LINK red that the P037/P038 fast-lane deviation left on main,
pruned the two stale drafter claims with live API verification, and its 💡
(make the control-only fast lane run `check_ideas.py --outbox` when the diff
touches `control/outbox.md`) is exactly the root-cause-sized fix: the PR that
introduces a violation pays for it itself. One thing visible only from here:
the backfill restored the LINK contract but left the fast-lane PRECEDENT
standing (P038's block cites "the P037 precedent" approvingly), so until that
kit-lane carve-out lands, any future control-only proposal slice can
reintroduce the same latent red — this session sidestepped the fork entirely
by drafting the idea file in-PR per the P017–P036 convention, which is the
cheaper discipline while the gate fix waits.

## Close-out

All pieces landed before this flip: card born in-progress @ `e0f8d98`, claim +
idea doc + index row @ `3c7628c`, outbox PROPOSAL 039 append @ `f4db492`
(heading stamp from real `date -u`), PR #320 opened READY (never draft;
auto-merge left to the enabler — no agent merge), PR activity subscribed.
Final verification on this tree at the flip: `python3 scripts/check_ideas.py
--outbox` exit 0 ("OK — outbox proposals and sim-ready ideas are consistent");
`python3 bootstrap.py check --strict` exit 0 once this badge flipped (its only
red before the flip was the designed born-red hold on this very card). Seeds
20261293–96 registered strictly above the P038 high-water 20261292. No
control/status.md, control/inbox.md, or sim-lab writes; timestamps from real
`date -u` throughout.
