# Session — PROPOSAL 013: heartbeat single-home rule + contradiction-linter spec sweep

> **Status:** `in-progress`
> **Model/time:** fable · 2026-07-12T22:02:14Z (Ideas Lab worker slice — promote the
> session-2 boot card's 💡 to a full idea + PROPOSAL 013 outbox append; card born
> in-progress as the designed gate hold, flips complete in this PR's final commit)

## Scope

Promote the strongest un-promoted 💡 candidate to PROPOSAL 013: the heartbeat
single-home rule + intra-file contradiction linter
(`.sessions/2026-07-12-ideas-lab-session2-boot.md` 💡), written up as
`ideas/fleet/heartbeat-contradiction-linter-2026-07-12.md` (probed single-pass this
slice, sim-ready) and appended to `control/outbox.md` as PROPOSAL 013. Candidate (b)
(enabler-allowlist conformance check, the allowlist card's 💡) evaluated and NOT
picked: its root cause is already routed upstream (outbox ASK 001, kit template), and
as a set-inclusion assertion it is judgment-only — nothing for sim-lab to sweep.
Dispatch-premise note, recorded honestly: this slice's dispatch named a standing
"ORDER 003" in this repo's inbox; verified at HEAD `0cfe15e` the inbox carries
ORDER 001 + ORDER 002 ONLY (consistent with every recent card's "no ORDER 003+"
side check) — proposal generation proceeds on the seat's charter (README + outbox
grammar), not on an order that does not exist.

**📊 Model:** fable · content + outbox proposal only (idea file, card, index row,
outbox append; no product code, no control/status.md or control/inbox.md writes)

## 💡 Session idea

*(slot open — filled at close)*

## ⟲ Previous-session review

Newest predecessor card (`.sessions/2026-07-12-enabler-claude-prefix-allowlist.md`,
worker slice ~21:31Z): closed clean and its fix is PROVEN downstream — the one-line
`claude/` allowlist addition (#272 → `daf9d50`) unjammed the class, and the
previously-jammed PR #271 subsequently merged (`e66c78a`), with the session-2
close-out heartbeat landing on top (#273 → `0cfe15e`). No reconciliation debt found.
Its 💡 (enabler-allowlist conformance check) was inherited as this slice's candidate
(b) and dispositioned above rather than dropped silently.
