# Session — PROPOSAL 033: assign-at-merge priced — the routed Option-3 build vs its own merge-queue re-validation tax (fleet-backlogs rotation, round 5)

> **Status:** `in-progress`
> **Model/time:** fable · 2026-07-13T08:38:25Z (Ideas Lab worker slice — draft the
> FLEET-BACKLOGS rotation round-5 head under standing owner ORDER 003. Card born
> in-progress as the designed gate hold; flips complete in this PR's final commit
> once the 💡 slot resolves)

**📊 Model:** fable · content + outbox proposal only (idea file, card, index row,
outbox append; no control/status.md or control/inbox.md writes; no checker or
script changes)

## Scope

Draft a genuinely new sim-shaped idea for the FLEET-BACKLOGS rotation slot,
round 5, under standing owner ORDER 003 (continuous pipeline — verified
open/standing at HEAD `2e5d73f` before any work: priority P1 · standing, "continue
coming up with new ideas, that is your main purpose"). Rounds 1–4 of the slot:
P019 (websites backlog → V021 approve), P021 (superbot backlog → V023 reject),
P025 (kit claims doctrine → V027 reject), P029 (V027's own routed lease-renewal
fix → V031 null). Candidates weighed on merit after reading the actual verdict
texts at sim-lab origin/main `c7340ae`:

- (a) **V023's routed Option-3 assign-at-merge build, its own cost side — WINNER.**
  V023 (reject) routed the build with the residual-tax evidence row and said
  verbatim "nothing here prices Option 3's build cost" (gate LIMITS line) and
  named "the Option-3 plan's build-cost side" a follow-up. The mechanism's
  operational price — a serialized merge queue with re-validation at the head —
  is un-simmed, the P–K arithmetic shows the frontier is genuinely open
  (V_q* sits between 0.5 h and 2 h at lam04·V2 against the parent's committed
  11.42 h), and every anchor is a committed number in
  `sims/verdict-023-renumber-treadmill/results.json`.
- (b) V015's contradiction-linter second-repo re-measure (the "advisory never
  red-gate until pair precision re-measured on a second repo's heartbeat corpus"
  clause) — real consumer, but it is a labeling/measurement task (single-auditor
  pair labels over a fresh corpus), not a pre-registerable stdlib sim; weaker fit
  for the harness and heavier moving parts (vendoring the kit linter spec).
- (c) V031's jitter-robust grace-rule design (fixed θ_r collapses under wake
  jitter, 79 pp vs 5.4) — genuinely open, but it would be the THIRD consecutive
  kit-claims head in this slot (P025 → P029 → this), and V031's own named next
  step is the LIVE warn-first counter, which a third model-sweep would preempt.
- (d) a superbot-next parity-checker / mineverse snapshot head — P025's committed
  survey already measured those sections sim-dry by their own probes ("nothing
  sim-shaped"; SECTION COMPLETE).

Written up as `ideas/superbot/assign-at-merge-queue-tax-2026-07-13.md` (probed
single-pass this slice, sim-ready) and appended to `control/outbox.md` as
PROPOSAL 033 (tail verified at HEAD before append: PROPOSAL 032 ·
2026-07-13T08:17:09Z). Seed registry: seeds 20260768–71 allocated strictly above
the P031 high-water 20260767 (P032 drew zero, per its own card). Dedup swept
tree-wide at `2e5d73f` (`rg -i 'merge.?queue|re.?validat|assign.?at.?merge|
pollaczek|m/d/1'`, bootstrap.py and .substrate excluded): every hit is the
P021/V023 family's own prose or unrelated content (websites merge-hold, review
queue rows); no proposal 001–032 and no sim-lab verdict V001–V032 (local clone
@ `c7340ae`, offset map read — V033/V034 for P031/P032 not yet ledgered) prices
a merge queue, re-validation latency, or any assign-at-merge mechanism cost.

## 💡 Session idea

*(fills in this PR's final commit — the designed gate hold)*

## ⟲ Previous-session review

Newest predecessor card (`.sessions/2026-07-13-proposal-032.md`, worker slice
~08:16Z): closed clean — the unrelated-domain round-4 head (Penney's game
responder-edge decay, fully exact zero-RNG census, PROPOSAL 032), with the
runner-up kill list recorded honestly (bus bunching, elevator dispatch,
chip-firing, interval packing — the last dying on a five-second theorem check).
Two habits consumed here: its explicit seed-registry note ("ZERO seeds drawn —
high-water 20260767 unchanged") is what makes this slice's 20260768+ allocation
mechanical rather than archaeological; and its 💡 (a committed candidate backlog
for the rotation slot, so survey kills stop being re-derived) is seconded — this
slice re-paid exactly that survey cost across candidates (a)–(d) above. One
honest gap it disclosed: it drafted with NO sim-lab clone (dedup on domains
only, from this repo's own writeups) — this slice had the clone and read the
verdict texts direct, which is the stronger form of the same dedup.
