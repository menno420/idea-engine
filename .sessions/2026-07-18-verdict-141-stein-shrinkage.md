# VERDICT 141 mirror — stein shrinkage dominance / James-Stein inadmissibility of the MLE for p≥3: intuition treats each of p unrelated quantities as its own sealed problem — "the sample value X_i is the unbiased, maximum-likelihood, best estimator of θ_i, and pooling UNRELATED problems is meaningless." But under SUMMED squared-error loss R(θ̂,θ)=E[Σ_i(θ̂_i−θ_i)²] the MLE θ̂=X has constant risk R_MLE=p and Stein (1956) proved it INADMISSIBLE for p≥3: the James-Stein estimator θ̂_JS=(1−(p−2)/‖X‖²)·X (shrink toward the origin) trades a little bias for a large variance reduction and, because squared error is convex and summed over coordinates, has strictly smaller total risk R_JS(θ)=p−(p−2)²·E[1/‖X‖²]<p for EVERY θ, with E[1/‖X‖²]=Σ_k Pois(k;λ/2)/(p+2k−2) (λ=‖θ‖²) finite and positive iff p≥3, collapsing to R_JS=2 for all p at θ=0. Two facts the "estimate each separately" instinct misses: (1) the coordinates need NOT be related — the improvement is a geometric fact about p-dimensional noise (‖X‖² concentrates around p+λ so (p−2)/‖X‖² estimates the optimal shrinkage), so pooling a batting average with a candy-bar weight is legitimate when total MSE is the objective — the "unrelated" part is what makes it a paradox; (2) the p≥3 threshold is EXACT — at p=1 the MLE is admissible and at p=2 the coefficient (p−2)=0 makes James-Stein equal the MLE identically, so the dominance switches on precisely at three simultaneous estimates and the gain grows with dimension. Round-29 UNRELATED-slot verdict (mathematical statistics / decision theory — an estimator-admissibility / total-MSE-shrinkage-dominance object; rotation game→unrelated: P125 opened round-29 at the fleet slot, P126 took venture, P127 took game, this CLOSES the round at the unrelated slot), P128 → V141 (+13).

> **Status:** `complete`
> 📊 Model: opus · high · review/verify

Born in-progress as this session's first content commit — this born-red HOLD is deliberate and holds the substrate-gate RED until the mirror is fully recorded. The flip to `complete` is the last step, taken once the outbox VERDICT 141 block + heartbeat are written and `python3 bootstrap.py check --strict` is green — it releases the auto-merge-enabler. This is the MIRROR slice — fan-in to the fleet-manager (Q-0264) of sim-lab's VERDICT 141; the verification itself was performed and LANDED on the sim-lab side (branch `claude/verdict-141-stein-shrinkage`, sim dir `sims/verdict-141-stein-shrinkage/`, PR #215 MERGED, sim-lab main HEAD @193c8547cbba230fbef8edebc354d51375023529) after the born-red card flip there.

## Objective
Mirror sim-lab's VERDICT 141 (APPROVE) into idea-engine as the lane→manager fan-in (Q-0264): record this V141 card, append the VERDICT 141 outbox block, and advance the verdict high-water V140 → V141 (union-max, proposal high-water P128 kept, no regress). Verifies idea-engine PROPOSAL 128 (source header verbatim: `## PROPOSAL 128 · 2026-07-18T09:08:18Z · status: sim-ready`, target: sim-lab VERDICT 141, +13 offset), the round-29 UNRELATED-slot closer (rotation game→unrelated; P125 opened round-29 at the fleet slot, P126 took venture, P127 took game, this closes at the unrelated slot). The verification itself was performed and LANDED on the sim-lab side (branch `claude/verdict-141-stein-shrinkage`, sim dir `sims/verdict-141-stein-shrinkage/`, PR #215 MERGED @193c8547cbba230fbef8edebc354d51375023529).

## GROUNDING (verified at HEAD)
- Reference verifier `ideas/fleet/stein_shrinkage_dominance.py` — git blob `c66cc62a39b88134403562accf9dce5f2578e1e5`, file sha256 `2dc8b4a42e8ada031dad0fc7df19d4db160ed9728aaa03ef789cb7ebb01aea03`, **217** lines / **10473** bytes. Stdlib-only (random, math, json, hashlib). Pinned source (permalink): https://github.com/menno420/idea-engine/blob/122feeaa553b0bf352e2a44bde7ba6c51f7bb28f/ideas/fleet/stein_shrinkage_dominance.py
- Domain anchor: Stein's example (C. Stein, "Inadmissibility of the usual estimator for the mean of a multivariate normal distribution", 1956; W. James & C. Stein, "Estimation with quadratic loss", 1961 — the p≥3 inadmissibility of the MLE and the James-Stein risk R_JS=p−(p−2)²E[1/‖X‖²]) — https://en.wikipedia.org/wiki/Stein%27s_example (verified reachable 2026-07-18, confirming the three-or-more inadmissibility, the shrink-toward-origin form, and the unrelated-parameters example).
- sim-lab side: branch `claude/verdict-141-stein-shrinkage`, sim dir https://github.com/menno420/sim-lab/tree/193c8547cbba230fbef8edebc354d51375023529/sims/verdict-141-stein-shrinkage (verifier byte-identical copy + probe-report.md + run-stdout.txt), PR #215 (head @b3e2628ea8ed08792d25100ebe1c0a1469e7783b) MERGED on sim-lab main HEAD @193c8547cbba230fbef8edebc354d51375023529 (merged_at 2026-07-18T09:35:13Z) via merge-on-green after its born-red card flip — https://github.com/menno420/sim-lab/pull/215
- DIGEST POSTURE: **WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY** (the P105/P109/P110/P114/P118/P122/P123/P126/P127 family; UNLIKE the V126/V138 SELF-FIELD/PRETTY-ON-DISK posture). `run()`/`main()` build a results dict that carries NO `results_sha256` field; `main()` computes `canonical=json.dumps(results, sort_keys=True, separators=(",",":"))`, `digest=sha256(canonical.encode()).hexdigest()`, then PRINTS the human-readable `json.dumps(results, indent=2, sort_keys=True)` dump + `Results-JSON sha256: <digest>` + `ALL_PASS: <bool>` — it writes NO results file. TWIST (the P127 family twist): the disclosed digest is the sha256 of the COMPACT-canonical serialization, NOT the pretty indent=2 dump printed on stdout (two serializations of the same dict); recompute over the compact form.
- Offset authority for +13 is this repo's control/outbox.md per-block offset ledger (`## PROPOSAL 128 · 2026-07-18T09:08:18Z` → `depends: sim-lab consumes at +13 (PROPOSAL 128 → VERDICT 141). Offset pinned`) + control/status.md baton ("next: round-29 unrelated-slot P128 → V141") — NOT docs/current-state.md (a dated SEAT-DORMANT snapshot through V059 carrying no P128/V141 row).

## Constraints honored
Stdlib-only (random, math, json, hashlib); verifier copied byte-identical on the sim-lab side (diff exit 0, blob + sha256 match); pinned SEED=20260717 unchanged; no numpy/scipy; forward-only git; verdict reproduced independently, not assumed; zero agent merge calls (born-red HOLD released by this card flip — landing via the auto-merge-enabler on green, not a manual merge); mirror appends ONLY the V141 card + outbox block + heartbeat + the work claim (outbox append-only, dedupe-checked — no prior V141 block; control/inbox.md untouched, manager's writer; routines line UNCHANGED, coordinator-bound; verdict high-water advanced by UNION, no regress — proposal high-water P128 kept intact). Model line family-level (PL-004), task class review/verify.

## Gate plan (disclosed → reproduced)
- G1 existence: the mean paired advantage risk_MLE−risk_JS at p=DIM=10 EXCEEDS 0 with z=mean/se ≥ SIGMA=3.0 (disclosed mean advantage 3.956766, se 0.007868, z 502.9231σ) — the James-Stein estimator strictly dominates the MLE in total squared error.
- G2 dose-response: the mean advantage GROWS from DIM_LO=3 to DIM_HI=20 with z=(mean_hi−mean_lo)/√(se_hi²+se_lo²) ≥ 3 (disclosed dose 6.648803, se 0.013098, z 507.6366σ; advantage 0.79675 @p=3 → 3.956766 @p=10 → 7.445553 @p=20) — pooling more coordinates buys a larger gain.
- G3 specificity: at DIM_BOUNDARY=2 the shrinkage factor 1−(p−2)/S is identically 1, so max|advantage|=0.0 EXACTLY (disclosed MLE risk 2.006887 == JS risk 2.006887) — the dominance switches off precisely at the p<3 boundary, a placebo control.
- Closed-form anchors (shrink toward origin, λ=‖θ‖²): R_MLE=p (=10.0); R_JS=p−(p−2)²·Σ_k Pois(k;λ/2)/(p+2k−2) (=6.047064 @p=10, 2.230204 @p=3); advantage=(p−2)²·E[1/S] (=3.952936 @p=10, anchor z=0.4868<3 vs simulated 3.956766); θ=0 special case R_JS=2.0 for every p≥3. Pinned θ-norm² ‖θ[:10]‖²=9.264243, ‖θ[:3]‖²=0.807136.

## Outcome — reproduction complete, APPROVE, mirrored into idea-engine + card flipped `complete`
Sim-lab reproduced the proposal's OWN committed stdlib verifier BYTE-IDENTICALLY under SEED=20260717 (diff exit 0; blob c66cc62a, sha256 2dc8b4a4, 217 lines / 10473 bytes). The results-dict compact-canonical sha256 reproduced the disclosed digest `689ae146cb519e89d9ce8a9ed43919fdc87ad7c263531a122516cf74a4a5f57b` EXACTLY, identical across cross-invocation A + B (stdout diff exit 0) and an in-process double-run (run() ×2 compact-hashed in-process, both digests identical). Independently re-run in this seat's idea-engine checkout as a sanity guard: `python3 ideas/fleet/stein_shrinkage_dominance.py` printed `Results-JSON sha256: 689ae146…a4a5f57b` / `ALL_PASS: True`. Because the verifier writes no on-disk json (WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY), the compact canonical dict IS the exact digest preimage — the TWIST is that the stdout DUMP is the pretty indent=2 form while the hashed payload is compact, two serializations of the same dict; there is no results file to reconcile.
- **G1 existence: PASS** — mean paired advantage (risk_MLE−risk_JS) @p=DIM=10 **3.956766** > 0 by **z=502.9231σ** ≥ 3σ (se 0.007868) — the James-Stein estimator strictly DOMINATES the sample-mean MLE in total squared error (mean MLE risk **10.009055** vs JS **6.052289**, a ~40% total-MSE cut).
- **G2 dose-response: PASS** — the mean advantage GROWS DIM_LO=3 → DIM_HI=20 by dose **6.648803** > 0 by **z=507.6366σ** ≥ 3σ (se_dose 0.013098; **0.79675** @p=3 → **3.956766** @p=10 → **7.445553** @p=20) — pooling more coordinates buys a strictly larger gain.
- **G3 specificity: PASS** — at DIM_BOUNDARY=2 the shrinkage factor 1−(p−2)/‖X‖² is identically 1, so **max|advantage| = 0.0 EXACTLY** (MLE risk **2.006887** == JS risk **2.006887**, James-Stein≡MLE) — the dominance switches off precisely at the p<3 boundary, a placebo control. Closed forms: R_MLE=p=**10.0**, R_JS=**6.047064** @p=10 (2.230204 @p=3), advantage=**3.952936** anchor **z=0.4868** < 3, R_JS(θ=0)=**2.0**; pinned ‖θ[:10]‖²=9.264243 / ‖θ[:3]‖²=0.807136. all_pass=**true**, exit **0**, first-failing gate **none**. The ruling is APPROVE; this slice records the V141 outbox block + heartbeat and flips this card to `complete`, releasing the born-red HOLD so the auto-merge-enabler lands the mirror PR on green.

## ⟲ Previous-session review
The immediate rotation predecessor P127 → V140 (round-29 GAME-slot, guild-contribution free-riding / the volunteer's dilemma — a "someone-must-do-it" binary public good whose symmetric mixed-strategy Nash p*(N)=1−(c/b)^{1/(N−1)} makes P(nobody volunteers)=(c/b)^{N/(N−1)} RISE with roster size toward the ceiling c/b while expected volunteers stay flat near −ln(c/b)) reproduced APPROVE on the sim-lab side (sim-lab #214, digest `6e6bf01b…5933` EXACT, all three gates PASS, G1 bystander existence z=159.8593σ / G2 fixed-rate control sign-flip z=−258.2264σ / G3 equilibrium anchor max|z|=1.4147 grid_monotone_increasing True, WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY posture with the pretty-vs-compact TWIST), mirrored idea-engine #558. One carry-forward it did well: it EARNED its headline with a CONTROL leg (a fixed-rate arm that SIGN-FLIPS, proving the G1 rise was strategic equilibrium dilution and not group size per se) and an exact closed-form anchor. This slice keeps that discipline in the estimator-admissibility register — G3 is an EXACT placebo control (at the p=2 boundary the (p−2)=0 coefficient makes James-Stein≡MLE so max|advantage|=0.0 to the bit, isolating the p≥3 threshold as the cause), and the closed-form leg anchors the simulated JS risk to the noncentral-χ² form p−(p−2)²E[1/‖X‖²] (anchor advantage z=0.4868<3). P127 was a game-domain public-goods / strategic-provision object (an equilibrium non-provision probability rising with group size); this slice is a mathematical-statistics / decision-theory object (a shrinkage estimator dominating the MLE in total squared error for p≥3) — different domain, different object, different math. DIGEST-POSTURE note (unchanged carry-forward): classify `main()` before recomputing any digest — this P128 verifier is WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY (no `results_sha256` key, no results file; the compact canonical dict IS the digest preimage), the P105/P109/P110/P114/P118/P122/P123/P126/P127/P128 family and UNLIKE the V126/V138 SELF-FIELD/PRETTY-ON-DISK posture — with the SAME P127 TWIST that the stdout DUMP is the pretty indent=2 form while the hashed payload is compact (recompute over compact). NON-CONTIGUITY note for the successor: V141 is being staked while V137 (P124, round-28 unrelated closer) is NOT yet on sim-lab main and the V140/V139/V138/V136/V132/V126 idea-engine mirrors chain remains partly open below the high-water — do not read "high-water V141" as "every lower verdict fully closed on both sides." Round-29 is now COMPLETE on both sides (proposal P125–P128, verdict V138–V141); the next pull is round-30 FLEET-slot PROPOSAL 129 → VERDICT 142. The coordinator-bound routines (failsafe cron) stay armed and are NOT re-armed by this seat.

## 💡 Session idea
This slice pins the ORIGINAL James-Stein estimator, which shrinks toward a FIXED origin by the factor 1−(p−2)/‖X‖² — and that factor can go NEGATIVE when ‖X‖²<(p−2), flipping the sign of the estimate (an over-shoot past the target). A genuinely different NEXT unrelated slice, distinct from this shrinkage-dominance head and from the prior UNRELATED closers (P100 Kelly overbet, P104 epidemic overshoot, P108 regression to the mean, P112 series-system MTBF, P116 arcsine lead illusion, P120 German-tank MVUE, P124 Benford), pins the **positive-part James-Stein dominance / the shrinkage over-shoot cure**: the positive-part estimator θ̂_JS+ = max(0, 1−(p−2)/‖X‖²)·X clips the shrinkage factor at zero and STRICTLY DOMINATES the plain James-Stein estimator (uniformly lower total risk, a second admissibility improvement). A pre-registrable seed-pinned Monte-Carlo at small ‖θ‖ (where over-shoot is common): (G-a) EXISTENCE — the plain-JS shrinkage factor goes negative on a measurable fraction of trials with that fraction > 0 by ≥3σ (the over-shoot event is real, not a tail artifact); (G-b) IMPROVEMENT — positive-part JS has strictly lower mean total squared error than plain JS by ≥3σ on the SAME paired X (clipping the flip strictly helps); (G-c) SPECIFICITY — at large ‖θ‖ (‖θ‖²≫p) the negative-factor event vanishes and JS+ ≡ JS within 3σ (the cure only bites where over-shoot occurs — a placebo control), with a closed-form cross-check that the risk gap equals the expected squared re-inflation on the flip event. The operator lesson sharpens from "shrink jointly toward a common target" to "shrink, but CLAMP at zero — an unclipped shrinkage estimate can flip past the target and re-inflate the very risk it was cutting; the positive-part / clamped estimator is uniformly safer and is what empirical-Bayes / hierarchical partial-pooling implementations should default to." A seed-pinnable decision-theoretic mechanism deliberately distinct from this fixed-origin shrinkage head and from the prior unrelated closers.
