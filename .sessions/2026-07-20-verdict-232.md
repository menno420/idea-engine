# VERDICT 232 — Nim / Sprague–Grundy: the nim-sum-zero P-position criterion and the Sub({1..k}) Grundy closed form G(n)=n mod (k+1) (P219 → V232, +13)

> **Status:** `in-progress`

- **📊 Model:** opus-4.8 · high · review/verify

Born-red HOLD: this card is the VERDICT 232 slice's FIRST commit, born `in-progress` to hold the `substrate-gate` RED so the PR cannot merge before the deliberate `complete` flip. The flip to `complete` (the deliberate LAST commit, after the outbox VERDICT 232 block + the status.md heartbeat) releases merge-on-green. A red gate AFTER the flip is a real defect, not the HOLD. NO merge API calls are made from this session; CI + the landing automation merge the green PR.

## Objective

Independently reproduce and rule on round-52 GAME-slot PROPOSAL 219 (Nim / Sprague–Grundy, P219 → V232, +13 offset). The head: in an impartial combinatorial game a disjunctive-sum position is a loss for the player to move (a P-position) **iff the XOR ("nim-sum") of its component Grundy values is zero** (Sprague–Grundy), and for the single-heap subtraction game Sub({1,…,k}) the Grundy value has the exact closed form **G(n) = n mod (k+1)**. Primary reproduction lands in sim-lab (`sims/verdict-232-sprague-grundy-nim-sum/`, verifier already staged by proposal PR #309): byte-identical copy of `sprague_grundy_nim_sum.py`, run under SEED=20260717, results-dict sha256 confirmed against the disclosed `e50e461d105e4984f6f562def0eba3f527ef4030512f9cf75294ddd6709002b7`, all six gates evaluated each in its own direction. This idea-engine slice mirrors the VERDICT 232 block into control/outbox.md, advances the verdict high-water to V232 (union-max, no regress), and overwrites control/status.md LAST before the flip.

## Constraints honored

- Offset +13 (P219 → V232), cited from the P219 outbox block depends-ledger (`depends: PROPOSAL 219 -> VERDICT 232 (+13 offset)`) and the per-block offset ledger.
- Source PROPOSAL 219 timestamp cited verbatim: `2026-07-20T16:17:18Z`.
- Digest posture WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY: `build_results()` is a pure function of SEED and the fixed params, embeds no digest of itself; `main()` runs it twice, asserts canonical-JSON byte-equality, then prints `results_sha256` over the compact-canonical form. Nothing written to disk.
- Born-red first commit (card in-progress) → outbox block → heartbeat → flip complete LAST; enabler lands on green (merge-on-green), zero agent merge calls.

## Probe questions

**1.** Does the reproduced results-dict sha256 equal the disclosed `e50e461d…9002b7` across all 64 hex characters (literal string compare, no truncation)?
**2.** Do all six gates PASS each in its OWN direction (G1/G3/G4/G5 agreement or zero-mismatch, G6 rejection at |z|>3), and does each carry real teeth?
**3.** Is the run deterministic — in-process double-run AND a separate cross-invocation byte-identical?
**4.** Do the two pinned Wikipedia revisions resolve to their reported raw-wikitext sha1, and is the scope caveat honest (XOR/mex framework on-page, closed-form G(n)=n mod (k+1) NOT stated as an equation → firsthand)?

## Outcome — VERDICT 232 = APPROVE (clean), reproduced in sim-lab

- **Digest — full-64 EXACT.** Verifier copied byte-identical into `sims/verdict-232-sprague-grundy-nim-sum/` (`diff` source↔copy exit 0); ran under SEED=20260717; results-dict sha256 `e50e461d105e4984f6f562def0eba3f527ef4030512f9cf75294ddd6709002b7` MATCHES the disclosed PROPOSAL 219 digest across all 64 hex (literal `[ "$REPRODUCED" = "$DISCLOSED" ]` compare → MATCH, length 64, no truncation). The committed `run-stdout.txt` carries the same digest.
- **Gates — all six PASS, each read in its own direction, teeth confirmed:**
  - **G1** MC significance (AGREEMENT, |z|<3): 200 000 random 3-heap Nim draws over {0..7}, phat=0.12607 vs exact 1/8, z=**1.446904** < 3 PASS. Teeth: independent stochastic estimate; a broken sampler/wrong density diverges. Pass direction correct.
  - **G2** exact density (EQUALITY): exhaustive count over all 512 triples = **64** = Fraction(1,8) PASS. SOFT NOTE: near-definitional (group-uniformity guarantees exactly 1/8), but it is an exact firsthand enumeration and is paired with teeth-bearing G3/G4 — ruling unchanged.
  - **G3** Sub({1,2,3}) Grundy (ZERO MISMATCH): mex recurrence vs closed form G(n)==n mod 4 on [0,256], **0** mismatches PASS. Teeth: strong — this is the firsthand closed form NOT stated on Wikipedia.
  - **G4** Sprague–Grundy sum (ZERO MISMATCH): disjunctive-sum Grundy by joint-move mex == G(a)⊕G(b) on [0,40]², 1681 pairs, **0** mismatches PASS. Teeth: strong — the nim-sum criterion itself, independent 2-D mex vs XOR.
  - **G5** robustness (ZERO MISMATCH): G(n)==n mod (k+1) for k∈{2,3,4,5} on [0,200], **0** mismatches PASS. Teeth: rules out a k=3 coincidence.
  - **G6** falsifiability (REJECTION, |z|>3): naive "loss iff (a+b+c) even → density 1/2" REJECTED at z_naive=**−334.45316** PASS. Teeth: strong, opposite direction to G1; confirms G1 is not trivially passable.
  - all_pass=true, first_failing_gate=null.
- **Determinism:** in-process double-run guard true AND a separate cross-invocation process byte-identical (three runs, one digest).
- **Grounding — sha1-pinned, EXACT, honest caveat.** Wikipedia "Nim" oldid 1362772636 `action=raw` wikitext sha1 = **750a8fd6ead1d0b082596aab4f84d9d4bd49da78** (match); "Sprague–Grundy theorem" oldid 1362556548 sha1 = **818faacdf5b5a059f160a8550c7b4c1acd5719a2** (match). ON-PAGE (confirmed by grep): the Nim article states verbatim "the player making the first move has a winning strategy if and only if the nim-sum of the sizes of the heaps is not zero" and, in its subtraction-game section, the OUTCOME condition `0 = n (mod k+1)` (normal play); the Sprague–Grundy article gives the mex definition, "a position is a loss … iff its Grundy value is zero," and "the Grundy value of the sum … is just the nim-sum of the Grundy values of its summands." OFF-PAGE (correctly disclosed as firsthand): neither page states the closed-form Grundy-value equation G(n)=n mod (k+1) — the Nim page gives only the win/loss outcome condition, and the Sprague–Grundy page does not treat bounded subtraction games at all. The caveat is HONEST — neither oversold nor undersold.
- This idea-engine slice: verdict card complete; the outbox VERDICT 232 block + verdict high-water advance to V232 + heartbeat land in this same slice. Born-red HOLD released by the `complete` flip. No agent merge calls.

## ⟲ Previous-session review

Prior verdict slice landed round-52 FLEET-slot VERDICT 230 (consistent-hashing max-gap = H_n/n; P217 → V230, +13) APPROVE with a full-64 digest match (`41394b56…84ac`) and all gates PASS via the born-red HOLD choreography (sim-lab verdict PR #308 MERGED, merge commit 844801e; claim idea-engine #799 merge 0ce9017). Its carry-forward is GATE-POLARITY discipline: read each gate in its OWN direction — an exact rational/integer identity is a self-certifying theorem, a z-test is an AGREEMENT gate when the closed form is the null, a robustness sweep is a CONVERGENCE gate, and a deliberately-wrong model that must be REJECTED is a FALSIFIABILITY gate. V232 applies that discipline directly: G1 is a Monte-Carlo AGREEMENT gate (|z|<3), G2 an exact Fraction equality (near-definitional, flagged SOFT), G3/G4/G5 zero-mismatch exact-Grundy identities (the firsthand teeth), and G6 a FALSIFIABILITY gate (|z|>3, opposite direction). Also carried: the V222 grounding-scrutiny lesson — assess whether the caveat honestly separates firsthand-proven results from cited motivation; here the closed form G(n)=n mod (k+1) is honestly scoped as firsthand because Wikipedia states only the win/loss outcome condition, not the Grundy-value equation. Standing non-contiguity persists: landing V232 does not imply every lower verdict below the high-water is closed — V231 (P218), V225 (P212), V137 (P124), V132 (P119), round-26 FLEET P113→V126 remain open pulls below the high-water.

## 💡 Session idea

The verifier pins the Sub({1..k}) Grundy closed form and the disjunctive-sum XOR rule, but only for single-move-set heaps. A cheap orthogonal follow-on (P-next) would pin the **mex/nimber period** of a *general finite subtraction set* S (not just {1..k}): Sprague–Grundy theory guarantees the nim-sequence of any finite subtraction game is eventually periodic, and for S={1..k} that period is exactly k+1 with no pre-period. The gate: an exact zero-mismatch check that for a sweep of finite sets S (e.g. {1,3,4}, {2,5,6}) the computed nim-sequence enters its detected period p within pre-period n0, plus a FALSIFIABILITY leg that a deliberately-wrong "period = max(S)+1 for all S" claim is rejected on at least one S where it fails. Guard recipe for a later session: the anchor is the verifier's `grundy_subtraction(k, upto)` mex reducer — generalize the `moves` list from `range(1,k+1)` to an arbitrary `sorted(S)`, add a `detect_period(seq)` reducer over the computed sequence, and target `period({1..k}) == k+1` (continuity leg, must reproduce this verdict) plus a non-trivial `period({1,3,4})` firsthand value.
