# Session — PROPOSAL 017: IRV monotonicity in close races (unrelated-domain rotation)

> **Status:** `in-progress`
> **Model/time:** fable · 2026-07-13T00:57:12Z (Ideas Lab worker slice — draft the
> ORDER 004 rule-3 unrelated-domain idea + PROPOSAL 017 outbox append; card born
> in-progress as the designed gate hold, flips complete in this PR's final commit)

## Scope

Draft a genuinely new idea in a domain COMPLETELY UNRELATED to the fleet, under the
standing owner ORDER 003 (continuous pipeline) + ORDER 004 night-run rotation rule 3
("fleet backlogs → venture's book/product space → game mechanics → COMPLETELY
UNRELATED domains (I want those too)", verified at HEAD `c99852c`): PROPOSALs 013/014
were fleet-meta, 015/016 game mechanics — this slice serves the rotation's first
unrelated slot. Domain picked after a three-candidate shortlist dedup (social choice /
apportionment / transit control — reasons in the idea's § Relations): 3-candidate
instant-runoff voting monotonicity, written up as
`ideas/fleet/irv-monotonicity-close-races-2026-07-13.md` (probed single-pass this
slice, sim-ready) and appended to `control/outbox.md` as PROPOSAL 017. The sim is
fully hermetic by design — every fixture (enumeration bounds, seeds, band constants)
is constructed by the sim itself, zero repo/network reads in the eventual verdict
session — with pre-registered APPROVE/REJECT/NULL bands where the NULL
(model-dependence) is a legitimate reportable outcome. Placement decide-and-flag:
sections are roster-derived and ad-hoc sections red `check_sections.py` (ORPHAN), so
the head is homed in `ideas/fleet/` per that checker's own non-lane carve-out, flagged
in the file rather than silently. Dedup swept vs PROPOSALs 001–016, the ideas/ tree,
and prior cards' 💡 lines (`rg -i 'voting|election|ballot|runoff|monotonicit|apportion'`,
bootstrap.py/.substrate excluded — zero prior social-choice content anywhere).

**📊 Model:** fable · content + outbox proposal only (idea file, card, index row,
claim file, outbox append; no product code, no control/status.md or control/inbox.md
writes)

## 💡 Session idea

(filled at session close — this PR's final commit, the born-red hold discipline)

## ⟲ Previous-session review

Newest predecessor card (`.sessions/2026-07-13-proposal-016.md`, worker slice
~00:35Z): closed clean — its PR #279 merged at `d1d1b6b` with the PROPOSAL 016 outbox
entry grammar-clean and its born-red card flipped complete in its final commit as
designed; ~7 minutes later the manager landed ORDER 004 (night-run directive, PR #280
`c99852c`), whose rotation rule 3 is exactly the authority this slice executes — the
pipeline's cadence is now order-to-execution within the hour. Its 💡 (a
machine-readable `couples:` field for sim-lab's VERDICT grammar, arming a tripwire on
recorded cross-build coupling debt) is still unconsumed and NOT re-minted here —
checked before writing this card's own 💡 (rg for couples/coupling across ideas/ +
.sessions/ — only its own card and idea file carry it). No reconciliation debt found.
