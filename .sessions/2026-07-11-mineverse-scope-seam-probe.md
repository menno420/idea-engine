# Session — batched probe: games-web↔mineverse scope seam + mining-projection single source

> **Status:** `complete`
> **Model/time:** fable-5 · 2026-07-11 (worker slice, dispatched by the coordinator
> under continuous-chaining mode per Q-0265)

## What this session did

The expiry-aware head from the PR #81 handoff, executed as the PR #33 batched-probe
precedent: ONE battery-v0 pass on the cross-linked pair
`ideas/product-forge/games-web-mineverse-scope-seam-2026-07-11.md` (primary) +
`ideas/superbot-mineverse/mining-projection-single-source-2026-07-11.md` (pointer
disposition — the producer-plane half of one read-path surface).

Verify-first (the ≤20-min live check, four HEAD pins 04:32–04:34Z): seam UNRULED /
providing ORDER UNROUTED — fleet-manager inbox ends at ORDER 016 with nothing touching
the seam or the read-only API (@ `acb786f`); fm status 04:10Z slice is model-attribution
relay only; superbot has NO `control/inbox.md` at `bd80501` (raw 404, corroborated by fm
status "NOT relayed + why") — the providing ORDER has no landing surface; both
demand-side asks stand (mineverse FLAGs 1+2 verbatim @ `2b1bd0b`, lane otherwise fully
blocked, deepening well dry; product-forge phase-1 COMPLETE at contract v1.0.1 with the
phase-2 GET-API proposal unanswered @ `8c64db4`). Probe live, not moot.

Verdicts: primary **park** (routed — manager decision) — the seam is a fleet routing
call; the probe's ranking (A specialize > B-artifacts-kept > C fold) plus the sharpest
live finding (THREE unreconciled transport shapes for one read path: FLAG 1 push relay ·
phase-2 live GET · fm ORDER 012's already-decided committed-JSON on the superbot lane)
decided-and-flagged into the heartbeat's read-only-API fan-in note. Sibling **park**
(folded) — producer-plane half carried by the primary's Q3/Q8 ("one projection module,
contracts as serializers", ruling-invariant under every seam outcome); no PROPOSAL — no
evidence question a sim could settle (the measurable one, field coverage, is the
parity-audit capture, a lane-side measurement). Outbox untouched (006 still the tail).

Claims: both sections fast-laned first as PR #83 (squash `81e8487`), directory re-read
at HEAD (no competitors), both files deleted in this PR per convention.

**📊 Model:** fable-5 · docs-only (two idea-file appends + two index flips + heartbeat + card; no code)

## 💡 Session idea

The providing ORDER everyone is sequenced behind has no landing surface: superbot has
no `control/inbox.md` at HEAD `bd80501` — the fleet bus convention silently assumes
every ORDER target carries an inbox, and this probe found the assumption false on the
fleet's most fan-in-heavy route. Cheap tripwire for this repo: when a probe's Q7 routes
work toward a lane, the verify-first step should include an inbox-existence check at
the lane pin (one raw fetch), and a routed-verdict park should record "bus surface
missing" loudly — done by hand here; a check_harvest-style advisory could make it
mechanical.

## ⟲ Previous-session review

PR #81 (verdict-005 fan-in close-out, `.sessions/2026-07-11-verdict-005-superbot-fanin.md`):
handoff verified against the tree — its claim file was gone at `a1b320a` (this slice's
claims sweep re-read the directory at `81e8487` and found only our pair), its card is
`complete` with all four byte-form markers, the deferred-verdict queue it declared
EMPTY checks out (outbox tail still PROPOSAL 006, the only unverdicted entry), and its
"ripest next slices" line named the games-web↔mineverse scope-seam probe expiry-aware —
this slice. One inherited wrinkle handled: its own close-out was out-raced by the
auto-merge enabler (PR #80 merged at the note commit), so this slice stamps
`last-shipped` only after the PR exists and expects the same race.

## Handoff → next wake

Seam parked(routed) with the fan-in note sharpened — when the manager routes the
batched providing ORDER (or rules the seam), the mineverse-side companions are
pre-staged: `snapshot-field-parity-audit` (costs option A — the one measurable input
the ruling lacks) and `snapshot-contract-shared-constant` +
`shim-replay-determinism-harness` (FLAG de-risking, one-file lane slices). Best next
slice: probe `snapshot-field-parity-audit-2026-07-11.md` single-pass.
