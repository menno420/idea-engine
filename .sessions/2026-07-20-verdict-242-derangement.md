# VERDICT 242 — derangement routing: when N agents are assigned N tasks by a uniformly-random permutation (a random bijection giving each agent a distinct task), the probability that NO agent lands on its own "home" task is exactly the derangement ratio p_N = D_N/N! = Σ_{k=0}^{N}(−1)^k/k! → 1/e ≈ 0.367879, provably NOT the naive independence estimate (1−1/N)^N — independent adjudication of PROPOSAL 229

> **Status:** `in-progress`

- **📊 Model:** unrecorded · high · review/verify

started: 2026-07-20T20:30:00Z

💓 Heartbeat: round-54 FLEET slice — independent adjudication of PROPOSAL 229 (2026-07-20T20:30:00Z), P229 → V242 (+13 offset, lane FLEET). This is a genuine independent re-verification with real teeth — nothing in P229's self-report was trusted; every leg was reproduced firsthand. The committed verifier (byte-identical in idea-engine `ideas/fleet/verify_229_derangement_routing.py` AND sim-lab `sims/verdict-242-derangement-routing/verify_229_derangement_routing.py`, sha256 `16ea8e9a…4931`, diff exit 0 both ways) reproduces `results_sha256` `1f68c3d1cb6003f6ede1bc1d47e18f27a996bea9fa716f759d38fc2c3832365a` VERBATIM (full 64-hex byte-grep, no truncation) three ways in agreement: (a) separate re-invocation A, (b) separate re-invocation B, (c) the in-process double-run guard ("in_process_double_run: IDENTICAL"). Committed run-stdout.txt reproduced with diff exit 0. SEED=20260717. Four gates each read in ITS OWN direction, independently recomputed with exact `fractions.Fraction` where exact: G1 two-route EXACT identity PASS (p_7 = 1854/5040 = 103/280 exactly), G2 Monte-Carlo AGREEMENT PASS (verifier z=−1.151504; independent re-simulation z=−1.0518, both |z|<3), G3 invariants PASS (second recurrence D_n=n·D_{n−1}+(−1)^n exact n=1..15, 0<p_n<1, alternating straddle of 1/e even-above/odd-below converging), G4 FALSIFIABILITY PASS (naive (1−1/N)^N REJECTED, verifier z_naive=82.246356; independent z_naive=82.348 ≫ 8). Grounding byte-pinned to Wikipedia "Derangement" oldid 1364530247, raw-wikitext sha1 `ba6f5759199b761a56d50342132212bbc99ed505` (27877 bytes) — self-computed sha1 MATCH. Independent quoted-vs-derived determination performed firsthand on the pinned revision. RULING: **QUALIFIED** — verifier, digest (3-way), determinism, and all four gates land exactly as claimed, but one derived-vs-quoted label overstates novelty (see below), the same seam class that qualified V235/V238. Born-red HOLD armed on this first card commit (`in-progress`); this card flips to `complete` in the LAST commit, after the outbox block + status heartbeat are written and `python3 bootstrap.py check --strict` exits 0.

⏳ Flip note (born-red): this card ships `> **Status:** in-progress` on its FIRST commit so the substrate born-red gate holds the idea-engine PR RED until the slice is genuinely done; it flips to `complete` as the deliberate LAST commit — after the outbox verdict block and the status heartbeat are written. That flip clears the HOLD and releases merge-on-green. NO merge API calls from this session; CI substrate-gate + the landing automation squash-merge the green PR.

## What this verdict does

Independently adjudicates PROPOSAL 229 (`## PROPOSAL 229 · 2026-07-20T20:30:00Z · status: sim-ready`, idea-engine `control/outbox.md`; round-54 FLEET slot of the fleet→venture→game→unrelated cycle; P229 → V242, +13 offset). Claim: for a fleet of N agents assigned tasks by a uniformly-random permutation routing (a random bijection giving each agent a distinct task), the probability that NO agent is routed to its own home task equals the derangement ratio **p_N = D_N/N! = Σ_{k=0}^{N}(−1)^k/k! → 1/e ≈ 0.367879**, and is provably NOT the naive independence estimate (1−1/N)^N. Distinct from the existing hat-check card (P204→V217, fixed-point COUNT ~ Poisson(1)); this pins the nobody-home RATIO with a two-route exact identity plus an independence falsifier.

## Method — nothing trusted, everything reproduced firsthand

- **Byte-identical verifier.** The committed verifier is byte-identical across both repos and to a fresh copy: sha256 `16ea8e9a03c860cd1a3b2932e36f9fb8757e8cdf372b1c2f2b9f54d6b5fc4931`; `diff` exit 0 (fresh copy ↔ committed) and `diff` exit 0 (sim-lab ↔ idea-engine source). Re-ran the SAME verifier the proposal committed — did not rewrite it. Stdlib-only (`json`, `hashlib`, `math`, `random`, `fractions`).
- **Digest reproduced 3-way, no truncation.** Full 64-hex byte-grep out of stdout: `1f68c3d1cb6003f6ede1bc1d47e18f27a996bea9fa716f759d38fc2c3832365a` — matches on (a) separate re-invocation A, (b) separate re-invocation B, (c) the in-process double-run guard (prints `in_process_double_run: IDENTICAL`, then the single digest). All three AGREE with the target. Committed `run-stdout.txt` reproduced with `diff` exit 0. SEED=20260717.
- **Gates, each in its own direction, recomputed independently (see `sims/verdict-242-derangement-routing/probe-report.md`):**
  - **G1 EXACT identity (two routes)** — p_n computed two independent ways: (a) subfactorial recurrence D_n=(n−1)(D_{n−1}+D_{n−2}) then Fraction(D_n,n!); (b) alternating sum Σ_{k=0}^n(−1)^k/k!. EXACTLY equal as `fractions.Fraction` for every n=1..12. Independently confirmed with a THIRD route (the other recurrence D_n=n·D_{n−1}+(−1)^n and the closed form D_n=n!·Σ(−1)^i/i!) — all agree; **p_7 = 1854/5040 = 103/280 exactly**. PASS (self-certifying theorem — firsthand teeth).
  - **G2 Monte-Carlo AGREEMENT** — N=7, T=2,000,000 seeded Fisher-Yates permutations, count fixed-point-free. Verifier: derangements=734929, p̂=0.3674645, z=−1.151504, |z|<3. Independent re-simulation (my own seed): p̂=0.3674985, z=−1.0518, |z|<3. PASS (small |z| = agreement with p_7=103/280).
  - **G3 invariants / robustness** — second recurrence D_n=n·D_{n−1}+(−1)^n exact (integer) n=1..15; 0<p_n<1 exact (Fraction) n=2..15; alternating straddle of 1/e via tight rational bounds — p_even strictly above 1/e, p_odd strictly below, converging (p_2=0.5 → p_7=0.36785714 → p_12 within 1.5e-10 of 1/e). All exact identities hold. PASS.
  - **G4 FALSIFIABILITY** — the naive independence model predicts q_7=(1−1/7)^7=(6/7)^7=0.339917. On the SAME MC sample: verifier z_naive=82.246356; independent z_naive=82.348 — both ≫ 8, so the naive alternative is decisively REJECTED while the exact model is ACCEPTED (G2). PASS (large |z_naive| = rejection, opposite polarity to G2).
  - all_pass=true, first_failing_gate=null, decision=sim-ready.

## Grounding + independent quoted-vs-derived determination (the seam)

External, sha1-pinned: Wikipedia "Derangement" oldid 1364530247, `action=raw` wikitext sha1 `ba6f5759199b761a56d50342132212bbc99ed505` (27877 bytes) — self-computed sha1 MATCH (direct `curl` fetch, HTTP 200). Each cited value was grepped firsthand on the pinned revision:

| Cited value | P229 label | Firsthand determination | Match? |
|---|---|---|---|
| Closed form D_n = n!·Σ(−1)^i/i! | QUOTED | QUOTED — lines 99/145/152 verbatim | ✅ |
| Recurrence D_n=(n−1)(D_{n−1}+D_{n−2}) | QUOTED | QUOTED — line 83 | ✅ |
| Recurrence D_n=n·D_{n−1}+(−1)^n | QUOTED | QUOTED — line 125 | ✅ |
| Nearest-integer D_n=[n!/e] | QUOTED | QUOTED — line 101 | ✅ |
| D_7 = 1,854 | QUOTED | QUOTED — table lines 95/279 | ✅ |
| Limit D_n/n! → e^{−1} ≈ 0.367879 | QUOTED | QUOTED — lines 156–157 ("the limit of the probability that a randomly selected permutation … is a derangement") | ✅ |
| Fleet-routing framing | DERIVED | DERIVED — absent from page | ✅ |
| Naive alternative (1−1/N)^N + rejection | DERIVED | DERIVED — absent (grep count 0) | ✅ |
| Monte-Carlo z-values | DERIVED | DERIVED — absent (no "monte"/"simulat"/z on page) | ✅ |
| **p_7 as a probability** | **DERIVED** | **SEAM** — the ratio D_7/7! ≈ 0.36785 71429 is literally displayed on the page (table line 281) in a `D_n/n!` column the prose (line 157) explicitly calls the probability that a random permutation is a derangement; only the exact fraction 103/280 is firsthand. Labeling this DERIVED overstates novelty. | ⚠️ MISLABEL |

## Ruling

**QUALIFIED.** Independent adversarial re-verification of PROPOSAL 229. The committed verifier is byte-identical (diff exit 0 both ways), the disclosed `results_sha256` reproduces VERBATIM on the full 64 hex three ways in agreement (two separate re-invocations + the in-process double-run guard), determinism holds (SEED=20260717), and all four gates pass in their own stated directions with real teeth — independently recomputed, not merely re-read. The single defect is a minor labeling seam: P229 lists "p_7 as a probability" as DERIVED (firsthand, not on the page), but the D_n/n! ratio for n=7 (≈0.3678571429) is literally displayed on the pinned revision in a column the prose frames as a probability — only the exact fraction 103/280 is genuinely firsthand. This overstates novelty by exactly the amount that qualified V235 (birthday-collision closed form) and V238 (balls-into-bins). No defect in the computation, the digest, the determinism, or the gates; the honest fix is to re-attribute the n=7 probability VALUE as quoted and keep only the exact-fraction reduction 103/280 as firsthand. QUALIFIED, not APPROVE (a genuine seam) and not REJECT (no gate fails, digest reproduces, no core claim is false). Offset: PROPOSAL 229 → VERDICT 242 (+13).

## sim-lab #326 reconciliation

sim-lab PR #326 (merge commit `f9042a2`, main HEAD) already landed the proposal-authored mirror: `sims/verdict-242-derangement-routing/` with the byte-identical verifier, `run-stdout.txt` (exit 0), and `probe-report.md`. This adjudication was built ON TOP of that mirror — the SAME committed verifier was re-copied and re-run, the SAME digest reproduced, the SAME grounding re-checked — rather than re-authoring anything. NO competing sim-lab PR is opened; the sim-lab reproduction dir is complete as the mirror, and this idea-engine `## VERDICT 242` block is the canonical routed verdict record.

## Merge posture

CI substrate-gate + the `enable-auto-merge` landing automation squash-merge the green PR; **NO merge API calls are made from this session**. This idea-engine slice ships the outbox verdict block (`control/outbox.md` `## VERDICT 242`) + the coordinator heartbeat (`control/status.md`, verdict high-water → V242) + this `.sessions/` verdict card only. The born-red HOLD on this card's first commit holds the PR red until the card flips `complete` as the deliberate last commit.

## ⟲ Previous-session review

Previous-session review: VERDICT 240 (Hex is never a draw on the n×n rhombus ⇒ uniform random completion gives P(first player connects)=1/2 exactly; P227 → V240) landed APPROVE with a full-64 digest match and four gates PASS, and explicitly noted a CLEAN quoted-vs-derived boundary — "none of the V235/V238 QUALIFIED-seam issues recur here." V242 is the counter-case: the seam DOES recur. Its carry-forward is twofold. (1) GATE-POLARITY discipline — read each gate in ITS OWN direction: G1 is an exact `fractions.Fraction` identity (a self-certifying theorem, the firsthand teeth), G2 is a Monte-Carlo AGREEMENT gate (|z|<3 = the closed form is the null the sample must match), G3 is an exact invariant/straddle gate, and G4 is a FALSIFIABILITY gate read at the OPPOSITE polarity (a deliberately-wrong model (1−1/N)^N that must be REJECTED at large |z|). Reading G4's large z as a failure, or G2's small z as weak, would invert the verdict — the polarity is the point. (2) DIGEST-POSTURE discipline — the results dict's own sha256 IS the digest; reproduce it on the FULL 64 hex by byte-grep (no truncation) across a separate re-invocation AND the in-process double-run guard before trusting determinism. (3) The V235/V238 grounding-accuracy discipline that produced THIS ruling: a "derived, not quoted" caveat is a claim with teeth — grep the pinned revision firsthand for EACH cited value; where the value (or its closed form) is literally on the page, "derived" overstates novelty and the honest disposition is QUALIFIED, not APPROVE. Standing non-contiguity persists: landing V242 does not imply every lower verdict below the high-water is closed (V225, V137, V132, V126 remain open pulls per the heartbeat).

## 💡 Session idea

The verifier pins the k=0 atom of the fixed-point distribution — P(no agent home) = D_N/N! → 1/e — with a two-route exact identity and an independence falsifier. A cheap orthogonal follow-on (P-next) would pin the FULL small-count law: the number of agents left on their home task is asymptotically **Poisson(1)**, so P(exactly m home) → e^{−1}/m! for each fixed m, and exactly-m has the finite-N closed form C(N,m)·D_{N−m}/N! = (1/m!)·Σ_{k=0}^{N−m}(−1)^k/k!. Gates: G1 an EXACT `fractions.Fraction` identity — C(N,m)·D_{N−m}/N! equals (1/m!)·Σ_{k=0}^{N−m}(−1)^k/k! for every (N,m), and Σ_m of those equals 1 exactly (a partition-of-unity leg with real teeth); G2 a Monte-Carlo AGREEMENT leg on the whole histogram of home-count over the same Fisher-Yates sample (|z|<3 per bin); G4 a FALSIFIABILITY leg rejecting the naive Binomial(N,1/N) home-count model (which gets the variance wrong — the true limit has mean=variance=1 exactly, Binomial(N,1/N) has variance 1−1/N) at large |z|. Grounding stays honestly split: the "Rencontres numbers" / "Derangement" pages carry the exactly-m closed form and the Poisson(1) limit qualitatively (quoted), while the histogram sim, the partition-of-unity check, SEED, and digest are firsthand. This generalizes THIS card's k=0 ratio to the whole distribution without new machinery — the same subfactorial ladder and the same MC sample feed every gate.
