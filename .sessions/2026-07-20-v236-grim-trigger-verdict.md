# VERDICT 236 — grim-trigger folk-theorem threshold: in an infinitely-repeated symmetric Prisoner's Dilemma with stage payoffs T>R>P>S, the grim-trigger strategy (cooperate until any defection, then defect forever) sustains mutual cooperation as a SUBGAME-PERFECT equilibrium iff the discount factor δ ≥ δ* with the EXACT closed-form threshold δ*=(T−R)/(T−P) — divided by temptation-minus-PUNISHMENT, NOT temptation-minus-sucker (T−S) — the punishment credible because (D,D) is itself a stage Nash equilibrium (S<P), so cooperation is NOT sustained for all δ>0 but only above that patience floor (canonical PD (5,3,1,0) → δ*=1/2) — reproduce PROPOSAL 223

> **Status:** `complete`

- **📊 Model:** Claude Opus · high · verdict-reproduction

started: 2026-07-20T17:58:00Z

💓 Heartbeat: round-53 GAME slice P223 → V236 (+13, lane game); reproduction verifier already landed byte-identical in sim-lab via PR #316 (MERGED, merge SHA 41d3bb8) — this slice mirrors the verdict block into control/outbox.md (V236 ruled APPROVE, verdict high-water ADVANCES V235→V236, union-max no regress) and updates the coordinator heartbeat. Digest target results_sha256 `7f00cea0bd40b2133ae9e91110c5112e8d5bf16bbcd90809a91911015215334f` (printed AND literal 64-char string compared, no truncation); reproduced byte-identical across an in-process double-run AND 3 separate re-invocation processes. Determinism CONFIRMED (in-process double-run guard True AND separate cross-invocation byte-identical). SEED=20260717, N_MC=200000, Z_GATE=3.0. Six pre-registered gates each read in its own direction. Grounding sha1-pinned to Grim trigger oldid 1292515670 (`beb2798d4ee3752cda422e6192a02e0ca3ea7926`), caveat HONEST. VERDICT 236 = APPROVE. Born-red HOLD armed on this first card commit (in-progress); this card flips to `complete` in the LAST commit to release merge-on-green.

⏳ Flip note (born-red): this card ships `> **Status:** in-progress` on its FIRST commit so the substrate born-red gate holds the idea-engine PR RED until the slice is genuinely done; it flips to `complete` as the deliberate LAST commit — only after the outbox block + status heartbeat are written and `python3 bootstrap.py check --strict` exits 0. That flip clears the HOLD and releases merge-on-green. NO merge API calls from this session; CI substrate-gate + the landing automation squash-merge the green PR.

## What this verdict does

Reproduces PROPOSAL 223 (P223 → V236, +13 offset, lane GAME of the round-53 fleet→venture→game→unrelated cycle): in an infinitely-repeated symmetric Prisoner's Dilemma with stage payoffs **T>R>P>S**, the **grim-trigger** strategy (cooperate until any defection, then defect forever) sustains mutual cooperation as a **subgame-perfect equilibrium iff δ ≥ δ*** with the EXACT closed-form threshold **δ*=(T−R)/(T−P)** — divided by temptation-minus-PUNISHMENT, NOT temptation-minus-sucker (T−S) — the punishment credible because **(D,D) is itself a stage Nash equilibrium (S<P)**, so cooperation is **NOT** sustained for all δ>0 but only at or above that patience floor (canonical PD (5,3,1,0) → δ*=1/2). The verifier `grim-trigger-folk-theorem.py` (byte-identical copy already landed in `sims/verdict-236-grim-trigger-folk-theorem/` via PR #316, MERGED) reproduces `results_sha256`, confirms determinism, and evaluates six pre-registered gates each in its own direction; the probe checks the byte-pinned grounding against the pinned Wikipedia revision.

## Method

- Independent byte-identical re-copy of the verifier (`diff -r` source↔copy exit 0, no differences), stdlib-only (`hashlib`, `json`, `math`, `random`, `fractions.Fraction`).
- Digest posture: WHOLE-DICT — the compact-canonical results dict's own sha256 IS the digest; `main()` builds the results twice in-process and asserts canonical-JSON byte-equality before printing the digest and exiting 0. Target `results_sha256` full-64 `7f00cea0bd40b2133ae9e91110c5112e8d5bf16bbcd90809a91911015215334f` (literal 64-char compare, no truncation); reproduced byte-identical across an in-process double-run AND 3 separate re-invocation processes; SEED=20260717, N_MC=200000, Z_GATE=3.0.
- Gates (each read in ITS OWN direction, against the proposal's OWN criteria) — see `sims/verdict-236-grim-trigger-folk-theorem/probe-report.md` for observed values:
  - **G1 EXACT INDIFFERENCE IDENTITY** — headline (5,3,1,0): δ*==1/2 AND the one-shot-deviation incentive gap V_C(δ*)−V_D(δ*)==0/1 exactly (`fractions.Fraction`). PASS (firsthand teeth — the boundary gap vanishes exactly at δ*).
  - **G2 EXACT GRID** — (5,3,1,0)→1/2, (5,4,1,0)→1/4, (5,2,1,0)→3/4, (9,8,1,0)→1/8, every boundary gap==0/1 and each 0<δ*<1. PASS.
  - **G3 MC AGREEMENT** — continuation-probability statistic at δ*=0.5, z=1.404082<3. PASS.
  - **G4 MC GRID** — max|z|=1.150506 (per-tuple 1/2→−1.150506, 1/4→0.654137, 3/4→0.630256, 1/8→−0.319587), all within 3σ. PASS.
  - **G5 FALSIFIABILITY** — naive δ_wrong=(T−R)/(T−S)=2/5 has exact E[D]=−2/3≠0, z_wrong=−140.372466 REJECTED (opposite direction to G3/G4). PASS.
  - **G6 FALSIFIABILITY** — "any δ>0 sustains cooperation" REJECTED at δ*/2=1/4: exact E[D]=−4/3<0 (deviation strictly profitable below threshold), z_below=−448.661725 REJECTED. PASS.
  - all_pass=true, first_failing_gate=null.
- Grounding byte-pinned to the `action=raw` wikitext of the revision; local raw bytes sha1 and MediaWiki API revision sha1 match.

## Grounding — honest scope

External, sha1-pinned: Wikipedia "Grim trigger" oldid 1292515670, action=raw wikitext sha1 `beb2798d4ee3752cda422e6192a02e0ca3ea7926` (raw local bytes AND MediaWiki API revision sha1 match). On-page (CONFIRMED): the article states the grim-trigger strategy, (D,D) as the credible unique-Nash punishment, the one-shot-deviation comparison, and the SPE condition δ≥1/2 for its single normalized PD. HONEST scope: the page does NOT give the general (T−R)/(T−P) closed form — correctly claimed as the doc's OWN firsthand contribution alongside the `fractions.Fraction` grid, the Monte-Carlo agreement legs, and BOTH falsifiability legs. No dishonest attribution found — caveat neither oversold nor undersold.

## Merge posture

CI substrate-gate + the `enable-auto-merge` landing automation squash-merge the green PR; **NO merge API calls are made from this session**. The sim-lab verifier + run-stdout.txt + probe-report.md already landed byte-identical via PR #316 (MERGED, merge SHA 41d3bb8), which ALSO bundled the sim-lab verdict session card (`.sessions/2026-07-20-verdict-236-grim-trigger-folk-theorem.md`, Status `complete`) and claim (`control/claims/2026-07-20-verdict-236.md`) — so the sim-lab verdict mirror is already complete and NO separate sim-lab verdict PR is opened for V236. This idea-engine slice ships the outbox verdict block + the coordinator heartbeat + this `.sessions/` verdict card only.

## ⟲ Previous-session review

Previous-session review: VERDICT 234 (reservoir sampling Algorithm R uniform k/n — every streamed item left in the size-k reservoir with probability exactly k/n; P221 → V234) landed APPROVE with a full-64 digest match and four gates PASS via the born-red HOLD choreography. Its carry-forward is GATE-POLARITY discipline: read each gate in ITS OWN direction — an exact Fraction identity is a self-certifying theorem, a z-test is an AGREEMENT gate when the closed form is the null, and a deliberately-wrong model that must be REJECTED is a FALSIFIABILITY gate read at the opposite polarity. V236 leans on the same discipline: G1/G2 are exact `fractions.Fraction` indifference identities (the firsthand teeth — δ*=(T−R)/(T−P) with a zero incentive gap at δ*), G3/G4 are Monte-Carlo AGREEMENT gates (|z|<3), and G5/G6 are FALSIFIABILITY gates that reject the wrong-sucker formula (T−R)/(T−S)=2/5 at z≈−140.37 and the "any δ>0 sustains cooperation" claim at z≈−448.66 — opposite direction to G3/G4. It also carries the V235 grounding-accuracy discipline: the caveat honestly separates firsthand-proven / off-page (the general (T−R)/(T−P) closed form, the grid, the sim, SEED, and digest) from cited on-page (the grim strategy, (D,D) credible punishment, the one-shot-deviation argument, and δ≥1/2 for the normalized PD). Standing non-contiguity persists: landing V236 does not imply every lower verdict below the high-water is closed (V225, V137, V132, V126 remain open pulls).

## 💡 Session idea

The verifier pins the grim-trigger cooperation floor at δ*=(T−R)/(T−P) for a symmetric two-player PD under grim (maximal, permanent) punishment. A cheap orthogonal follow-on (P-next) would pin the **milder-punishment variant**: replace grim reversion with a **finite T-period stick-then-return** (a "penal code" that reverts to (D,D) for exactly k rounds, then forgives back to cooperation). The SPE floor rises above (T−R)/(T−P) — the shorter the stick, the more patience cooperation demands — and diverges as k→1 (one-period punishment cannot deter for any δ<1 once (T−R) exceeds the single-round punishment loss). Gate: G1 an EXACT identity — for the k-period stick, an exact `fractions.Fraction` indifference condition yields a closed-form δ*_k that is strictly greater than the grim δ* for every finite k and decreases monotonically to the grim floor as k→∞; plus a CONTINUITY leg that k→∞ recovers THIS verdict's (T−R)/(T−P); plus a FALSIFIABILITY leg that the grim floor is REJECTED as the threshold for finite k (the two punishment regimes must be distinguished, not conflated). Grounding: the "Grim trigger" and "Folk theorem (game theory)" pages treat limited/optimal-penal-code punishment in their own sections, so the pin stays honestly on-page for the qualitative statement and off-page for the k-period closed form, the sim, SEED, and digest.
