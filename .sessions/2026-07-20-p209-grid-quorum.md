# Session — PROPOSAL 209: Grid quorums beat the majority at √N (round-50 FLEET opener)

> **Status:** `in-progress`

📊 Model: Claude Opus · high · idea/planning

💡 Session idea: arrange N=k² replicas on a k×k grid and let each quorum be one full row ∪ one full column (size 2k−1 ≈ 2√N); any two such quorums intersect because a row and a column always cross at exactly one cell, so mutual-exclusion / read-write intersection is guaranteed with a √N quorum instead of a majority ~N/2 — and STRUCTURE, not size, buys it (two RANDOM subsets of the same size 2k−1 are disjoint with positive probability). This is the Maekawa grid quorum. Exhaustive `Fraction` enumeration + Monte-Carlo disjoint-rate, `SEED = 20260717`.

## ⟲ Previous-session review

previous-session review: round-49 closed its full rotation — P205 (Bélády's anomaly, FLEET), P206 (independent screens ladder → V219 ruled), P207 (all-pay rent dissipation → V220 ruled), P208 (Pólya urn uniform finish → V221 in flight). This card opens round-50 with the FLEET slot (P209 → V222, +13). Heartbeat: authoring born-red; verifier green (all five gates), preflight run to green, PR opened READY, awaiting the substrate-gate.

## 💡 Session idea

The head, model, and gates are complete on the first commit; only the Status value is drafted `in-progress` so the born-red `[session-card-hold]` HOLD holds the PR red until the work lands. The flip to `complete` is the last commit and releases merge-on-green. Born-red HOLD with zero doc findings is the intended state, not a defect.
