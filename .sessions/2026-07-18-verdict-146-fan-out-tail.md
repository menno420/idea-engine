# VERDICT 146 mirror — the fan-out tail amplification / fleet-ops · reliability tail-latency: the folk model of a scatter-gather is that a rare per-leaf slowdown stays rare at the request level — "each of N leaf servers misses its latency SLO only p=1% of the time, so a fanned-out request is basically always fast." It is NOT. A request that fans out to N independent leaves and must wait for the LAST of them is slow whenever ANY leaf is slow, so its slow probability is the union P_slow(N)=1−(1−p)^N, which climbs toward 1 as fan-out width N rises even for a tiny per-leaf p. At p=0.01 the MEDIAN request is already slow at only N*=ln2/(−ln(1−p))=ln2/(−ln0.99)≈0.693/p≈69 leaves (n_star=69, real-valued root 68.967564, bracketed P_slow(68)<0.5≤P_slow(69)), and at N=100 the request is slow 1−0.99^100=63.4% of the time — while the MEAN number of slow leaves is merely LINEAR at N·p (just ~1 slow leaf at N=100). The tail is amplified super-linearly by fan-out width even though the average slow-leaf count is unremarkable, because "slow" is a union over leaves, not an average. Round-31 FLEET-slot (a series-reliability / union-bound tail-latency object, deliberately DISTINCT from the prior fleet heads — P121 hedged-request tail cure (the p95-deferred second copy that CURES this tail), P129 correlated-vote Condorcet ceiling, P125 two-choices routing — this is the object the hedge cures: the all-of-N wait-for-the-last tail itself; rotation fleet→venture→game→unrelated, P133 opened round-31 at the fleet slot (fan-out tail), P134 took the venture slot (blended-churn LTV)), P133 → V146 (+13).

> **Status:** `in-progress`
> 📊 Model: Claude Opus · high · review/verify

Born in-progress as this session's first content commit — this born-red HOLD is deliberate and holds the substrate-gate RED until the mirror is fully recorded. The flip to `complete` is the last step, taken once the outbox VERDICT 146 block + heartbeat are written and `python3 bootstrap.py check --strict` is green — it releases the auto-merge-enabler. This is the MIRROR slice — fan-in to the fleet-manager (Q-0264) of sim-lab's VERDICT 146; the verification itself was performed and LANDED on the sim-lab side (branch `claude/verdict-146-fan-out-tail`, sim dir `sims/verdict-146-fan-out-tail/`, PR #221 MERGED, sim-lab main HEAD @4917c5930ad4b8516c451e0d31e3173f24be5215) after the born-red card flip there.

## Objective
Mirror sim-lab's VERDICT 146 (APPROVE) into idea-engine as the lane→manager fan-in (Q-0264): record this V146 card, append the VERDICT 146 outbox block, and reconcile the verdict high-water by UNION — V146 is a LOWER, non-contiguous fill (V147 for P134 already landed via mirror #570), so the high-water STAYS V147 (no regress, no advance; proposal high-water P134 kept). Verifies idea-engine PROPOSAL 133 (source header verbatim: `## PROPOSAL 133 · 2026-07-18T12:25:02Z · status: sim-ready`, target: sim-lab VERDICT 146, +13 offset), the round-31 FLEET-slot opener (rotation fleet→venture→game→unrelated; P133 opened round-31 at the fleet slot, P134 took venture). The verification itself was performed and LANDED on the sim-lab side (branch `claude/verdict-146-fan-out-tail`, sim dir `sims/verdict-146-fan-out-tail/`, PR #221 MERGED @4917c5930ad4b8516c451e0d31e3173f24be5215).

## GROUNDING (verified at HEAD)
- Reference verifier `ideas/fleet/fan_out_tail_amplification.py` — git blob `021a63b372008055ac1e18c666a1b6915bb048ea`, file sha256 `8f971ee0bb9a612b01ba85a6a5acfea0015d8f7c079c5fc0299a3e18435d3c7e`, **136** lines / **4237** bytes. Stdlib-only (hashlib, json, math, random). Pinned source (permalink): https://github.com/menno420/idea-engine/blob/12bd4eccf88acba8c5977f013f80d67eba9cdd25/ideas/fleet/fan_out_tail_amplification.py
- Domain anchor: the union-bound / series-reliability tail — a request served by ALL of N independent components is slow whenever ANY component is slow, P_slow(N)=1−(1−p)^N, the same structure behind "The Tail at Scale" (Dean & Barroso, CACM 56(2):74–80, 2013), where high-percentile latency of a scatter-gather is dominated by the slowest of many parallel leaves and the cure is to hedge the slow few — https://research.google/pubs/the-tail-at-scale/ (verified reachable 2026-07-18). The crossover N*=ln2/(−ln(1−p)) is the median of the geometric-in-N slow probability.
- sim-lab side: branch `claude/verdict-146-fan-out-tail`, sim dir https://github.com/menno420/sim-lab/tree/4917c5930ad4b8516c451e0d31e3173f24be5215/sims/verdict-146-fan-out-tail (verifier byte-identical copy + probe-report.md + run-stdout.txt), PR #221 (head @89356826c84c8c2fa356f6aa2307883dd3540f78) MERGED on sim-lab main HEAD @4917c5930ad4b8516c451e0d31e3173f24be5215 (merged_at 2026-07-18T13:37:45Z) via merge-on-green after its born-red card flip — https://github.com/menno420/sim-lab/pull/221
- DIGEST POSTURE: **WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY** (the P105/P109/P110/P114/P118/P122/P123/P126/P127/P128/P129/P130/P131/P132/P134 family; UNLIKE the V126/V138 SELF-FIELD/PRETTY-ON-DISK posture). `run()`/`main()` build a results dict that carries NO `results_sha256` field; `main()` computes `canonical=json.dumps(results, sort_keys=True, separators=(",",":"))`, `digest=sha256(canonical.encode()).hexdigest()`, then PRINTS the human-readable `json.dumps(results, indent=2, sort_keys=True)` dump + `Results-JSON sha256: <digest>` — it writes NO results file. TWIST (the P127+ family twist): the disclosed digest is the sha256 of the COMPACT-canonical serialization, NOT the pretty indent=2 dump printed on stdout (two serializations of the same dict); recompute over the compact form. The computation lives in `run()` (returns the results dict), so the in-process double-run calls `run()` twice directly.
- Offset authority for +13 is this repo's control/outbox.md per-block offset ledger (`## PROPOSAL 133 · 2026-07-18T12:25:02Z` → `depends: sim-lab consumes at +13 (PROPOSAL 133 → VERDICT 146). Offset pinned.`) + control/status.md baton — NOT docs/current-state.md (a dated SEAT-DORMANT snapshot through V059 carrying no P133/V146 row).

## Constraints honored
Stdlib-only (hashlib, json, math, random); verifier copied byte-identical on the sim-lab side (diff exit 0, blob + sha256 match); pinned SEED=20260717 unchanged; no numpy/scipy; forward-only git; verdict reproduced independently, not assumed; zero agent merge calls (born-red HOLD released by the card flip — sim-lab PR #221 landed via merge-on-green, this mirror lands via the auto-merge-enabler on green, not a manual merge); mirror appends ONLY the V146 card + outbox block + heartbeat (outbox append-only, dedupe-checked — no prior V146 block; control/inbox.md untouched, manager's writer; routines line UNCHANGED, coordinator-bound; verdict high-water reconciled by UNION — V147 kept, V146 a lower non-contiguous fill, no regress; proposal high-water P134 kept intact). Branch cut from origin/main @787483a (after the V147 mirror #570 landed); no divergence at branch-sync. Model line family-level (PL-004), task class review/verify.

## Gate plan (disclosed → reproduced), order G1→G2→G3
- G1 union-bound tail (headline): at N=100 the Monte-Carlo slow-request fraction matches the EXACT union P_slow(100)=1−0.99^100 within z<3σ (binomial se on the exact p) (disclosed z=−0.336; MC 0.6335 vs exact 0.633968).
- G2 median crossover: n_star=69 is the smallest N with P_slow(N)≥0.5, bracketed (P_slow(68)<0.5≤P_slow(69); real-valued root ln2/(−ln0.99)=68.967564), AND the MC slow fraction at N*=69 matches the exact P_slow(69) within z<3σ (disclosed z=−0.015; MC 0.500142 vs exact 0.500163, bracket True, n_star==69).
- G3 mean-slow-leaves linear: at N=100 the MC mean slow-leaf count matches the EXACT linear anchor N·p=1.0 within z<3σ (se from the Binomial variance N·p(1−p)) (disclosed z=+0.055; MC 1.000158 vs exact 1.0).
- Exact anchors: P_slow(10)=0.095618, P_slow(69)=0.500163, P_slow(100)=0.633968, P_slow(200)=0.866020; mean slow leaves N·p linear (0.1/0.69/1.0/2.0); crossover root 68.967564 → n_star=69.

## Outcome — reproduction complete, APPROVE, mirrored into idea-engine + card flipped `complete`
Sim-lab reproduced the proposal's OWN committed stdlib verifier BYTE-IDENTICALLY under SEED=20260717 (diff exit 0; blob 021a63b3, sha256 8f971ee0, 136 lines / 4237 bytes). The results-dict compact-canonical sha256 reproduced the disclosed digest `4b3de5012a6cedc99de8e446c3fdd0aa79b1988fd0594c3cbb9e33702131c42d` EXACTLY, identical across cross-invocation A + B (stdout diff exit 0) and an in-process double-run (`run()` ×2 compact-hashed in-process, both digests identical). Independently re-run in this seat's idea-engine checkout as a sanity guard: `python3 ideas/fleet/fan_out_tail_amplification.py` printed `Results-JSON sha256: 4b3de50…131c42d`. Because the verifier writes no on-disk json (WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY), the compact canonical dict IS the exact digest preimage — the TWIST is that the stdout DUMP is the pretty indent=2 form while the hashed payload is compact.
- **G1 union-bound tail: PASS** — MC slow-request fraction at N=100 **0.6335** vs exact P_slow(100)=1−0.99^100 **0.633968** within **z=−0.336** < 3σ — the whole request is slow whenever any of 100 independent leaves is slow, so a 1%-per-leaf tail is a 63.4% request tail.
- **G2 median crossover: PASS** — n_star=**69** (bracket **True**: P_slow(68)<0.5≤P_slow(69); real-valued root **68.967564**); MC slow fraction at N*=69 **0.500142** vs exact P_slow(69) **0.500163** within **z=−0.015** < 3σ — the MEDIAN request goes slow at only a ~69-way fan-out, exactly the ln2/(−ln0.99) crossover.
- **G3 mean-slow-leaves linear: PASS** — MC mean slow leaves at N=100 **1.000158** vs exact N·p **1.0** within **z=+0.055** < 3σ — the AVERAGE slow-leaf count is merely linear (~1 leaf), decisively separated from the super-linear 63.4% request tail: the tail is amplified by the union over leaves, not by the mean. Anchors: P_slow(100)=**0.633968**, n_star=**69** (root **68.967564**), N·p=**1.0**. all_pass=**true**, exit **0**, first-failing gate **none**. The ruling is APPROVE; this slice records the V146 outbox block + heartbeat and flips this card to `complete`, releasing the born-red HOLD so the auto-merge-enabler lands the mirror PR on green.

## ⟲ Previous-session review
The immediate predecessor landed idea-engine verdict mirror P134 → V147 (round-31 VENTURE-slot, the cohort-blended LTV understatement — collapsing a heterogeneous customer book to a single blended churn c̄ and reading LTV=m/c̄ UNDERSTATES the true portfolio E[m/c], because 1/c is convex so Jensen forces E[m/c]≥m/c̄, the gap driven by churn DISPERSION not level ~m·Var(c)/c̄³, ~30% on churn U[0.05,0.35]: true ln(7)/0.30=6.486367 vs blended 5.0) reproduced APPROVE on the sim-lab side (sim-lab #220 @c6bf5e5, digest `f45e6609…f489b` EXACT, all three gates PASS in order — G1 understatement-bias z=171.221384 / G2 matches-closed-form |z|=0.363449 vs 6.486367 / G3 dispersion-driven z=170.572090 with the narrow-band gap collapsing to 0.004628), mirrored idea-engine #570 @787483a. One carry-forward it did well: G3 was a dispersion ISOLATION control — holding mean churn fixed at 0.20 while shrinking the spread from [0.05,0.35] to [0.19,0.21] collapsed the understatement from 1.49 to ~0.005, proving the gap is convexity-of-dispersion not a level artifact — the mechanism was the deliverable, not a magnitude assertion. This slice keeps that discipline in the reliability register: G2 is not just a magnitude check but a STRUCTURAL one (the median crossover n_star=69 is bracketed by P_slow(68)<0.5≤P_slow(69) AND matches the analytic root 68.967564, so the "median goes slow at ~69 leaves" claim is a located structural fact, not a fit), and G3 is a SEPARATION gate (the mean slow-leaf count is pinned LINEAR at N·p=1.0 precisely to separate it from the super-linear 63.4% union tail, so "the tail is not the mean" is proven on the pinned world). Object shift: V147 was a convex-mean / Jensen-gap venture object (1/c convexity making a blended-churn LTV understate E[m/c]); this V146 slice is a union-bound TAIL-PROBABILITY / series-reliability fleet object (the slow probability of an all-of-N scatter-gather, 1−(1−p)^N, and its median crossover N*) — different domain, different object, different math, and a z-profile like V145's near-anchor gates (all three |z| ≤ 0.34, the MC lands ON its exact closed forms) rather than V147's astronomically-tight headline gates, because here every gate anchors the MC estimate to its own exact closed form rather than rejecting a folk-vs-reality gap. DIGEST-POSTURE note (carry-forward): classify `main()` before recomputing any digest — this P133 verifier is WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY (no `results_sha256` key, no results file; the compact canonical dict IS the digest preimage), the P105/P109/P110/P114/P118/P122/P123/P126/P127/P128/P129/P130/P131/P132/P134 family and UNLIKE the V126/V138 SELF-FIELD/PRETTY-ON-DISK posture — with the SAME P127+ TWIST that the stdout DUMP is the pretty indent=2 form while the hashed payload is compact (recompute over compact); the computation lives in `run()`, so the in-process double-run calls `run()` twice directly. NON-CONTIGUITY note for the successor: V146 (P133, round-31 FLEET slot) mirrors as a LOWER, non-contiguous fill — V147 (P134, VENTURE slot) already landed at #570, so the verdict high-water STAYS V147 (union-max, no regress, no advance); do not read "V146 mirrored" as advancing the high-water. Also still open below the high-water: V137 for P124, V132 for P119, and the round-26 FLEET-slot P113 → V126 (idea-engine mirror #527) — do not read "high-water V147" as "every lower verdict fully closed on both sides." Round-31 verdict side now has V146 (fleet) + V147 (venture) landed; next is round-31 GAME-slot PROPOSAL 135 → VERDICT 148 (+13, rotation venture→game). The coordinator-bound routines (failsafe cron) stay armed and are NOT re-armed by this seat.

## 💡 Session idea
This slice pins the ALL-of-N (wait-for-the-last) fan-out with a SINGLE homogeneous per-leaf slow probability p and INDEPENDENT leaves — the pure union bound P_slow(N)=1−(1−p)^N. A genuinely different NEXT fleet slice, distinct from this all-of-N head and from the hedged-request tail cure (P121/V134, which prices the p95-deferred second copy), pins the **quorum / k-of-N tail collapse**: a scatter-gather that returns as soon as the FASTEST N−k+1 of N leaves respond (tolerating up to k−1 stragglers — partial results, best-of-R replica reads, a batch needing only a quorum) is slow only when MORE than k−1 leaves are slow, so the request-slow probability drops from the union 1−(1−p)^N to the binomial upper tail P(Binom(N,p) ≥ k), and the median crossover pushes far past N*=0.693/p. Pre-registrable: (G-a) at k=1 (tolerate zero stragglers = wait-for-all) the quorum tail reproduces this slice's exact union 1−(1−p)^N (the k=1 boundary IS the all-of-N case); (G-b) for a fixed straggler budget k the MC request-slow fraction tracks the exact binomial upper tail P(Binom(N,p) ≥ k) within 3σ, decisively below the union bound — one tolerated straggler (k=2) at N=100, p=0.01 drops the slow rate from 63.4% to ~26.4% (1 − 0.99^100 − 100·0.01·0.99^99); (G-c) a DOSE-RESPONSE control shows the slow rate is strictly decreasing in the straggler budget k and the crossover N*_k grows super-linearly in k, with a ≥3σ separation between k=1 and k=2 at fixed N — isolating quorum depth, not fan-out width, as the tail lever. The operator lesson sharpens from "waiting for all N leaves inherits a 1−(1−p)^N tail that bites at ~69 leaves" to "tolerate a small straggler budget (return on a quorum, not the last leaf) — one tolerated straggler converts the union tail into a binomial upper tail and buys back most of the amplification for a bounded correctness cost." A seed-pinnable reliability mechanism deliberately distinct from this all-of-N union head and the hedged-request cure.
