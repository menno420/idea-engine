# Session — PROPOSAL 030: adaptive versioning on early signal (venture rotation, round 4 — the V020 named follow-up)

> **Status:** `in-progress`
> **Model/time:** fable · 2026-07-13T07:22:26Z (Ideas Lab worker slice — draft the
> VENTURE rotation round-4 head under standing owner ORDER 003: rounds 1–3 were
> P018 (books → V020 null), P022 (trading → V024 null), P026 (trading → V028
> approve); round 4 returns to the books half deliberately — V020's own "What it
> did NOT settle" names adaptive-K-on-early-signal as its follow-up, and the
> mode-fork NULL it left behind is exactly what a signal-directed policy could
> dissolve. Card born in-progress as the designed gate hold, flips complete in
> this PR's final commit once the 💡 slot resolves)

**📊 Model:** fable · content + outbox proposal only (idea file, card, index row,
claim file, outbox append; no control/status.md or control/inbox.md writes; no
checker or script changes)

## Scope

Draft a genuinely new sim-shaped idea under standing owner ORDER 003 (continuous
pipeline). Candidates weighed on the actual verdict reports, not summaries:
(a) the V020 follow-up — the report's own "Adaptive K … named follow-up, not
scope creep" line plus the mode-fork NULL ("the publication mode itself is the
binding fork"); (b) a venture pricing/bundling head — passed: the lane itself
already routed webhook price elasticity to sim-lab (the P018 precedent clause),
and pricing sims collide with Q-0259 r.4 forecast discipline with no committed
anchor to chain; (c) operationalizing V028's APPROVE into an N=96 slot-allocation
row — passed: V028's own marginal-config price list IS that guidance (direction
decoys a pure tax, short horizons nearly bar-free) and its APPROVE consequence
already executes ORDER 033 as written, so a slot sim would largely re-read
committed tables. (a) was strongest: a named, unbuilt follow-up whose parent NULL
left the lane holding a mode-conditional rule it cannot apply without deciding
the mode first. Written up as
`ideas/venture-lab/adaptive-versioning-early-signal-2026-07-13.md` (probed
single-pass this slice, sim-ready) and appended to `control/outbox.md` as
PROPOSAL 030. Chained anchors: V020's committed ruling row quoted verbatim from
`sims/verdict-020-book-versioning/results.json` @ sim-lab `76dc487`, tolerances
pre-checked ≥ 2.5σ at design time (the P029 rule; the V028 gate-calibration
lesson). Seed-registry note: seeds 20260760–63, allocated strictly above the
P029 high-water 20260759. Dedup swept tree-wide (`rg -i 'adaptive|two-stage|
early[ -]signal|sequential'` over ideas/ + .sessions/ + control/ + docs/, both
trees) — hits are the parent's and P026's own "named follow-up"/over-scope lines
and unrelated maker prose; no proposal 001–029 or sim-lab verdict V001–V030
runs an adaptive/sequential policy.

## 💡 Session idea

*(slot open — fills in this PR's final commit)*

## ⟲ Previous-session review

Newest predecessor card (`.sessions/2026-07-13-proposal-029.md`, worker slice
~06:48Z): closed clean at `0e168bf` (PR #297) — the fleet-backlogs round-4 head
(lease-renewal claim expiry), notable for applying the successor-move to its own
slot's parent for the first time and for running V027's named zero-tooling
git-log probe AT DRAFTING (the first fleet datapoint of its kind, multisets
quoted into the fixture). Two of its habits are consumed here: the explicit
seed-registry note (its high-water 20260759 is what this slice's 20260760–63
allocate above), and the explicit-alternatives dedup (its card named the
round-4 alternatives it passed on with reasons; this card does the same for
candidates (b) and (c) above, in Scope). Its 💡 — the chained-anchor `source:`
pin + preflight advisory for hand-quoted parent constants — is directly relevant
to this slice's own anchor block and remains unbuilt; this card's anchor
paragraph cites the parent results.json path@sha explicitly, which is exactly
the prose form that 💡 would mechanize.
