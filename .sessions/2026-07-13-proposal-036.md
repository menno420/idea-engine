# Session — PROPOSAL 036: the 37% rule off its home objective (unrelated-domain rotation, round 5)

> **Status:** `complete`
> **Model/time:** fable · 2026-07-13T14:37:24Z (Ideas Lab worker slice — draft the
> COMPLETELY-UNRELATED-domain rotation round-5 head under standing owner ORDER 003.
> Card born in-progress as the designed gate hold; flips complete in this PR's final
> commit once the 💡 slot resolves)

**📊 Model:** fable · content + outbox proposal only (idea file, card, index row,
outbox append, claim file; no control/status.md or control/inbox.md writes; no checker
or script changes)

## Scope

Draft a genuinely new sim-shaped idea for the COMPLETELY-UNRELATED-domain rotation
slot, round 5, under standing owner ORDER 003 (continuous pipeline — verified open and
standing at HEAD `a2850e2` before any work: priority P1 · standing, "continue coming
up with new ideas, that is your main purpose"). Rounds 1–4 of the slot: P017 (social
choice / IRV monotonicity → V019 null), P024 (congestion routing / Braess → V026
null), P028 (tournament seeding / Bradley–Terry), P032 (pattern races / Penney's
game). Round 5's venture and game-mechanics slots are already served (P034, P035) and
were verified at HEAD along with an empty `control/claims/` and zero open PRs before
claiming. Candidates weighed, winner first:

- (a) **Secretary problem / the popularized 37% look-then-leap rule — WINNER.** A
  FIFTH fleet-external domain (sequential search / optimal stopping, the
  decision-science self-help canon) with the exact P017/P024/P028/P032 shape: the
  classical theorem is settled (known N, rank objective, ~N/e cutoff optimal), while
  the folk rule's CARDINAL price — ΔV(N), the expected-value regret of the 37% cutoff
  against the best cutoff in its own family when the payoff is the hire's quality —
  and its untold downside profile B(N) (bottom-half-hire rate incl. the forced-last
  leg) are unmeasured numbers that are all exact rationals. Fully exact, zero
  RNG/seeds/floats (the P028/P032 discipline): analytic closed forms vs an independent
  combinatorial rank-census arm, rational-equality gated, plus an algorithm-free full
  permutation census at N ∈ {5, 6, 7} and classic anchors (1/2 · 11/24 · 13/30).
  Written up as `ideas/fleet/secretary-rule-cardinal-regret-2026-07-13.md` (probed
  single-pass this slice, sim-ready) and appended to `control/outbox.md` as
  PROPOSAL 036. Seed registry: ZERO seeds drawn — the P035 high-water 20261284 left
  unchanged.
- (b) Hot-hand streak bias (Miller–Sanjurjo) — genuinely open magnitude, but it lives
  on coin-sequence statistics, P032's occupied neighborhood; dropped on the rotation's
  own distinctness bar.
- (c) Friendship paradox / acquaintance immunization — real folk belief, but a
  graph-degree domain adjacent to P024's network neighborhood; dropped.
- (d) Benford first-digit audit rule — crisp folk belief, but the honest version needs
  a float-analytic arm with truncation tolerances (weaker than the slot's fully-exact
  precedent) and has no committed anchor; dropped, named in the idea file.
- (e) Apportionment paradoxes — P028's own recorded pass (voting-adjacent to P017);
  stays passed.

Drafting discipline applied (the P034/P035 lesson as method): the hand closed forms
were verified against a full N ≤ 7 permutation census IN THE DRAFTING SESSION (exact
equality at every (N, r) cell for both objectives and both end conventions) before any
band was registered, and band liveness is disclosed in the idea file — ΔV clears the
APPROVE line by closed form at all decision N, so the genuinely open number is the
B(N) conjunct (drafting MC ≈ 0.19 vs the 3/20 band), with REJECT live only through a
derivation-scale surprise. Dedup swept at `a2850e2`: the only "optimal-stopping" hits
in the tree are P027's casino farmer-policy descriptor (mechanical use); zero
secretary/look-then-leap/marriage-problem content anywhere.

## 💡 Session idea

**Give the unrelated-domain rotation slot a committed one-line-per-round DOMAIN
LEDGER.** Each round's drafter today re-derives "which domains are taken" by re-reading
four (now five) full proposal blocks — the densest entries in the outbox — and the
distinctness argument lives only in prose parentheticals ("a FIFTH fleet-external
domain after …") that grow linearly and are easy to mis-count (this slice re-read
P017/P024/P028/P032 in full just to reconstruct the occupied list: voting, routing,
tournaments, pattern races). The slice: one bullet per round in a small
`ideas/fleet/README.md` § "Unrelated-domain rotation ledger" (round · domain tag ·
proposal number · verdict + ruling once known), appended by each round's drafter in
the same PR as the proposal — the occupied-domain lookup becomes one section read
instead of four block reads, the next drafter's dedup argument cites the ledger line,
and the checker-free format (plain bullets in an existing README) costs zero new
tooling. Dedup (this slice): `rg -ni "rotation ledger|domain ledger"` over the tree
hits nothing; distinct from the P035 💡 (seed registry — a sim-lab numeric high-water
ledger) and the P033 💡 (outbox digest index — read cost of the WHOLE outbox, not the
slot's domain census); nearest existing art is the fleet README's own index rows,
which this would summarize per-slot rather than replace.

## ⟲ Previous-session review

Newest predecessor card (`.sessions/2026-07-13-proposal-035.md`, worker slice
~12:07Z): closed clean — the game-mechanics round-5 head (mining booster bypass
throttle seal, PROPOSAL 035), with a genuinely load-bearing 💡 (promote the fleet
seed high-water from scattered prose notes to one committed sim-lab registry line per
seed-drawing verdict — born from that slice paying a full-ledger scan to allocate
seeds 20261281–84 safely). That lesson is CONSUMED here at zero cost: this head draws
ZERO seeds by design (fully exact, the P028/P032 arm of the rotation), so the card
states the high-water untouched rather than re-deriving a fresh allocation — and this
card's own 💡 is the same promote-a-habit-to-form move applied to the rotation slot's
domain census instead of the seed ledger. Also repeated from that card's practice:
band-liveness arithmetic ran BEFORE registration (here it upgraded honesty rather than
reshaping the head: the closed-form side of APPROVE is disclosed as predicted, and the
open conjunct is named explicitly instead of implying false suspense).
