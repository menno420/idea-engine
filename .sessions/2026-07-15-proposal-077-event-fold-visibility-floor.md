# Session — PROPOSAL 077: the festival the engine cannot show — superbot-idle's timed-events design commits a piecewise-exact event fold AND a "visibly richer in the exact T6 proportion" promise that exact integer arithmetic makes jointly unsatisfiable: the canonical tier-1 start cell pays ZERO extra for every event multiplier below ×2 (99/99 dead E values, first paying E exactly 200), a +10% window quantizes to +0% or +100% (jackpot 20/11), and each of the three repairs breaks a different committed sentence (fleet-backlogs slot, round 16 OPENER, superbot-idle)

> **Status:** `complete`
> **Model/time:** fable · 2026-07-15T23:05:44Z (Ideas Lab worker slice — draft the
> round-16 FLEET-BACKLOGS rotation slot opener under standing owner ORDER
> 003/004; round 15 fully served: fleet P073 (#437), venture P074 (#438), game
> P075 (#439), unrelated P076 (#440, merged 2026-07-15T11:37:34Z by
> github-actions[bot]) — so round 16 REOPENS at fleet backlogs per ORDER 004
> rule 3. Slot spacing history confirmed: …P065, P069, P073 → P077 (spacing 4).)

- **📊 Model:** fable-class · high · idea/planning

*(card born in-progress at 2026-07-15T23:05:44Z as the designed session-gate
hold; flipped complete in this PR's final commit. PR #442.)*

## Scope

Draft a genuinely new sim-shaped idea for the FLEET-BACKLOGS rotation slot,
round 16 opener, under standing owner ORDER 003 (continuous pipeline) and
ORDER 004 rule 3, with the EAP extension to 2026-07-21 (ORDER 015) as the
standing frame — no ORDER newer than 015 at sync (2026-07-15T22:51:28Z, HEAD
`97b8865`).

Harvest source: **SUPERBOT-IDLE — a genuinely FRESH fleet-slot source.** Tap
history disclosed per the P053–P073 precedent: the fleet slot's prior draws
are websites (P019, P053), superbot (P021/P033/P061), substrate-kit
(P025/P029), fleet-manager (P037, P065), curious-research (P041, P049),
mineverse (P045, P073), trading (P057), and the seat's own bus (P069) —
superbot-idle has never been drawn by this slot. Two prior sessions
deliberately DID NOT draft this exact head, and both drop reasons are now
consumed: P059's runner-up note dropped "timed-events scoping" for "values
unregistered pending SIM-002; no committed constants to pin" — this head
prices the committed fold SHAPE, the committed graduation identity, and the
committed 18-pack base-rate lattice, never a bonus value (the theorems
quantify over the whole sane multiplier band, which is exactly why NOW, before
step 2 of the doc's build plan re-registers shapes, is the cheapest moment);
the P061 drafter's runner-up note dropped idle heads for ORDER 005/006 intake
congestion — those SIM-REQUEST batches are long served. Baton note: the P076
card batons round 16's GAME slot (P079) toward superbot-idle by
least-recently-drawn; this fleet tap takes a DIFFERENT document class (an
unbuilt engine-invariant scoping doc, `docs/design/timed-events-scoping.md`,
not a game-balance surface) and leaves the P079 baton intact — disclosed in
the idea file's Dedup with zero shared fixtures.

Head: **the exactness-vs-visibility collision.** The scoping doc (status
`plan`, nothing built — the P073 "cheapest moment" class) commits BOTH (i) the
piecewise-exact seventh-factor fold `rate = base·count·up·pr·mi·theme·event //
10_000_000_000` with the graduation identity `(x·100) // 10^10 == x // 10^8`,
and (ii) the promise that the world runs "visibly richer during the window,
for idlers and actives alike, in the exact T6 proportion". Exact integer
arithmetic makes (i) and (ii) jointly unsatisfiable on the committed low-rate
lattice (all 18 packs ship tier-1 `base_rate: 1`; 17 ship tier-2 `5`): the
canonical start cell (rate exactly 1/s, pre-floor product exactly 10^8) pays
ZERO extra for every integer event pct 101–199 (99/99 dead, first paying pct
exactly 200 — a registered margin-0 contact); the full 214,500-cell committed
lattice has 4,151 alive-but-dead cells at ×1.10 (1.94%), 675 at ×1.25, 56 at
×1.50, 14 at ×1.75, 0 at ×2.00 exactly; a +10% window is delivered as +100%
on the (1,1,0,2,8,110) cell (max realized/nominal exactly 20/11) and as +0%
on the start cell — never as +10%. The three repairs each break a different
committed sentence: V038's min-visible-delta floor kills deadness but
overshoots nominal by the same 20/11 on every dead cell; a milli-rate
granularity migration zeroes the dead census (certificate: min milli-delta 100
≥ 99 at ×1.10) but kills the doc's "zero new save state" § 3 promise; a
registered rate-floor precondition R\* = 20 caps the deviation at exactly 1/22
(4.545%) but concedes the start state — the exact audience a re-engagement
event targets. Every numeral RAN this session: scratchpad `draft_p077.py`,
26/26 checks PASS, exit 0, ~11 s (V080 live-verify + V084 NO-DERIVED-LITERALS).

Deliverables this slice: this card (born-red first commit), the claim file,
`ideas/superbot-idle/event-fold-visibility-floor-2026-07-15.md` + the
`ideas/superbot-idle/README.md` index row, the `control/outbox.md` PROPOSAL
077 append (append-only, real `date -u`, status sim-ready), and ONE terminal
claim prune (the P076 drafter — PR #440 verified merged at live GitHub
2026-07-15T11:37:34Z, merged_by github-actions[bot], this session before
deletion). Seeds 20261690–693 — allocated from 20261690 per the coordinator
relay (20261680–683 are P076/V089's registered set; the gap 20261684–689 is
the disclosed in-flight buffer). Arm R reporting-only seeded 20261690/691/692,
aux 20261693 never read; the Arm-R draw-order grammar is registered as a
fixture line (the V089 lesson).

## Constraints honored

- control/inbox.md untouched (manager-written); control/status.md untouched
  (coordinator-only heartbeat). Newest inbox ORDER at HEAD is ORDER 015
  (2026-07-15T03:37:08Z, EAP extended to 2026-07-21) — no ORDER newer than
  015 (checked at sync 2026-07-15T22:51:28Z); the continuous-pipeline duty
  (ORDER 003/004) is the standing authority for this slice, re-confirmed live
  by the owner in the coordinator chat this session (auto-merge + loop resume
  confirmation, 2026-07-15).
- Numbering verified at HEAD `97b8865`: newest PROPOSAL = 076 (live outbox
  tail + both dated archives counted 76/57/8 blocks); `PROPOSAL
  077`/`proposal-077` collision-grepped clean; zero open PRs
  (list_pull_requests state=open → []); the only claim on file was P076's
  (pruned this PR after live merge verification).
- Grounding FIRSTHAND: superbot-idle read on a fresh read-only clone at HEAD
  `884aeae9687742a389a2e2086a4cc930e5a4f3ee` via the local git proxy (fetched
  2026-07-15T22:57Z) — the scoping doc, the engine fold, the 18-pack theme
  catalog, and the V038 kinship all read from that tree. pokemon-mod-lab (the
  only fully-untapped fleet source) probed once and WALLED: private repo —
  public HTTPS clone prompts for credentials, MCP GitHub scoped to
  idea-engine + sim-lab only ("not configured for this session", verbatim) —
  captured per the CAPABILITIES discovery rule.
- sim-lab dedup-swept READ-ONLY on a fresh shallow clone @ `d212882` (fetched
  2026-07-15T23:01:06Z, newest VERDICT 089): zero hits for timed events /
  event_pct / festival / piecewise folds. Nearest kins disclosed and argued in
  the idea file: V038's ASK-1 upgrade-feltness staircase (its "felt = integer
  rate delta" definition is imported as a fixture definition; its min-delta
  fallback is priced HERE as repair arm (a)), V084/P071's trophy-record
  quantization ceiling (same integer-floor family, different repo, different
  object — a reward-record ceiling vs a design-doc joint-unsatisfiability),
  P059's dropped runner-up (drop reason consumed above).
- Seed sweep boundary-aware at HEAD `97b8865`: genuine in-tree high-water
  20261683 (P076's registered set 20261680–683); the sim-lab clone's genuine
  high-water 20261683 (V089's set) with 20261664 / 20261833 / 202670087 /
  2026964142 re-confirmed DATA-not-seed by the recorded discrimination rule.
  Allocation starts at 20261690 per the coordinator relay, strictly above the
  registered set and the disclosed gap 20261684–689.
- Drafting-time verification discipline (V080 live-verify + V084
  NO-DERIVED-LITERALS): every numeral registered in the idea file was PRODUCED
  by the drafting script this session (`draft_p077.py`, 26/26 PASS, exit 0,
  stdlib-only, ~11 s): the canonical-cell dead band (99/99, first paying pct
  200), the full 214,500-cell lattice census (alive 214,496; dead 4,151 / 675
  / 56 / 14 / 0 across the ×1.10–×2.00 grid; zero dead cells above rate 20),
  the jackpot ratio 20/11 with its witness cell, the staircase envelope with
  both edges attained, the three repair arms (overshoot 20/11; milli-census 0
  with certificate 100 ≥ 99; deviation bound exactly 1/22), the partition-
  equivalence pencil calendars, four typed must-equal contacts, and the Arm-R
  previews (seeds 20261690/691/692 → (49, 40533190487) / (41, 39898597051) /
  (43, 39906644697); aux 20261693 never read). Zero hand-derived or scaled
  numerals anywhere in the registered set.

## 💡 Session idea

**When a design doc commits an invariant AND a promise in the same breath,
price their CONJUNCTION before either ships — the cheapest counterexample to
a system is often two of its own sentences multiplied together.** The
timed-events doc is unusually careful: its exactness argument is correct
(T5 confirmed it), its candidate analysis is honest, its § 3 determinism
constraints are right. The defect is not in any sentence — it is in the AND.
The piecewise-exact fold forces the delivered bonus to be an integer
staircase step; the proportionality promise requires that step to scale;
on the committed rate-1 start state the step is zero for the entire sane
band. Neither sentence can see this alone: the collision only exists at the
conjunction, and it costs one multiplication to find (smallest pre-floor
product × the percent's fraction vs the fold denominator — 10^8 × 0.5 <
10^10). The drafting surprise worth keeping: the OVER-pay side fell out of
the same theorem uninvited — the staircase that pays a +10% promise as +0%
on one cell pays it as +100% one milestone away (exactly 20/11), so the
repair debate is not "make it pay more" but "choose which committed sentence
to amend", and pricing all three repair arms inside the same fixture turned
a bug report into a decision table. Transferable audit, one sentence: for
every percent-shaped promise that passes through a single integer floor,
compute floor-step ÷ promised-delta at the smallest committed operating
point — if it exceeds 1, the promise is a staircase and the doc should say
so before a player reads it. Pattern name for the battery: CONJUNCTION
PRICING (multiply the invariant against the promise at the worst committed
cell).

## ⟲ Previous-session review

Previous session (the P076 drafter, PR #440 @ `828b18e`, merged
2026-07-15T11:37:34Z by github-actions[bot]): reviewed with a distinctive
wrinkle — that drafter was KILLED by a container restart AFTER its flip
commit pushed, and its PR still landed clean, which is itself evidence the
born-red ceremony works: every landing-critical action (card flip, push,
PR-ready) had already executed in-order, the enabler workflow armed
auto-merge server-side with nobody home, and nothing about the merge needed
the drafter alive. Verified live this session: PR #440 merged at
2026-07-15T11:37:34Z (mcp pull_request_read, merged_by github-actions[bot]),
its seed ledger holds (20261680–683 in-tree exactly where its claim said;
nothing ≥ 20261690 anywhere before this slice), its numbering baton held
(newest PROPOSAL = 076, zero collisions at 077), and its round-15-close
baton ("round 16 opens at the fleet-backlogs slot with P077") was consumed
by this slice as written. No correctness fault found in P076's landing; the
one operational note it could not write for itself: its terminal
prune (this session's) had to be someone else's first act, and the card's
"next free block 20261690 if the in-flight-buffer convention holds" line
made that mechanical — a good argument for always writing the
next-session-facing ledger lines BEFORE the flip, since the flip may be the
last thing a session ever does. Ceremony carried verbatim from P073–P076
(born-red card, three-field 📊 payload, terminal prune only after live merge
verification, seed ledger with the disclosed gap); extensions this slice:
the WALLED-source disclosure (pokemon-mod-lab probed once, exact errors
captured — the first fleet-slot draw where the untapped-source preference
hit a genuine wall and the ledger says so), and the consumed-drop
disclosure (two prior sessions' reasons for NOT drafting this head quoted
and answered in the Dedup — a stronger freshness argument than absence of
overlap). Continuity notes for the coordinator: round 16 is now OPEN with
the fleet slot served (P077, this PR) — venture (P078) is next per ORDER
004 rule 3, then game (P079, batoned to superbot-idle live mechanics —
UNCHANGED by this slice: this tap was the unbuilt timed-events scoping doc,
not a game-balance surface), then unrelated (P080, must argue non-adjacency
against at least matching / group testing / error detection); the outbox is
199,843 B post-append — the NEXT append almost certainly triggers the 200KB
roll, budget for it; seed high-water after this PR: 20261693 (P077's
registered set 20261690–693), next free block 20261700 if the
in-flight-buffer convention holds (gap 20261694–699 to disclose).
