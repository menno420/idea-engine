# VERDICT 251 — independent risk-pooling / diversification variance law: for a mutual pool of n members each carrying an INDEPENDENT Bernoulli(p) unit loss, the pooled per-member cost L̄=S/n (S~Binomial(n,p)) obeys the exact insurance law E[L̄]=p (fair premium, scale-free in n) and Var[L̄]=p(1−p)/n — so at p=1/10, n=100 the single-member cost sd 0.3 falls to exactly 0.03 (a √n=10× cut, pooled Var=9/10000) with the mean premium unchanged, while the naive comonotonic ρ=1 belief Var[L̄]=p(1−p)=9/100 (no diversification) is falsified — reproduce PROPOSAL 238

> **Status:** complete

> **📊 Model:** Claude Opus · high · review/verify

started: 2026-07-21T01:43:13Z

💓 Heartbeat: VENTURE slice P238 → V251 (+13, lane venture); reproduction verifier already landed byte-identical in sim-lab via the mirror PR #336 (merge b17ef84) — this slice mirrors the verdict block into control/outbox.md (V251 ruled APPROVE, verdict high-water ADVANCES V250→V251, union-max no regress). Digest target results_sha256 `1e0520c849597c1b6a4135598ee76617488c49659c1820e1841b255b9115cbbb` (printed AND literal 64-char string compared, no truncation); reproduced byte-identical across an in-process double-run guard (in_process_double_run_identical: true) AND a separate re-invocation process. SEED=20260717. Four pre-registered gates each read in its own direction. Grounding sha1-pinned to Wikipedia "Variance" oldid 1364418626 (`fc0e7d78428fe749117408dbeceb3d004b978c6f`) + "Binomial distribution" oldid 1359203125 (`c6efae850469278e57d9b578c96871400e61c6b2`), split HONEST. VERDICT 251 = APPROVE. Born-red HOLD armed on this first card commit (in-progress); this card flips to `complete` in a later commit to release merge-on-green.

⏳ Flip note (born-red): this card ships `> **Status:** in-progress` on its FIRST commit so the substrate born-red gate holds the idea-engine PR RED until the slice is genuinely done; it flips to `complete` as the deliberate LAST commit — only after the coordinator confirms the outbox block + the status heartbeat and `python3 bootstrap.py check --strict` exits 0. That flip clears the HOLD and releases merge-on-green. NO merge API calls from this session; CI substrate-gate + the landing automation squash-merge the green PR.

## What this verdict does

Reproduces PROPOSAL 238 (P238 → V251, +13 offset, lane VENTURE): for a mutual pool of **n members**, each carrying an **independent Bernoulli(p)** unit loss, the pooled per-member cost **L̄=S/n** (with **S~Binomial(n,p)**) obeys the exact insurance / diversification law **E[L̄]=p** (a fair premium, scale-free in n) and **Var[L̄]=p(1−p)/n**. The headline instance **p=1/10, n=100** gives premium **0.1** and pooled **Var=9/10000** (sd **0.03**) — a **√n=10×** cut of the single-member cost sd **√(9/100)=0.3** with the mean premium unchanged — while the naive **comonotonic ρ=1** belief **Var[L̄]=p(1−p)=9/100** for every n (no diversification benefit) is falsified. The verifier `verify_238_risk_pooling.py` (byte-identical copy already landed in `sims/verdict-251-risk-pooling/` via PR #336, merge b17ef84) reproduces `results_sha256`, confirms determinism, and evaluates four pre-registered gates each in its own direction; the probe checks the byte-pinned grounding against the two pinned Wikipedia revisions.

## Method

- Independent byte-identical re-copy of the verifier (`diff` source↔copy exit 0), stdlib-only (`json`, `hashlib`, `math`, `random`, `fractions.Fraction`).
- Digest posture: WHOLE-DICT — the compact-canonical results dict's own sha256 IS the digest; `main()` builds the results twice in-process and asserts canonical-JSON byte-equality before printing the digest and exiting 0. Target `results_sha256` full-64 `1e0520c849597c1b6a4135598ee76617488c49659c1820e1841b255b9115cbbb` (literal 64-char compare, no truncation); reproduced byte-identical across an in-process double-run guard (`in_process_double_run_identical: true`, decision PASS) AND a separate re-invocation process; SEED=20260717.
- Gates (each read in ITS OWN direction, against the proposal's OWN criteria) — see `sims/verdict-251-risk-pooling/repro-report.md` for observed values:
  - **G1 EXACT** — via `fractions.Fraction`: Var[L̄]=9/10000 computed two independent ways, Route A the closed-form Var(S)/n² and Route B the full-binomial-pmf reconstruction E[(S/n)²]−E[S/n]², EQUAL (difference exactly 0). PASS (firsthand teeth — the pooled variance is an exact rational, matched by two disjoint routes).
  - **G2 MC AGREEMENT** — pooled Bernoulli(1/10) draws over the n=100 pool; the empirical mean cost agrees with p=0.1 and the empirical Var[L̄] agrees with 9/10000, max|z|=1.4513, |z|<3 [Z_ACCEPT=3.0]. PASS.
  - **G3 INVARIANCE** — (i) Var[L̄]·n==p(1−p) EXACTLY across n∈{1,10,100,1000}; (ii) the coefficient of variation is invariant to claim severity (scale). PASS.
  - **G4 FALSIFIABILITY** — on the SAME MC sample the naive comonotonic variance Var=9/100 (the ρ=1 no-diversification limit) is REJECTED at |z|=21771.77, |z|≥8 [Z_REJECT=8.0], opposite direction to G2. PASS.
  - all_gates_pass=true, first_failing_gate=null.
- Independent cross-check (own throwaway derivation): for independent X₁…Xₙ ~ Bernoulli(p), Var(S)=Var(ΣXᵢ)=ΣVar(Xᵢ)=n·p(1−p) (Bienaymé), and scaling with a=1/n gives Var(L̄)=Var(S/n)=Var(S)/n²=p(1−p)/n while E[L̄]=E[S]/n=p; at p=1/10, n=100 this is 9/10000 exactly, sd 0.03 = a √n=10× cut of 0.3, the comonotonic ρ=1 limit keeping the cross-covariance and giving 9/100 for every n — reproduces non-circularly.
- Grounding byte-pinned to the `action=raw` wikitext of each revision; local raw bytes sha1 and MediaWiki API revision sha1 match on both pages.

## Grounding — honest scope

External, sha1-pinned: Wikipedia "Variance" oldid 1364418626, action=raw wikitext (64971 bytes) sha1 `fc0e7d78428fe749117408dbeceb3d004b978c6f` (raw local sha1sum AND MediaWiki API revision sha1 match; rev 2026-07-16T11:46:43Z), and Wikipedia "Binomial distribution" oldid 1359203125, action=raw wikitext (55316 bytes) sha1 `c6efae850469278e57d9b578c96871400e61c6b2` (raw local sha1sum AND API revision sha1 match; rev 2026-06-13T20:20:06Z). On-page (CONFIRMED by grep): the scaling rule Var(aX)=a²Var(X) and Bienaymé's sum-of-independent-variances identity Var(ΣXᵢ)=ΣVar(Xᵢ) (Variance page), and the binomial variance npq=np(1−p) (Binomial distribution page). HONEST scope: the pooled insurance law Var[L̄]=p(1−p)/n, the 0.3→0.03 √n=10× reduction, and the comonotonic ρ=1 foil numbers are correctly claimed as the doc's OWN firsthand contribution (grep count 0 on both pinned raw wikitexts), alongside the four-gate reproduction and the comonotonic falsification. The σ²/n variance-of-the-mean and the np(1−p) binomial variance are honestly QUOTED (textbook), the pooled-insurance framing + exact rational instance (9/10000) + gate battery honestly DERIVED — clean seam, nothing oversold as novel.

## Merge posture

CI substrate-gate + the `enable-auto-merge` landing automation squash-merge the green PR; **NO merge API calls are made from this session**. The sim-lab verifier + run-stdout.txt + repro-report.md already landed byte-identical via the mirror PR #336 (merge b17ef84) — reproduction-only, no verdict block / no high-water advance, its repro-report.md deferring the canonical ruling to this coordinator-owned slice; left untouched, NO competing sim-lab PR opened. This idea-engine slice ships the outbox verdict block + this `.sessions/` verdict card; the status.md high-water heartbeat is handled in a later isolated commit after review.

## ⟲ Previous-session review

Previous-session review: VERDICT 250 (slotted-ALOHA finite-n throughput ceiling — for n agents contending for one slotted resource each transmitting per slot with probability p, the single-slot success throughput S(n,p)=n·p·(1−p)^(n−1) is maximised exactly at p*=1/n giving the exact rational ceiling S_max(n)=(1−1/n)^(n−1)=(n−1)^(n−1)/n^(n−1) — 1/2, 4/9, 27/64 at n=2,3,4 — strictly decreasing to 1/e≈0.367879 from above, the naive "offered load = throughput = 1" provably wrong; P237 → V250) landed APPROVE with a full-64 digest match, four gates each in its own direction, and a clean quoted-vs-derived split (the 1/e ceiling honestly quoted, the finite-n refinement honestly derived). Its carry-forward is GATE-POLARITY discipline: read each gate in ITS OWN direction — an exact Fraction identity is a self-certifying theorem, a z-test is an AGREEMENT gate when the closed form is the null, and a deliberately-wrong model that must be REJECTED is a FALSIFIABILITY gate at the opposite polarity. V251 leans on the same discipline: G1 is an exact Fraction identity (Var[L̄]=9/10000 matched by two disjoint routes — the firsthand teeth), G2 is a Monte-Carlo AGREEMENT gate (max|z|=1.4513<3), G3 blends the exact Var[L̄]·n==p(1−p) invariance across n with a severity-invariant CV, and G4 is a FALSIFIABILITY gate rejecting the naive comonotonic Var=9/100 at |z|=21771.77. It also carries the grounding-accuracy discipline: the split honestly separates the on-page scaling + Bienaymé + npq variance (QUOTED) from the off-page pooled p(1−p)/n law, the √n reduction, the comonotonic foil, the sim, SEED, gate grids, and digest (DERIVED). Standing non-contiguity persists: landing V251 does not imply every lower verdict below the high-water is closed — V137 is the known gap directly below the top run (138→251), and V225, V132, V126 remain open pulls.

## 💡 Session idea

The verifier pins the ρ=0 (fully independent) baseline of the pooling law. A cheap orthogonal follow-on (P-next) would pin the **correlated** companion rather than the independent floor: for an equicorrelated pool with pairwise correlation ρ∈[0,1], the pooled variance is Var[L̄]=p(1−p)·[ρ + (1−ρ)/n], which interpolates between THIS verdict's independent ρ=0 law p(1−p)/n and the comonotonic ρ=1 foil p(1−p), and floors at ρ·p(1−p) as n→∞ (an effective pool size N_eff→1/ρ — diversification saturates, it does not vanish). Gate recipe: G1 an EXACT identity — the equicorrelated closed form recovers p(1−p)/n at ρ=0 and p(1−p) at ρ=1 exactly (Fraction check on a small n×ρ grid); a FALSIFIABILITY leg that the "risk keeps falling as 1/n forever" belief is REJECTED for ρ>0 (the variance floors at ρ·p(1−p), not 0); and a continuity leg that ρ→0 recovers THIS verdict's 9/10000 instance. Grounding: the same "Variance" page carries the general Var(ΣXᵢ)=ΣVar(Xᵢ)+Σ_{i≠j}Cov(Xᵢ,Xⱼ) identity, so the pin stays honestly on-page for the covariance-sum decomposition and off-page for the exact equicorrelated instantiation, the N_eff→1/ρ saturation, the sim, SEED, and digest.
