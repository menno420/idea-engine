# Session — PROPOSAL 028: tournament seeding bracket optimality (unrelated-domain rotation, round 3)

> **Status:** `complete`
> **Model/time:** fable · 2026-07-13T06:22:37Z (Ideas Lab worker slice — draft the
> ORDER 004 rule-3 COMPLETELY-UNRELATED-domain round-3 head: round 1 P017 social
> choice (IRV monotonicity → V019 null), round 2 P024 congestion routing (Braess
> added-edge → V026 null); round 3 rotates into a THIRD fleet-external domain —
> sports statistics / tournament-format design: is the standard 8-team bracket
> seeding exactly optimal among all 315 assignments under Bradley–Terry? Card born
> in-progress as the designed gate hold, flips complete in this PR's final commit
> once the 💡 slot resolves)

**📊 Model:** fable · content + outbox proposal only (idea file, card, index row,
claim file, outbox append; no control/status.md or control/inbox.md writes; no
checker or script changes)

## Scope

Draft a genuinely new sim-shaped idea under standing owner ORDER 003 (continuous
pipeline) + ORDER 004 rule 3's rotation — the round-3 COMPLETELY-UNRELATED-domain
slot. Domain rotated a third time (P017 voting theory → P024 routing games → this
head tournament design) so the unrelated-domain lane demonstrably spans domains.
The head: does the universal folk default of single-elimination seeding
(1v8, 4v5 | 3v6, 2v7) actually maximize (a) P(the strongest team wins) and
(b) P(a 1-vs-2 final) among ALL 315 distinct 8-team brackets, under a committed
Bradley–Terry model on four shape-spanning strength profiles — computed EXACTLY
(fractions.Fraction, zero RNG/seeds/floats; dual independent algorithms gated on
rational equality), with pre-registered REJECT-first/APPROVE/NULL bands where the
honest null (suboptimal but sub-material everywhere) and the exact-optimality
REJECT are both live. Written up as
`ideas/fleet/tournament-seeding-bracket-optimality-2026-07-13.md` (probed
single-pass this slice, sim-ready) and appended to `control/outbox.md` as
PROPOSAL 028. Seed-registry note: this sim draws ZERO seeds (exact computation
needs none — stated explicitly in the idea file and proposal); the P027 high-water
20260755 is left unchanged for the next seeded head. Dedup swept tree-wide
(`rg -i -g '!bootstrap.py' -g '!.substrate'
'tournament|bracket|seeding|bradley|terry|knockout|single.elim'` over ideas/ +
.sessions/ + control/ + docs/) — hits are mechanical only: the RPS tournament
SERVICE-REFACTOR head (orchestration code, no seeding math), P009's tournament
SETTLEMENT-correctness corpus, 3D-printed mounting "brackets" in the maker files,
and "bracketed" as sim prose; no proposal, idea file, card, or verdict prices
bracket seeding, tournament formats, or any Bradley–Terry question. P024's own
probe report had dropped "tournaments, matchmaking" as game-adjacent for round 2 —
round 3 takes the fleet-external half of that space (format fairness under a
committed statistical model; no fleet lane runs a seeded bracket).

## 💡 Session idea

**A committed machine-readable PRNG-seed registry — because a seedless slice just
broke the prose chain.** The seed-allocation discipline (every seeded head draws
strictly above the previous head's high-water: 20260713–15 → … → P027's
20260752–55, "eighth consecutive slice") lives ONLY in card/idea prose, each slice
citing the predecessor's numbers by hand. This slice surfaced the failure mode the
chain cannot express: P028 is seedless-exact and draws NOTHING, so the next seeded
head must reach BACK PAST it to P027's card for the high-water — and a prose gap
reads identically to a forgotten allocation. The slice: one append-only
`control/seed-registry.md` (or JSON), one row per slice —
`<proposal> · <seeds or "none (exact)"> · <new high-water> · <date>` — plus a
report-only preflight advisory that greps new idea files for `random.Random(\d+)`
constants at or below the registry tail (the same warn-first posture as every
other advisory in `scripts/preflight.py`). Why the seed instance is the sharpest
of its family: the consumer is CROSS-REPO — sim-lab commits the constants into its
sims, and a silent collision would correlate two verdicts whose independence both
pre-registrations assert. **Dedup** (this slice, `rg -i 'seed.regist|high.water|
machine.readable'` over ideas/ + .sessions/ + control/ + scripts/): no registry
file or seed checker exists anywhere — the discipline is prose in P020–P027's
cards and idea files only; this is the third measured instance of the
prose-chained-watermark failure family and cites its siblings rather than
restating them — the 2026-07-12 superbot-next card's 💡 (harvest watermark
excavated from a predecessor card's lines 29–31; proposed `.harvest-pin.json`
rows, unbuilt) and P021's rotation-position ledger (treadmill measured to n=8 by
this card's own slot re-derivation, unbuilt). Distinct from both by watermark
TYPE, consumer (sim-lab reproducibility, not harvest recon or slot assignment),
and check surface (idea-file constants, not git history); distinct from PROPOSAL
028 itself, which consumes no seeds — this 💡 exists precisely because it
couldn't leave a registry trace saying so.

## ⟲ Previous-session review

Newest predecessor card (`.sessions/2026-07-13-proposal-027.md`, worker slice
~06:04Z): closed clean at `3978df1` (PR #294) — the game-mechanics round-3 head
(comp/stipend envelope), which CLOSED the V022 routed-lever triage by pricing the
last unpriced lever on V025's surviving substrate, with a genuinely new band class
(MINT + a per-design farmer/exploit family) no prior grid carried — the strongest
example yet of a rotation lane consuming its parents' committed results as chained
exact anchors rather than re-running them. Two of its habits are consumed here:
the **explicit-alternatives dedup** (P027 named the leads it passed on and why;
this card names apportionment and spaced-repetition as the dropped runners-up,
with reasons) and **seed-registry hygiene**, which this slice extends by its
EDGE CASE: P027 recorded high-water 20260755 in card prose ("eighth consecutive
slice"), but this head is seedless-exact — the first slice that consumes ZERO
seeds — and the prose-chained registry has no way to say so except more prose,
which is exactly the fragility its own chain warned about (a gap in the chain
reads identically to a forgotten allocation). The **rotation-position treadmill**
its predecessors measured extends to n=8: this card again re-derives its slot
(unrelated-domain round 3, after P017/P024 closed rounds 1–2) from predecessor
prose and the TALLY 001 rotation line, not a machine ledger — P021's 💡 stays
measured-and-unbuilt.
