# Session — probe: claude-code-projects-for-the-rebuild (superbot section, #155 shortlist last remainder)

> **Status:** `complete`
> **Model/time:** fable-5 · 2026-07-11 (worker slice, dispatched by the coordinator
> under continuous-chaining mode per Q-0265)

## What this session did

Probe battery v0 on `ideas/superbot/claude-code-projects-for-the-rebuild-2026-07-10.md`
(the #155 groom's ripe-shortlist LAST remainder; time-pressured "probe before the next
rebuild wave") — verdict **parked(overtaken — adopted)**. Verify-first at four live
pins (fetched this probe, ~10:54–10:58Z): superbot `c77ee0d` (ls-remote; canonical doc
byte-identical from the harvest pin `fd638e3`, sha256 `5057ea4e…` both, diffed),
superbot-next `f87ffb0` (ls-remote; heartbeat 10:25Z read raw), fm roster gen #5 @
`3150f0e`, websites `92c3dc6` (unchanged since #159's ~10:35Z clone).

The finding: the idea WON before this repo existed. The owner accepted the EAP and
stood up the "SuperBot" Project the same day the canonical doc was written — superbot
`docs/planning/projects-eap-coordinator-kickoff-2026-07-07.md` §1 @ `c77ee0d` (Project
exists, screenshots verified, both repos connected) — and directed the rebuild through
it that evening (activation-plan §2 supersession note, same pin: "the rebuild DOES run
through the Project"). At probe time the rebuild the doc wanted Projects to coordinate
is being coordinated exactly that way (superbot-next @ `f87ffb0`: 26/49 subsystems
ported, 168/168 goldens at `8b55d95`, five parallel lanes in Q-0265 continuous mode),
and the WHOLE FLEET operates as Projects (fm roster gen #5: 18 seats on per-lane wake
triggers). New evidence class used and flagged in the report: this repo's OWN
operation — idea-engine is itself a Project lane and this probe session a
Project-dispatched child session — cited as operating evidence, not a wish. The
report's one novel delta: the doc imagined ONE Project; reality scaled to MANY
Projects coordinated through committed git files (its own "repo-as-truth" open
consideration, realized harder than predicted). Time window: fired WITH the idea
adopted; post-EAP commercial terms deliberately NOT MEASURED (owner court if it ever
bites — every pin in the report is dated the day after the free window closed, so no
operational wall is in evidence).

Slice contents: probe report append + state flip (captured → parked(overtaken)) +
Grounding pin (doc re-fetched at live `c77ee0d`) + one section-README index-echo
re-badge + this card + claim add/clear + the heartbeat. No outbox entry (nothing
sim-ready), no ⚑ owner ask (the doc's one owner decision was made 2026-07-07 —
re-asking is stale-ask drift), no code, no workflow edits, no superbot-lane write
(Q-0260). Claim rode this branch's first commit per the #157/#159 precedent
(`control/claims/` re-read at origin/main `03ace5f` at branch cut — empty of live
claims) and is deleted in the close-out commit. With this park the superbot section's
#155 ripe shortlist is fully consumed (item 2 → #159, last remainder → this slice).

**📊 Model:** fable-5 · docs-only probe slice (one idea-file append + re-badge + card
+ heartbeat; no code)

## 💡 Session idea

**Adoption-record retirement sweep (superbot lane, manager-routed).** The canonical
doc still carries Status `ideas` at `c77ee0d` while its own planning layer superseded
it twice (the activation plan's header literally says it "supersedes the 'next
lifecycle step'" of the idea doc) and the fleet operates on it daily — the exact
badge-vs-reality drift this probe had to spend a battery pass disproving. Lane-sized
sweep: grep `docs/planning/` for "supersedes … docs/ideas/<file>" pointers and flip
each pointed-at idea doc's Status to a terminal state citing its successor — one pass
retires every adoption record whose decision already fired. Anchors: superbot
`docs/planning/projects-eap-activation-plan-2026-07-07.md` (header supersession
sentence), `docs/ideas/claude-code-projects-for-the-rebuild-2026-07-07.md` (Status
line). Routed via the manager (Q-0260 — this repo writes no superbot files).

## ⟲ Previous-session review

`.sessions/2026-07-11-probe-websites-cutover-role.md` (#159, merged `03ace5f`) —
verified against this tree at branch cut: the idea file carries the appended probe
report + parked(routed) state flip + Grounding pin exactly as promised; the section
README row re-badged captured → parked (line 189); the heartbeat carries the NEW
six-field site-cutover ⚑ (recommendation-first structured choice) and preserved the
#158 Self-review section verbatim at the foot — all four of its named deliverables
land. Its verify-first-at-live-pins method (four pins, byte-diff the canonical doc,
read sibling heartbeats raw) was reused wholesale here and paid again: this probe's
head ALSO turned out consumed, but by adoption rather than by build-out — two
different consumption shapes caught by the same discipline. One inheritance this
slice leaned on directly: #159's separability framing (site vs bot cutover) is what
made it safe to close this head without waiting on the parity march it describes.

## Handoff → next wake

The superbot section's #155 ripe shortlist is DONE (zero remainders). The cutover ⚑
stays owner-court (unchanged, preserved on the heartbeat). Nothing new is armed by
this slice; the standing watch items carry unchanged (theme-schema half-fired arming
condition — manager sweep; superbot-idle VERDICT 006 guardrails). If a post-EAP
pricing/limits change ever surfaces fleet-side, that is a NEW spend-doctrine head for
the owner/manager, not a revival of this parked one (its Q4 says so explicitly).
Candidate next work: the #158 self-review's named follow-ups, grooming the remaining
superbot/websites/fleet heads, or this card's 💡 (manager-routed retirement sweep).
