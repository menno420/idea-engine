# PROPOSAL 145 — Cache-stampede / request-coalescing herd law: caching a HOT key does not reduce its worst-case origin recompute load, it CONCENTRATES it into a herd of size Poisson(λT) that grows LINEARLY with request rate (a hotter key stampedes harder) — and request coalescing (single-flight) collapses that herd to exactly 1, INDEPENDENT of rate, while a longer TTL does nothing to herd size (round-34 FLEET slot opener)

> **Status:** `in-progress`
> 📊 Model: Claude Opus · high · idea/planning

Born-red HOLD: this card is the session's FIRST content commit (after the claim) and is born `in-progress` to hold the `substrate-gate` RED so the PR cannot merge before the deliberate `complete` flip. The flip to `complete` is the LAST step, taken once the verifier is committed, the digest reproduced EXACTLY across a deterministic double-run, the PROPOSAL 145 outbox block + heartbeat are written, and `python3 bootstrap.py check --strict` is green apart from this born-red card HOLD itself (the only exit-affecting red; every other finding is a never-exit-affecting advisory). A red gate AFTER the flip is a real defect, not the HOLD.

## Objective

Draft and land round-34 FLEET-slot PROPOSAL 145 — the cache-stampede / request-coalescing herd law. Folk belief: "a cache with a near-100% hit ratio protects the origin, and a HOTTER key is SAFER because it is almost always served from cache, so to tame origin load you raise the hit ratio or lengthen the TTL." Counterintuitive claim: when a hot key expires it is stale for one recompute window T, and under a plain TTL cache WITHOUT coordination every request arriving in [0,T) independently recomputes it at the origin, so the per-expiry origin herd is exactly Poisson(λT) — expected size λT, which GROWS LINEARLY with request rate (the Wikipedia example verbatim: 10 req/s × 3 s render = 30 simultaneous recomputes). Request coalescing (single-flight / dogpile lock) lets only the first in-window request recompute and makes the rest wait, collapsing the herd to exactly 1 (E = 1−e^{−λT} ≈ 1) — INDEPENDENT of λ; the cure is coalescing, not a longer TTL (which leaves herd size λT untouched). Ship a markdown-first card, a committed stdlib-only verifier (SEED=20260717, exact Poisson-mean anchor + Monte-Carlo, three ordered gates), a deterministic double-run, an outbox block (P145 → V158, +13), and a heartbeat update. VERDICT 158 (P145 → V158, +13) is the next independent slice, NOT written here.

## Constraints honored

- Round-34 FLEET-slot OPENER (rotation fleet→venture→game→unrelated; round-33 closed P141 fleet / P142 venture / P143 game / P144 unrelated, so round-34 opens at the FLEET slot). Offset +13 (P145 → V158) cited from the P144 depends-ledger + control/status.md; the verdict is NOT authored here.
- Digest posture: WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY (the P105…P144 family) with the P127+ TWIST — main() builds a results dict with NO `results_sha256` key (every float rounded to 6 dp), computes `payload=json.dumps(results, sort_keys=True, separators=(",",":"))` then `digest=sha256(payload)`, PRINTS the pretty `json.dumps(results, indent=2, sort_keys=True)` dump + `Results-JSON sha256:`, and writes NO on-disk JSON — so the disclosed digest IS the sha256 of the compact canonical dict, and the pretty stdout dump is a different serialization.
- Stdlib-only verifier (random, json, hashlib, math — no numpy/scipy); pinned SEED=20260717; three ≥3σ-convention /se gates (two |z|<3 verifier-correctness gates + one z≥3 head); deterministic double-run asserted in-process and confirmed across two fresh CLI invocations (byte-identical, exit 0).
- Outbox `## PROPOSAL 145 · … · status: sim-ready` block appended once (append-only, deduped) matching the P141/P144 skeleton (target / idea / question / done-when / depends / loop).
- Status heartbeat: proposal high-water P144 → P145 (union-max, no regress); verdict high-water V156 kept; the non-contiguity flags V137/V132/V126 preserved; routines line UNCHANGED (coordinator-bound); baton → next: V158 for P145; then round-34 venture-slot P146. control/inbox.md left untouched (manager-written).
- Born-red first commit (card in-progress + claim) → verifier + idea doc → outbox + status → flip complete LAST.

## GROUNDING

Round-34 FLEET-domain slot of the fleet-lane proposal→verdict pipeline (ORDER 003 continuous-pipeline standing-active; ORDER 018 overnight loop). Idea source (firsthand, this repo): https://github.com/menno420/idea-engine/blob/main/ideas/fleet/cache-stampede-coalescing-2026-07-18.md · verifier (ground truth): https://github.com/menno420/idea-engine/blob/main/ideas/fleet/cache_stampede_coalescing.py — stdlib-only (random, json, hashlib, math). Grounding commit: idea-engine main c58f29b17ccf40b0cd8258735724c828d6bb8fe5 (fetched 2026-07-18T20:10:49Z), the branch point for `claude/proposal-145-cache-stampede-coalescing`. Pinned world: SEED=20260717, T_RECOMPUTE=1.0, SIGMA_GATE=3.0; CFG_LOW λ=5 / CFG_HIGH λ=30 / CFG_HEAD λ=30 coalesced, R=4000 each; per-expiry naive herd = count of Poisson(λ) arrivals in [0,T) = exactly Poisson(λT); coalesced recomputes = 1 if herd≥1 else 0; head statistic = paired amplification (naive−coalesced) vs zero. External reference (reachable): cache stampede / dogpile / thundering-herd + probabilistic early expiration (Vattani, Chierichetti & Lowenstein, "Optimal Probabilistic Cache Stampede Prevention", VLDB 2015) — https://en.wikipedia.org/wiki/Cache_stampede (verified reachable 2026-07-18 via WebFetch; the page's own example — 10 req/s × 3 s render = 30 concurrent recomputes — is λT=30 verbatim). Offset authority for +13 is this repo's control/outbox.md per-block ledger (P144 depends-line) + control/status.md heartbeat; docs/current-state.md is a dated SEAT-DORMANT snapshot (ledger only through V059) and carries NO P145/V158 row.

## Probe questions

**1. Is the mechanism genuinely new against the fleet backlog?** Yes. The dedup section names the nearest neighbours and the distinguishing object each time: metastable-retry-storm-collapse is a bistable retry congestion-collapse (hysteresis), not a single-expiry herd; hedged-request-tail-cure ISSUES duplicates as a tail-latency cure (opposite direction — here duplicates are the problem, coalescing the cure); erlang-b / checkout-pooling are queueing loss/delay systems; fan-out-tail / two-choices are fan-out/placement objects; epidemic-overshoot is SIR contagion. This card's object is the Poisson(λT) origin-recompute count at ONE cache expiry and its O(1) collapse under single-flight — a concurrency/coordination result, the first cache-stampede / coalescing head in the lane.

**2. Do all three pre-registered gates PASS in order on the /se margin?** Yes. G1 CFG_LOW naive herd 4.91225 vs exact Poisson mean λT=5, z=−2.522567 (|z|<3) PASS; G2 CFG_HIGH naive herd 30.083 vs exact Poisson mean λT=30, z=+0.963546 (|z|<3) PASS — 6× the CFG_LOW herd at 6× the rate, the herd scales linearly; G3 head coalescing paired amplification (naive 30.13825 − coalesced 1.0 = 29.13825, se 0.085288) z=+341.643962 (≥3) PASS — coalescing removes ~29 redundant recomputes/expiry, a ~30× → 1× collapse. all_gates_pass=true, first_failing_gate=None, exit 0.

**3. Does the digest reproduce EXACTLY and deterministically?** Yes — results-dict sha256 81929730f04afec22fcacffd0d44d81609c6d2d2e395dd0803ec77ee29f19422, byte-identical across two fresh CLI invocations and asserted in-process; WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY (the dict carries no results_sha256 key, no on-disk JSON, floats rounded 6 dp), with the P127+ twist (pretty indent=2 stdout dump, compact canonical hashed preimage).

**4. Does the proposal advance the proposal high-water without regressing anything?** Yes — proposal high-water P144 → P145 (union-max, round-34 FLEET slot opens above round-33 UNRELATED-slot P144); verdict high-water V156 kept; V137 for P124, V132 for P119, and the round-26 FLEET-slot P113 → V126 (idea-engine mirror #527 @cee9162) remain open/backfilled pulls below the verdict high-water — do not read "V156" as "every lower verdict closed on both sides".

## Outcome

PROPOSAL 145 drafted and staged sim-ready. On the pinned world (SEED=20260717, T=1.0, CFG_LOW λ=5 / CFG_HIGH λ=30 / CFG_HEAD λ=30 coalesced, R=4000, the P104…P144 /se convention) a stdlib Monte-Carlo of one cache-expiry event confirms the herd law: the naive per-expiry origin herd is Poisson(λT) (G1 λT=5 z=−2.52, G2 λT=30 z=+0.96, both |z|<3 — the herd is correct and scales 6× with 6× rate), and request coalescing collapses it to exactly 1 (G3 paired amplification 29.14, z=+341.6 ≥3). all_gates_pass=true, exit 0, first_failing_gate None; digest 81929730f04afec22fcacffd0d44d81609c6d2d2e395dd0803ec77ee29f19422 reproduced EXACT across a deterministic double-run. Design correction (transferable): the origin's worst-case load per hot key is the per-expiry herd λT, NOT the miss rate — so a higher hit ratio does not bound origin load, the hottest keys make the largest bursts, and a longer TTL lowers expiry frequency but leaves herd size untouched; the fix is a coordination primitive (single-flight / request coalescing / a per-key recompute lock, or probabilistic early expiration), which makes the per-expiry origin cost O(1) regardless of rate. The verifier + idea doc + outbox P145 block + heartbeat are written; the flip to `complete` releases the merge-on-green backstop. VERDICT 158 (P145 → V158, +13) is the next independent slice, not authored here.

## ⟲ Previous-session review

The round-33 UNRELATED-slot P144 (hot-hand streak-selection bias, Miller & Sanjurjo 2018) landed sim-ready with an exact-enumeration anchor and results-dict sha256 1c262684…dac3, three gates PASS (G1 z=−0.384486 / G2 z=+0.660242 / G3 z=+223.601743); its baton named "round-34 fleet-slot P145" as the successor's pull, and this slice executes exactly that pull, opening round-34 at the FLEET slot with zero re-derivation of the rotation. Two carry-forwards honored: (i) the DIGEST posture — P144 and this card are both the WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY family with the P127+ TWIST (compact canonical hashed payload, pretty indent=2 stdout dump, floats rounded 6 dp, no on-disk JSON), reconciled against the verifier text, not assumed; (ii) the exemplar STRUCTURE — the Erlang-B P141 card's three-config / three-gate shape (two |z|<3 verifier-correctness gates validating the MC against an exact closed form, one z≥3 counterintuitive head) is mirrored here (Poisson-mean anchors for G1/G2, paired coalescing amplification for G3). Continuity carried forward: the verdict high-water is non-contiguous — V137 for P124, V132 for P119, and V126 for P113 (idea-engine mirror #527 @cee9162) remain open/backfilled pulls below it. The coordinator-bound routines (failsafe trig_01MzoLVHMDuL28UPkKnbmHCm, pacemaker trig_01R26BEgXj5KVZkHA4Ri6AMi) stay armed and are NOT re-armed by this proposal slice.

## 💡 Session idea

The verifier pins a single recompute-window model — a FIXED window of length T where every in-window request recomputes — and gates the herd size (Poisson(λT)) and its O(1) coalesced collapse. The load-bearing generalization the gates do NOT touch is the PROBABILISTIC-EARLY-EXPIRATION frontier (XFetch, Vattani et al. 2015): instead of a hard TTL, each read recomputes early with probability rising as expiry approaches (recompute when `now − δ·β·ln(rand) ≥ expiry`, δ = last recompute cost, β a tuning knob), which spreads recomputes BEFORE the value goes stale so no herd ever forms — WITHOUT any lock or cross-request coordination. A genuinely different next FLEET slice pins that axis: hold λ, T fixed and sweep β over a grid, then pre-register (G-a) a stampede-elimination gate — at the tuned β the expected number of concurrent recomputes at any instant is ≥3σ BELOW the plain-TTL herd λT (early expiration alone, no lock, kills the stampede); (G-b) an overhead-bound gate — the extra recomputes XFetch performs (early refreshes that a hard TTL would not have done) stay ≥3σ below a small budget (it is cheap, not a busy-loop); and (G-c) a coordination-free control — XFetch's per-instant recompute count does not depend on whether requests can see each other (each reader rolls independently), distinguishing it from the single-flight lock this card models. That converts "coalescing collapses the herd" into "here is the lock-free early-expiration knob β and its stampede-vs-overhead trade-off" — a seed-pinnable probabilistic-early-expiration mechanism distinct from this slice's single-flight two-state model and from every prior fleet head.
