# PROPOSAL 129 — the correlated-vote Condorcet jury ceiling (round-30 FLEET slot, P129 → V142, +13)

> **Status:** `in-progress`
> 📊 Model: opus · high · idea/planning

Born in-progress as this session's first commit (born-red HOLD); flips to complete as the deliberate last step after the heartbeat, releasing the landing workflow.

## Objective
Draft and land the round-30 FLEET-slot proposal (P129): one genuinely new, counterintuitive, stdlib-simulable fleet-domain pure-mechanism head, distinct from the prior fleet heads P101 (winner's-curse task auction), P105 (delegation-loop collapse), P109 (correlated-fleet VARIANCE floor), P113 (two-choices routing cliff), P117 (Young–Daly checkpoint interval), P121 (hedged-request tail cure), P125 (USL retrograde cliff). Domain: multi-agent binary-decision aggregation / social-choice reliability — the correlated-vote breakdown of the Condorcet Jury Theorem: N agents each independently correct with probability p>1/2 vote on a binary question by MAJORITY; independence drives majority accuracy → 1 (Condorcet), but a shared common-factor error correlation ρ>0 caps accuracy at a closed-form CEILING A_∞=Φ(Φ⁻¹(p)/√ρ) < 1, so more voters cannot buy certainty and the crowd saturates near N≈1/ρ. With pre-registered ≥3σ /se gates, a paired common-random-numbers latent-Gaussian one-factor probit design, an independence control leg, and a Gaussian-orthant closed-form ceiling anchor. Fanned to sim-lab as VERDICT 142 (+13). This slice does NOT write the verdict.

## Constraints honored
- HARD-SYNC to origin HEAD (idea-engine 358ec4f) before reading; sibling union taken IN if origin/main advances mid-slice.
- Claim filed FIRST; born-red card is the first commit (HOLD); branch pushed; PR opened READY; heartbeat before flip; Status→complete is the last commit.
- Round-30 rotation fleet→venture→game→unrelated: this OPENS round-30 at the FLEET slot (P128 closed round-29 at the unrelated slot). P129 is the next unused number (P128 high-water, confirmed against control/outbox.md per-block ledger).
- Monte Carlo pinned SEED=20260717, ≥3σ gates on the /se margin (z on an estimated statistic, se=std/√TRIALS — the P104…P128 convention). Stdlib-only verifier (random, math, json, hashlib), no third-party. Model line family-level, PL-004 task class idea/planning.

## What happened
[[fill: filled at close-out — mechanism, pinned world, gate results, dry-sim numbers, results-dict sha256 + digest posture, files]]

## ⟲ Previous-session review
[[fill: filled at close-out — immediate rotation predecessor review + offset-ledger confirmation]]

## 💡 Session idea
[[fill: filled at close-out — the transferable lesson]]

## GROUNDING
[[fill: filled at close-out — origin sha + the Condorcet-jury / correlated-vote anchor URL verified reachable + firsthand verifier reproduction]]
