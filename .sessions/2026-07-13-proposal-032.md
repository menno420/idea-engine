# Session — PROPOSAL 032: Penney's game responder-edge decay (unrelated-domain rotation, round 4 — a FOURTH fleet-external domain)

> **Status:** `complete`
> **Model/time:** fable · 2026-07-13T08:16:09Z (Ideas Lab worker slice — draft the
> COMPLETELY-UNRELATED-domain rotation round-4 head under standing owner ORDER 003:
> rounds 1–3 were P017 (social choice → V019 null), P024 (congestion routing → V026
> null), P028 (tournament seeding → VERDICT 030 by the offset map; its outcome was
> NOT read this slice — no sim-lab clone in this worker's environment, and the
> rotation-slot dedup needs only the DOMAINS, all three read from this repo's own
> committed writeups). Card born in-progress as the designed gate hold, flips
> complete in this PR's final commit once the 💡 slot resolves)

**📊 Model:** fable · content + outbox proposal only (idea file, card, index row,
outbox append; no control/status.md or control/inbox.md writes; no checker or
script changes)

## Scope

Draft a genuinely new sim-shaped idea in a real-world domain COMPLETELY UNRELATED
to the fleet, different from all three prior unrelated picks (P017 IRV voting,
P024 Braess routing, P028 tournament seeding), under standing owner ORDER 003
(continuous pipeline; rotation rule per ORDER 004 rule 3, ratified by the morning
tally). ORDER 003 verified standing at HEAD `1c79289` before any work. Candidates
weighed on merit, then decided: (a) **Penney's game responder-edge decay — WINNER**
(crisp falsifiable folk-belief anchor "never go first" + the flip-rule beater;
fully exact, zero-RNG census over 56/240/992 ordered word pairs at L ∈ {3,4,5};
dual independent exact algorithms — Conway leading numbers vs automaton absorption
— gated on rational equality, the pattern VERDICT 030's proposal P028 established
for the rotation lane; NULL genuinely live: the L=4/5 minimax values are not in
any popular table and the bands 13/25 / 3/5 are honest guesses registered before
any code); (b) bus bunching on a loop line — real domain but several invented
constants (dwell/board rates) with no committed anchor and a mushier folk default;
(c) elevator dispatch exact enumeration — state space explodes past toy sizes,
weak reader consequence; (d) chip-firing/sandpile stabilization — exact and
beautiful but no falsifiable folk belief; (e) calendar interval packing —
unweighted greedy is optimal by THEOREM, the question dies at pre-registration.
Written up as `ideas/fleet/penney-game-responder-edge-decay-2026-07-13.md`
(probed single-pass this slice, sim-ready; homed in ideas/fleet/ per the
check_sections non-lane carve-out with the placement flagged, the P017/P024/P028
precedent) and appended to `control/outbox.md` as PROPOSAL 032 (tail verified at
HEAD before append: PROPOSAL 031 · 2026-07-13T07:49:09Z). Seed-registry note:
ZERO seeds drawn — the P031 high-water 20260767 left unchanged for the next
seeded head (the P028 seedless precedent). Dedup swept tree-wide at `1c79289`
(`rg -i 'penney|nontransitive|non-transitive|coin.?sequence|leading.number|
pattern.?race'` over ideas/ + control/ + .sessions/ + docs/, bootstrap.py and
.substrate excluded): ZERO domain hits; the secondary "coin" sweep hits only the
casino family's even-money archetype prose (bankroll ruin, not pattern races) and
P028's "near a coin flip" simile.

## 💡 Session idea

**A committed candidate backlog for the unrelated-domain rotation slot — stop
burning a fresh domain survey every round.** Every unrelated-domain round pays
the same open-ended cost twice: hunt candidate domains from scratch, then
re-derive each one's kill tests (crisp folk-belief anchor? exact/hermetic arm
reachable? NULL genuinely live? reader consequence real?) — and the LOSERS are
then buried as prose inside individual writeups (P028 killed apportionment +
spaced-repetition; this slice killed bus bunching, elevator dispatch,
chip-firing, and interval packing — interval packing died on a five-second
theorem check ANY earlier round could have recorded). The slice: one committed
file, `ideas/fleet/unrelated-domain-backlog.md` — one row per candidate domain
{domain · folk-belief anchor · cheapest exact arm · NULL-liveness note ·
consequence sketch · status (open / killed(reason) / promoted → PROPOSAL nnn)}
— appended in the same PR as each rotation head (winners flip to promoted,
fresh kills land as killed rows), so each round's survey becomes a lookup plus
a delta instead of a re-derivation. **Dedup** (this slice, `rg -i
'domain backlog|candidate backlog|runner.?up'` over ideas/ + .sessions/ +
control/): distinct from P021's 💡 (a rotation-POSITION ledger — which slot
fires next; this is a candidate-CONTENT backlog for one slot — what to run when
it fires), from P030's 💡 (verdict open-threads index — follow-ups of LANDED
verdicts; this catalogs domains never yet run), and from P019/V021 (lane
backlog DEPTH signalling, not content); P017's 💡 (the `ideas/offworld/`
section) is the natural HOME for the file if that carve-out ever lands —
complementary, not overlapping.

## ⟲ Previous-session review

Newest predecessor card (`.sessions/2026-07-13-proposal-031.md`, worker slice
~07:46Z): closed clean — the game-mechanics round-4 head (V018's excluded third
trigger), and its sharpest habit is consumed here: it probed a candidate LIVE
(anonymous blobless clone of superbot-games) instead of assuming a wall, per the
CAPABILITIES discovery rule, and named every passed alternative WITH its
disqualifying evidence; its 💡 (the deferred-constant ledger, with the small
greppable defer-vocabulary) is also exactly the kind of cross-repo tripwire this
seat keeps re-deriving by hand — seconded here as another datapoint.
