# VERDICT 159 mirror — quota-attainment cliff bunching: a sales-comp ACCELERATOR with a CLIFF at 100% quota is a convex kink in each rep's payoff, so rational reps RE-TIME deals across the period boundary — a rep just under quota pulls next-period deals forward to clear the cliff (BUNCHING in the [Q, Q(1+eps)] band), a rep past the cliff pulls forward MORE to book at the accelerated rate now ("a hot period borrows from the next") — and even with TRUE demand i.i.d. across periods the OBSERVED bookings show excess band mass, a SPURIOUS NEGATIVE lag-1 autocorrelation of team totals, and EXACT horizon conservation, so the QoQ volatility / mean-reversion a forecaster reads off bookings is a comp-plan ARTIFACT, not the market; a sales-ops / comp-design head (round-34 VENTURE slot, backfill below the V161 high-water), P146 → V159 (+13) — QUALIFIED-APPROVE (first-failing-gate null; results-dict sha256 90e5ea44d1b7ccd2a230d4c3dd74eef8798ec56cf54794553f9e434efce00ea4 EXACT; G1 bunching z=+239.736603 ≥3 / G2 autocorr diff −0.145509 < 0 z=−13.149401 magnitude ≥3 / G3 conservation 0.0 both worlds + Dist B band_z=+175.223184 ac_z=−13.109365 ≥3; sim-lab verdict-159 sim, branch claude/verdict-159-quota-cliff-bunching, byte-for-byte reproduction). Reproduced == disclosed to the digit; verifier byte-identical, deterministic across three fresh cross-invocation runs. QUALIFIED per the V156 precedent: the proposal's own Model basis declares the LOAD-BEARING no-ratchet accelerator choice — under a quota-RATCHET plan the over-quota incentive flips to defer/sandbag and the G2 autocorrelation SIGN pushes toward smoothing; bunching and conservation are ratchet-robust, the G2 headline sign is regime-dependent and ungated.

> **Status:** `complete`
> 📊 Model: Claude Fable · high · review/verify

Born-red HOLD: this card holds the PR red from its first commit until the slice is complete and verified; the flip to `complete` (the deliberate LAST commit) releases the merge-on-green backstop. No gate is bypassed; a red gate AFTER the flip is a real defect, not the HOLD. Born in-progress as this session's FIRST content commit (after the claim) — this born-red HOLD holds the `substrate-gate` (blocking `session-log` guard) red so the PR cannot merge before the deliberate flip. The flip to `complete` is the LAST step, taken once the reproduction is confirmed, the digest reproduced EXACTLY, the VERDICT 159 outbox block + heartbeat are written, and `python3 bootstrap.py check --strict` is green apart from this born-red card HOLD itself. This is the MIRROR slice — fan-in to the fleet-manager (Q-0264) of sim-lab's VERDICT 159; the verification itself is performed and staged on the sim-lab side (sim-lab reproduction on branch claude/verdict-159-quota-cliff-bunching, sim dir `sims/verdict-159-quota-cliff-bunching/`).

## Objective

Mirror the round-34 VENTURE-slot verdict P146 → V159 (+13) into idea-engine: append a `## VERDICT 159 mirror …` block to `control/outbox.md` (append-only, none exists at branch time — verified by grep at HEAD), record V159 as a LOWER FILL in `control/status.md` (verdict high-water STAYS V161 — union-max, no regress; V159 closes the round-34 quad V158–V161; keep the non-contiguity flags V137/V132/V126), set the baton next = round-35 fleet-slot P149 → V162, and land a born-red session card that HOLDs the PR red until the deliberate `complete` flip. The head under verdict: a comp accelerator with a cliff at 100% quota re-times deals it does not create — under-quota reps in the pull window pull next-period deals forward smallest-first just to clear Q (bunching), over-quota reps ≥ 1+OVER_THRESH pull forward largest-first up to ACCEL_BANK_CAP to bank the accelerated rate ("a hot period borrows from the next"), so observed bookings bunch just above quota and anti-correlate period-to-period while horizon demand is EXACTLY conserved — the folk belief "a cliff just sharpens focus; aggregate bookings and their trend are unaffected" fails on all three artifacts, and a forecaster reading mean-reversion off comp-period bookings is measuring the comp plan, not demand.

## GROUNDING

Fan-in to the fleet-manager (Q-0264) of sim-lab's VERDICT 159 verifying idea-engine PROPOSAL 146 (`## PROPOSAL 146 · 2026-07-18T20:49:07Z · status: sim-ready`, idea-engine control/outbox.md), offset +13 (P146 → V159), the round-34 VENTURE slot (rotation fleet→venture→game→unrelated; round-34 opened at the fleet slot with P145 → V158, and the venture slot P146 is the last round-34 verdict gap — V158/V160/V161 already landed). Idea source (reachable): https://github.com/menno420/idea-engine/blob/main/ideas/venture-lab/quota-attainment-cliff-bunching-2026-07-18.md . Verifier (firsthand, ground truth): https://github.com/menno420/idea-engine/blob/cb24ad8/ideas/venture-lab/quota_cliff_bunching.py — committed idea-engine git blob `3eb972c2c9f878d30d2563a4554f0d4d9c55882b`, file sha256 `856f0fd91e8c48c645a14aa3d5a48301ad61989b115a9468cf0a0bd2a0f071d1`, 346 lines / 14286 bytes, stdlib-only (hashlib, json, math, random — no numpy/scipy). Verifier landed via idea-engine PR #596 (P146) at main `cb24ad8`; this mirror branches off idea-engine main `2b34c31` (V161 mirror landed #600; append-only union). Pinned world: SEED=20260717, N_REPS=50, N_PERIODS=8, QUOTA=12.0, DEALS_PER=12 (Dist A amounts ~ Exponential(mean 1.0)), PULL_WINDOW=0.20, OVER_THRESH=0.20, ACCEL_BANK_CAP=0.50, BAND_EPS=0.10, M_TRIALS=800, SIGMA=3.0, CONSERVATION_TOL=1e-6; Dist B = poisson_count_unit; one random.Random(SEED) single stream, Dist A then Dist B. Estimator: PAIRED Monte-Carlo difference on the /se convention — per trial re-draw true demand, derive the observed grid by the deterministic relocation rule, z = mean_diff / (stdev_diff/sqrt(M_TRIALS)); the pairing nets out the shared finite-sample lag-1 autocorrelation bias (true_ac_mean −0.129676 ≈ −1/(P−1)). External references (reachable): Saez, "Do Taxpayers Bunch at Kink Points?", NBER w7366, https://www.nber.org/papers/w7366 + https://en.wikipedia.org/wiki/Commission_(remuneration) + https://en.wikipedia.org/wiki/Autocorrelation (verified reachable 2026-07-18 per the P146 block). Offset authority for +13 is this repo's control/outbox.md per-block ledger (P146 depends-line) + control/status.md heartbeat; docs/current-state.md is a dated SEAT-DORMANT snapshot (ledger only through V059) and carries NO P146/V159 row.

## Constraints honored

- Mirror slice — the verification is staged and reproduced on the sim-lab side (branch claude/verdict-159-quota-cliff-bunching, byte-for-byte reproduction of the committed verifier); this seat fans the verdict in and does NOT re-run the verifier here (reproduction posture inherited from V114–V161).
- Digest posture: WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY (the P105…P148 family) with the P127+ TWIST — main() builds a results dict with NO `results_sha256` key, computes `payload=json.dumps(results, sort_keys=True, separators=(",",":"))` then `digest=sha256(payload)`, PRINTS the pretty `json.dumps(results, indent=2, sort_keys=True)` dump + `Results-JSON sha256:`, writes NO on-disk JSON (every float rounded to 6 dp) — the disclosed digest IS the sha256 of the compact canonical dict; resolved against the script text, not assumed.
- Outbox `## VERDICT 159 mirror …` block appended once (append-only, deduped — grep confirms no existing block) matching the V156/V158/V160/V161 skeleton (source / idea / ruling / three gate bullets / cross-check / sim / digests / offset / loop).
- QUALIFIED-APPROVE, the V156 precedent — the digest reproduced EXACT, the verifier byte-identical, three fresh invocations byte-identical, all three gates PASS in order on the /se margin (first-failing-gate null); QUALIFIED because the disclosed LOAD-BEARING model choice (pure above-quota accelerator, no ratchet) sets the SIGN of the G2 headline (a ratchet plan flips over-quota re-timing to defer/sandbag → smoothing) and the verifier ships no ratchet-control world gating that dependence; bunching (G1) and conservation (G3a) are ratchet-robust.
- Status heartbeat: verdict high-water STAYS V161 (union-max, NO regress); V159 recorded as the round-34 VENTURE lower fill (round-34 COMPLETE V158–V161 once landed); proposal high-water P148 kept; the non-contiguity flags V137/V132/V126 preserved; routines line UNCHANGED (coordinator-bound); baton → next round-35 fleet-slot P149 → V162. control/inbox.md left untouched (manager-written).
- Born-red first commit (card in-progress, after the claim) → outbox + status → flip complete LAST.

## Probe questions

**1. Does the disclosed results-dict sha256 reproduce EXACTLY, and is the verifier byte-identical?** Yes — results-dict sha256 90e5ea44d1b7ccd2a230d4c3dd74eef8798ec56cf54794553f9e434efce00ea4, reproduced == disclosed to the digit (all 64 hex); the sim-lab verifier is byte-identical to the committed idea-engine source (diff exit 0; file sha256 856f0fd91e8c48c645a14aa3d5a48301ad61989b115a9468cf0a0bd2a0f071d1, git blob 3eb972c2c9f878d30d2563a4554f0d4d9c55882b, 346 lines / 14286 bytes), and stdout is byte-identical across THREE fresh python3 invocations (all exit 0, identical digest). WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY posture (floats rounded 6 dp, no on-disk JSON; the compact canonical dict's own sha256 IS the digest — the P127+ TWIST).

**2. Do all three pre-registered gates PASS in order G1 → G2 → G3 on the /se margin?** Yes — G1 BUNCHING obs_band_mean 126.16375 vs true_band_mean 51.66, excess 74.50375, z=+239.736603 (≥3 one-sided); G2 SPURIOUS NEGATIVE lag-1 autocorr obs −0.275185 vs true −0.129676, diff −0.145509 < 0, z=−13.149401 (magnitude ≥3); G3 conservation_max_rel 0.0 (Dist A) and 0.0 (Dist B) ≤ 1e-6 + robustness Dist B band_z=+175.223184 and ac_z=−13.109365 (both ≥3σ). all_pass=true, exit 0, first_failing_gate=null.

**3. Is the spurious-negative-autocorrelation reading sound against the finite-sample bias?** Yes — the sample lag-1 autocorrelation of a length-8 i.i.d. series is biased ≈ −1/(P−1) ≈ −0.14, and the counterfactual world sits right at it (−0.129676); the gate is PAIRED (observed − true per trial), netting out the shared bias and isolating the relocation's increment (observed −0.275185 is nearly double the bias; diff −0.145509, z=−13.149401). The head is the difference, not the level.

**4. Why QUALIFIED-APPROVE rather than clean APPROVE, and does the ledger stay union-max?** QUALIFIED per the V156 precedent: the proposal's own Model basis declares the no-ratchet accelerator as the LOAD-BEARING choice — over-quota PULL-FORWARD (which powers the G2 negative sign, "a hot period borrows from the next") inverts to defer/sandbag under a quota-RATCHET plan, pushing the sign toward positive/smoothing — and no ratchet-control world gates that dependence (the ρ* sign-flip sweep is the named next venture slice); unlike V160/V161 the disclosed model-dependence is not itself proven by a control-vs-head contrast, and unlike V157/V158 there is no closed-form anchor. What is unconditional: bunching (G1), exact conservation (G3a), and distribution-robustness (G3b). Ledger: verdict high-water STAYS V161 (V159 is a LOWER FILL closing the round-34 quad V158–V161); V137/P124, V132/P119, V126/P113 (mirror #527 @cee9162) remain open/backfilled below — the high-water must NOT be read as "every lower verdict closed on both sides".

## Outcome

**QUALIFIED-APPROVE — mirror landed.** The reproduction is confirmed on the sim-lab side, now MERGED: sim-lab PRIMARY PR #235 (https://github.com/menno420/sim-lab/pull/235), squash-merged to sim-lab main @`685b8d36746a4225438f48811357d1397e734740` (branch claude/verdict-159-quota-cliff-bunching, byte-for-byte reproduction, `diff` exit 0). The results-dict sha256 `90e5ea44d1b7ccd2a230d4c3dd74eef8798ec56cf54794553f9e434efce00ea4` reproduced == disclosed to the digit; all three pre-registered gates PASS in order G1 → G2 → G3 (G1 bunching z=+239.736603; G2 spurious negative lag-1 autocorr diff −0.145509 < 0, z=−13.149401; G3 conservation 0.0 both worlds + Dist B band_z=+175.223184, ac_z=−13.109365), all_pass=true, first_failing_gate=null, exit 0 across three fresh invocations. The `## VERDICT 159 mirror` outbox block is appended exactly once (dedupe-checked) and the heartbeat records V159 as the round-34 VENTURE lower fill — verdict high-water STAYS V161, round-34 COMPLETE (V158–V161), baton next: round-35 fleet-slot P149 → V162. QUALIFIED per the V156 precedent: the disclosed LOAD-BEARING no-ratchet accelerator choice sets the G2 headline sign (ratchet regime flips it toward smoothing); bunching, conservation and distribution-robustness are unconditional. This flip to `complete` is the deliberate LAST commit releasing the merge-on-green backstop; zero agent merge calls.

## ⟲ Previous-session review

Prior card `2026-07-18-verdict-161-will-rogers.md` (V161 mirror, idea-engine PR #600 @2b34c31) landed the round-34 UNRELATED-slot fan-in cleanly — exact digest `79b7443f…306470`, all gates PASS, an APPROVE whose model-dependence was gate-proven by its own G2-conservation-vs-G3-head contrast, and whose ledger discipline (high-water V161 with the VENTURE-slot V159 explicitly flagged OPEN below, non-contiguous) named exactly the gap this V159 mirror now closes — the right handoff, consumed here with zero re-derivation.

## 💡 Session idea

This slice's world gives EVERY rep the same re-timing ability, so the artifact magnitudes are population-maximal; the actionable next venture slice inverts the estimator — a BUNCHING-PREVALENCE inversion head: fix the P146 world but give only an unknown fraction φ of reps the ability to re-time (the rest book true demand as it arrives), pre-register (G-a) monotone identifiability — the observed [Q, Q(1+eps)] band excess and the |lag-1 autocorr| increment are both strictly increasing in φ, each step of a pinned φ-grid separated ≥3σ; (G-b) an inversion gate — a method-of-moments estimator φ̂ built ONLY from observed bookings (band excess relative to a smooth attainment-density interpolation, the Saez bunching estimator applied to comp data) recovers the true φ within a pre-registered CI at ≥3σ for two pinned φ values; and (G-c) the conservation control (horizon totals unchanged at every φ). That converts this verdict's warning ("the artifact exists") into a measurement instrument ("HOW MUCH of your team re-times, estimated from the attainment histogram alone — no counterfactual needed"), a seed-pinnable venture head distinct from the P146 doc's own named ratchet-ρ* sweep and from the sim-lab card's aggregation-window cure idea.
