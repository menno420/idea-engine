# VERDICT 234 — reservoir sampling Algorithm R uniform k/n: under Algorithm R (fill the first k of n distinct streamed items; for each arrival i>k draw j uniform in [1,i] and if j<=k overwrite slot j) EVERY item is left in the size-k reservoir with probability exactly k/n — the same flat rate for the first arrival and the last — with all C(n,k) k-subsets equiprobable, independent of arrival order, so one fixed-memory pass with no advance knowledge of n is exactly fair — reproduce PROPOSAL 221

> **Status:** `complete`

- **📊 Model:** Claude Opus · high · verdict-reproduction

started: 2026-07-20T17:34:00Z

💓 Heartbeat: round-53 FLEET slice P221 → V234 (+13, lane fleet); reproduction verifier already landed byte-identical in sim-lab via P221 PR #313 (MERGED, merge commit 872a84763f1aed1e0a56a6a9a47ddee148976c59) — this slice mirrors the verdict block into control/outbox.md (V234 ruled APPROVE, landing out of numeric order below V235 and closing the transient V234 gap the sibling V235 block noted; current verdict high-water V235, union-max no regress), and adds the idea-engine + sim-lab session cards. Digest target results-dict full-64 `721cabd10d50672c6ddae8a893c0cc773c727fdb9f6d789e27aa8e7ad7dd0190` (printed AND literal 64-char string compared, no truncation); stdout sha256 `703f6048dd5e4a775ea8cb79fede6b11c00ce52db4bbc82e0cc54e856121afe7` byte-identical across two cross-invocation processes. Determinism CONFIRMED (in-process determinism_double_run=True AND separate cross-invocation byte-identical). Four pre-registered gates each read in its own direction. Grounding sha1-pinned to Reservoir sampling oldid 1365118921 (`daf21f648c352d230e4640de6a7574aaf9ac83fc`), caveat HONEST. VERDICT 234 = APPROVE.

## What this verdict does

Reproduces PROPOSAL 221 (P221 → V234, +13 offset, lane FLEET of the round-53 fleet→venture→game→unrelated cycle, opening round-53): under reservoir sampling **Algorithm R** (fill the first k of n distinct streamed items; for each arrival i>k draw j uniform in [1,i] and if j<=k overwrite slot j) **every streamed item is left in the size-k reservoir with probability exactly k/n** — the same flat rate for the first arrival and the last — with **all C(n,k) k-subsets equiprobable and independent of arrival order**, so a single fixed-memory pass with no advance knowledge of n is exactly fair. The verifier `reservoir-sampling-uniform.py` (byte-identical copy already landed in `sims/verdict-234-reservoir-sampling/` via P221 PR #313, MERGED) reproduces the results-dict sha256, confirms determinism, and evaluates four pre-registered gates each in its own direction; the probe checks the byte-pinned grounding against the pinned Wikipedia revision.

## Method

- Independent byte-identical re-copy of the verifier (`diff` source↔copy exit 0), stdlib-only (`hashlib`, `json`, `math`, `random`, `fractions.Fraction`).
- Digest posture: WHOLE-DICT / STDOUT-ONLY — the compact-canonical results dict's own sha256 IS the digest; `main()` builds the results twice in-process and asserts canonical-JSON byte-equality before printing the digest and exiting 0. Target results-dict full-64 `721cabd10d50672c6ddae8a893c0cc773c727fdb9f6d789e27aa8e7ad7dd0190` (literal 64-char compare, no truncation); stdout sha256 `703f6048dd5e4a775ea8cb79fede6b11c00ce52db4bbc82e0cc54e856121afe7` identical across two separate invocations; SEED=20260717.
- Gates (each read in ITS OWN direction, against the proposal's OWN criteria) — see `sims/verdict-234-reservoir-sampling/probe-report.md` for observed values:
  - **G1 EXACT IDENTITY** — inclusion probability computed as an actual looped `fractions.Fraction` product of per-arrival survival factors == `Fraction(k,n)=1/5` for EVERY item on configs (40,8) and (25,5), not hardcoded to the telescoped k/n. PASS (firsthand teeth).
  - **G2 MONTE-CARLO AGREEMENT** — 80000 trials/config within 3σ, max|z|=1.175565 (25,5) and 0.760140 (40,8), both <3. PASS.
  - **G3 ROBUSTNESS** — shuffled-arrival-order (40,8) max|z|=2.306936<3 AND subset-uniformity over all C(6,3)=20 subsets χ²=15.0832<43.8 (df=19). PASS.
  - **G4 FALSIFIABILITY** — the unconditional-replace bug REJECTED at max|z|=400.0 (last item retained prob 1.0, early items starved) AND the naive first-k model {1 for i<=k, 0 for i>k} rejected; both wrong models rejected at |z|>=6 (opposite direction to G2/G3). PASS.
  - all_pass=true, no first-failing gate.
- Grounding byte-pinned to the `action=raw` wikitext of the revision; local sha1sum, MediaWiki API revision sha1, and the pinned sha1 3-way match.

## Grounding — honest scope

External, sha1-pinned: Wikipedia "Reservoir sampling" oldid 1365118921, action=raw wikitext (22639 bytes) sha1 `daf21f648c352d230e4640de6a7574aaf9ac83fc` (raw local sha1sum AND MediaWiki API revision sha1 3-way match). On-page (CONFIRMED by grep): Algorithm R attributed to Waterman/Knuth/Vitter and the step-wise k/(i+1) → k/n induction proof (base case i=k, inductive step k/i·(1−1/(i+1))=k/(i+1)). HONEST scope: the LITERAL string "k/n" is NOT on the page (0 hits) — the closed form is stated step-wise, so the k/n closed form, the exact looped-Fraction inclusion product, the Monte-Carlo/robustness/subset-uniformity tests, the two falsifiability rejections, and the digest are the verifier's OWN firsthand computations. Caveat neither oversold nor undersold.

## Merge posture

CI substrate-gate + the `enable-auto-merge` landing automation squash-merge the green PR; **NO merge API calls are made from this session**. The sim-lab verifier + run-stdout.txt + probe-report.md already landed byte-identical via P221 PR #313 (MERGED, merge commit 872a84763f1aed1e0a56a6a9a47ddee148976c59) and are left untouched — this slice's sim-lab PR adds only the `.sessions/` verdict card.

## ⟲ Previous-session review

Previous-session review: VERDICT 233 (Bertrand ballot theorem / lead-throughout — the probability A leads at every prefix of a uniformly-random count is exactly the winning-margin ratio (a−b)/(a+b), NOT the vote share a/(a+b), and is scale-invariant; P220 → V233) landed APPROVE with a full-64 digest match and four gates PASS via the born-red HOLD choreography. Its carry-forward is GATE-POLARITY discipline: read each gate in ITS OWN direction — an exact Fraction/integer identity is a self-certifying theorem, a z-test is an AGREEMENT gate when the closed form is the null, and a deliberately-wrong model that must be REJECTED is a FALSIFIABILITY gate read at the opposite polarity. V234 leans on the same discipline: G1 is an exact looped-Fraction inclusion identity (the firsthand teeth — k/n regardless of arrival position), G2/G3 are Monte-Carlo/robustness AGREEMENT gates (|z|<3, χ² under critical), and G4 is a FALSIFIABILITY gate that rejects the unconditional-replace bug at max|z|=400.0 and the naive first-k model — opposite direction to G2/G3. It also carries the V222 grounding-accuracy discipline: the caveat honestly separates "firsthand-proven / off-page" (the k/n closed form, the sim, SEED, gate grids, and digest) from "cited on-page" (Algorithm R and the step-wise k/(i+1)→k/n induction), and flags that the literal string "k/n" is not on the page. Standing non-contiguity persists: landing V234 does not imply every lower verdict below the high-water is closed (V225, V137, V132, V126 remain open pulls).

## 💡 Session idea

The verifier pins the size-k reservoir inclusion probability at exactly k/n for Algorithm R (fixed reservoir size, equal weights). A cheap orthogonal follow-on (P-next) would pin the **weighted variant — Algorithm A-Res / A-ExpJ (Efraimidis–Spirakis)** — where each streamed item i carries a positive weight w_i and is kept with probability proportional to its weight, so the size-1 inclusion probability of item i is exactly w_i / Σ w_j (Algorithm R being the equal-weight w_i≡1 special case that recovers k/n → 1/n for k=1). Gate recipe: G1 an EXACT identity — for k=1 over a small disclosed weight grid, `Fraction(brute favorable, trials)` is replaced by an exact enumeration `Fraction(w_i, Σ w_j)` equal to the closed form (the A-Res key U^(1/w) ordering reduces to that probability); plus a FALSIFIABILITY leg that the *unweighted* k/n model is REJECTED once the weights are non-uniform (the two mechanisms must be distinguished, not conflated); plus a continuity leg that setting all w_i≡1 recovers THIS verdict's k/n (Algorithm R as the equal-weight limit). Grounding: the same "Reservoir sampling" page treats the weighted A-Res / A-ExpJ algorithms in its own section, so the pin stays honestly on-page for the statement and off-page for the sim, SEED, and digest.

