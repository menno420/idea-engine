# PROPOSAL 149 — coordinated omission: closed-loop latency measurement is blind to the tail it is meant to catch

> **Status:** `complete`
> 📊 Model: Claude Opus · high · idea/planning

Born-red HOLD: this card is the session's FIRST content commit (alongside the claim) and is born `in-progress` to hold the `substrate-gate` RED so the PR cannot merge before the deliberate `complete` flip. Flipping to `complete` (the deliberate LAST commit, after the verifier + idea doc + outbox P149 block + heartbeat) releases the merge-on-green backstop. The flip is taken once the verifier is committed, the disclosed results-dict sha256 reproduces EXACTLY across a deterministic double-run, the PROPOSAL 149 outbox block + heartbeat are written, and `python3 bootstrap.py check --strict` is green apart from this born-red card HOLD itself. A red gate AFTER the flip is a real defect, not the HOLD.

## Objective

Draft and land round-35 FLEET-slot PROPOSAL 149 — coordinated omission: a closed-loop / synchronous load generator (one that waits for each request to return before issuing the next) cannot send the requests it was scheduled to send during a service stall, so it silently OMITS the backlog those coordinated requests would have measured. The measured latency distribution is therefore biased LOW — p99/p99.9 can be understated by orders of magnitude — because the measurement method, not the system under test, sets the observed tail. Ship a markdown-first card, a committed stdlib-only deterministic verifier (SEED=20260717, ≥3σ /se gates, disclosed results-dict sha256), a deterministic double-run, an outbox block (P149 → V162, +13), and a heartbeat update. VERDICT 162 (P149 → V162, +13) is the next independent slice, NOT authored here.

## Constraints honored

- Round-35 FLEET slot (rotation fleet→venture→game→unrelated; round-35 opens at the FLEET slot with P149). Offset +13 (P149 → V162) — the verdict is NOT authored here. Offset authority = this repo's control/outbox.md P149 depends-ledger ("sim-lab consumes at +13 (PROPOSAL 149 -> VERDICT 162). Offset pinned; authority = this outbox per-block ledger + control/status.md (predecessor P148 -> V161).") + the control/status.md heartbeat (proposal high-water P149, verdict high-water V161); docs/current-state.md is a dated SEAT-DORMANT snapshot (ledger only through V059) and carries NO P149/V162 row.
- Digest posture: WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY (the P105…P148 family) with the P127+ TWIST (compact canonical hashed preimage, pretty indent=2 stdout dump, floats rounded 6 dp, no on-disk JSON). CONFIRMED against the script text: main() builds a results dict with NO results_sha256 key (every float rounded to 6 dp), computes payload=json.dumps(results, sort_keys=True, separators=(",",":")) then digest=sha256(payload), PRINTS the pretty json.dumps(results, indent=2, sort_keys=True) dump + `Results-JSON sha256: <digest>`, writes NO on-disk JSON, and asserts an in-process double-run byte-identity — so the disclosed results-dict sha256 12d3ce4fce9a4f1cc218be0ee6f5dbc945d42a5e45173a46044704524dbfab5d IS the sha256 of the compact canonical dict (the pretty stdout dump is a different serialization).
- Stdlib-only verifier (hashlib, json, math, random — no numpy/scipy); pinned SEED=20260717; /se z convention (z = mean/se); ordered ≥3σ gates. Exact gate ladder (z_gate=3.0, require z≥3, first-failing-gate short-circuits): G1 tail-blindness z = mean(log(open_p99/closed_p99))/se → G2 omitted-backlog z = mean(open_stall_count − closed_stall_count)/se → G3 correction-recovery z = mean(log(corrected_p99/closed_p99))/se; R=200 replicates, replicate i seeded random.Random(SEED+i). Model line family-level (PL-004), task class idea/planning.
- Outbox `## PROPOSAL 149 · … · status: sim-ready` block appended once (append-only, deduped) matching the P145…P148 skeleton (target / idea / question / done-when / depends / loop). DONE — `## PROPOSAL 149 · 2026-07-19T01:21:17Z · status: sim-ready` appended once at the END of control/outbox.md (no prior P149 block); P149 → V162 (+13) mapping cited, verdict NOT authored here.
- Status heartbeat: proposal high-water P148 → P149 (union-max, no regress); verdict high-water kept (this is a PROPOSAL slice, no verdict authored); routines line UNCHANGED (coordinator-bound). DONE — control/status.md rewritten preserving all field keys: proposal high-water P148 → P149; verdict high-water STAYS V161; routines line UNCHANGED (coordinator-bound); baton set to "round-34 COMPLETE (V158–V161); round-35 fleet-slot P149 DRAFTED (coordinated omission); next: V162 for P149; then round-35 venture-slot P150".
- Born-red first commit (card in-progress + claim) → verifier + idea doc → outbox + status → flip complete LAST.

## GROUNDING

Round-35 FLEET-slot of the fleet/cross-cutting proposal→verdict pipeline. Pinned world: SEED=20260717, ≥3σ /se gates, disclosed results-dict sha256. Verifier (firsthand, ground truth): https://github.com/menno420/idea-engine/blob/c49830ddd7defc2e9f104380961d2b48635b5f15/ideas/fleet/coordinated_omission.py@d276fb5047473a5bc569f8747c1dd7a04311bbc5 — committed @94ab183; git blob d276fb5047473a5bc569f8747c1dd7a04311bbc5, file sha256 6780d06a213ad27d218331447bb922898a1eb8aa716396bf343590c16eda762c, 323 lines / 12310 bytes, stdlib-only (hashlib, json, math, random). Pinned constants: SEED=20260717, R=200 replicates (replicate i draws random.Random(SEED+i)); λ=1000 → τ=1 ms; horizon T=1.0 s; base service Exp(mean μ_s=0.1 ms) so utilisation μ_s/τ=0.1 (low); n_stalls=6 of duration D=50 ms (D/τ=50 ≫ 1) at per-replicate non-overlapping offsets shared by both loops; server FROZEN during each stall; both loops consume the SAME pre-drawn service-time list (N_svc=12000); percentile q=0.99 (nearest-rank); z_gate=3.0. Closed-form / analytic anchor: a stall of duration D at inter-arrival τ buries ~D/τ requests the closed loop omits (here ~50 each × 6 ≈ 300), so open p99 ~ D while closed p99 ~ the fast between-stall path. External reference (reachable): coordinated omission — Gil Tene (HdrHistogram author), "How NOT to Measure Latency" / "Your Load Generator Is Probably Lying to You"; the CO-correction is HdrHistogram's recordValueWithExpectedInterval(value, expectedInterval). Grounding branch-point: idea-engine main; verifier + idea doc committed @94ab183 on branch claude/proposal-149-coordinated-omission.

## Probe questions

**1. Is the mechanism genuinely new against the fleet backlog?** Yes — it is a MEASUREMENT artifact (the load generator's pacing), NOT a queueing law about a real system property. A grep of ideas/ and control/ at HEAD for "coordinated omission" / "coordinated-omission" returned zero prior heads. The two nearest fleet heads both describe REAL waits: inspection-paradox-wait-inflation (2026-07-14) length-biases the sampled wait UPWARD by unbiased sampling of a real interval-length distribution (opposite sign, different actor); service-variance-wait-tax (2026-07-18, Pollaczek–Khinchine) is a queueing law where service-time variance inflates the TRUE mean wait. Coordinated omission changes NO true latency — the open-loop latencies ARE the truth; the artifact is that a closed-loop MEASUREMENT of that same truth omits the tail (biased DOWN by omission, not up by length-bias). First coordinated-omission / closed-loop-tail-blindness card in the fleet lane.

**2. Do all pre-registered gates PASS in order on the /se margin?** Yes — all three PASS in order at SEED=20260717: G1 tail-blindness mean log(open/closed p99)=+4.643332 z=+2505.733369; G2 omitted-backlog mean(open−closed stall count)=+294.635 z=+5148.278412; G3 correction-recovery mean log(corr/closed p99)=+4.408601 z=+2606.670555 — all ≫ 3.0; all_pass=true, first_failing_gate=null, exit 0.

**3. Does the digest reproduce EXACTLY and deterministically?** Yes — both the CLI double-run and the in-process double-run assertion produce results-dict sha256 12d3ce4fce9a4f1cc218be0ee6f5dbc945d42a5e45173a46044704524dbfab5d, byte-identical; every float rounded to 6 dp, replicate i seeded random.Random(SEED+i), no on-disk JSON, exit 0.

**4. Does the proposal advance the proposal high-water without regressing anything?** Yes — proposal high-water ADVANCES P148 → P149 (round-35 FLEET slot, union-max, no regress); verdict high-water STAYS V161 (this is a PROPOSAL slice — no verdict authored). Baton: round-34 COMPLETE (V158–V161); round-35 fleet-slot P149 DRAFTED (coordinated omission); next: V162 for P149; then round-35 venture-slot P150.

## Outcome

sim-ready. On the pinned world (SEED=20260717, R=200, λ=1000 / τ=1 ms, 6 stalls of D=50 ms, utilisation 0.1), the closed-loop harness reports p99 ≈ 0.47 ms while the true open-loop p99 is ≈ 48.6 ms — a ~104× understatement (mean_open_over_closed_p99 = 103.855766) produced by the pacing ALONE (both loops run the byte-identical server, stall schedule and service-time list). The open loop carries ~300 backlogged requests across the 6 stalls (~50 = D/τ each) where the closed loop carries ~6 (one frozen sample per stall, diff 294.635 at z ≈ 5148σ). All three ≥3σ gates PASS in order: G1 tail-blindness z=+2505.733369 / G2 omitted-backlog z=+5148.278412 / G3 correction-recovery z=+2606.670555; the HdrHistogram-style CO-correction lifts the measured p99 from 0.47 ms to 38.4 ms (recovery fraction 0.788774). all_pass=true, first_failing_gate=null, exit 0; results-dict sha256 12d3ce4fce9a4f1cc218be0ee6f5dbc945d42a5e45173a46044704524dbfab5d reproduced EXACT across a deterministic double-run. Transferable rule (Q-0264): a closed-loop / synchronous load generator cannot measure the tail it stalls in — its p99/p99.9 is set by the measurement method, not the system, and understates the truth by orders of magnitude during any stall; guard it by driving load open-loop (decouple issue time from completion time) or applying an HdrHistogram-style coordinated-omission correction. Shipped to sim-lab for VERDICT 162 (P149 → V162, +13).

## ⟲ Previous-session review

Newest OTHER complete session card: `.sessions/2026-07-18-verdict-157-hot-hand-selection.md` (VERDICT 157 mirror — hot-hand streak-selection bias, P144 → V157, clean APPROVE, digest 1c262684…b2dac3 EXACT, born-red HOLD released on the deliberate complete flip). Baton picked up: round-34 COMPLETE (V158–V161) — this slice OPENS round-35 at the FLEET slot with P149. Carry-forwards honored: the digest-posture classification is honored (WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY, P127+ compact-canonical twist — verified against the script text, not assumed); SEED pinned 20260717; born-red HOLD held from the first content commit until this deliberate complete flip (merge-on-green released here, no agent merge call); routines line UNCHANGED (coordinator-bound); proposal high-water advanced by UNION (P148 → P149), verdict high-water kept (V161).

## 💡 Session idea

round-35 venture-slot P150 is the next independent slice. A clean coordinated-omission FOLLOW-ON (named in the doc's Model basis, not authored here): a HIGH-BASE-UTILISATION variant — raise μ_s/τ so the between-stall path is no longer sub-millisecond and re-measure the open-vs-closed p99 gap; the prediction is the gap PERSISTS as long as D ≫ τ (the closed loop still omits the backlog) while the closed baseline rises, isolating that the understatement is driven by stall-omission, not the low base rate. Deduped: distinct from the FROZEN-stall head shipped here and from the two nearest fleet queueing-law heads (inspection-paradox-wait-inflation, service-variance-wait-tax).
