# PROPOSAL 198 — Weitzman's Pandora's Box: the optimal search order IGNORES expected rewards — open boxes by decreasing reservation value z_i (c_i = E[(R_i − z_i)^+]), and the index rule's value EXACTLY equals the brute-force optimum (round-47 VENTURE slot; P198 → V211, +13)

> **Status:** `complete`
> 📊 Model: Claude Opus · high · idea/planning
> heartbeat: 2026-07-20T05:10:39Z — born-red HOLD: this card is PROPOSAL 198's FIRST commit, born `in-progress` to hold the `substrate-gate` RED so the PR cannot merge before the deliberate `complete` flip. Flipping to `complete` (the LAST commit, after the doc + verifier + outbox block) releases the merge-on-green enabler. A red gate AFTER the flip is a real defect, not the HOLD.

## 💡 Session idea
The natural follow-on is the Olszewski–Weber (2015) generalization cited on the grounding page: the Pandora's rule stays optimal not just for `max_{i∈S} x_i − Σc_i` but for a whole class of payoff functionals. A distinct follow-up head (rule-survives-generalized-payoff, not a re-run of the base index identity) would pin one such generalized objective, prove the reservation-index order still equals the brute-force optimum under it, and mark the boundary where the rule finally breaks (recall costs / correlated boxes / discounting) — converting "the index rule is optimal" from a single-payoff fact into a characterized-class fact.

## ⟲ Previous-session review
Prior session landed round-47 FLEET-slot PROPOSAL 197 (Jackson product-form independence — coupled feedback M/M/1 queues carry non-Poisson internal flow yet factor EXACTLY into independent M/M/1 marginals) via idea-engine: doc + verifier `jackson-product-form-independence.py` (results-dict sha256 29d55fa57612782046831e792beb1584cb5c688bc181b6363d12774b56cae258), born-red HOLD released on the complete flip, targeting sim-lab VERDICT 210 (+13). This P198 slice serves the round-47 VENTURE slot (rotation fleet→venture→game→unrelated) at the VENTURE lane; proposal high-water advances P197→P198 (union-max, no regress), verdict high-water stays V209 (no regress).

## Objective
Author and land round-47 VENTURE-slot PROPOSAL 198 (Weitzman's Pandora's Box / reservation-value index rule) as sim-ready: idea doc + stdlib verifier + outbox block + this card, born-red HOLD released on the complete flip, landing on green via merge-on-green (zero agent merge calls). Target sim-lab VERDICT 211 (+13 offset).

## Constraints honored
- Offset +13 (P198 → V211), cited from the outbox per-block ledger + status.md (predecessor P197 → V210).
- Digest posture WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY: compact-canonical sha256 over the results dict IS the digest; no self field; exact `fractions.Fraction` in the gate math (only the surprise z-score touches float, ROUND_DP=10); no on-disk JSON; SEED=20260717; in-process double-run + separate cross-invocation byte-identical.
- Born-red first commit (this card `in-progress`) → doc + verifier → outbox block → flip `complete` LAST; enabler lands on green (merge-on-green), zero agent merge calls.
- External content grounding (Wikipedia "Search theory", byte-pinned raw-wikitext sha1), NOT a house self-reference. Grounding pin is a 40-hex wikitext sha1, not a 64-char sha256.

## Pinned world + gates
N=4 boxes, M=400 random small-rational instances per regime; box i opening cost c_i, reward R_i on distinct ints 0..10 with rational weights; keep the single best reward (outside option 0), no discounting. Four exact memoized evaluators: `reservation_value` (solve c=E[(R−z)^+] exact + assert), `optimal_value` (brute-force DP over ALL policies), `index_policy_value` (forced Weitzman rule), `greedy_mean_value` (forced open-highest-mean / stop-at-max-mean). Gates: **G1 EXACT** index_policy_value == optimal_value for every instance, both regimes (base_mismatch=0, shift_mismatch=0). **G2 SURPRISE** mean regret (optimal − greedy) > 0 at ≥3σ: mean_regret≈0.6668916165, z_regret=15.507320578, 289/400 strictly regretful (HIGH z = mean-greedy genuinely suboptimal). **G3 NON-MONOTONICITY** z-order ≠ mean-order 305/400, optimum opens lower-mean box first 153/400, declines higher-mean box 236/400; witness printed (box 0 mean 29/4 highest yet box 1 z=34/5 opened first, index=optimal=55/9 vs greedy=11/2). **G4 ROBUSTNESS** costs ×3: mismatch=0, mean_regret≈3.7351738463, z_regret=19.0496891702. all_pass=true.

## GROUNDING
English Wikipedia "Search theory" § reservation value oldid 1347561989 (2026-04-07), raw wikitext self-computed sha1 `1f24a2c787536d8aa6cfd57447678b1a7a8ccd13` (20419 bytes, matches MediaWiki's own revision sha1), fetched live 2026-07-20. Wikitext states the Pandora's box model (Weitzman 1979), the payoff `max_{i∈S} x_i − Σ_{i∈S} c_i`, and the Pandora's rule verbatim: "open the boxes by decreasing order of reservation value until the opened box that maximizes her payoff exceed highest reservation value of the remaining boxes." External content citation, not a house pin. Caveat: the defining equation c_i = E[(R_i − z_i)^+] is in the Weitzman 1979 primary source (cited on-page), not written in this revision; formulas live in `<math>` markup — disclosed in the doc.

## Disclosed digest
results-dict sha256 `a0cb19cc5574c17ea982580f1546a46ece84d9a32fbd06beb1e3290cacd1fcad` (full 64 hex), all_pass=true, exit 0, byte-identical across in-process double-run and separate cross-invocation.
