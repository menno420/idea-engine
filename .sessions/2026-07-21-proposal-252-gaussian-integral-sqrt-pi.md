# PROPOSAL 252 — The Gaussian integral ∫_{−∞}^{∞} e^{−x²} dx = √π: a self-contained pure-math closed form (the "unrelated / pure-math" slot). The irrational √π is verified through the DIMENSIONLESS even-moment ratios R_m = M_{2m}/M_0 = (2m−1)!!/2^m = (2m)!/(4^m·m!), which are exactly rational (√π cancels): the integration-by-parts recurrence M_{2m} = ((2m−1)/2)·M_{2m−2} and the closed form agree with 0 mismatches over m=0..12 in exact fractions.Fraction. A uniform-importance Monte-Carlo estimate of ∫_{−6}^{6} e^{−x²} dx agrees with √π·erf(6) at |z|=0.70; the dimensionless ratio is exactly scale-invariant (65 Fraction checks, 0 mismatches) and the full integral obeys √a·∫e^{−ax²}dx = √π across 6 (a,μ) configs (max |z|=1.54); the naive foil confusing it with the standard-normal normalizer ∫e^{−x²/2}dx = √(2π) ≈ 2.5066 is REJECTED on the same sample at |z_foil|=133.8.

> **Status:** in-progress

> **📊 Model:** Claude Opus · high · idea/planning

started: 2026-07-21T11:15:00Z

💓 Heartbeat: 2026-07-21T11:15:00Z — FLEET slice — new PROPOSAL 252 (pure-mathematics / analysis lane — the "unrelated / pure-math" slot) → sim-lab VERDICT 265 (+13 offset). Ships the sim-ready idea doc `ideas/fleet/gaussian-integral-sqrt-pi-2026-07-21.md` + firsthand verifier `ideas/fleet/verify_252_gaussian_integral_sqrt_pi.py` (stdlib only: json, hashlib, math, random, fractions, sys) + this born-red card + the `## PROPOSAL 252` outbox block in ONE first commit. Digest target `results_sha256 = f8d553495590f7e0dc52d702e6b3dbb813464b4b48e8bc3a159f140b1e88c0d8` (printed AND compared as a literal 64-char string, no truncation); reproduced byte-identical across an in-process double-run guard (`in_process_double_run: IDENTICAL`), a `--selfcheck` path, AND a separate re-invocation process. Verifier file sha256 `1f348d8524b2a4f6f662c880455662c0fe1daa00226a0fc24e76464f51dd5115`. SEED=20260717. Four pre-registered gates each read in its OWN direction: G1 EXACT (fractions.Fraction, m=0..12 — recurrence route R_m=∏(2j−1)/2 == closed form (2m)!/(4^m·m!) == (2m−1)!!/2^m, and (2m−1)!!·2^m·m! == (2m)!, checked=13, mismatches=0), G2 MC AGREEMENT (N=400000 uniform-importance draws, ∫_{−6}^{6} e^{−x²} dx estimate 1.776277 vs target √π·erf(6)=1.772454, z=0.7004<3), G3 INVARIANCE (exact scale-invariance of R_m across a∈{1,2,3,5,1/2}, 65 checks, 0 mismatches + MC scale/translation law √a·∫e^{−a(x−μ)²}dx=√π across 6 configs, max |z|=1.5375), G4 FALSIFIABILITY (opposite direction, SAME sample — the √(2π) standard-normal-normalizer foil REJECTED at z_foil=−133.7896, |z_foil|≫50, while √π accepted at z=0.7004). Grounding sha1-pinned to Wikipedia "Gaussian integral" oldid 1365252560 (`6d8b0d941ef6028c1331a63443a49c199f3c1c2d`, 22230 bytes; API sha1 == self-computed hashlib.sha1), split HONEST with disclosed seams (the closed form, the scale/translation form, and the even-moment closed form are QUOTED verbatim — so the firsthand contribution is the exact Fraction verification apparatus + MC + falsification; "moment" and "integration by parts" are grep 0 — my label + my recurrence route, which the page reaches by parameter differentiation instead; the √(2π) foil confusion is my construction). Born-red HOLD armed on this first card commit (in-progress).

⏳ Flip note (born-red): this card ships `> **Status:** in-progress` on its FIRST commit so the substrate born-red gate holds the idea-engine PR RED until the slice is reviewed. The coordinator (owner) flips it to `complete` as the deliberate LAST step, only after confirming the outbox block + `python3 bootstrap.py check --strict` reds ONLY on this HOLD. That flip clears the HOLD and releases merge-on-green. NO merge API calls from this session; NO status.md heartbeat edit; NO `## VERDICT` block; NO verdict / proposal high-water advance from this worker. The gate battery is GREEN (all four gates PASS, determinism holds three ways) — the red is the born-red HOLD alone, not a failing gate.

## What this proposal does

Adds a fleet PROPOSAL establishing the **Gaussian (Euler–Poisson) integral** ∫_{−∞}^{∞} e^{−x²} dx = √π as a self-contained, exactly-verifiable pure-math closed form — the "unrelated / pure-math" slot, orthogonal to every fleet, venture, and game head. Because √π is irrational, the **exact** teeth come from the **dimensionless even-moment ratio** R_m = M_{2m}/M_0 = (2m−1)!!/2^m = (2m)!/(4^m·m!), which is exactly rational (√π cancels) and satisfies the integration-by-parts recurrence M_{2m} = ((2m−1)/2)·M_{2m−2}. The verifier confirms this in exact `fractions.Fraction` (0 mismatches, m=0..12), estimates √π by Monte-Carlo integration, exhibits the exact scale-invariance of R_m plus the `√a·∫e^{−ax²}dx = √π` scale/translation law, and **falsifies** the single most plausible confusion — that ∫e^{−x²}dx equals the standard-normal normalizer √(2π). **Distinctness:** a pure-analysis closed form and its exact rational moment structure — no lane, no cash-flow, no game value, no Markov chain. `gaussian integral` / `sqrt(pi)` / `wallis` / `e^{-x` are grep-0 across both repos; orthogonal to random-walk return (P228), coprime density → 6/π² (P232, which uses ζ(2) — this uses NO zeta), Burnside (P236), non-crossing Catalan (P240), Buffon (P244), Cantor set (P248), derangements (P229), Bulgarian solitaire (P251).

## Method

- Firsthand verifier, stdlib-only (`json`, `hashlib`, `math`, `random`, `fractions`, `sys`).
- Digest posture: WHOLE-DICT — the canonical results dict's own sha256 IS the digest; `main()` builds the results twice in-process and asserts canonical-JSON byte-equality before printing the digest and exiting 0. Target `results_sha256` full-64 `f8d553495590f7e0dc52d702e6b3dbb813464b4b48e8bc3a159f140b1e88c0d8` (literal 64-char compare, no truncation); reproduced byte-identical across an in-process double-run guard, a `--selfcheck` path, AND a separate re-invocation; SEED=20260717. Verifier file sha256 `1f348d8524b2a4f6f662c880455662c0fe1daa00226a0fc24e76464f51dd5115`.
- Exact backbone (`fractions.Fraction`, no floats): `moment_ratio_recurrence(m, a)` = ∏(2j−1)/(2a), `moment_ratio_closed_form(m)` = (2m)!/(4^m·m!), `double_factorial_odd(m)` = (2m−1)!!; the dimensionless ratio is scale-invariant, so √π never enters the exact arithmetic. Monte-Carlo: uniform importance sampling `X ~ U[μ−hw, μ+hw]`, estimator `2·hw·e^{−a(X−μ)²}`; iid draws → plain iid SE (no batch means). `random.seed(SEED)` re-seeded at the START of each MC gate.
- Gates (each read in ITS OWN direction, against the proposal's OWN criteria):
  - **G1 EXACT** — m=0..12, three routes agree exactly + (2m−1)!!·2^m·m! == (2m)!; `checked=13`, `mismatches=0`. PASS.
  - **G2 MC AGREEMENT** — N=400000, ∫_{−6}^{6} e^{−x²} dx estimate `1.776277` (SE `0.005459`) vs target √π·erf(6)=`1.772454`, `z=0.7004 < 3` [Z_ACCEPT=3.0]. PASS.
  - **G3 INVARIANCE** — (a) exact scale-invariance of R_m across a∈{1,2,3,5,1/2}, `exact_checked=65`, `exact_mismatches=0`; (b) MC scale/translation law √a·∫e^{−a(x−μ)²}dx=√π across 6 configs, `max |z|=1.5375 < 3`. PASS.
  - **G4 FALSIFIABILITY** — OPPOSITE direction, SAME sample: the √(2π) foil (`2.506628`) REJECTED at `z_foil=−133.7896`, `|z_foil| ≫ 50` [Z_REJECT=50.0], while √π accepted at `z=0.7004`. PASS.
  - `all_pass = true`, `first_failing_gate = null`, decision `PASS`.
- Independent cross-check (own throwaway derivation): integration by parts on ∫x^{2m}e^{−x²}dx with u=x^{2m−1}, dv=x·e^{−x²}dx gives the boundary term [−½ x^{2m−1} e^{−x²}]_{−∞}^{∞}=0 and ∫x^{2m}e^{−x²}dx = ((2m−1)/2)∫x^{2m−2}e^{−x²}dx — the exact recurrence G1 uses; the page reaches the same closed form by ∂ⁿ/∂αⁿ under the integral, an independent route, matching R_m=(2m−1)!!/2^m.
- Grounding byte-pinned to the MediaWiki API wikitext of the revision; local raw-bytes `hashlib.sha1` and the API revision sha1 match.

## Grounding — honest scope

External, sha1-pinned: Wikipedia "Gaussian integral" oldid 1365252560, wikitext (22230 bytes) sha1 `6d8b0d941ef6028c1331a63443a49c199f3c1c2d` (raw local sha1 == API revision sha1), fetched 2026-07-21. **QUOTED** (grep > 0): the core closed form `\int_{-\infty}^\infty e^{-x^2}\,dx = \sqrt{\pi}` (grep `\sqrt{\pi}`=8); the names "Gaussian integral" / "Euler–Poisson integral" / "Carl Friedrich Gauss" (Euler=2, Poisson=2, Gauss=20), de Moivre 1733 / Gauss 1809; the scale+translation form `\int_{-\infty}^{\infty} e^{-a(x+b)^2}\,dx = \sqrt{\frac{\pi}{a}}` (grounds G3); the even-moment closed form `\int_{-\infty}^\infty x^{2n} e^{-\alpha x^2}\,dx = \sqrt{\frac{\pi}{\alpha}}\frac{(2n-1)!!}{(2\alpha)^n}` (grep `(2n-1)!!`=3, `double factorial`=4) — exactly the G1/G3 moment identity; the constant `\sqrt{2\pi}` appears (grep=2, normal-distribution/Fresnel-adjacent). **DERIVED** firsthand (grep 0): the word "moment" (my label; the integral itself is quoted); "integration by parts" as the recurrence route (grep 0 — the page uses parameter differentiation, an independent route to the same closed form); the dimensionless scale-invariant ratio framing `a^m·R_m(a)=(2m−1)!!/2^m`; the exact `Fraction` zero-mismatch identity across three routes; the uniform-importance Monte-Carlo estimator and every z-value; the "√π confused with √(2π)" foil framing; SEED; digest. **Disclosed seams:** the article is RICH (22230 bytes) — the closed form, the scale/translation form, and the even-moment closed form are QUOTED verbatim with a full polar-coordinates proof, so nothing is oversold as a novel theorem; the firsthand contribution is the exact `Fraction` verification apparatus + the MC agreement + the invariance legs + the √(2π) falsification + every z-value + the digest. Nothing oversold as novel: the Gaussian integral is textbook (de Moivre 1733; Gauss 1809; the pinned page), cited as such.

## Merge posture

CI substrate-gate + the `enable-auto-merge` landing automation squash-merge the green PR once the born-red HOLD is released; **NO merge API calls are made from this session**. This slice ships the sim-ready idea doc + the firsthand verifier + this born-red `.sessions/` card + the `## PROPOSAL 252` outbox block in ONE first commit. NO `control/status.md` edit, NO `## VERDICT` block, NO verdict / proposal high-water advance from this worker — the proposal high-water ADVANCE (P251 → P252) is the coordinator's call in a later isolated step. The PR opens READY; the in-progress card holds it RED until the owner reviews and flips. The gate battery itself is GREEN — the only red is the born-red HOLD.

## ⟲ Previous-session review

Previous-session review: PROPOSAL 251 (Bulgarian solitaire — the deterministic self-map on integer partitions, triangular-total convergence to the staircase δ_k within EXACTLY k²−k steps, non-triangular totals orbiting in a period>1 cycle; the "non-triangular n also settles" foil rejected at |z|≈4·10⁴; P251 → V264) landed sim-ready with a full-64 digest, four gates each in its own direction, and a clean quoted-vs-derived split (the move + triangular-convergence + the k²−k "or fewer" upper bound QUOTED, the exact tightness DERIVED by enumeration). Its carry-forward is GATE-POLARITY discipline: read each gate in ITS OWN direction — here G1 is a self-certifying EXACT rational identity (three moment-ratio routes agree in `Fraction`, 0 mismatches), G2 is an AGREEMENT gate against the honest analytic target √π·erf(6) (|z|=0.70<3), G3 is an INVARIANCE gate read as exactness (scale-invariance of the dimensionless ratio, 0 mismatches) plus a small-|z| MC scale/translation law, and G4 is a FALSIFIABILITY gate at the OPPOSITE polarity (the √(2π) confusion rejected at |z|=133.8 on the same sample). It also carries the grounding-accuracy + honest-seam discipline: 251 disclosed the page's k²−k is an "or fewer" bound while the tightness is the firsthand strengthening; this slice discloses the article is RICH (the closed form + scale form + moment closed form are QUOTED verbatim), so the firsthand contribution is the exact `Fraction` verification apparatus, not a novel theorem — and discloses that "moment"/"integration by parts" are grep 0 (my label + my recurrence route, which the page reaches by parameter differentiation) and that the √(2π) foil is my construction. Standing non-contiguity persists: landing P252 does not imply every lower proposal/verdict below the high-water is closed — known gaps and open pulls remain the coordinator's ledger, not this worker's.

## 💡 Session idea

The verifier pins the classical `e^{−x²}` Gaussian integral and its even moments over ℝ. A cheap orthogonal follow-on (P-next) would pin the **multivariate** Gaussian integral already named on the SAME rich page: ∫_{ℝⁿ} e^{−½ xᵀA x} dⁿx = √((2π)ⁿ/det A) for symmetric positive-definite A — the determinant / eigenvalue closed form. That keeps the exact-rational-backbone recipe (G1 becomes an EXACT identity on a small **rational** SPD matrix A: verify √(det A · result²/(2π)ⁿ) collapses to 1 via `Fraction` on the eigen/Cholesky factorization, 0 residual), G2 an AGREEMENT gate estimating the n=2 integral by importance sampling, G3 an INVARIANCE gate under an orthogonal change of basis (the value depends only on det A, not the rotation — an exact `Fraction` check across several rational rotations), and a FALSIFIABILITY leg that "the value depends on tr A rather than det A" is rejected at large |z|. Grounding stays honestly on the same pinned revision (which states the multivariate form + the det-A dependence), off-page for the exact rational SPD instance, the importance-sampling path, SEED, and digest — with the rotation-invariance (det, not trace) distinction as the headline honest seam, a clean orthogonal-invariance contrast to this slice's scale-invariance.
