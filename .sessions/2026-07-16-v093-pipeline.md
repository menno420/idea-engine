# Session — v093 pipeline: VERDICT 093 (P080, cycle-following coupling lever) via sim-lab → outbox V093 mirror + heartbeat → close-out flip

> **Status:** `complete`
> **Model/time:** fable · 2026-07-16T05:49:59Z → 2026-07-16T06:28:20Z (Ideas Lab
> worker slices — consume the v092 close-out heartbeat's top baton at HEAD
> `8ad2bb4`: run PROPOSAL 080 (the round-16 unrelated closer — correlation
> design under shared randomness, the cycle-following coupling lever)
> through sim-lab as VERDICT 093, then mirror the verdict into this repo's
> outbox and close out.)

- **📊 Model:** fable-class · high · review/verify

*(card born in-progress at 2026-07-16T05:49:59Z as the designed session-gate
hold; flipped complete in this PR's final commit at 2026-07-16T06:28:20Z,
after the pipeline work landed. The task-class is review/verify, not the
predecessor's idea/planning: this session drafted no proposal — its core
work was the verdict run and the ledger close-out; P081 drafting is
deliberately the successor's first slice.)*

## Scope

Run the V093 pipeline under standing owner ORDER 003/004 with the EAP
extension to 2026-07-21 (ORDER 015) as the standing frame:

1. **VERDICT 093** — run PROPOSAL 080 (cycle-following coupling lever,
   `ideas/fleet/cycle-following-coupling-lever-2026-07-16.md`, seeds
   20261714–717 with aux 20261717 never read) through sim-lab and register
   the verdict. The sim-lab side lands on its own sim-lab branch/PR; this
   card covers only the idea-engine slice.
2. **Claims hygiene (first commit)** — prune the terminal v092 claim
   (`control/claims/2026-07-16-v092-pipeline.md`): its PR #446 merged @
   main `8ad2bb4`, and the v092 close-out heartbeat's baton orders the
   prune. This session's claim lands in the same first commit.
3. **Close-out (this branch)** — V093 mirror appended to
   `control/outbox.md` (append-only, real `date -u`, compact V092-style
   mirror block; proposal blocks never edited), session close-out heartbeat
   on `control/status.md`, and this card's flip as the deliberate last
   step.

Also carried: the `.substrate/guard-fires.jsonl` advisory-telemetry delta
(2 lines) from this session's close-out verify runs — the checker's own
instruction is to commit the delta with the session, not revert it (rides
the flip commit).

## Results (all three slices)

1. **VERDICT 093 — DONE (sim-lab).** P080 ran hermetically at sim-lab:
   PR #164, merged @ sim-lab main `ce5d1a6`
   (ce5d1a68f579434ea238be4fe7cb696fed6eb1d8, merged_by
   github-actions[bot] 2026-07-16T06:19:58Z). Verdict **REJECT** on all
   four registered clauses (R1 lever, R2 marginal invariance, R3
   concentration + floor, R4 repairs) — "with a shared randomness source
   the joint success rate is a design variable the marginals do not
   constrain" — 61/61 self-checks exit 0, twin evaluators REJECT/REJECT,
   zero anomalies (54 matched / 0 mismatched / 6 honest vacancies),
   byte-identical double run, both Arm-R preview triples reproduced
   exactly (seeds 20261714–715; aux 20261717 never read). Full record:
   sim-lab `sims/verdict-093-cycle-following-coupling-lever/REPORT.md`
   @ 6982d81 + the VERDICT 093 outbox block there (appended
   2026-07-16T06:14:30Z). Offset +13 held (P080 → V093, seventeenth row,
   unbroken).
2. **Claims hygiene — DONE (first commit 637438e).** Terminal v092 claim
   pruned per the baton; this session's claim
   `control/claims/2026-07-16-v093-pipeline.md` landed in the same
   commit; born-red card opened PR #447 READY.
3. **Close-out — DONE (this branch, PR #447).** (a) V093 mirrored into
   this repo's outbox (compact V092-grammar mirror block appended
   2026-07-16T06:25:25Z; the P080 proposal block deliberately NOT edited —
   append-only, P058–P079 precedent; the mirror IS PROPOSAL 080's
   status-flip record) — commit b533173; (b) **heartbeat**:
   control/status.md overwritten in the standing grammar — wakes line
   carried in substance verbatim (failsafe cron
   trig_01FYrWqjWeGVUTLg51arsHFr LIVE, owner rebind decision pending),
   verify reported honestly (pre-flip strict check exit 1, only red = this
   card's designed hold), pipeline state honest: DRY post-V093, P081
   drafting is the successor's first pull; seed baton next free 20261718 —
   commit f8d545e; (c) this flip + the guard-fires telemetry delta.

## Constraints honored

- control/inbox.md untouched (manager-written); newest ORDER at 8ad2bb4 is
  015 (EAP through 2026-07-21), orders 001–014 closed.
- control/outbox.md append-only: `VERDICT 093` dedupe-grepped clean
  idea-engine-side before the append (0 hits); no prior entry edited;
  PROPOSAL 080's own status line untouched per the V092-block precedent.
- sim-lab read-only from this seat's slices: the VERDICT 093 run was
  sim-lab's own session on its own branch/PR; this seat only read its
  merged record at origin/main ce5d1a6 to quote numbers verbatim.
- No merge actions from this seat: commit + push + report only; landing is
  the repo's CI-side auto-merge-enabler on green after this flip.
- No triggers/routines armed, deleted, or audited; no send_later. The
  failsafe cron stays as the coordinator close-out left it.
- Timestamps real `date -u`; model recorded family-level only.

## 💡 Session idea

**The V092 mirror-block template survived its first reuse with exactly one
field that had to change for a structural reason rather than new values:
the consumer clause.** The v092 card's 💡 flagged the grammar GAP (no
committed rule says whether verdicts get mirrored here) and offered its
V092 block as a candidate template. This session is the first reuse data
point, and the result is sharp: header / idea / verdict / offset / report /
measured mapped field-for-field onto V093 with only values swapped — but
the target line's consumer sentence has no stable form. V092 had a lane
CONSUMER (superbot-idle); V093's P080 is a fleet-external head with NONE
(deliverable = the rotation lane's sixteenth-domain coverage row + the
transferable joint-risk correction; routing the manager's per Q-0260).
If the manager registers the template (the v092 card's routing ask, still
open), the consumer clause should be a typed alternative —
`consumer: <lane>` vs `consumer: none (fleet-external; deliverable = …)` —
not prose re-invented per head type; fleet-external heads recur every
rotation round by design, so the "none" arm is a quarter of all mirrors,
not an edge case. *(Deduped against recent cards: the v092 card's 💡 is
the grammar gap itself; the #444/P079 card's is push+flip atomicity; the
#445 card's is heartbeat-recorded live conclusions; the P078 card's is the
same-UTC-day-roll corner — none carry the first-reuse finding or the
consumer-clause instability.)*

## ⟲ Previous-session review

Previous session: the **v092 pipeline** (PR #446, merged @ main
`8ad2bb4`). Its baton was accurate on every point this session consumed:
(1) P080 was genuinely sim-ready — VERDICT 093 re-derived every registered
numeral from scratch with zero trust and matched all of them (54
compared-and-matched, 0 mismatched, 6 honest vacancies; both Arm-R preview
triples exact under the registered draw-order grammar), so the drafter's
V084 no-derived-literals discipline again bought a zero-anomaly verdict;
(2) the claim-prune order was correct (the v092 claim was terminal and
pruned cleanly in 637438e, no collision); (3) the seed ledger was exact
(P080's 20261714–717 collision-free, next free 20261718 confirmed); (4)
the offset row it predicted (P080 → V093, seventeenth) held. One
carried-forward residual its 💡 anticipated: the verdict-mirror grammar
question remains unrouted — this session reused its candidate template
and logged the one unstable field (see 💡 above) rather than re-opening
the ask.
