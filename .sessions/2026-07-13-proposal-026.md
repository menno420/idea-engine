# Session — PROPOSAL 026: round-3 breadth budget under the q99 bar (venture rotation, round 3, trading half)

> **Status:** `complete`
> **Model/time:** fable · 2026-07-13T05:36:14Z (Ideas Lab worker slice — draft the
> ORDER 004 rule-3 VENTURE round-3 head: round 1 took the books half (P018 → V020
> null), round 2 the trading half (P022 → V024 null); round 3 stays on the trading
> half DELIBERATELY — V024's own recommendation flags its burden-bar q99(N) table as
> "round-3 pre-registration default material", the lane's decision window is open
> (no round 3 registered per P022's committed drafting pin @ lane `7f7ca07`), and
> the bar without its power side is half a tool; card born in-progress as the
> designed gate hold, flips complete in this PR's final commit once the 💡 slot
> resolves)

**📊 Model:** fable · content + outbox proposal only (idea file, card, index row,
outbox append; no control/status.md or control/inbox.md writes; no checker or
script changes)

## Scope

Draft a genuinely new sim-shaped idea under standing owner ORDER 003 (continuous
pipeline) + ORDER 004 rule 3's rotation — the round-3 VENTURE slot. VERDICT 024
(P022's null) priced the SIZE of the trading lane's selection test: the null q99 of
the best-of-N Sharpe margin, the bar that rises as ORDER 033's "more strategies,
more stocks, more indicators" multiplies the config count (matched-arm q99: N=6
0.399 → N=96 0.619). What nobody has priced is the POWER side: does expanding the
round-3 grid past that rising bar buy any additional TRUE discoveries, or does the
bar eat the margin? The head: a pre-registered planted-signal power sweep on V024's
own panel machinery — persistent cross-sectional drift dispersion (strength κ ×
persistence horizon E) planted into the matched-drift arm, the lane's R3 rule
verbatim, shortlist = configs clearing the in-run null q99(N) bar, measuring
true-keep yield Y per design grid N ∈ {6, 10, 24, 96} (G10 = the targeted
short-horizon expansion) — REJECT (blanket expansion never pays) checked first.
V024's committed per-cell q99 values ride as chained cross-verdict anchors (the
P023 precedent) and its measured axis findings (vol INERT, drift binding) shrink
this grid by citation. Written up as
`ideas/trading-strategy/round3-breadth-budget-q99-power-2026-07-13.md` (probed
single-pass this slice, sim-ready) and appended to `control/outbox.md` as
PROPOSAL 026. Fresh PRNG seeds 20260748–51 allocated strictly above P025's
high-water 20260747 (seed-registry hygiene, seventh consecutive slice). Dedup swept
tree-wide (`rg -i -g '!bootstrap.py' -g '!.substrate' 'power analysis|true
discovery|planted (edge|signal)|breadth budget|false discovery|q99 bar'`) — hits
only V024's own tally lines; no proposal, idea file, or card asks the power/coverage
question, and no sim-lab verdict V001–V027 (local clone @ `5e356ed`) plants a true
signal in any selection sim.

## 💡 Session idea

**Machine-checkable anchor pins for chained verdicts.** Chained-anchor sims —
P023 (parent V022's near-miss cell), this head (six 4-decimal q99 values from
V024's results.json) — copy the parent's committed numbers into their fixture
JSON BY HAND, and the failure mode is already on the record: V024's own REPORT
discloses "three transcription slips in the committed hand-pin DERIVATIONS"
caught only because a human re-derived them, and a slipped anchor constant is
worse than none (the gate then validates against a wrong number with full
confidence). The slice: the fixture grammar gains an optional `anchors:` block
where each anchor constant carries its machine address — parent file path +
JSON pointer (e.g. `sims/verdict-024-keep-margins-noise/results.json #
main_leg.cells[vol=IID,sbh=1.15,rho=0.3].rates."0.0010".q."6".q99`) — and the
verdict runner's self-check phase re-reads the parent's committed file at that
address and asserts byte-equality with the quoted constant before any leg runs.
Still fully hermetic (one LOCAL read of the sim-lab tree's own committed
history — the same in-repo evidence class the P023/P026 drafting already
cites), zero new tooling beyond ~15 lines in the runner's fixture loader, and
it converts anchor transcription from trust to verification for every future
chained head. **Dedup** (this slice, `rg -i 'anchor.*(pointer|address|path)|
json.pointer|transcription' -g '!bootstrap.py' -g '!.substrate'` over ideas/ +
.sessions/ + control/ + the sim-lab clone): V024's slip disclosure and P023's
hand-quoted anchor constants are the evidence; NOTHING implements anchor
verification anywhere in either tree. Distinct from the proposal itself (which
USES hand-quoted anchors under a ±0.05 statistical tolerance — this rule makes
the QUOTING step exact, orthogonal to the MC tolerance on reproduction) and
from P025's 💡 claims-merged rule (git-fact pruning; different surface, same
"replace trust with a mechanical read" family).

## ⟲ Previous-session review

Newest predecessor card (`.sessions/2026-07-13-proposal-025.md`, worker slice
~04:45Z): closed clean at `a123fda` — the fleet-backlogs round-3 head (claim-expiry
horizons), which already carries its verdict: V027 REJECT (silence is the wrong
signal; the lease-renewal slice routed), the fastest proposal→verdict turnaround of
the night. Two of its habits are consumed here: **seed-registry hygiene** (this
slice draws 20260748–51 strictly above its 20260747 high-water — seventh
consecutive card on the discipline) and its `## Model basis` dogfood (n=3 here: this
head's number rides on the planted epoch-dispersion family, and the idea file names
it plus the single most-likely-to-flip alternative — the lane's REAL persistence
structure, which is exactly the NULL-case live probe). One honest observation its 💡
must inherit: the n=6 claim litter P025 measured as its `claims-merged` rule's
day-one catch was swept by hand hours later (the morning-tally PR pruned all six;
`control/claims/` holds only README.md at HEAD `3b9a728`) — the rule remains
unbuilt, and its value now rests on the next accumulation, which the doctrine
guarantees. The **rotation-position treadmill** extends to n=6: this card again
re-derives its slot (venture round 3, after P022 closed venture round 2) from
predecessor prose rather than a machine ledger; P021's 💡 stays measured-and-unbuilt.
