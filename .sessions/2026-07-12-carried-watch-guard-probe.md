# Session — single-pass probe: fleet carried-watch verdict-inheritance guard

> **Status:** `complete`

- **📊 Model:** fable-5 · single-pass battery (no panel triggers) · docs-only
  probe slice (one probe append + state flip + index re-badge + card; no code)

**Section-collision flag (dispatch boundary — no claim file):** this slice is
barred from `control/`, so no `control/claims/` entry exists; this card's
born-red first commit carried the `ideas/fleet/` collision flag per the
PR #222/#225/#243/#244/#247 workflow convention. Scope: ONE probe —
`ideas/fleet/carried-watch-verdict-inheritance-guard-2026-07-12.md` (probe
report append + state flip) plus the fleet README index row re-badge and this
card.

## What this session did

Single-pass probe (battery v0, no panel triggers) of
`ideas/fleet/carried-watch-verdict-inheritance-guard-2026-07-12.md` — the
#247 capture (PR #244 groom's one unrouted candidate), source bullet at
websites `docs/ideas/backlog.md` line 158 @ blob `e14bb15`. Process-class
head: a carried heartbeat watch must carry its OWN `verified <ISO>` evidence
stamp + `per <pointer>` so "re-verified this wake" and "copied N times since
anyone looked" become distinguishable — the temporal sibling of merge-hold's
spatial finding.

Verify-first, live: **source re-pinned byte-exact** (fetch hashed to blob
`e14bb15408b1f45de14eae72efe990024f0e548c` by `git hash-object`,
2026-07-12T03:49Z). **Kit-side commit-pinned** — substrate-kit live HEAD
`8a544a63d9b98bab41ac3ba0e31f8863216b7582` by `git ls-remote` (the git
transport is open; anonymous `api.github.com` 403 captured live, the known
wall — so no reported-not-verified fallback was needed): the planted-contract
template `src/engine/templates/control-README.md.tmpl` at that commit (blob
`71c9ce4`) prescribes ZERO watch grammar and no per-line freshness token
(sole "verified" = the OWNER-ACTION `VERIFIED-NEEDED` field), and kit
`docs/decisions.md` (blob `f9b5d52`) has no watch/stamp decision — nearest is
D-0009's registry-layer "last-seen = the evidence date; staleness reads as
dark". **Kill test NOT triggered on either leg** (the other leg:
heartbeat-ladder-field's regeneration-from-source moots verbatim carry only
at the manager ROSTER layer, not status watches).

The probe's decisive specimen check, measured this slice: this repo's own
`control/status.md` @ main `6d40f6f` carries **14 inherited watch/flag
entries** (ten "MANAGER SWEEP FLAGS carried unchanged from fc0bab6" + four
"SWEEP NOTES preserved", line 10) and **zero carry a `verified <ISO>` stamp**;
the fc0bab6 carry-by-pointer is now SEVEN overwrites deep (#220→#249, `git
log --follow control/status.md`); the two "from #157" watches crossed the
a9b41f6→fc0bab6 coordinator GENERATION HANDOFF; four further "Carried
unchanged" mint-exclusion items are bare names with no justifying pointer at
all.

Verdict: **parked(routed — kit/manager layer)** — fifth-touch RIDER on the
merge-hold-routed substrate-kit slice (#243's four touches: its touch (3) is
the same planted `control/README.md` this head's grammar paragraph lands in,
its touch (2) the same kit checker engine this head's advisory joins):
carried-watch grammar `WATCH: <claim> · verified <ISO> · per <pointer>` +
report-only `--watch-freshness` advisory (always exit 0); /fleet badge = a
manager rider. Nothing sim-shaped — a deterministic lint over one file;
sending it to sim-lab would be asking the simulator to confirm a grep. No
outbox proposal; routing goes via the manager sweep (this slice cannot write
`control/`).

**Manager-sweep flag (rides this card + the idea file):** the fifth-touch
BUNDLING question — this head's grammar + checker halves edit the SAME kit
surfaces as the merge-hold four-touch slice; recommend riding it as a fifth
touch, and if the manager judges that slice saturated, this splits cleanly
into its own kit slice (grammar paragraph + advisory are self-contained).
Manager decides at bundling; the probe only names the seam.

## Close-out

**Evidence:**

- ideas touched (2): `ideas/fleet/carried-watch-verdict-inheritance-guard-2026-07-12.md`
  (state flip captured→parked(routed — kit/manager layer…) + probe report v0
  append), `ideas/fleet/README.md` (index row re-badge, state-echo family
  match)
- sessions touched (1): `.sessions/2026-07-12-carried-watch-guard-probe.md`
- code touched: none · control touched: none (dispatch boundary; READ-ONLY
  reads of `control/status.md` — the specimen — and `control/README.md` —
  the planted contract; the manager-sweep flag rides the idea file + this
  card, not a heartbeat line)
- git: branch `probe/carried-watch-verdict-inheritance-guard` off main
  `6d40f6f` (prefix per `automerge.branch_patterns` + the merge-hold probe
  precedent), born-red card first commit `860774d`, probe commit `7176d44`,
  this close-out commit flips the card complete; draft PR flipped ready on
  green per dispatch — never merged by this slice, auto-merge never armed by
  this slice. The kit auto-draft `.sessions/2026-07-12-session.md` left
  untracked per the #243/#247 merged pattern (each slice's own named card
  satisfies the gate).
- verify: `python3 bootstrap.py check --strict` and
  `python3 scripts/preflight.py` run before push (strict red pre-final-commit
  was the designed born-red hold only; both exit 0 on the final tree).

**Judgment (the half only the session knows):**

- Decisions made: no D-entry — probe verdict only. One counting rule,
  declared: "inherited watch/flag entries" = the heartbeat's two EXPLICIT
  carry blocks (ten fc0bab6 flags + four preserved SWEEP NOTES = 14); the
  four bare "Carried unchanged" mint-exclusion names were tallied separately
  because they lack even a carry pointer — the count understates rather than
  inflates the precondition.
- Next session should know: the routed slice's spec lives in the probe's Q8
  (three kit touches: grammar paragraph + `--watch-freshness` advisory +
  /fleet-badge template note; the per-repo preflight shim was priced and
  SKIPPED — kit territory pre-adoption). The real cost is Q4(d): watch-class
  line IDENTIFICATION — today's watches are free prose inside two giant
  heartbeat lines, so the grammar change (one-watch-per-line or a `watch:`
  prefix), not the checker, is what changes coordinator writing habits.

## 💡 Session idea

Two probes in two days (merge-hold, this one) budgeted for the "blob by
hash-object, commit unpinned" honesty fallback because anonymous
`api.github.com` 403s — but the GIT transport is open, and
`git ls-remote <repo> <ref>` + a raw fetch AT the returned SHA turns any
upstream citation into a commit-pinned one for two cheap commands (this probe
did it live: kit HEAD `8a544a6`, template blob byte-identical at the pinned
commit). Encode the recipe as the named pin convention in the probe method
(README § probe battery, or the check_harvest pin family's docs): NEVER
report commit-unpinned without trying ls-remote first — the fallback wording
should be reserved for surfaces the git transport also walls. Dedup:
`check_harvest` pins cover harvest entries, `docs/CAPABILITIES.md` documents
the api wall; neither names the ls-remote+raw-at-SHA pin recipe for
probe-time upstream cites.

## ⟲ Previous-session review

The #247 capture card's handoff held up as a probe brief: its three named
probe questions (kit template grammar at live HEAD, checker home, /fleet
badge price) were exactly the three things worth verifying, and its "kit
template is the actual carrier surface" routing read confirmed against the
live tree. Two adoptions from the merge-hold probe card (#243): the born-red
collision-flag ceremony verbatim, and its ls-remote transport — upgraded
here from HEAD-verify to full commit-pinning of the kit-side grounding. One
refinement on the capture: its state-line relations block made the dedup
sweep nearly free — the eight adjacent heads were already catalogued, so
probe-time Q7 was verification, not discovery.

## Handoff → next wake

Facts for the coordinator heartbeat (NOT written here — `control/` is
coordinator-only): the #247 capture is now PROBED —
`carried-watch-verdict-inheritance-guard` parked(routed — kit/manager layer:
fifth-touch rider on the merge-hold substrate-kit slice; /fleet badge =
manager rider); manager-sweep flag = the fifth-touch bundling question
(above); the fleet index row re-badged; no outbox proposal, nothing
sim-shaped; specimen numbers for the sweep: 14/14 unstamped @ `6d40f6f`,
fc0bab6 carry 7 overwrites deep, #157 watches crossed the generation
handoff. This head was the PR #244 groom's last open disposition item —
that chain is closed.
