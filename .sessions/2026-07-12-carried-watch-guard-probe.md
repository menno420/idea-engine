# Session — single-pass probe: fleet carried-watch verdict-inheritance guard

> **Status:** `in-progress`

- **📊 Model:** fable-5 · single-pass battery (no panel triggers) · docs-only
  probe slice (one probe append + state flip + index re-badge + card; no code)

**Section-collision flag (dispatch boundary — no claim file):** this slice is
barred from `control/`, so no `control/claims/` entry exists; this card's
born-red first commit carries the `ideas/fleet/` collision flag per the
PR #222/#225/#243/#244/#247 workflow convention. Scope: ONE probe —
`ideas/fleet/carried-watch-verdict-inheritance-guard-2026-07-12.md` (probe
report append + state flip) plus the fleet README index row re-badge and this
card.

## What this session did

Probe in flight — report lands with the next commit; this card flips
`complete` in the slice's final commit before the PR is marked ready.
