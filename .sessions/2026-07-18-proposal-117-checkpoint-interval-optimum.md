# PROPOSAL 117 — the checkpoint-interval optimum (round-27 FLEET slot, P117 → V130, +13)

> **Status:** `in-progress`

Born in-progress as this session's first commit (born-red HOLD); flips to complete as the deliberate last step after the heartbeat, releasing the landing workflow.

## Objective
Draft and land the round-27 FLEET-slot opener (P117): one genuinely new, counterintuitive, stdlib-simulable fleet-domain head — the Young–Daly optimal checkpoint interval. With pre-registered ≥3σ /se gates and a same-run U-shape + square-root-law control. Fanned to sim-lab as VERDICT 130 (+13). This slice does NOT write the verdict.
