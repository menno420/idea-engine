# Session — repo seed (dispatch copilot, superbot round-3 session)

> **Status:** `complete`
> **Model/time:** fable-5 · 2026-07-10 ~17:4xZ (seeded from the superbot dispatch part-3
> session at the owner's ask; superbot PR #1955 is that session's card)

## What this session did

Born-right seed per fleet-manager `docs/gen2-blueprint.md` §1 + the founding package
(superbot `docs/planning/round3-founding-package-idea-engine-2026-07-10.md` v2, owner
ruling Q-0264):

- substrate-kit v1.7.0 adopted (`bootstrap.py adopt`) **and engaged**: all interview
  slots answered with this repo's real design values, docs rendered live
  (`render --live`, 0 unfilled), mode `guided`, `check --strict` green at commit.
- CI gate wired: `.github/workflows/substrate-gate.yml` (staged copy installed).
- Pipeline contract: root `README.md` (sections convention · idea grammar · probe
  battery v0 · outbox grammar · gen-3 landing conventions).
- `control/outbox.md` seeded (append-only proposals; sim-lab direct-pull + manager).
- Section tree: 10 sections derived from the fleet manifest's active lanes at HEAD
  (fleet · superbot · superbot-next · substrate-kit · websites · trading-strategy ·
  venture-lab · superbot-games · pokemon-mod-lab · gba-homebrew), README stub each;
  `ideas/superbot/` carries the Q-0264.8 reference index (superbot `docs/ideas/` stays
  canonical — nothing migrated).
- `claims/` + `review-queue.md` seeded (blueprint day-0 items; shared ground
  pre-resolved).

- **📊 Model:** fable-5 · high · docs-only (repo seed)

## 💡 Session idea

**Manifest-derived section sync checker** — a tiny stdlib script (`scripts/check_sections.py`)
that diffs `ideas/` sections against the fleet manifest's active rows and flags
missing/orphaned sections. The section convention is prose-only at seed, which is exactly
the drift class superbot documented (a convention without its checker drifts before the
next session reads it — superbot #880→#882). Good first PROCESS idea for this repo's own
`ideas/fleet/` section, and a natural early-wake slice.

## ⟲ Previous-session review

The previous slice of this lane's story (the founding-package drafting in superbot part 3)
shipped a v1 package that assumed the superbot-homed design — the owner redesigned it to
own-repo the same day. The miss: repo-home was a real fork that deserved a
possibility-space pass (superbot Q-0254) before drafting, not an assumption. This seed
inherits the fix: the design is now provenance-backed (Q-0264) instead of assumed.

## Handoff → the Idea Engine coordinator's first boot

The founding package's §2 boot steps 1–2 (seed) are DONE by this session. Your first
boot starts at step 3: the first probe (the probe-battery idea itself), then arm the
routine (step 4) and heartbeat (step 5). The walking-skeleton PR proof (branch → PR →
gate → merge-on-green) is YOUR first-slice job — this seed landed as the bootstrap
commit on main, so the merge path is still unproven.
