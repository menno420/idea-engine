# VERDICT 148 mirror — balance-triangle pick-rate inversion

Mirrors the sim-lab PRIMARY VERDICT 148 into idea-engine. Reproduce PROPOSAL 135 (round-31 GAME slot, P135 → V148, +13): in a weighted rock-paper-scissors balance triangle (skew-symmetric zero-sum game, cycle R>S>P>R with winning margins a=margin(R beats S), b=margin(S beats P), c=margin(P beats R)), does the mixed-strategy Nash equilibrium play each unit in proportion to the margin of the matchup it is NOT in (x*_R:x*_P:x*_S = b:a:c), so that buffing R's margin a from the symmetric 0.2 to 0.4 moves x* from (1/3,1/3,1/3) to (R 0.25, P 0.50, S 0.25) — DROPPING R's own pick rate and DOUBLING its counter P's — while the game value stays 0 and the intuitive uniform (1/3-each) meta is exploitable by (a−c)/3? "Balance = equal pick rates" is the folk reading; the equilibrium inverts it to inverse-margin weighting.

> **Status:** `complete`
> 📊 Model: Claude Opus · high · review/verify

Born red by design: this card lands `in-progress` in the FIRST commit so the substrate-gate HOLD holds the PR red until the reproduction is proven and independently audited; the deliberate LAST commit flips it to `complete`, clearing the HOLD and releasing merge-on-green (ORDER 003). No gate is bypassed. This born-red HOLD is the ONLY reason the gate reads red pre-flip; a red gate AFTER the flip is a real defect, not the HOLD. This mirror's outbox V148 block + heartbeat + the sim-lab merge SHA are appended on the flip commit.

## Objective
Reproduce the committed P135 reference verifier `ideas/superbot-games/balance_triangle_pick_rate_inversion.py` byte-identical under its pinned world (SEED=20260717, TRIALS=200000, SIGMA_GATE=3.0, skewed margins a/b/c=0.4/0.2/0.2, symmetric 0.2/0.2/0.2) and confirm: (a) the compact-canonical results-dict sha256 equals the disclosed digest `92de2d5454c34f274869bc83d302682909b376380c16e60b6afcb12756424a4b`, (b) all three gates hold as disclosed, in order G1→G2→G3, (c) cross-invocation runs are byte-identical AND an in-process double-run is deterministic, (d) the exact anchors reproduce (x*_skewed=(0.25,0.50,0.25), (a−c)/3=0.0666667, symmetric exploitability 0.0). Primary: sim-lab (VERDICT 148, +13 offset); this is the idea-engine mirror.

## GROUNDING (verified at HEAD)
- P135 reference verifier — idea-engine `ideas/superbot-games/balance_triangle_pick_rate_inversion.py` @ `06d72c4`: file sha256 `659b49a4a38ff96a28e81529533d88271cee5a8f88eecb20269c1e517cc09974`, git blob `04997dae5536da9e6128e7dd2ae81f71be3bddd1`, **211** lines / **8935** bytes. Permalink: https://github.com/menno420/idea-engine/blob/06d72c4de68064601088927ed2c4e884d02d0a6b/ideas/superbot-games/balance_triangle_pick_rate_inversion.py (PROPOSAL 135 landed via idea-engine #572).
- P135 outbox block (disclosed digest + gates + offset ledger) — idea-engine `control/outbox.md` @ `06d72c4`: https://github.com/menno420/idea-engine/blob/06d72c4de68064601088927ed2c4e884d02d0a6b/control/outbox.md
- Disclosed results-dict sha256: `92de2d5454c34f274869bc83d302682909b376380c16e60b6afcb12756424a4b`
- Offset authority (cited verbatim from the P135 depends-ledger): "sim-lab consumes at +13 (PROPOSAL 135 → VERDICT 148). Offset pinned." (idea-engine `control/outbox.md` P135 block) — PROPOSAL 135 → VERDICT 148 (+13). The live offset authority is this outbox's per-block ledger + idea-engine `control/status.md`, not `docs/current-state.md` (a dated SEAT-DORMANT snapshot through V059).
- Domain reference: von Neumann (1928) minimax theorem; the value of a skew-symmetric matrix game is 0 with a symmetric optimal strategy (Gale, Kuhn & Tucker 1950); generalized / weighted rock-paper-scissors mixed equilibria (https://en.wikipedia.org/wiki/Rock_paper_scissors) — verified reachable 2026-07-18. Nash (1951) existence.
- DIGEST POSTURE: **WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY**. `main()` calls `run()` to build a results dict that carries NO `results_sha256` field, computes `payload=json.dumps(results, sort_keys=True, separators=(",",":"))`, `digest=sha256(payload.encode()).hexdigest()`, then PRINTS the human-readable `json.dumps(results, indent=2, sort_keys=True)` dump followed by `Results-JSON sha256: <digest>`. It writes NO results file. **Twist:** the disclosed digest is the sha256 of the COMPACT-canonical serialization (sort_keys + `(",",":")` separators), NOT the pretty indent=2 dump printed on stdout. The computation lives in `run()`, so the in-process double-run calls `run()` twice and compact-hashes each return value.

## Constraints honored
Stdlib-only (hashlib, json, math, random); verifier copied byte-identical (diff exit 0, sha256 match); pinned SEED=20260717 unchanged; no numpy/scipy; forward-only git; verdict reproduced independently, not assumed; zero agent merge calls (merge-on-green lands claude/* on the green head SHA per ORDER 003).

## Gate plan (disclosed → reproduced), order G1→G2→G3
- **G1 EQUILIBRIUM/INDIFFERENCE:** with the opponent fixed at the closed-form mix x*=(0.25,0.50,0.25), each of the row's three pure actions has MC mean payoff within z<3σ of 0 — x* makes the field indifferent (value 0). Disclosed max|z|=**1.314**.
- **G2 INVERSION:** in the SKEWED triangle against the intuitive UNIFORM meta, the best pure response R has MC mean payoff **+0.06721 > 0** with z_exist=**+120.37 ≥ 3σ** AND matches the exact anchor (a−c)/3=**0.066667** with z_anchor=**+0.975 < 3σ**. "Equal pick rates" is exploitable; balance needs inverse-margin weighting.
- **G3 PLACEBO:** in the SYMMETRIC triangle the exact uniform-exploitability is **0.0 for every action** (max|exact|==0.0 exactly) AND the MC best-action payoff vs uniform is within z<3σ of 0 (z=**+0.594**) — the effect vanishes when margins are equal (caused by asymmetry, not by the sim).

## Probe questions (independent-audit checklist)
**1.** Does the results-dict sha256 reproduce the disclosed `92de2d54…24a4b` EXACTLY across cross-invocation A/B and an in-process double-run? — State the four digests. (Answer: yes, all four == disclosed.)
**2.** Is the verifier byte-identical to the P135 source (`diff` exit 0, sha256 match)? — State the exit code. (Answer: yes, diff exit 0, sha256 `659b49a4…c09974`.)
**3.** Do all three gates PASS in the pre-registered order G1→G2→G3 with the disclosed z-values, and does the verifier exit 0? (Answer: yes — G1 max|z|=1.314, G2 z_exist=+120.37 / z_anchor=+0.975, G3 max|exact|=0.0 / z=+0.594, exit 0.)
**4.** Is the equilibrium claim structural, not a magnitude artifact — is x* the exact rational mix (0.25,0.50,0.25)=b:a:c and is the uniform-meta exploitability the exact closed form (a−c)/3, with the symmetric placebo exactly 0.0? (Answer: yes — the anchors are exact rationals reproduced to the printed digits.)

## Outcome — APPROVE (exact reproduction)
- Verifier copied **byte-identical** from idea-engine `ideas/superbot-games/balance_triangle_pick_rate_inversion.py` — `diff` exit **0**, file sha256 `659b49a4a38ff96a28e81529533d88271cee5a8f88eecb20269c1e517cc09974` (211 lines / 8935 bytes, git blob `04997dae5536da9e6128e7dd2ae81f71be3bddd1`).
- Reproduced digest `92de2d5454c34f274869bc83d302682909b376380c16e60b6afcb12756424a4b` **== disclosed digest EXACTLY (MATCH)**. Cross-invocation A + B both printed byte-identical stdout with this digest, and an in-process double-run (`run()` ×2, compact-hashed in-process) produced the identical results dict AND identical digest both times, both == disclosed; all exit 0.
- **G1 equilibrium: PASS** — indifference mean payoffs R/P/S = +0.000556/+0.000387/+0.000719, z R/P/S = +1.014/+1.226/+1.314, max|z|=**1.3143865915026838** < 3σ — x*=(0.25,0.50,0.25) makes every pure action indifferent (value 0).
- **G2 inversion: PASS** — R vs uniform MC payoff **+0.0672109999998512 > 0**, z_exist=**+120.3689727128182 ≥ 3σ**, matches anchor (a−c)/3=**0.06666666666666667** by z_anchor=**+0.9748529872924236 < 3σ** — the uniform "equal pick rates" meta is exploitable and the exact exploit is (a−c)/3, so buffing a raises x*_P (P's ∝a share) not x*_R, inverting the folk reading.
- **G3 placebo: PASS** — symmetric-triangle exact uniform-exploitability max|·|=**0.0 exactly** (R/P/S all 0.0), MC best-action-vs-uniform payoff +0.000217 by z=**+0.5939951919337725 < 3σ** — the effect vanishes at equal margins (asymmetry-caused, not sim artifact).
- Anchors (all reproduced): x*_skewed=(R 0.25, P 0.50, S 0.25); x*_symmetric=(1/3,1/3,1/3); g2_exact_vs_uniform_skewed R/P/S = +0.0666667/0.0/−0.0666667; all_pass=**true**, exit **0**. This final flip to `complete` is the LAST commit that releases the born-red HOLD; landing is via merge-on-green (ORDER 003) with zero agent merge calls. Sim-lab PRIMARY PR: https://github.com/menno420/sim-lab/pull/223 — MERGED on sim-lab main @`2565820cb1310660258eb1076f6296086bfb89dc` (merge_commit_sha; head @`29a5185`, merged_at 2026-07-18T14:44:31Z). Full probe evidence: sim-lab `sims/verdict-148-balance-triangle/probe-report.md`. **APPROVE.**

## ⟲ Previous-session review
Prior loop landed VERDICT 146 (P133 the fan-out tail amplification, round-31 FLEET slot, +13, sim-lab PR #221 merged `4917c59`, digest `4b3de501…31c42d`) — APPROVE, byte-identical reproduction across cross-invocation A/B + a deterministic in-process double-run, all three gates PASS in order (G1 union-bound tail z=−0.336 vs P_slow(100)=1−0.99^100, G2 median crossover n_star=69 bracketed z=−0.015, G3 mean-slow-leaves linear z=+0.055 vs N·p=1.0). It did well to anchor every gate on the exact closed form (union tail, geometric-in-N median, linear mean) so the amplification is a structural union-over-leaves claim rather than a magnitude gap, and its ⟲ review carried the honest self-nit that V144's card shipped an unbackfilled `Sim-lab PR: (this PR)` and pledged in-kind backfill — a discipline this card inherits (its own sim-lab PR URL is left to the flip commit and should be backfilled). DIGEST-POSTURE carry-forward: V146 and this V148 are BOTH WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY (results dict carries no `results_sha256`, no file written, compact-canonical serialization is the digest preimage while the stdout dump is the pretty indent=2 form) and both put the compute entry in `run()`, so the in-process double-run calls `run()` twice directly — the V146 mechanics carry straight over. Object shift: V146 was a union-bound TAIL-PROBABILITY / series-reliability object; this V148 is a matrix-GAME / equilibrium object (the symmetric Nash mix of a skew-symmetric zero-sum triangle, x*∝inverse margins), a different domain and object, with a split z-profile — a near-anchor equilibrium gate (all |z|≤1.314, the MC lands ON value 0) plus one astronomically-tight existence gate (z_exist=+120.37, the folk-vs-reality exploit gap) alongside its near-anchor match (z_anchor=+0.975). NON-CONTIGUITY note for the successor: V146 (P133, FLEET) and this V148 (P135, GAME) both sit at/below the V147 high-water; the verdict high-water is the union-max V148 on this mirror but the owner-facing high-water remains non-contiguous (V137 for P124, V132 for P119, V126 mirror #527 for P113 still open below) — do not read "V148" as "every lower verdict fully closed on both sides". Coordinator-bound routines stay armed and are NOT re-armed by this seat.

## 💡 Session idea
This slice pins a 3-unit skew-symmetric triangle with a SINGLE buffed margin (a: 0.2→0.4) and reads the equilibrium shift analytically (x*∝ b:a:c). A genuinely distinct NEXT game-slot slice, distinct from this 3-cycle head, pins the **n-unit weighted-tournament equilibrium and its support-collapse threshold**: extend the cycle to k>3 units on a circulant beat-graph (each unit beats the next m of the ring by margins w_1..w_m) and ask when a buff does not merely re-weight the equilibrium but DROPS a unit out of the support entirely (x*_i → 0), so the meta contracts from k viable picks to k−1 — a phase transition the 3-cycle can't show because every skew-symmetric 3×3 game with positive margins keeps full support. The pre-registrable structure: (G-a) at k=3 the circulant reduces to this slice's exact x*=b:a:c with value 0 (the k=3 boundary IS this case); (G-b) for a fixed k the equilibrium support is exactly the set solving Mx=0 with x≥0, and a large-enough single-margin buff pushes the weakest unit's equilibrium weight to exactly 0 at a computable threshold, MC-confirmed within 3σ by that unit's realized play frequency collapsing to 0; (G-c) a DOSE-RESPONSE control shows support size is monotone non-increasing in the buff magnitude with a ≥3σ separation across the threshold — isolating support-collapse (a roster-diversity lever) from mere re-weighting. The designer lesson sharpens from "buffing a unit can lower its own pick rate and raise its counter's" to "past a margin threshold a buff doesn't just shift the meta — it deletes a unit from competitive play, shrinking roster diversity, so balance patches must watch the support boundary, not just the pick-rate vector." A seed-pinnable game-equilibrium mechanism deliberately distinct from this 3-cycle inverse-margin head.
