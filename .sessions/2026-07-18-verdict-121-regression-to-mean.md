# VERDICT 121 mirror — the regression-to-the-mean selection illusion: take a noisy repeated measure (pre X, post Y of the same unit, test-retest reliability rho=0.5, nothing done between reads), select the underperformers (X below cutoff c=−1 SD), apply an INERT intervention, and re-measure — the selected group's mean IMPROVES by exactly Delta*=(rho−1)·E[X|X<c]=+0.762568 SD (a hundred-sigma "effect" from an intervention that does nothing) purely from regression to the mean, while the WHOLE population shows zero change (no time trend) and the over-performer top tail shows a symmetric spurious DECLINE; the transferable correction is that a group SELECTED for being extreme on a noisy metric regresses toward the mean by (1−rho) of its selection gap, manufacturing a spurious before/after shift of exactly (rho−1)·E[X|selected] — use a randomized control (or the unselected population) as the counterfactual, or subtract the regression prediction before crediting the intervention, never the naked before/after delta (P108, +13) — APPROVE

> **Status:** `in-progress`
> 📊 Model: opus-4.8 · low · review/verify

Born in-progress as this session's first content commit — this born-red HOLD is deliberate and holds the substrate-gate until the mirror is fully recorded. The flip to `complete` is the last step, after the outbox VERDICT 121 block + heartbeat are written and `python3 bootstrap.py check --strict` is green. This is the MIRROR slice — fan-in to the fleet-manager of sim-lab's VERDICT 121; the verification itself was performed and landed on the sim-lab side (PR #193 @2957197, merged 2026-07-18T02:03:12Z).

## GROUNDING
Fan-in to the fleet-manager (Q-0264) of sim-lab's VERDICT 121 verifying idea-engine PROPOSAL 108 (`## PROPOSAL 108 · 2026-07-18T01:33:34Z · status: sim-ready`, idea-engine control/outbox.md), offset +13 (P108 → V121), the round-24 UNRELATED slot closer (rotation fleet→venture→game→unrelated; P105 opened fleet, P106 took venture, P107 took game, P108 closes the round at the unrelated slot). Domain: statistics / experimental design / measurement — Galton's regression to the mean (1886), a fleet-external pure-mechanism head, deliberately a DIFFERENT unrelated domain from the round-22 closer P100 (information theory / Kelly betting) AND the round-23 closer P104 (epidemiology / SIR final-size). Sim-lab reproduced the proposal's OWN committed stdlib verifier (`ideas/fleet/regression_to_mean.py`, committed idea-engine blob `e3340104dc2f2ab630b3fb842088dc981dd58369`, file sha256 `ffd91dd4c5abf957915c2700b27e157f718fa8b6dfb18989a67c4870490e71c2`) VERBATIM under the pinned SEED=20260717 (byte-identical copy, diff exit 0, sha256 ffd91dd4c5abf957915c2700b27e157f718fa8b6dfb18989a67c4870490e71c2), confirmed all three pre-registered gates hold in order G1→G2→G3 on the /se margin at their thresholds, and reproduced the results-dict sha256 `35d65a5060d0582a5fd0d041c2d72635657c7f8c62ac024d4ee004b285ed8aed` (all 64 hex digits) EXACTLY against the proposal's disclosed expected digest (disclosed = reproduced = independently recomputed), deterministic across two byte-identical fresh `python3` runs (exit 0). Sim-lab side: PR #193 (branch `claude/verdict-121-regression-to-mean`; sim dir `sims/verdict-121-regression-to-mean/` — verifier copy + regression_to_mean_results.json + run-stdout.txt), LANDED on sim-lab main @2957197 via merge-on-green after the born-red card flip, merged 2026-07-18T02:03:12Z. Offset authority for +13 is this repo's control/outbox.md per-block offset ledger + control/status.md heartbeat; docs/current-state.md is a dated SEAT-DORMANT snapshot (ledger only through V059) and carries NO P108/V121 row.

## Verdict — APPROVE (first-failing-gate None)
Sim-lab reproduced the proposal's OWN committed stdlib verifier (`ideas/fleet/regression_to_mean.py`) VERBATIM under the pinned SEED=20260717; all three gates PASS in order G1→G2→G3 on the /se margin, reproducing the disclosed dry-sim EXACTLY. On the pinned world (SEED=20260717, RHO=0.5, C_THRESH=−1.0, N_UNITS=20000, TRIALS=400, SIGMA_GATE=3.0, IMPROVE_MIN=0.50; derived m_sel=E[X|X<c]=−phi(c)/Phi(c)=−1.525135, Delta*=(rho−1)·m_sel=+0.762568, top-tail Delta_top*=−0.762568), a stdlib Monte-Carlo over standard bivariate-normal repeated measures Y=rho·X+√(1−rho²)·Z (select X<c with NO intervention) confirms the illusion: the selected underperformers' mean IMPROVES by ~0.763 SD purely from regression to the mean, the whole population shows no change, and the top tail declines symmetrically. all_pass=true, exit 0.
- G1 spurious-improvement: PASS — Delta_sel=**0.762986** (se 0.000842) ≥ floor IMPROVE_MIN 0.50 by z=**312.4059σ** ≥3σ AND Delta_sel>0 — an INERT intervention on the bottom tail (X<−1 SD) shows a large, significant "effect" (closed-form anchor +0.762568; the sim mean sits right on it).
- G2 selection-necessity control: PASS — the whole-population before/after change Delta_pop=**4.5e-05** (se 0.000372) is within 3σ of 0 by |z|=**0.1204** (<3) — there is NO global time trend, so G1 is 100% selection-on-extremes + regression to the mean, not drift.
- G3 closed-form anchor MATCH: PASS — Delta_sel=0.762986 vs the exact Galton prediction Delta*=(rho−1)·E[X|X<c]=0.762568 by |z|=**0.4969** (<3) — the Monte-Carlo improvement matches the regression coefficient to within noise, proving the effect IS the RTM mechanism (not merely the inequality). Top tail Delta_top=−0.764375 (se 0.000738), the symmetric spurious decline.

The mechanism: for a noisy repeated measure with test-retest reliability rho<1, E[Y|X=x]=rho·x, so a group selected because X is extreme-low (X<c) has an X that is partly non-repeating luck — on re-measure Y drifts back toward the mean by exactly the (1−rho) fraction of the selection gap, manufacturing a spurious before/after improvement of exactly (rho−1)·E[X|X<c]>0 from an intervention that does nothing, while the unselected whole population shows zero change and the over-performer top tail shows a symmetric spurious decline. Design correction (transferable): whenever an intervention is applied to a group SELECTED for being extreme on a noisy metric and judged by its own before/after change, subtract the regression prediction (rho−1)·E[X|selected] before crediting the intervention, or use a randomized control (or the unselected population) as the counterfactual — never the naked before/after delta. all_pass=true, SEED=20260717.

## cross-check
Sim-lab reproduced the proposal's OWN committed stdlib verifier (`ideas/fleet/regression_to_mean.py` — random, math, json, hashlib; per-trial Monte Carlo of standard bivariate-normal repeated measures Y=rho·X+√(1−rho²)·Z, select X<c with NO intervention, Phi via math.erf — no numpy/scipy) VERBATIM: byte-identical to the committed idea-engine source (blob e3340104dc2f2ab630b3fb842088dc981dd58369, file sha256 ffd91dd4c5abf957915c2700b27e157f718fa8b6dfb18989a67c4870490e71c2, diff exit 0), exit 0, cross-invocation reproducible (two fresh `python3` runs, stdout + results-json byte-identical run-to-run). The committed `regression_to_mean_results.json` carries a `results_sha256` SELF-field: the verifier computes `digest = sha256(json.dumps(results, sort_keys=True, separators=(",",":")))` over the results dict BEFORE inserting the field (main() lines 174–176), then writes `results["results_sha256"]=digest` into the file — so the disclosed digest is over the compact canonical preimage of the results dict with the `results_sha256` field OMITTED (P104/P107-style field-omission posture, DISTINCT from P105's sibling-key posture — resolved against the script text, not assumed). The run-stdout `Results-JSON sha256:` line == an independent recompute of that field-omitted compact preimage == the proposal's disclosed expected digest EXACTLY: **35d65a5060d0582a5fd0d041c2d72635657c7f8c62ac024d4ee004b285ed8aed** (all 64 hex digits, disclosed = reproduced = independently recomputed). All three gate results match the proposal's disclosed dry-sim EXACTLY (G1 z=312.4059σ, G2 |z|=0.1204, G3 |z|=0.4969); means match (G1 Delta_sel=0.762986, G2 Delta_pop=4.5e-05, G3 anchor 0.762568, Delta_top=−0.764375). REFERENCE-VERIFIER REPRODUCTION slice (same posture as V114/V115/V116/V117/V118/V120, distinct from an independent reimplementation): the proposal ships the verifier and pins the exact sha256, so the tightest check is a byte-level reproduction of that artifact — and it landed to the digit. Sim-lab artifact: `sims/verdict-121-regression-to-mean/` (regression_to_mean.py + regression_to_mean_results.json + run-stdout.txt), landed via PR #193 (sim-lab main @2957197, merge-on-green after the born-red flip, merged 2026-07-18T02:03:12Z). Pinned world: SEED=20260717, RHO=0.5, C_THRESH=−1.0, N_UNITS=20000, TRIALS=400, SIGMA_GATE=3.0, IMPROVE_MIN=0.50.

## ⟲ Previous-session review
The immediately prior VERDICT mirror (V120, the energy-cap overflow-forfeiture tax, round-24 GAME slot) landed on the idea-engine side via PR #511 (main @2e6a18d), disclosing a clean reference-verifier reproduction and a field-omitted digest posture identical to this slice's. One honest observation: that V120 card's baton correctly named "round-24 UNRELATED-slot PROPOSAL 108 → VERDICT 121 (+13)" as the successor's pull — which this slice executes with zero re-derivation — but its loop line also flagged that VERDICT 119 (P106 retention-survivorship mirage, round-24 VENTURE) landed only its fast-lane claim (main @7e04e58) with the mirror still owed, and that gap is STILL open at this HEAD (d445094): the outbox records V118 and V120 but NOT V119, so the game verdict (V120) and now this unrelated verdict (V121) both land ahead of the venture verdict (V119) in numeric-out-of-order sequence. That is a real, load-bearing carry-forward: recording V121 does NOT complete round-24 — V119's mirror remains a pending pull. This slice honors the append-only + dedupe discipline (confirmed no pre-existing V121 block before appending) and does NOT assert round-24 completeness. The coordinator-bound routines (failsafe trig_01MzoLVHMDuL28UPkKnbmHCm, pacemaker trig_01R26BEgXj5KVZkHA4Ri6AMi) stay armed and are NOT re-armed by a fan-in seat. Stale work claims under control/claims/ (verdict-116…121) are the successor's to prune once their PRs are terminal.

## 💡 Session idea
The proof pins a SINGLE selection cutoff (c=−1 SD) and a SINGLE reliability (rho=0.5) and shows the spurious shift is exactly (rho−1)·E[X|X<c]=+0.762568 SD. But the two knobs interact in a way an operator can exploit adversarially: because E[X|X<c]=−phi(c)/Phi(c) grows without bound as the cutoff moves further into the tail, the manufactured "effect" (rho−1)·E[X|X<c] is UNBOUNDED in the selection extremeness — the more aggressively you triage (the deeper the bottom-decile → bottom-percentile → bottom-permille cutoff), the LARGER the fake improvement, with no real intervention. A genuinely different next UNRELATED slice pins the cutoff as a swept axis (c ∈ {−0.5, −1.0, −1.5, −2.0, −3.0} SD) at fixed rho and pre-registers two gates: (1) the simulated spurious improvement Delta_sel(c) is strictly MONOTONE-INCREASING as c moves into the tail and tracks the closed-form (rho−1)·(−phi(c)/Phi(c)) within 3σ at every cutoff — quantifying that "we fixed the worst X% and it improved" reports a BIGGER fake win precisely when the triage is more selective; and (2) the ratio Delta_sel(c)/|E[X|X<c]| is invariant in c and equals (1−rho) within the G3 noise floor — isolating that the ENTIRE cutoff-dependence lives in the selection mean, not the regression coefficient, so the audit correction (subtract (1−rho)·selection-gap) is cutoff-robust. That converts the flat "subtract the regression prediction" into the actionable "the spurious effect SCALES with how extreme your selection is — the most aggressive triage produces the most persuasive fake result, and a dashboard that re-scores an ever-narrower worst-tail will report ever-larger phantom gains — so the regression correction must be applied per-cutoff, and any 'we remediated the extreme tail' claim is most suspect exactly when the tail was cut deepest," a seed-pinnable, ≥3σ-gateable mechanism (cutoff-swept spurious-shift curve) distinct from this slice's fixed-cutoff result and from the outbox's flat use-a-control correction.
