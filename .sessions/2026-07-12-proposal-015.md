# Session — PROPOSAL 015: generator purchase path (T10 cost curve)

> **Status:** `in-progress`
> **Model/time:** fable · 2026-07-12T23:05:37Z (Ideas Lab worker slice — draft a NEW
> content-lane idea + PROPOSAL 015 outbox append; card born in-progress as the
> designed gate hold, flips complete in this PR's final commit)

## Scope

Draft a genuinely new, product-facing idea under the standing owner ORDER 003
(`## ORDER 003 · 2026-07-12T21:56:16Z · status: new`, verified present at HEAD
`390a89b` — "make sure there are new ideas coming and being tested for each
repo"): the superbot-idle generator purchase path, written up as
`ideas/superbot-idle/generator-purchase-path-t10-2026-07-12.md` (probed
single-pass this slice, sim-ready) and appended to `control/outbox.md` as
PROPOSAL 015. Steering honored: PROPOSALs 013/014 were both fleet/infra
meta-heads, so this slice picked from the content lanes — the strongest candidate
was the head the 2026-07-11 superbot-idle batch card deliberately dropped with
"natural sequencing is after the SIM-001 verdict anyway": sim-lab VERDICT 006
(approve, 10/10) has since landed, firing that recorded arming condition. Premise
verified live this slice at superbot-idle HEAD `c753bc8` (ls-remote + blobless
clone: no purchase path in `idle_engine/`, T10 row verbatim at both pins, engine
drift since the verdicted pin diffed and disclosed). Dedup swept vs PROPOSALs
001–014, the ideas/ tree, both repos' control files, and sim-lab docs/sims
(nearest neighbor: PROPOSAL 006, whose SIM-001 text excludes T10 by name; details
in the idea's § Relations).

**📊 Model:** fable · content + outbox proposal only (idea file, card, index row,
outbox append; no product code, no control/status.md or control/inbox.md writes)

## 💡 Session idea

*(slot open — filled in this PR's final commit)*

## ⟲ Previous-session review

Newest predecessor card (`.sessions/2026-07-12-proposal-014.md`, worker slice
~22:27Z): closed clean — its PR #276 merged at `390a89b` (now HEAD) with the
PROPOSAL 014 outbox entry grammar-clean and its born-red card flipped complete in
its final commit as designed. Two things worth carrying: (1) its 💡
(incident-evidence fixture convention — commit the verbatim artifact + verification
facts at incident time) is load-bearing INSIDE its own proposal (P014's done-when
demands hermetic committed fixtures), a tight capture-to-use loop worth imitating;
(2) 013 and 014 back-to-back were both fleet-process heads drafted from
coordination-plane incidents — sound individually, but the pipeline's coverage
duty (ORDER 003 done-when: "idea coverage spanning both repos' domains") needed
the swing back to a content lane, which this slice's dispatch steering makes
explicit. No reconciliation debt found.
