# Session — PROPOSAL 027: comp/stipend envelope — the last routed lever (game-mechanics rotation, round 3)

> **Status:** `complete`
> **Model/time:** fable · 2026-07-13T06:03:23Z (Ideas Lab worker slice — draft the
> ORDER 004 rule-3 GAME-MECHANICS round-3 head: round 1 P020 → V022 reject (odds
> lever), round 2 P023 → V025 reject (entry-fee lever — "fee fixes SAFE, not
> SINK"); round 3 takes the head V025's own follow-ups block routes by name: the
> comp/stipend envelope, the THIRD and last unpriced lever of V022's routed
> list; card born in-progress as the designed gate hold, flips complete in this
> PR's final commit once the 💡 slot resolves)

**📊 Model:** fable · content + outbox proposal only (idea file, card, index row,
claim file, outbox append; no control/status.md or control/inbox.md writes; no
checker or script changes)

## Scope

Draft a genuinely new sim-shaped idea under standing owner ORDER 003 (continuous
pipeline) + ORDER 004 rule 3's rotation — the round-3 GAME-MECHANICS slot.
VERDICT 022 rejected the odds lever and routed three non-odds levers; VERDICT
025 rejected the entry-fee lever (closing the parent's SAFE gap 57× on the
even-money shape but reproducing the SINK walls) and its recommendation names
the successor verbatim: "the comp/stipend envelope sim — the THIRD routed
lever, now the last unpriced one." The head: a pre-registered four-band sweep
on V025's surviving ticket substrate — comp designs D1 qualified stipend / D2
handle rebate / D3 loss rebate × four sizes × takes {0.05, 0.10} × shapes
T1/T2 — asking whether comp rescues the two pre-registered target cells (C1 =
the FUN-dead sink-firm take; C2 = the tiered SAFE near-miss 0.0608) while a
new MINT band (max over policies ∪ a per-design farmer family of E[session net
incl. comp] ≤ 0) guards the faucet, REJECT checked first. Alternative leads
passed on merit: superbot-idle T11 rebuy pacing is registered lane-side WITH
the mechanic per P015's own card; mining/fishing reward-pacing pins hit the
V001/V008 earn-rate-baseline wall directly; a browser-game difficulty pin has
no committed in-tree baseline to chain to tonight. Written up as
`ideas/superbot/casino-comp-stipend-envelope-2026-07-13.md` (probed
single-pass this slice, sim-ready) and appended to `control/outbox.md` as
PROPOSAL 027. Fresh PRNG seeds 20260752–55 allocated strictly above P026's
high-water 20260751 (seed-registry hygiene, eighth consecutive slice). Dedup
swept tree-wide (`rg -i -g '!bootstrap.py' -g '!.substrate'
'comp[/ -]?stipend|stipend|loss.rebate|rakeback|cashback'` over ideas/ +
.sessions/ + control/ + docs/ and the sim-lab clone @ `5e356ed`) — hits only
the two parents' own lever lists and routing lines; no proposal, idea file,
card, or sim-lab verdict V001–V027 prices a comp design, and no prior grid
carries a MINT/faucet band or an exploit-policy (farmer) leg.

## 💡 Session idea

**A committed adversary-policy library for economy sims.** Every
game-mechanics verdict in the V022 line hand-rolls its own attack set —
V022's stake policies (fixed-fraction, constant, capped martingale), V025's
re-buy policies (R1/R5/RG/MC), this head's farmer family (wash-qualifier,
cap-chaser, 12-variant optimal-stopping harvester) — and every one closes
with the same unproven bracket clause ("any policy is bounded between X and
Y — stated, not proven"). The slice: sim-lab commits a versioned
`sims/lib/adversaries/` module — each policy a pure stdlib kernel with a
one-paragraph BRACKET CLAIM header (what behavior class it bounds, and the
evidence row: which verdict measured it binding, e.g. RG binding SAFE in
V025's c ≥ 25 cells, the D3 stopper binding MINT here) — so the next
economy-lever sim (loyalty points, daily bonuses, season passes) IMPORTS the
attack set instead of re-inventing it, "max over swept policies" becomes
comparable across verdicts by construction, and the bracket clauses
accumulate as library lemmas instead of per-report caveats. Zero new
machinery beyond a module move: the kernels already exist byte-for-byte in
the three committed sims. **Dedup** (this slice, `rg -i 'policy
library|exploit.polic|adversar(y|ial).{0,20}(library|catalog|registry)'`
over ideas/ + .sessions/ + control/ + docs/ and the sim-lab clone @
`5e356ed`): hits only this slice's own files; sim-lab's `sims/` tree has no
shared policy module (each verdict dir is self-contained by the hermetic
rule — the library would be committed FIXTURE art imported at build time,
not a runtime read, so hermeticity is preserved the same way the V025 kernel
shape was "reusable art" for this head). Distinct from P026's 💡 (anchor
pins: verifying QUOTED constants; this shares CODE with provenance) and from
the proposal itself (which uses the farmer family; this makes it importable).

## ⟲ Previous-session review

Newest predecessor card (`.sessions/2026-07-13-proposal-026.md`, worker slice
~05:36Z): closed clean at `0716140` (PR #293) — the venture round-3 head
(breadth budget under the q99 bar), the power complement to V024's size-only
bar, with its grid shrunk BY CITATION from the parent's measured axis findings
(vol inert, drift binding) — the most surgical reuse of a parent verdict in
the P013–P026 run. Two of its habits are consumed here: **seed-registry
hygiene** (this slice draws 20260752–55 strictly above its 20260748–51
high-water — eighth consecutive card on the discipline) and the chained-anchor
pattern its 💡 sharpened: its "machine-checkable anchor pins" observation (a
hand-quoted anchor constant is worse than none when it slips — V024's own
disclosed transcription slips are the evidence) applies with full force to
THIS slice, which hand-quotes eight-plus constants from V022/V025's committed
results (the exact-Fraction FUN anchors, the 0.0608 near-miss, the 5.2%/25.2%
price tags) — the `anchors:` block with parent-file JSON pointers remains
unbuilt, and this card is now the third accumulation of its motivating
failure surface. The **rotation-position treadmill** extends to n=7: this
card again re-derives its slot (game-mechanics round 3, after P023 closed
round 2) from predecessor prose and the tally, not a machine ledger — P021's
💡 stays measured-and-unbuilt.
