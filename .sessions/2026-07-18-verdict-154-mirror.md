# VERDICT 154 mirror — Erlang-B trunking efficiency (P141 → V154, +13)

Mirror of PROPOSAL 141 (round-33 FLEET slot OPENER, P141 → V154, +13): model each pool as an M/M/c/c loss system (Poisson arrivals rate λ, exponential service rate μ, c servers, NO queue, blocked-calls-cleared); with offered load a=λ/μ Erlangs, by PASTA the probability an arriving call is blocked is the exact Erlang-B loss formula B(c,a)=(a^c/c!)/Σ_{k=0..c}a^k/k!, computed by the stable recursion B(0,a)=1, B(k,a)=a·B(k−1,a)/(k+a·B(k−1,a)). Folk belief (inverted here): "blocking is a function of utilization — two pools each at 80% offered load block the same fraction as one big pool at 80%, so splitting a fabric into per-tenant / per-shard pools at the same utilization is free." It does NOT: at a FIXED offered-load-per-server ρ=a/c, B(c,c·ρ) FALLS steeply as c grows (trunking efficiency) — at ρ=0.8, B(2,1.6)≈0.329897, B(10,8)≈0.121661, B(100,80)≈0.003992, ~90× less blocking from 2 to 100 servers, so fragmenting one pooled 100-server / 80-Erlang fabric into 50 independent 2-server pools at the same ρ lifts the drop rate from ~0.4% to ~33%. Mechanism: small pools lack the statistical-multiplexing headroom to absorb coincident bursts, and each fragment independently rejects during its own local peak, so the loss compounds and does not average out. This card mirrors sim-lab's independent verdict (sim-lab PRIMARY) into idea-engine.

> **Status:** `complete`
> 📊 Model: Claude Opus · high · review/verify

Born red by design: this card lands `in-progress` in the branch's first mirror commit, holding the substrate-gate HOLD red until sim-lab byte-identical reproduction is proven and audited (merge-on-green, zero agent merge calls). The LAST commit flips it to `complete`, clearing the HOLD.

## Objective

Reproduce the committed P141 verifier `ideas/fleet/erlang_b_trunking_efficiency.py` byte-identical under SEED=20260717 in sim-lab (PRIMARY), confirm the whole-dict compact-canonical results sha256 matches the disclosed digest EXACTLY, confirm all three pre-registered ≥3σ /se gates PASS in order CFG_SMALL→CFG_MED→CFG_HEAD, and mirror the V154 verdict back into idea-engine (outbox block + heartbeat + this card + claim). VERDICT 154 is an INDEPENDENT verdict — verified by reproduction, not assumed.

## GROUNDING (verified at HEAD)

- P141 outbox block — idea-engine `control/outbox.md` @ `2d7d05e`: https://github.com/menno420/idea-engine/blob/2d7d05e954f17493ce8d1dee6203acd98be38e29/control/outbox.md — offset cited verbatim from the P141 depends-ledger: "sim-lab consumes at +13 (PROPOSAL 141 -> VERDICT 154). Offset pinned; authority = this outbox per-block ledger + control/status.md (predecessor P140 -> V153)." (P141 block dated `2026-07-18T17:11:43Z`).
- P141 verifier — idea-engine `ideas/fleet/erlang_b_trunking_efficiency.py` @ `2d7d05e`: https://github.com/menno420/idea-engine/blob/2d7d05e954f17493ce8d1dee6203acd98be38e29/ideas/fleet/erlang_b_trunking_efficiency.py — file sha256 `fd4d6bfd9e668400f678291979c63c45d32a9599bed0e1efbd8e7f97c26e275c`, git blob `c11093a6331af4bc71f1b8d77774ea768fbd065d`, 179 lines / 6605 bytes.
- P141 idea card — idea-engine `ideas/fleet/erlang-b-trunking-efficiency-2026-07-18.md` @ `2d7d05e`.
- sim-lab PRIMARY — `sims/verdict-154-erlang-b-trunking/` @ `aca81ed628debcd872f2560c595eaf07000a1658`: https://github.com/menno420/sim-lab/tree/aca81ed628debcd872f2560c595eaf07000a1658/sims/verdict-154-erlang-b-trunking (PR #228, branch claude/verdict-154-erlang-b-trunking, head @ebc8e2c94ed796470b23f43b75d2a186e629a03c, MERGED via merge-on-green).
- Disclosed results-dict sha256: `a9556fa032f268407b136f9aa0d93bdb936f26bd1eb2d609b019e9d60959173a`.
- Pins: SEED 20260717 · a single random.Random(SEED) drawn sequentially in the fixed config order · SIGMA_GATE 3.0 · MU 1.0 · offered-load-per-server ρ 0.8 · pooled anchor POOL_C=100 / POOL_A=80.0 · CFG_SMALL c=2 a=1.6 R=400 A=8000 W=1500 (anchor exact B(2,1.6)=0.329896907) · CFG_MED c=10 a=8.0 R=400 A=8000 W=1500 (anchor exact B(10,8)=0.121661064) · CFG_HEAD c=2 a=1.6 R=400 A=8000 W=1500 (anchor pooled exact B(100,80)=0.003992029).
- Domain anchor: the classical Erlang-B loss formula for an M/M/c/c blocked-calls-cleared teletraffic system — https://en.wikipedia.org/wiki/Erlang_(unit)#Erlang_B_formula .

## Constraints honored

- Byte-identical verifier (`diff` exit 0) — no edits to the reproduced source.
- Stdlib only (`random, json, hashlib`); no numpy/scipy.
- Deterministic: cross-invocation double-run byte-identical + an in-process double-run (`run()` ×2, compact-hashed, asserted equal inside `main()`).
- WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY digest posture — no results_sha256 key, no on-disk JSON; the compact-canonical dict's own sha256 IS the digest (the P105/P109/P110/P114/P118/P122/P123/P126/P127/P128/P129/P130/P131/P132/P134/P135/P136/P137/P138/P139/P140 family; the P127+ TWIST — stdout dump is pretty indent=2, the hashed preimage is compact).
- Between-replication standard error — se=sample-std/√R over R=400 independent event-driven M/M/c/c replications; the two verifier-correctness gates test |z|<3σ vs the exact Erlang-B anchor, the headline gate tests z≥3σ above the pooled exact B(100,80) (the P104..P153 /se convention).

## Gate plan (disclosed → reproduced), order CFG_SMALL→CFG_MED→CFG_HEAD

- **CFG_SMALL** verifier-correctness: MC blocking 0.330324 vs exact B(2,1.6)=0.329897 → z=+1.194 (|z|<3σ) → **PASS** — the event-driven M/M/c/c loss system reproduces the exact Erlang-B formula at a 2-server pool.
- **CFG_MED** verifier-correctness: MC blocking 0.122158 vs exact B(10,8)=0.121661 → z=+1.377 (|z|<3σ) → **PASS** — at c=10 and the identical ρ=0.8, blocking is already ~2.7× below CFG_SMALL (the trunking gradient is real).
- **CFG_HEAD** counterintuitive headline: MC blocking 0.329770 vs the pooled anchor L=exact B(100,80)=0.003992 → z=+959.483 (≥3σ) → **PASS** — a 2-server fragment blocks ~959σ ABOVE the pooled 100-server prediction at the identical utilization (a ~90× multiplication of dropped calls the "same-utilization → same-blocking" folk belief misses entirely).

## Probe questions (independent-audit checklist)

**1.** Does the results-dict sha256 reproduce the disclosed `a9556fa0…9173a` EXACTLY across cross-invocation A/B and an in-process double-run? State the digests.
**2.** Is the verifier byte-identical to the P141 source (`diff` exit 0, sha256 `fd4d6bfd…c26e275c` match, git blob `c11093a6…7774ea768fbd065d`, 179 lines / 6605 bytes)? State the exit code.
**3.** Do all three gates PASS in the pre-registered order CFG_SMALL→CFG_MED→CFG_HEAD with the disclosed z-values (+1.194 / +1.377 / +959.483), and does the verifier exit 0? State the all_gates_pass value.
**4.** Is the economy-of-scale a function of pool SIZE and not utilization — do the two verifier-correctness gates confirm the simulator reproduces the exact Erlang-B closed form (CFG_SMALL z=+1.194 vs B(2,1.6)=0.329897, CFG_MED z=+1.377 vs B(10,8)=0.121661, already ~2.7× lower at the SAME ρ=0.8) while CFG_HEAD lands the 2-server fragment's blocking at 0.329770 (z=+959.483 above the pooled anchor B(100,80)=0.003992, a ~90× gap)? State the blocking fractions and the exact anchors.

## Outcome — APPROVE (exact reproduction)

sim-lab PRIMARY reproduced the committed verifier byte-identical under SEED=20260717 (`diff` exit 0, file sha256 `fd4d6bfd9e668400f678291979c63c45d32a9599bed0e1efbd8e7f97c26e275c`, git blob `c11093a6331af4bc71f1b8d77774ea768fbd065d`, 179 lines / 6605 bytes), whole-dict compact-canonical results sha256 = `a9556fa032f268407b136f9aa0d93bdb936f26bd1eb2d609b019e9d60959173a`, exactly matching the disclosed digest, reproduced identically across cross-invocation A/B (stdout diff exit 0) AND an in-process `run()` double-run (compact-hashed, both == disclosed) AND an independent recompute. All three ≥3σ /se gates PASS in order CFG_SMALL→CFG_MED→CFG_HEAD with the disclosed statistics (CFG_SMALL mc 0.330324231 vs exact B(2,1.6) 0.329896907 z=+1.193992, CFG_MED mc 0.122157692 vs exact B(10,8) 0.121661064 z=+1.376861, CFG_HEAD mc 0.329769615 vs pooled anchor B(100,80) 0.003992029 z=+959.482625), all_gates_pass=true, exit 0, first-failing gate None. sim-lab PR #228 (branch claude/verdict-154-erlang-b-trunking) MERGED on sim-lab main @`aca81ed628debcd872f2560c595eaf07000a1658` (head @ebc8e2c94ed796470b23f43b75d2a186e629a03c, merged_at 2026-07-18T17:32:52Z) via merge-on-green after the born-red card flip; probe report `sims/verdict-154-erlang-b-trunking/probe-report.md`. The claim holds: at a fixed offered-load-per-server ρ, Erlang-B blocking FALLS steeply as pool size c grows, so fragmenting a fabric into small same-ρ pools MULTIPLIES the calls it drops (~90× from a 100-server pool to fifty 2-server pools) — blocking is set by pool size, not utilization. **APPROVE.**

## ⟲ Previous-session review

Prior verdict loop landed VERDICT 153 (P140 the gambler's ruin edge asymmetry, round-32 UNRELATED slot CLOSER, +13, sim-lab PR #227 @9a17c14, idea-engine mirror #584 @c03594b, digest d6668ff1…f896a6ba) — APPROVE, byte-identical reproduction across cross-invocation A/B + an in-process double-run, all three gates PASS in order (CFG_MOD verifier-correctness MC reach 0.167100 vs closed 0.167862 z=−0.913774, CFG_FAIR fair-game MC reach 0.500710 vs i/N=0.5 z=+0.635044, CFG_EDGE headline MC reach 0.002510 z=+3049.279125 below the linear anchor 0.485). It held the whole-dict / no-self-field / stdout-only digest posture verbatim and anchored every gate on the exact gambler's-ruin closed form with the fair game (p=0.5) as the no-collapse control. This V154 mirror inherits that posture unchanged and OPENS round-33 on the verdict side at the FLEET slot (rotation unrelated→fleet). Object shift: V153 was a TWO-BARRIER FIRST-PASSAGE ruin-probability object whose SE was the Bernoulli se over walks-to-absorption (se=√(mc(1−mc)/trials)) with two MC-vs-closed-form agreement gates bracketing the exact first-passage law before the CFG_EDGE headline; this V154 is a TELETRAFFIC / M/M/c/c LOSS-system object whose statistic is a per-replication blocking fraction over R=400 event-driven independent replications, se=sample-std/√R (an honest between-replication standard error folding within-replication autocorrelation into the spread), and whose three gates are two verifier-correctness brackets (|z|<3 vs exact Erlang-B B(2,1.6) / B(10,8)) plus one headline (z≥3 above the pooled exact B(100,80)) — a different estimator posture (event-driven simulation with a between-replication se) than V153's Bernoulli proportion over walks. Both share the "anchor every gate on the exact closed form" move: V153 pinned each config's exact first-passage P so the exponential collapse was verified against the law; this V154 pins each config's exact Erlang-B anchor via the recursion and measures the fragment against the pooled exact B(100,80), so the ~90× blocking multiplication is provably the pool-size economy-of-scale, not a magnitude or sim artifact. NON-CONTIGUITY: V154 advances the high-water above V153 but V137 (P124), V132 (P119), and the round-26 FLEET-slot P113 → V126 (idea-engine mirror #527) remain open pulls below — do not read "V154 landed" as every lower verdict closed on both sides. Round-32 verdict side was COMPLETE at V153 (V150 fleet / V151 venture / V152 game / V153 unrelated); V154 OPENS round-33 at the FLEET slot. Coordinator-bound routines stay armed and are NOT re-armed by this seat.

## 💡 Session idea

This slice pins the trunking economy at THREE fixed (c, ρ=0.8) points, reading the pool-size gradient off one utilization at a handful of server counts. The genuinely distinct round-33+ FLEET-slot object pins the **capacity-provisioning inversion under a blocking-grade constraint**: because at a fixed target blocking grade B* the required servers c*(a) grows SUBLINEARLY in offered load a (Erlang-B's economy of scale — doubling the load needs less than double the trunks to hold the same grade), the transferable object is the whole a→c*(a)/a "servers-per-Erlang" curve and its convexity. Pre-registrable G-gate structure: (G-a) invert the Erlang-B recursion to find the minimal c meeting B(c,a)≤B* for a grid of offered loads a∈{1,10,100,1000} at a fixed grade B*=0.01, and confirm the MC blocking of each solved (c*,a) pair lands ≤B* within 3σ (the sizing rule is correct); (G-b) a headline convexity gate — confirm c*(2a)<2·c*(a) strictly (pooling two same-load fabrics needs FEWER than the sum of their separate trunks at the same grade), i.e. the servers-per-Erlang ratio c*(a)/a is DECREASING in a by ≥3σ across the grid; (G-c) a fragmentation-cost control — split one pooled (c*,a) fabric into k equal same-ρ sub-pools and confirm the aggregate blocking rises by ≥3σ (this slice's headline, re-cast as the provisioning PENALTY of fragmentation). The operator lesson sharpens from "small pools block ~90× more at the same utilization" to "capacity is SUBADDITIVE under a blocking-grade constraint — pooling n same-load fabrics needs strictly fewer trunks than sizing them separately, so per-tenant / per-shard capacity carve-outs pay a compounding trunk tax." A seed-pinnable teletraffic mechanism deliberately distinct from this fixed-point three-config blocking-multiplication head, from the M/G/1 Pollaczek-Khinchine wait-variance head P137 (a single-server DELAY object driven by service-time variance), and from the M/M/c Erlang-C delay head (an expected-WAIT queueing object) — this stays a multi-server LOSS system but shifts the object from blocking-at-fixed-size to servers-at-fixed-grade.
