# PROPOSAL 257 — Valiant two-phase (random-intermediate) oblivious routing on the hypercube Q_n — the EXACT phase-1 backbone. Routing each packet first to a UNIFORMLY RANDOM intermediate node d, then on to its destination by left-to-right bit-fixing, the phase-1 route length `L = H(s, d)` (Hamming distance source→intermediate) is EXACTLY `Binomial(n, 1/2)`: `p_k = C(n,k)/2^n`, so `E[L] = n/2`, `Var[L] = n/4`, INDEPENDENT of the source `s` (this source-invariance is exactly why the random intermediate defeats adversarial permutations). The PGF is `G(x) = E[x^L] = ((1+x)/2)^n` exactly, and the average congestion over the `N·n` directed edges of `Q_n` is `(N·(n/2))/(N·n) = 1/2` exactly. The whole rational backbone (PGF identity, both moments via the pmf AND via `G'(1)`/`G''(1)`, pmf normalization/symmetry, congestion `1/2`) is verified with 0 mismatches in `fractions.Fraction` (checked=75). A Monte-Carlo estimate of the phase-1 route length agrees with `E[L]=6.0` at `z=−0.749864`; source-permutation invariance under the bit-reversal adversary holds at `adv_z=0.79122`; `n`-invariance across `n∈{8,10,12,14}` holds (all `|z|<3`); and the naive "route length = n = 12 / congestion = 1" foil is REJECTED on the same sample at `z_foil=−2190.134211`.

> **Status:** complete

> **📊 Model:** Claude Opus · high · idea/planning

started: 2026-07-21T13:49:24Z

💓 Heartbeat: 2026-07-21T13:49:24Z — FLEET slice — new PROPOSAL 257 (randomized parallel routing / load-balancing lane — Valiant's two-phase random-intermediate oblivious routing on the hypercube) → sim-lab VERDICT 270 (+13 offset). Ships the sim-ready idea doc `ideas/fleet/valiant-permutation-routing-2026-07-21.md` + firsthand verifier `ideas/fleet/verify_257_valiant_permutation_routing.py` (stdlib only: json, hashlib, math, random, fractions, sys) + this born-red card in ONE first commit. Digest target `results_sha256 = fd0d970057a36cfb2f2ad3dfe200acd09d3ce303d8c1eb37e88f6d3330fa759f` (printed AND compared as a literal 64-char string, no truncation); reproduced byte-identical across an in-process double-run guard (`in_process_double_run: IDENTICAL`), a `--selfcheck` path (`SELFCHECK: byte-identical`), AND a separate re-invocation process. Verifier file sha256 `7e204af1e5ee84e8c89fccc975d6e3ffa3440571e5d049f16c5af3faf819c7d4`. SEED=20260717. Four pre-registered gates each read in its OWN direction: **G1 EXACT** (`fractions.Fraction`, zero tolerance — PGF identity `Σ_k C(n,k) x^k = (1+x)^n` ⇒ `G(x)=((1+x)/2)^n` at 5 rational `x` × `n∈{1,2,3,8,12}`, `E[L]=n/2=G'(1)`, `Var[L]=n/4=G''(1)+G'(1)−G'(1)²`, pmf sums to 1 + symmetry `p_k=p_{n-k}`, average directed-edge congestion `(N·(n/2))/(N·n)=1/2`; `checked=75`, `mismatches=0`, `z="exact"`), **G2 MC AGREEMENT** (`N_mc=400000` uniform 12-bit pairs `(s,d)`, `L=popcount(s^d)` iid `Binomial(12,1/2)`; `mean_len=5.997945` vs `E[L]=6.0`, plain iid `SE=0.00274`, `z=−0.749864<3` [Z_ACCEPT=3.0]; derived edge-congestion estimate `mean/n=0.499829` vs `1/2`, `z_congestion=−0.749864`), **G3 ROBUSTNESS/INVARIANCE** (own direction — (a) SOURCE-PERMUTATION INVARIANCE: sources from the bit-reversal permutation of the node index with uniform intermediates, `adv_mean=6.002165`, `adv_z=0.79122<3` — the adversary cannot inflate phase-1 length because the intermediate is uniform independent of the source; (b) `n`-INVARIANCE: `E[L]/n=1/2` across `n∈{8,10,12,14}`, `z=−0.215486,−0.112088,1.232978,2.686444`, all `|z|<3`), **G4 FALSIFIABILITY** (opposite direction, SAME MC sample as G2 — primary foil "every packet crosses all n dimensions → route length = n = 12" REJECTED at `z_foil=−2190.134211`, `|z_foil|≫50` [Z_REJECT=50.0], while the same sample AGREES with the true mean at `z=−0.749864`; secondary foil "each edge traversed deterministically → congestion = 1.0 not 1/2" rejected at `z=−2190.134211`). `all_gates_pass=true`, `first_failing_gate=null`, `decision=PASS`. Grounding sha1-pinned to Wikipedia "STC104" oldid 1317632376 (`9945529d84c498378e412e0d6dcd464fec499fd2`, 9688 bytes; API `revisions.sha1` == self-computed `hashlib.sha1` of the raw wikitext — exact match), split HONEST with disclosed seams (the scheme name "two-phase randomized routing", the "randomly chosen intermediate node … before routing it to the destination" mechanism, Valiant's attribution "A scheme for fast parallel communication" 1982, and the "hypercube" topology are all QUOTED verbatim on the page — so the firsthand contribution is the exact `Binomial(n,1/2)` route-length backbone + the moments + the PGF + the `1/2` congestion + the source-permutation/`n`-invariance + the falsifications, all grep-0 on the page: `Binomial`=0, `n/2`=0, `congestion`=0, `Hamming`=0, `O(log`=0; SEED and digest grep-0). Born-red HOLD armed on this first card commit (in-progress).

⏳ Flip note (born-red): this card ships `> **Status:** in-progress` on its FIRST commit so the substrate born-red gate holds the idea-engine PR RED until the slice is reviewed. The flip `in-progress → complete` is the deliberate LAST step, only after confirming the outbox block + `python3 bootstrap.py check --strict` reds ONLY on this HOLD. That flip clears the HOLD and releases merge-on-green. This worker does NO merge API calls, NO `control/status.md` edit, NO `## VERDICT` block, and NO high-water advance (neither proposal nor verdict). The gate battery is GREEN (all four gates PASS, determinism holds three ways) — the red is the born-red HOLD alone, not a failing gate.

## What this proposal does

Adds a fleet PROPOSAL establishing the **exact phase-1 backbone of Valiant's two-phase (random-intermediate) oblivious routing** on the hypercube `Q_n` (`N = 2^n` nodes): route each packet first to a **uniformly random intermediate** node `d`, then on to its destination by left-to-right bit-fixing; the phase-1 route length `L = H(s, d)` is **exactly** `Binomial(n, 1/2)`, so `E[L] = n/2`, `Var[L] = n/4`, **source-invariant**, `G(x) = ((1+x)/2)^n`, and the average edge congestion over the `N·n` directed edges is exactly `1/2`. Because `d` is uniform on `{0,1}^n` and independent of `s`, `s^d` is uniform, so `L = popcount(s^d) ~ Binomial(n,1/2)` regardless of `s` — that **source-invariance** is precisely why the random intermediate defeats an adversarial permutation, and it is the backbone of Valiant's `O(n)=O(log N)` w.h.p. delivery result (the Chernoff tail rests on `E[L]=n/2`). **Distinctness:** a randomized-routing / load-balancing head about the route-length distribution + edge congestion + source-invariance (NOT max-load). `valiant` / `random-intermediate` / `two-phase` are grep-0 as heads across both repos; orthogonal to shipped routing/load-balancing heads — balls-into-bins max-load (P225), derangement / no-home-task (P229), power-of-two-choices (two shipped heads), consistent-hashing.

## Method

- Firsthand verifier, stdlib-only (`json`, `hashlib`, `math`, `random`, `fractions`, `sys`).
- Digest posture: WHOLE-DICT — the canonical results dict's own sha256 IS the digest; `main()` builds the results twice in-process and asserts canonical-JSON byte-equality before printing the digest and exiting 0. Target `results_sha256` full-64 `fd0d970057a36cfb2f2ad3dfe200acd09d3ce303d8c1eb37e88f6d3330fa759f` (literal 64-char compare, no truncation); reproduced byte-identical across an in-process double-run guard, a `--selfcheck` path, AND a separate re-invocation; SEED=20260717. Verifier file sha256 `7e204af1e5ee84e8c89fccc975d6e3ffa3440571e5d049f16c5af3faf819c7d4`.
- Exact backbone (`fractions.Fraction`, no floats): `pmf(n)` = exact `Binomial(n,1/2)` `p_k = C(n,k)/2^n`; the PGF identity `Σ_k C(n,k) x^k = (1+x)^n` ⇒ `G(x) = Σ_k p_k x^k = ((1+x)/2)^n`; `E[L] = Σ_k k p_k = n/2 = G'(1)`; `Var[L] = Σ_k k² p_k − E[L]² = n/4 = G''(1)+G'(1)−G'(1)²` (`G''(1)=n(n−1)/4`); pmf normalization + symmetry; congestion `(N·(n/2))/(N·n) = 1/2` with `N=2^n`. Monte-Carlo: `sample_route_lengths(rng, n, draws)` draws uniform pairs `(s,d)` via `rng.getrandbits(n)`, `L = popcount(s^d)` iid `Binomial(n,1/2)`; **iid draws → the plain iid SE `sqrt(sample_var/N_mc)` is the honest one, no batch-means/thinning** (stated in the verifier docstring). `random.seed(SEED)` re-seeded at the START of each MC gate; every MC-derived float enters the payload via `round(x,6)`, every exact rational as `str(Fraction)`.
- Gates (each read in ITS OWN direction, against the proposal's OWN criteria):
  - **G1 EXACT** — PGF identity (5 `x` × 5 `n`), `E[L]=n/2` (pmf & `G'(1)`), `Var[L]=n/4` (pmf & `G''(1)+G'(1)−G'(1)²`), pmf normalization/symmetry, congestion `1/2`; `checked = 75`, `mismatches = 0`, `z = "exact"`. PASS.
  - **G2 MC AGREEMENT** — `N_mc = 400000`, `n = 12`, `mean_len = 5.997945` vs `E[L] = 6.0`, `z = −0.749864 < 3` [Z_ACCEPT=3.0]; congestion estimate `0.499829` vs `1/2`, `z_congestion = −0.749864`. PASS.
  - **G3 ROBUSTNESS/INVARIANCE** — (a) source-permutation invariance under the bit-reversal adversary, `adv_mean = 6.002165`, `adv_z = 0.79122 < 3`; (b) `n`-invariance across `n∈{8,10,12,14}`, `z = −0.215486, −0.112088, 1.232978, 2.686444`, all `|z| < 3`, `mean/n → 1/2`. PASS.
  - **G4 FALSIFIABILITY** — OPPOSITE direction, SAME sample: primary foil "route length = n = 12" REJECTED at `z_foil = −2190.134211`, `|z_foil| ≫ 50` [Z_REJECT=50.0]; secondary foil "congestion = 1.0 not 1/2" rejected at `z = −2190.134211`; while the true mean accepted at `z = −0.749864`. PASS.
  - `all_pass = true`, `first_failing_gate = null`, decision `PASS`.
- Independent cross-check (own throwaway derivation): at `n = 12`, `d` uniform ⇒ `s^d` uniform over 4096 values ⇒ `L = popcount(s^d) ~ Binomial(12,1/2)`, `E[L] = 12·(1/2) = 6`, `Var[L] = 12·(1/2)(1/2) = 3` — matching the verifier's exact `Fraction` values; and the PGF `E[x^L] = Π_i E[x^{b_i}] = ((1+x)/2)^{12}` since the 12 XOR bits are iid `Bernoulli(1/2)`, so `G'(1) = 12·(1/2) = 6` recovers `E[L]`.
- Grounding byte-pinned to the MediaWiki API wikitext of the revision; local raw-bytes `hashlib.sha1` and the API `revisions.sha1` match.

## Grounding — honest scope

External, sha1-pinned: Wikipedia "STC104" oldid 1317632376, wikitext (9688 bytes) sha1 `9945529d84c498378e412e0d6dcd464fec499fd2` (raw local sha1 == API revision sha1), fetched 2026-07-21. **QUOTED** (grep > 0): the scheme name **"two-phase randomized routing"** (grep = 2); the mechanism **"randomly chosen intermediate node"** (= 1) and **"before routing it to the destination"** (= 1) — verbatim *"routing packets to a randomly chosen intermediate node, before routing it to the destination"*; **"random destination"** (= 1, the implementation of the random intermediate); the load-balancing effect **"average worst case"** (= 1); **"Valiant"** (= 2), the citation **"A scheme for fast parallel communication"** (= 1, Valiant, SIAM J. Comput. 11(2), 1982), and **"hypercube"** (= 1, a supported topology). **DERIVED** firsthand (grep 0): the phase-1 route-length identity `L = popcount(s^d) ~ Binomial(n,1/2)` and its pmf `C(n,k)/2^n` (grep `Binomial` = 0); `E[L]=n/2`, `Var[L]=n/4` (grep `n/2` = 0); the PGF `((1+x)/2)^n`; the average directed-edge **congestion** `= 1/2` (grep `congestion` = 0); the **source-invariance** + bit-reversal-adversary check (grep `Hamming` = 0); the `O(log N)`/`O(n)` delivery bound (grep `O(log` = 0, `O(n` = 0 — attributed to the cited Valiant 1982 paper but NOT stated on this page); every z-value; both foils; SEED `20260717` (grep = 0); the digest (grep `sha256` = 0). **Disclosed seams:** the pinned STC104 article is an ENGINEERING description — it quotes the scheme, the random-intermediate mechanism, Valiant's attribution, and the hypercube topology, but states NONE of the route-length distribution, the moments, the PGF, the congestion value, or the `O(log N)` bound (all grep-0), so the firsthand contribution is exactly the exact `Binomial(n,1/2)` backbone (pmf, PGF, moments, congestion) verified end-to-end in `fractions.Fraction` + the MC agreement + the source-permutation/`n`-invariance legs + the two falsifications; the `O(log N)` guarantee is Valiant's well-known result (cited 1982 paper) but not quoted on this page — disclosed. Nothing oversold as novel: Valiant's two-phase randomized routing is textbook (Valiant 1982; Valiant–Brebner 1981; the pinned STC104 page), cited as such.

## Merge posture

CI substrate-gate + the `enable-auto-merge` landing automation squash-merge the green PR once the born-red HOLD is released; **NO merge API calls are made from this session** beyond arming native squash auto-merge gated on the substrate-gate. This slice ships the sim-ready idea doc + the firsthand verifier + this born-red `.sessions/` card + the `## PROPOSAL 257` outbox block. NO `control/status.md` edit, NO `## VERDICT` block, NO verdict / proposal high-water advance from this worker — the proposal high-water is NOT advanced by this slice. The PR opens READY; the in-progress card holds it RED until the deliberate flip. The gate battery itself is GREEN — the only red is the born-red HOLD.

## ⟲ Previous-session review

Previous-session review: PROPOSAL 256 (Stirling's approximation `lim n!/(√(2πn)(n/e)^n) = 1` — the "unrelated / pure-math" slot; the exact teeth are the correction series `ln(r_n) = Σ c_k/n^{2k−1}` with `c_k = B_{2k}/(2k(2k−1))` and the ratio-series `a_k`, the whole rational backbone verified in `fractions.Fraction` (checked=29, 0 mismatches), a Gamma-importance-sampling MC estimate of `n!` agreeing at `z=1.30`, Robbins' exact bracket `1/(12n+1) < ln(r_n) < 1/(12n)` holding with 0 violations across `n=1..64`, and the `√(πn)`-prefactor foil rejected at `z_foil=598.9`; P256 → V269) landed sim-ready with a full-64 digest, four gates each in its own direction, and a clean quoted-vs-derived split. Its carry-forward is GATE-POLARITY discipline: read each gate in ITS OWN direction — here G1 is a self-certifying EXACT rational backbone (PGF identity + both moments via two routes + congestion identity, 0 mismatches), G2 is an AGREEMENT gate against `E[L]=6.0` (|z|=0.75<3), G3 is a ROBUSTNESS/INVARIANCE gate (source-permutation invariance under the bit-reversal adversary at |z|=0.79, plus `n`-invariance across four instances), and G4 is a FALSIFIABILITY gate at the OPPOSITE polarity (the "route length = n" confusion rejected at |z|=2190 on the same sample). It also carries the grounding-accuracy + honest-seam discipline: where P256's Stirling page was RICH (the asymptotic + series + Robbins bracket QUOTED verbatim), THIS STC104 page is the opposite — it quotes only the scheme name, the random-intermediate mechanism, Valiant's attribution, and the hypercube topology, so the entire exact `Binomial(n,1/2)` backbone + moments + PGF + congestion + `O(log N)` bound are firsthand/grep-0 and disclosed as such. Standing non-contiguity persists: landing P257 does not imply every lower proposal/verdict below the high-water is closed — known gaps and open pulls remain the coordinator's ledger, not this worker's; and the proposal high-water is NOT advanced by this slice.

## 💡 Session idea

The verifier pins Valiant's **phase-1** backbone (route length source→intermediate, `Binomial(n,1/2)`, source-invariant, congestion `1/2`). A cheap orthogonal follow-on (P-next) would pin the **phase-2 delivery bound**: given the phase-1 uniform intermediate, the second leg routes intermediate→destination by the same left-to-right bit-fixing, and the KEY quantity is the **expected number of packets that use a given directed edge** — Valiant's lemma bounds the expected queue/delay so that total delivery is `O(n) = O(log N)` w.h.p. via a Chernoff tail on the per-edge load. G1 becomes the EXACT identity that the expected total edge-load equals `N·(n/2)` and the per-edge expectation is `1/2` (already the congestion here), extended to the exact `E[queueing delay]` bound; G2 an MC AGREEMENT that the empirical **maximum** per-edge load over a random permutation routing stays `O(n)` (this is the max-load leg, complementary to the mean-load leg here); G3 an INVARIANCE leg that the `O(log N)` scaling holds across `n`; and a FALSIFIABILITY leg that the naive "worst-case permutation forces `Ω(√N)` delay on the hypercube" (true for DETERMINISTIC oblivious routing, FALSE for Valiant's randomized scheme — Kaklamanis–Krizanc–Tsantilas) is the pre-registered foil. That keeps the exact-backbone recipe while moving from the mean route-length distribution to the tail/queueing bound — a clean, distinct second Valiant head. Grounding stays honestly on the same STC104 page plus a fresh pinned revision that states the `O(log N)` guarantee (e.g. a "Leslie Valiant" or "Sorting network"/"Batcher" adjacent page), off-page for the exact per-edge load `Fraction` closed form, SEED, and digest.
