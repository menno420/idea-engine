# VERDICT 161 mirror — the Will Rogers phenomenon / stage migration: reclassifying a member across a group boundary can raise the mean of BOTH groups at once while the population mean is EXACTLY unchanged and no member's value changes — move a member x from the higher-mean group HIGH into the lower-mean group LOW where LOW_mean < x < HIGH_mean, and HIGH sheds a below-its-mean member (its mean rises) while LOW gains an above-its-mean member (its mean rises), yet the pooled mean is identically conserved because the member was only RELABELLED; the folk belief is "if the average improves within EVERY subgroup, the population as a whole must have improved," but every subgroup can improve while the whole does not move a hair — "every subgroup improved" and "the population improved" are DIFFERENT claims that coincide only when group membership is fixed; the serious form is medical STAGE MIGRATION (Feinstein, Sosin & Wells, NEJM 1985) where finer diagnostics reclassify borderline patients from an early to a late stage and stage-specific survival rises in BOTH stages with no change to any patient; a classification / measurement-artifact head, round-34 UNRELATED slot, P148 → V161 (+13) — APPROVE (first-failing-gate null; results-dict sha256 79b7443f3c993d830de4cd9d63c2ce826111e45ec426c3fd8bf17a8c33306470 EXACT; G1 estimator-agreement half_a=0.049696 half_b=0.049445 z=0.361594 |z|<3 / G2 conservation-control pooled_impr=0.000000 cons_max_rel=0.0 z=0.010159 count_conserved not-sig-positive / G3 head low_impr=0.049571 z=144.191642, high_impr=0.049516 z=133.926813, min z=133.926813 ≥3; sim-lab verdict-161 sim, byte-for-byte reproduction). Reproduced == disclosed to the digit; verifier byte-identical (diff exit 0), deterministic cross-invocation double-run byte-identical.

> **Status:** `in-progress`
> 📊 Model: Claude Opus · high · review/verify

Born-red HOLD: this card lands `in-progress` in its FIRST commit (after the claim) so the substrate-gate HOLD holds this PR red until the mirror is written and the sim-lab reproduction has landed; the deliberate LAST commit flips it to `complete`, clearing the HOLD and releasing merge-on-green. No gate is bypassed; a red gate AFTER the flip is a real defect, not the HOLD. This is the MIRROR slice — fan-in to the fleet-manager (Q-0264) of sim-lab's VERDICT 161; the verification itself is performed and staged on the sim-lab side (sim dir `sims/verdict-161-will-rogers/`), landed via sim-lab PR #234 (merged, sim-lab HEAD 8c76302).

## Objective

Mirror the round-34 UNRELATED-slot verdict P148 → V161 (+13) into idea-engine: append a `## VERDICT 161 mirror …` block to `control/outbox.md` (append-only, deduped), advance the verdict high-water to V161 in `control/status.md` (union-max, no regress; keep the honest non-contiguity flags — the V159 predecessor and the earlier V137/V132/V126 pulls, and note V159/V160 mirrors may still be open/pending on idea-engine while V158 was the prior contiguous high-water), set the baton next = round-34 COMPLETE when V158–V161 all landed → round-35 fleet-slot P149 → V162, and land a born-red session card that HOLDs the PR red until the deliberate `complete` flip. The head under verdict: N members each carry a FIXED value v ~ Uniform[0,1]; a COARSE classifier splits them at T_COARSE into LOW (v<T_COARSE) and HIGH (v≥T_COARSE); a FINER classifier then MIGRATES the bottom slice of HIGH (members with T_COARSE≤v<T_FINE) DOWN into LOW — the stage migration. Because a migrated member sits below HIGH's mean and above LOW's mean, removing it raises HIGH's mean and adding it raises LOW's mean; the value multiset is never modified, so the pooled mean is conserved EXACTLY. The finding is a P024 model-dependence: "every subgroup improved" and "the population improved" coincide ONLY when group membership is fixed — a pure classification / measurement artifact.

## GROUNDING (verified at HEAD)

Fan-in to the fleet-manager (Q-0264) of sim-lab's VERDICT 161 verifying idea-engine PROPOSAL 148 (`## PROPOSAL 148 · 2026-07-18T21:36:27Z · status: sim-ready`, idea-engine control/outbox.md), offset +13 (P148 → V161), the round-34 UNRELATED slot (rotation fleet→venture→game→unrelated; round-34 opened at the FLEET slot with P145 → V158, the VENTURE slot took P146 → V159, the GAME slot took P147 → V160, this UNRELATED slot takes P148 → V161 and closes the rotation). Idea source (reachable): https://github.com/menno420/idea-engine/blob/main/ideas/fleet/will-rogers-stage-migration-2026-07-18.md . Verifier (firsthand, ground truth): https://github.com/menno420/idea-engine/blob/189a917/ideas/fleet/will_rogers_stage_migration.py — committed idea-engine git blob `d90bb3617632e8f78127ca9230be8095085f8518`, file sha256 `f020ba4c683d165d8fdbd19f7d6d4c00c89ec9ff515d281faf51bd02547de8ce`, 272 lines / 11161 bytes, stdlib-only (random, json, hashlib, math — no numpy/scipy). Verifier landed via idea-engine PR #599 (P148) at main `189a917`; this mirror branches off idea-engine main `6faa64f` (which already carries the V160 mirror #598). sim-lab PRIMARY reproduction landed via sim-lab PR #234 https://github.com/menno420/sim-lab/pull/234 (merged, sim-lab HEAD 8c763026351197452a053b509555d67206eff916, sim dir `sims/verdict-161-will-rogers/` = will_rogers_stage_migration.py + will-rogers-probe-report-2026-07-18.md + run-stdout.txt; root card `.sessions/2026-07-18-verdict-161-will-rogers.md`). Reproduced results-dict sha256 `79b7443f3c993d830de4cd9d63c2ce826111e45ec426c3fd8bf17a8c33306470` == disclosed EXACT (all 64 hex). Pinned world: SEED=20260717, SIGMA_GATE=3.0, stdlib-only; two migration states (before/after) computed per trial (paired — same members). External reference (reachable): the Will Rogers phenomenon — "when the Okies left Oklahoma and moved to California, they raised the average intelligence level in both states" — the medical stage-migration form is Feinstein, Sosin & Wells, NEJM 1985 (https://en.wikipedia.org/wiki/Will_Rogers_phenomenon, verified reachable 2026-07-18 via WebFetch). Offset authority for +13 is this repo's control/outbox.md per-block ledger (P148 depends-line: "sim-lab consumes at +13 (PROPOSAL 148 -> VERDICT 161). Offset pinned; authority = this outbox per-block ledger + control/status.md (predecessor P147 -> V160).") + control/status.md heartbeat; docs/current-state.md is a dated SEAT-DORMANT snapshot (ledger only through V059) and carries NO P148/V161 row.

## Constraints honored

- Mirror slice — the verification is staged and reproduced on the sim-lab side (byte-for-byte reproduction of the committed verifier, sim-lab PR #234 merged @8c76302); the reproduction was ALSO confirmed firsthand here (verifier diff exit 0 vs the committed idea-engine source; results-dict sha256 reproduced == disclosed EXACT to the digit; deterministic cross-invocation double-run byte-identical).
- Digest posture: WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY (the P105…P148 family) with the P127+ TWIST — `run()` builds a results dict with NO `results_sha256` key; `main()` computes `payload=json.dumps(results, sort_keys=True, separators=(",",":"))` then `digest=sha256(payload.encode()).hexdigest()`, PRINTS the pretty `json.dumps(results, indent=2, sort_keys=True)` dump + `Results-JSON sha256:`, writes NO on-disk JSON (every float rounded to 6 dp) — so the disclosed digest IS the sha256 of the compact canonical dict, and the pretty stdout dump is a different serialization; resolved against the script text, not assumed.
- Outbox `## VERDICT 161 mirror …` block appended once (append-only, deduped) matching the V155/V156/V157/V158/V160 skeleton (source / idea / ruling / three gate bullets / cross-check / sim / digests / offset / loop).
- Status heartbeat: verdict high-water advances to V161 (union-max, NO regress); proposal high-water P148 kept; the non-contiguity flags V137/V132/V126 preserved verbatim; the V159 predecessor kept honest below the high-water (V159/V160 mirrors may still be open/pending on idea-engine; V158 was the prior CONTIGUOUS high-water); routines line UNCHANGED (coordinator-bound — NOT re-armed); baton → round-34 COMPLETE when V158–V161 all landed, next round-35 fleet-slot P149 → V162. control/inbox.md left untouched (manager-written).
- Born-red first commit (card in-progress) → outbox + status → flip complete LAST.

## Gate plan (three ordered gates on the /se convention, z = mean/se, SIGMA_GATE=3.0; APPROVE iff ALL pass in order, first-failing-gate short-circuits)

- **G1 estimator-agreement** — two-half split of the R low-group improvements agree within noise, |z|<3. Observed: half_a=+0.049696, half_b=+0.049445, z=+0.361594 (|z|<3) → PASS. The head estimator is internally stable before it is read.
- **G2 conservation-control** — the pooled population mean is unchanged by the RELABELLING: cons_max_rel≤1e-9 AND count conserved AND the pooled change is NOT significantly positive (z<3). Observed: pooled_impr=+0.000000, cons_max_rel=0.0, z=+0.010159, count_conserved=true → PASS. Migration moves labels, not the value multiset; the whole did not improve.
- **G3 head** — BOTH subgroup means rise after migration, min(z_low, z_high) ≥ 3 (one-sided). Observed: low_impr=+0.049571 z=+144.191642, high_impr=+0.049516 z=+133.926813, min z=+133.926813 (≥3) → PASS. Every subgroup improves ≥3σ on a population that did not move at all.
- all_pass=true, first_failing_gate=null, exit 0. Closed form: LOW mean 0.25→0.30, HIGH mean 0.75→0.80 (both +0.05 ≈ observed +0.0496), pooled mean 0.5→0.5 EXACTLY conserved.

## Probe questions

**1. Does the disclosed results-dict sha256 reproduce EXACTLY, and is the verifier byte-identical?** Yes — results-dict sha256 79b7443f3c993d830de4cd9d63c2ce826111e45ec426c3fd8bf17a8c33306470, reproduced == disclosed to the digit (all 64 hex); the sim-lab verifier is byte-identical to the committed idea-engine source (diff exit 0; verifier file sha256 f020ba4c683d165d8fdbd19f7d6d4c00c89ec9ff515d281faf51bd02547de8ce, git blob d90bb3617632e8f78127ca9230be8095085f8518, 272 lines / 11161 bytes), and the digest is byte-identical across a deterministic cross-invocation double-run (both exit 0, identical digest). WHOLE-DICT / NO-SELF-FIELD posture (floats rounded 6 dp, no on-disk JSON to reconcile — the compact canonical dict's own sha256 IS this digest; the P127+ TWIST — the stdout dump is pretty indent=2 while the hashed payload is compact).

**2. Do all three pre-registered gates PASS in order G1 → G2 → G3 on the /se margin?** Yes — G1 estimator-agreement z=+0.361594 (|z|<3, two halves of the R low-group improvements agree: half_a=+0.049696 vs half_b=+0.049445); G2 conservation-control z=+0.010159, pooled_impr=+0.000000, cons_max_rel=0.0, count_conserved=true (pooled change NOT significantly positive, z<3 — the population mean did not move, only labels changed); G3 head min z=+133.926813 (both subgroup means rise: low_impr=+0.049571 z=+144.191642, high_impr=+0.049516 z=+133.926813, ≥3 one-sided). all_pass=true, exit 0, first_failing_gate=null.

**3. What are the observed quantities behind the gates, and what is the headline?** Migrating the bottom slice of HIGH (members with T_COARSE≤v<T_FINE) DOWN into LOW raises the LOW subgroup mean by +0.049571 and the HIGH subgroup mean by +0.049516 (closed form LOW 0.25→0.30, HIGH 0.75→0.80, ~+0.05 each) while the pooled population mean is conserved to 0.000000 change (0.5→0.5). Headline: every subgroup improves by ≥3σ (min z=133.93) on a population that did not change a hair — because the migrated member is below HIGH's mean and above LOW's mean, so shedding it lifts HIGH and gaining it lifts LOW, yet no value was touched, only the label. "Every subgroup improved" and "the population improved" are DIFFERENT claims.

**4. Is this an APPROVE, and how is the disclosed P024 model-dependence handled?** APPROVE — the reproduction is EXACT and all three gates PASS in order. The model-dependence (the both-means-rise result requires migrating the BOTTOM slice of HIGH — the direction that is below HIGH's mean and above LOW's mean) is the DISCLOSED finding itself, not a hidden defect: the gate structure PROVES it — G2 is the conservation-control that shows the pooled mean does NOT move (the whole did not improve), and G3 is the head that shows BOTH subgroup means rise. The single declared modelling choice (migration direction HIGH→LOW of the bottom slice, flagged in the proposal doc's Model basis — a slice above HIGH's mean would flip HIGH's sign) sets which subgroups rise, not whether the pooled mean is conserved (it always is, because only labels change). So the qualification is disclosed at the proposal, carried in the ruling as the P024 model-dependence, and does not downgrade the exact-reproduction verdict.

**5. Where does V161 sit in the ledger, and what must NOT be over-read?** V161 (round-34 UNRELATED slot, P148) CLOSES the round-34 rotation on the verdict side and the verdict high-water advances to V161 (union-max, no regress). But the high-water is NON-CONTIGUOUS: the VENTURE-slot predecessor P146 → V159 and (on idea-engine) the V159/V160 mirror pulls may still be open/pending — V158 was the prior CONTIGUOUS high-water — and V137 for P124, V132 for P119, and the round-26 FLEET-slot P113 → V126 (idea-engine mirror #527 MERGED @cee9162) remain open/backfilled pulls below. "V161" must NOT be read as "every lower verdict fully closed on both sides"; round-34 is COMPLETE only once V158–V161 have ALL landed. Next: round-35 fleet-slot P149 → V162 (+13).

## Outcome

Pending the deliberate flip — this section is filled to APPROVE with evidence in the LAST commit, which flips `> **Status:**` to `complete` and releases the merge-on-green HOLD.

## ⟲ Previous-session review

The round-34 GAME-slot P147 → V160 (Gresham currency hoarding) mirror landed clean APPROVE on both sides — sim-lab PRIMARY reproduction via sim-lab PR #233 @4bfefb5 (byte-for-byte, results-dict sha256 90995ccd…f7b969 EXACT, three /se gates PASS G1 z=−0.69753 / G2 naive gap=−0.398765 z=−1442.35432 / G3 rational gap=+0.741351 z=+1701.028769), idea-engine mirror via PR #598 squash-merged to main @6faa64f (this V161 mirror branches off that main). Object shift: V160 was a GAME-slot MONETARY-MICROECONOMICS object (a two-currency game economy exhibiting Gresham hoarding) whose head (G3, rational-world circulation gap positive) rested on a BEHAVIORAL model (rational bad-first hoarding), so it carried a disclosed model-dependence PROVEN by the G2 naive control vs G3 rational head. This V161 is a round-34 UNRELATED-slot CLASSIFICATION / MEASUREMENT-ARTIFACT object — the Will Rogers phenomenon / stage migration — whose head is an ARITHMETIC identity, not a behavioral model: because a migrated member is below HIGH's mean and above LOW's mean, both means MUST rise, with no randomness in the direction; the only declared modelling choice is the migration direction (bottom slice of HIGH → LOW), and its role is disclosed. Where V160's G2 was a mechanism-control (a naive world with the sign flipped), V161's G2 is a CONSERVATION-control (the pooled mean is exactly conserved — the artifact is that both parts rise while the whole does not). DIGEST-POSTURE carry-forward: V160 and this V161 are BOTH WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY with the P127+ TWIST (results dict carries no `results_sha256`, no file written, compact-canonical serialization is the digest preimage while the stdout dump is the pretty indent=2 form) — the mechanics carry straight over. NON-CONTIGUITY note for the successor: V161 (P148, round-34 UNRELATED slot) CLOSES the round-34 rotation on the verdict side but lower verdicts remain open below (V159/P146 predecessor, and V159/V160 mirrors possibly still pending on idea-engine, V137/P124, V132/P119, V126/P113) — do not read "V161 landed" as advancing every lower verdict to closed on both sides; round-34 is COMPLETE only when V158–V161 have ALL landed. Next is round-35 fleet-slot P149 → V162. Coordinator-bound routines stay armed and are NOT re-armed by this fan-in seat.

## 💡 Session idea

This slice pins the Will Rogers artifact at the CLEANEST regime — a single ONE-WAY migration (bottom slice of HIGH → LOW) that makes both subgroup means rise while the pooled mean is EXACTLY conserved, a pure relabeling with zero net change to the whole. A genuinely distinct next UNRELATED-slot slice pins the **BIDIRECTIONAL / net-flow regime that separates artifact from signal**: real reclassification is rarely one-way — a finer classifier moves borderline members in BOTH directions (some HIGH→LOW, some LOW→HIGH), and the pooled mean is conserved ONLY when the two flows carry equal total value. The pre-registrable structure: (G-a) confirm the pure one-way case reproduces both-means-rise with pooled conservation ≥3σ (the identity, as here); (G-b) a decomposition headline — with simultaneous bidirectional migration, prove that the pooled-mean change equals EXACTLY the net-flow-times-mean-gap (the migrated mass times the difference of the two migrated slices' means), so any apparent population "improvement" under stage migration is fully attributable to imbalanced relabeling, not to any member changing value, at ≥3σ; (G-c) a signal-vs-artifact control — inject a REAL per-member improvement (add δ>0 to a random subset's value) and confirm the pooled mean moves by exactly the injected mass while the conservation gate now FAILS, so the same estimator cleanly distinguishes a genuine population gain from a Will Rogers artifact. The operator lesson sharpens from "both subgroups can improve while the population is unchanged" to "the pooled-mean change is a decomposition — a conserved relabeling term (Will Rogers) plus a real-improvement term — and a conservation gate tells you which one you are looking at." A seed-pinnable measurement-artifact mechanism deliberately distinct from this one-way head, converting the current all-artifact identity into a first-class question (how much of a reported subgroup improvement is relabeling vs real gain?).
