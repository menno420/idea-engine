# Session — single-pass probe: websites fleet-program-pulse-feed

> **Status:** `in-progress`
> **Model/time:** fable-5 · 2026-07-12 (worker slice, dispatched by the coordinator
> under continuous-chaining mode per Q-0265)

## What this session is doing

Single-pass probe (battery v0) of
`ideas/websites/fleet-program-pulse-feed-2026-07-10.md` — the PROPOSAL 002 probe
Q2's unclaimed generalization (fleet-general git-history graph exporter →
committed schema-stamped `pulse.json` per repo + fleet rollup).

Born-red first commit per convention; probe report, state flip, index flip and
card close-out follow on this branch.

Coordinator-imposed deviations, declared up front: this slice opens the PR
**draft** (not READY) and never merges — the coordinator lands it; and this
slice writes **nothing under `control/`** (no claim file, no heartbeat) per its
dispatch boundary. Section-collision risk accepted and noted instead of claimed.
