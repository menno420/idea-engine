# VERDICT 250 — slotted-ALOHA finite-n throughput ceiling: for n agents contending for one shared slotted resource, each transmitting per slot with probability p (a slot succeeds iff EXACTLY one agent transmits), the single-slot success throughput S(n,p)=n·p·(1−p)^(n−1) is maximised exactly at p*=1/n, giving the exact rational ceiling S_max(n)=(1−1/n)^(n−1)=(n−1)^(n−1)/n^(n−1) — 1/2, 4/9, 27/64 at n=2,3,4 — strictly decreasing in n and converging FROM ABOVE to 1/e≈0.367879 (about 36.8%), while the naive "offered load = throughput = 1" is provably wrong — reproduce PROPOSAL 237

> **Status:** `in-progress`

> **📊 Model:** Claude Opus · high · review/verify

started: 2026-07-21T01:15:46Z

💓 Heartbeat: FLEET slice P237 → V250 (+13, lane fleet); reproduction verifier already landed byte-identical in sim-lab via the mirror PR #335 (merge 6a415078) — this slice mirrors the verdict block into control/outbox.md (V250 ruled APPROVE, verdict high-water ADVANCES V249→V250, union-max no regress). Digest target results_sha256 `fb72d76e05c3e41d3f671f7d324a76dde84ea815a6360be1652508db4fc0cb9a` (printed AND literal 64-char string compared, no truncation); reproduced byte-identical across an in-process double-run guard (in_process_double_run_identical: true) AND a separate re-invocation process. SEED=20260717. Four pre-registered gates each read in its own direction. Grounding sha1-pinned to ALOHAnet oldid 1350721913 (`16dda63ad347e0a81ba248ae066b4c3aede1e6b4`), caveat HONEST. VERDICT 250 = APPROVE. Born-red HOLD armed on this first card commit (in-progress); this card flips to `complete` in a later commit to release merge-on-green.

⏳ Flip note (born-red): this card ships `> **Status:** in-progress` on its FIRST commit so the substrate born-red gate holds the idea-engine PR RED until the slice is genuinely done; it flips to `complete` as the deliberate LAST commit — only after the coordinator confirms the outbox block + the status heartbeat and `python3 bootstrap.py check --strict` exits 0. That flip clears the HOLD and releases merge-on-green. NO merge API calls from this session; CI substrate-gate + the landing automation squash-merge the green PR.

## What this verdict does

Reproduces PROPOSAL 237 (P237 → V250, +13 offset, lane FLEET): for a fleet of **n agents** contending for one shared slotted resource, each transmitting per slot with probability **p** (a slot succeeds iff **exactly one** agent transmits), the single-slot success throughput **S(n,p)=n·p·(1−p)^(n−1)** is maximised exactly at **p*=1/n**, giving the exact rational ceiling **S_max(n)=(1−1/n)^(n−1)=(n−1)^(n−1)/n^(n−1)** — 1/2, 4/9, 27/64 at n=2,3,4 — **strictly decreasing** in n and converging **from above** to **1/e≈0.367879** (about 36.8%), while the naive "offered load = throughput = 1" is provably wrong. The verifier `verify_237_slotted_aloha.py` (byte-identical copy already landed in `sims/verdict-250-slotted-aloha-throughput/` via PR #335, merge 6a415078) reproduces `results_sha256`, confirms determinism, and evaluates four pre-registered gates each in its own direction; the probe checks the byte-pinned grounding against the pinned Wikipedia revision.

## Method

- Independent byte-identical re-copy of the verifier (`diff` source↔copy exit 0), stdlib-only (`json`, `hashlib`, `math`, `random`, `fractions.Fraction`).
- Digest posture: WHOLE-DICT — the compact-canonical results dict's own sha256 IS the digest; `main()` builds the results twice in-process and asserts canonical-JSON byte-equality before printing the digest and exiting 0. Target `results_sha256` full-64 `fb72d76e05c3e41d3f671f7d324a76dde84ea815a6360be1652508db4fc0cb9a` (literal 64-char compare, no truncation); reproduced byte-identical across an in-process double-run guard (`in_process_double_run_identical: true`, decision PASS) AND a separate re-invocation process; SEED=20260717.
- Gates (each read in ITS OWN direction, against the proposal's OWN criteria) — see `sims/verdict-250-slotted-aloha-throughput/repro-report.md` for observed values:
  - **G1 EXACT OPTIMALITY** — via `fractions.Fraction`: the closed form S_max(n)=(1−1/n)^(n−1) equals the binomial success probability n·p·(1−p)^(n−1) at p*=1/n exactly (error 0), p*=1/n dominates every competitor on the k/200 rational grid, and the exact ceilings 1/2, 4/9, 27/64 are recovered at n=2,3,4. PASS (firsthand teeth — the optimum sits exactly at p*=1/n).
  - **G2 MC AGREEMENT** — n∈{2,3,5,10,25,100}, 200000 slots each; empirical success rate at p*=1/n agrees with the exact ceiling, max|z|=2.2817, |z|<3 [Z_ACCEPT=3.0]. PASS.
  - **G3 INVARIANCE + STRUCTURE** — relabel-invariance (success count byte-identical under a nontrivial agent permutation) PLUS the exact strict monotone DECREASE of S_max(n) in n AND the strict 1/e lower bound (S_max(n)→1/e from above, S_max(n)>1/e for all finite n). PASS.
  - **G4 FALSIFIABILITY** — on the SAME MC sample the naive rule "throughput = offered load = 1.0" is REJECTED at min|z|=445.44, |z|≥6 [Z_REJECT=6.0], opposite direction to G2. PASS.
  - all_gates_pass=true, first_failing_gate=null.
- Independent cross-check (own throwaway derivation): dS/dp = n·(1−p)^(n−2)·(1−np) = 0 on (0,1) gives the unique interior optimum p*=1/n, whence S_max(n)=(1−1/n)^(n−1); recomputed as exact Fractions n=2→1/2, n=3→4/9, n=4→27/64, each strictly above 1/e and strictly decreasing — the ceiling reproduces non-circularly.
- Grounding byte-pinned to the `action=raw` wikitext of the revision; local raw bytes sha1 and MediaWiki API revision sha1 match.

## Grounding — honest scope

External, sha1-pinned: Wikipedia "ALOHAnet" oldid 1350721913, action=raw wikitext (34678 bytes) sha1 `16dda63ad347e0a81ba248ae066b4c3aede1e6b4` (raw local sha1sum AND MediaWiki API revision sha1 match; rev 2026-04-23T16:07:37Z). On-page (CONFIRMED by grep): the slotted-ALOHA maximum throughput 1/e≈0.368=36.8% at offered load G=1, the channel efficiency "36%", and the continuous law S_slotted=G·e^(−G). HONEST scope: the page carries only the continuous Poisson G·e^(−G) limit — the exact finite-n binomial ceiling (1−1/n)^(n−1)=(n−1)^(n−1)/n^(n−1), the optimal p*=1/n, and the monotone-from-above approach to 1/e are correctly claimed as the doc's OWN firsthand contribution (grep count 0 on the pinned raw wikitext), alongside the four-gate reproduction and the offered-load falsification. The 1/e ceiling is honestly QUOTED, the finite-n refinement honestly DERIVED — clean seam, nothing oversold as novel.

## Merge posture

CI substrate-gate + the `enable-auto-merge` landing automation squash-merge the green PR; **NO merge API calls are made from this session**. The sim-lab verifier + run-stdout.txt + repro-report.md already landed byte-identical via the mirror PR #335 (merge 6a415078) — reproduction-only, no verdict block / no high-water advance, its repro-report.md deferring the canonical ruling to this coordinator-owned slice; left untouched, NO competing sim-lab PR opened. This idea-engine slice ships the outbox verdict block + this `.sessions/` verdict card; the status.md high-water heartbeat is handled in a later isolated commit after review.

## ⟲ Previous-session review

Previous-session review: VERDICT 249 (counting distinct necklaces of n beads in k colours under rotation, the cyclic group C_n orbit count — N_k(n)=(1/n)Σ_{d|n}φ(d)k^(n/d)=130 for (n=6,k=3), with the naive "divide by group order" k^n/n=121.5 provably wrong; P236 → V249) landed APPROVE with a full-64 digest match, four gates each in its own direction, and a clean quoted-vs-derived split (the "Burnside" name honestly flagged as ours, the page crediting Pólya). Its carry-forward is GATE-POLARITY discipline: read each gate in ITS OWN direction — an exact Fraction identity is a self-certifying theorem, a z-test is an AGREEMENT gate when the closed form is the null, and a deliberately-wrong model that must be REJECTED is a FALSIFIABILITY gate at the opposite polarity. V250 leans on the same discipline: G1 is an exact Fraction optimality identity (the firsthand teeth — the peak sits exactly at p*=1/n), G2 is a Monte-Carlo AGREEMENT gate (|z|<3), G3 blends relabel-invariance with the exact monotone-decrease + 1/e lower bound, and G4 is a FALSIFIABILITY gate rejecting the naive offered-load=1 rule at |z|≈445. It also carries the grounding-accuracy discipline: the caveat honestly separates the on-page 1/e ceiling (QUOTED) from the off-page finite-n refinement, p*=1/n, the sim, SEED, gate grids, and digest (DERIVED). Standing non-contiguity persists: landing V250 does not imply every lower verdict below the high-water is closed (V225, V137, V132, V126 remain open pulls).

## 💡 Session idea

The verifier pins the single-slot success ceiling for the SYMMETRIC case (every agent shares one transmit probability p). A cheap orthogonal follow-on (P-next) would pin the **stability / delay** companion rather than the peak throughput: the slotted-ALOHA system with new-arrival + retransmission dynamics has a well-known bistability — the same offered load G maps to two operating points, and the finite-n success curve S(n,p)=n·p·(1−p)^(n−1) is single-peaked, so a naive "increase p to raise throughput" control rule overshoots past p*=1/n into congestion collapse. Gate recipe: G1 an EXACT identity — the two roots of S(n,p)=c for a sub-ceiling target c are exact algebraic conjugates around p*=1/n (Fraction/`sympy`-free closed check on a small n grid); a FALSIFIABILITY leg that the monotone "throughput increases in p" rule is REJECTED for p>1/n (the curve turns over); and a continuity leg that the peak recovers THIS verdict's S_max(n). Grounding: the same "ALOHAnet" page discusses the throughput-vs-offered-load curve and the collapse beyond the peak, so the pin stays honestly on-page for the qualitative bistability and off-page for the exact finite-n two-root instantiation, the sim, SEED, and digest.
