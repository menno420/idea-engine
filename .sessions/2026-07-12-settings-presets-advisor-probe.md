# Session — single-pass probe: settings-presets-and-ai-template-advisor (split-price the two-part capture against rebuild reality)

> **Status:** `complete`
> **Model/time:** fable-5 · 2026-07-12 (worker slice, dispatched by the coordinator)

## What this session did

Single-pass probe (battery v0, panel not escalated — link-index head over an
answered owner posture, reversible park, no security/data/spend blast radius) of
`ideas/superbot/settings-presets-and-ai-template-advisor-2026-07-10.md` (912-byte
link-index, TOP-5 #1). Verify-first reads at superbot-next N=`d3dba9b` reshaped
the head: the dispatch premise (no preset grammar anywhere at superbot-next) is
PARTLY CLOSED — band 1 already minted and fenced the Q-0215/D-0025 settings-preset
grammar on 2026-07-08 (`PresetKind` + compile fences, numeric pickers live,
D-0071 behavior presets applying through one audited write), `PresetKind.TEXT`
has zero consumers (`dm_template` still a bare str), the §2 AI advisor is
owner-gated captured-only (Q-0070, restated verbatim by Q-0215's scope boundary),
and the nav golden's `presets_checked` arm-later leg awaits Q-0232 interface
presets — a DIFFERENT preset sense, not this head's.

Verdict: **park(split-priced)** — built half = §1 grammar+fences at target; open
half = one in-lane TEXT-consumer slice needing no new decisions, plus the §2
advisor behind its owner lift. Nothing sim-shaped, NO outbox entry.

Shipped: probe report v0 appended to the idea file; state forward-only
captured → parked(split-priced — …); section README index badge updated naming
built half AND open half; this card. Checks green pre-push:
`python3 bootstrap.py check --strict` and `python3 scripts/preflight.py`
(all 10 checks). No control/ writes.

**📊 Model:** fable-5 (ORDER 001 standing rule; harness-reported family)

## 💡 Session idea

When a target repo overloads one word across senses (three "preset" kinds at N:
value-seed / operation-bundle / visibility-bundle), a probe should write the
sense-map FIRST and price every dependency edge against the named sense —
otherwise conflation mints phantom blockers both ways (this head neither blocks
nor is blocked by the nav golden's preset leg, which waits on Q-0232's sense,
and the "grammar race" this probe was minted under had already been won four
days before the race was called).

## ⟲ Previous-session review

The previous card (`.sessions/2026-07-12-check-sections-roster-subrows.md`,
hygiene fix: check_sections tolerates roster gen #9 sub-rows) closed with the
`roster_sections` shape-skip guard recipe as its handoff (extend the shape-skip
block in `scripts/check_sections.py` if roster gen #10+ adds a new Lane-cell
shape) — no overlap with this slice beyond the shared preflight it unblocked.
Its card-complete-BEFORE-final-push rule is adopted here: this card flips
complete before the content push, so the gate is green on the head SHA.

## Handoff → next wake

Forwardable watch (Rule-6, body-exists-is-not-body-wired): when the first
`preset_kind=text` spec lands at superbot-next, verify a text-preset picker
actually RENDERS — the widget dispatch handles `numeric_presets` only today.
Also: the nav golden's `presets_checked` leg awaits Q-0232 interface presets,
NOT this head — do not re-link them. Nothing else in flight from this slice.
