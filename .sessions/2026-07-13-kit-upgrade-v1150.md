# Session — kit upgrade v1.10.0 → v1.15.0 (verified dist, enabler carve-outs re-applied)

> **Status:** `in-progress`
> **Model/time:** fable-5 · 2026-07-13T23:00:23Z → (in flight; execution worker
> on branch `claude/kit-upgrade-v1150`, claim landed via fast-lane PR #365 —
> this card is born red as the designed gate hold and flips complete as the
> deliberate last commit)

**📊 Model:** fable-5 · kit upgrade slice (bootstrap.py dist swap v1.10.0 →
v1.15.0 sha-verified against release.json, substrate.config.json automerge
branch_patterns fix adding `claude/*`, regenerated
.github/workflows/auto-merge-enabler.yml with both host customizations
re-applied byte-faithful, .substrate/ state/backup/upgrade-report, kit-planted
docs linked from docs/AGENT_ORIENTATION.md; no control/status.md, no
docs/current-state.md, no control/inbox.md writes)

⚑ Self-initiated under ORDER 009 (owner-live EAP final-night kickoff);
coordinator-dispatched counterpart of sim-lab#115 (sim-lab kit upgrade,
merged); no dedicated inbox ORDER — decide-and-flag.

## Scope

Upgrade the idea-engine substrate kit from v1.10.0 to v1.15.0 (breaking=false
across 1.10.1–1.15.0): (1) pre-upgrade config fix — append `claude/*` to
`substrate.config.json` → `automerge.branch_patterns` (the kit consumes a
populated list verbatim; the `claude/*` fallback only applies when the list is
EMPTY, so without this the regen drops claude/ arming); (2) run
`python3 bootstrap.py.new upgrade` with `release.json` placed beside the dist
so sha256 + version verification RUNS (a silent skip is unacceptable); (3)
post-regen, re-apply the two self-marked HOST CUSTOMIZATIONs to
`.github/workflows/auto-merge-enabler.yml` — the `card` arm-skip step (+ its
`steps.card.outputs.skip == '0'` clause in the arm step's if) and the
`HEAD_REF` env + `--body "Head-ref: $HEAD_REF"` squash-body stamp; (4)
fix-forward any `check --strict` findings (sim-lab#115 precedent: newly
planted docs read as orphaned → link them from `docs/AGENT_ORIENTATION.md`);
(5) card flip as last commit. Does not touch the live claims
(claude-order-008-housekeeping.md, claude-proposal-049-magnet-press-fit-band.md)
or their files.

## 💡 Session idea

(finalized at flip time — placeholder-free by contract; the idea must be
something real observed during this upgrade, recorded in the flip commit)

## ⟲ Previous-session review

(previous-session review finalized at flip time: predecessors are the ORDER
008 housekeeping pass, PR #363, and the sim-lab#115 kit-upgrade session whose
orphaned-docs fix-forward this slice inherits as method)

## Close-out

(written at flip time)
