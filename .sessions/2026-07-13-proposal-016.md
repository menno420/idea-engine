# Session — PROPOSAL 016: encounter coexistence (cooldown-namespace contract)

> **Status:** `in-progress`
> **Model/time:** fable · 2026-07-13T00:34:44Z (Ideas Lab worker slice — draft a NEW
> content-lane idea + PROPOSAL 016 outbox append; card born in-progress as the
> designed gate hold, flips complete in this PR's final commit)

## Scope

Draft a genuinely new, product-facing idea under the standing owner ORDER 003
(`## ORDER 003 · 2026-07-12T21:56:16Z · status: new`, verified present at HEAD
`8d0cd53` — "make sure there are new ideas coming and being tested for each
repo"): the encounter-surface coexistence question, written up as
`ideas/superbot/encounter-coexistence-cooldown-contract-2026-07-13.md` (probed
single-pass this slice, sim-ready) and appended to `control/outbox.md` as
PROPOSAL 016. Steering honored: PROPOSAL 015 was superbot-idle, so this slice
picked a DIFFERENT content lane — the strongest candidate is the coupling debt
both encounter verdicts left standing: sim-lab VERDICT 001 (wild encounters,
cooldown 900 s) and VERDICT 008 (grid encounters, cooldown 600 s) each pinned
solo-tuned defaults for two consumers of ONE shared encounter-resolution engine,
V008's own recommendation records the coupling ("whichever build lands first
fixes it"), and neither verdict ran a joint trace. Premise verified live this
slice: superbot HEAD `0f991a8` carries NO Encounters cog, NO activity-spawn
engine, NO grid-encounter slice (tree scan via blobless clone — the only
encounter code is the pure creature-game catch roll), and superbot-games HEAD
`64b3371` ships `games/shared/encounter/interface.py` ("one resolution core
serves all three Q-0186 triggers") with ZERO cooldown/rate surface — the window
is open and the namespace rule is uncontracted. Dedup swept vs PROPOSALs
001–015, the ideas/ tree, and sim-lab's verdict ledger (nearest neighbors:
PROPOSALs 003/008, each single-surface by their own text; details in the idea's
§ Relations).

**📊 Model:** fable · content + outbox proposal only (idea file, card, index row,
outbox append; no product code, no control/status.md or control/inbox.md writes)

## 💡 Session idea

*(empty — filled at close-out in this PR's final commit)*

## ⟲ Previous-session review

Newest predecessor card (`.sessions/2026-07-12-proposal-015.md`, worker slice
~23:05Z): closed clean — its PR #277 merged at `b5f6791` with the PROPOSAL 015
outbox entry grammar-clean and its born-red card flipped complete in its final
commit as designed. Two things worth carrying: (1) the pipeline consumed it
FAST — sim-lab pulled it as INTAKE 015 at 00:18:41Z (~70 min after the append)
and finalized VERDICT 017 (conditional: committed count-stacking shape rejected
36/36, unlock-only reshape passes at exactly (C2=900, R2=5)) — the
proposal-to-verdict loop is now same-night, which raises the bar on dedup
hygiene: a new proposal must be checked against verdicts that may have landed
HOURS ago (done this slice: V016/V017 read at sim-lab `2e28767` before
drafting). (2) Its 💡 (pre-register T11 post-prestige rebuy pacing with the
mechanic's landing PR) is now PARTLY consumed by VERDICT 017 itself — the
verdict's recommended v1 shape IS run-scoped re-buy after prestige, so the T11
registration ask should ride the lane's economy-v1.md re-registration rather
than a fresh capture; noted here so no one re-mints it as new. No
reconciliation debt found.
