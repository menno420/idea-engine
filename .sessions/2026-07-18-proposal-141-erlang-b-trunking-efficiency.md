# PROPOSAL 141 — Erlang-B trunking efficiency: fragmenting a loss-system fabric into small independent pools at the same offered-load-per-server MULTIPLIES blocking (round-33 FLEET slot OPENER)

> **Status:** complete
> 📊 Model: Claude Opus · high · idea/planning

Born-red HOLD released: this card held the PR red from its first commit until the slice was complete and verified; flipping to `complete` here (the deliberate LAST commit) releases the landing workflow. No gate is bypassed; a red gate AFTER the flip is a real defect, not the HOLD.

## Objective

Draft and land round-33 FLEET-slot PROPOSAL 141 — Erlang-B trunking efficiency. Each pool is an M/M/c/c loss system (Poisson arrivals rate λ, exponential service rate μ, c servers, NO queue, blocked-calls-cleared); offered load a=λ/μ Erlangs; by PASTA the blocking probability is the exact Erlang-B loss formula B(c,a)=(a^c/c!)/Σ_{k=0}^{c}(a^k/k!), computed by the stable recursion B(0,a)=1, B(k,a)=a·B(k−1,a)/(k+a·B(k−1,a)). The counterintuitive headline: at a FIXED offered-load-per-server ρ=a/c, B(c,cρ) FALLS steeply as c grows (trunking efficiency) — same utilization, dramatically less blocking as the pool grows — so fragmenting one pooled 100-server fabric into 50 independent 2-server pools at the same ρ=0.8 lifts the drop rate from ~0.4% to ~33%. Ship a markdown-first card, a committed stdlib verifier (SEED=20260717, three ordered ≥3σ /se gates CFG_SMALL → CFG_MED → CFG_HEAD, whole-dict sha256), a real dry-sim, an outbox block (P141 → V154, +13), and a heartbeat update. VERDICT 154 (P141 → V154, +13) is the next independent slice, NOT written here.

## Constraints honored

- Markdown-first card + committed stdlib verifier (hashlib/json/random only), SEED=20260717, a single `random.Random(SEED)` drawn sequentially in the fixed config order, three ordered gates on the exact Erlang-B loss law computed from the pinned constants.
- Deterministic: byte-identical double run (in-process + cross-invocation), exit 0, disclosed results-dict sha256 (WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY posture — no results_sha256 key, no on-disk JSON, stdout pretty indent=2 dump distinct from the compact hashed preimage; the P136+ fleet-lane family posture).
- Born-red first commit → PR READY at once → flip complete last, after the heartbeat.
- Outbox PROPOSAL 141 block appended once per grammar (+13 offset → V154); did NOT write the verdict.
- Deduped against ideas/ + the full outbox history — distinct from every prior queueing head (service-variance-wait-tax P137 M/G/1 WAIT, checkout-pooling Erlang-C DELAY, variance-blind-provisioning, two-choices-routing-cliff placement); this is a multi-server LOSS system (Erlang-B blocking).

## What happened

Synced to origin/main HEAD c03594b (VERDICT 153 mirror — gambler's ruin edge asymmetry, P140 → V153, landed via PR #584; round-32 COMPLETE on both sides, proposals P137–P140 / verdicts V150–V153). Re-scanned control/claims/ and open PRs for a competing P141 — none (only open pull was #527 VERDICT 126 mirror; the sole 141-token claim is verdict-141-stein-shrinkage, a VERDICT not this proposal). Claimed P141, branched `claude/proposal-141-erlang-b-trunking-efficiency`, opened PR #585 READY on the born-red card. origin/main did NOT move during the slice (re-fetch confirmed still c03594b), so no merge was needed. Committed the pinned stdlib verifier + idea card + outbox P141 block + heartbeat: the real dry run passed all three ordered gates (CFG_SMALL verifier-correctness MC blocking 0.330324 vs exact B(2,1.6)=0.329897 z=+1.194 PASS; CFG_MED verifier-correctness MC blocking 0.122158 vs exact B(10,8)=0.121661 z=+1.377 PASS; CFG_HEAD counterintuitive headline MC blocking 0.329770 vs pooled anchor L=exact B(100,80)=0.003992029 z=+959.483 ≥3 PASS — a 2-server fragment blocks ~83× the pooled-fabric rate at the identical 0.8 offered-load-per-server), byte-identical across an in-process double-run + a cross-invocation A/B (diff exit 0), exit 0, results-dict sha256 a9556fa032f268407b136f9aa0d93bdb936f26bd1eb2d609b019e9d60959173a (WHOLE-DICT / NO-SELF-FIELD / STDOUT-ONLY posture; stdout pretty indent=2, compact canonical hashed preimage). Pinned world: SEED=20260717, μ=1.0, SIGMA_GATE=3.0, offered-load-per-server ρ=0.8 for every config, pooled anchor L=exact B(100,80); CFG_SMALL c=2/a=1.6/R=400/A=8000/W=1500, CFG_MED c=10/a=8.0/R=400/A=8000/W=1500, CFG_HEAD c=2/a=1.6/R=400/A=8000/W=1500 (event-driven M/M/c/c per replication, two exponential draws/arrival in fixed order, blocked-calls-cleared; se = sample-std/√R, the honest independent-replication standard error). Authored the fleet-lane card (dedup draws the line vs checkout-pooling-single-line Erlang-C DELAY/queue, service-variance-wait-tax P137 M/G/1 WAIT inflation from service-time VARIANCE, variance-blind-provisioning-trap, and two-choices-routing-cliff balls-in-bins placement — this is a multi-server LOSS system, the object is the BLOCKING probability B(c,a) and its economy-of-scale in pool size). Appended the outbox PROPOSAL 141 block (+13 → V154, offset cited from the predecessor P140 → V153 depends-ledger row + the status baton), updated the heartbeat (proposal high-water P140 → P141, verdict high-water V153 kept, baton → next: V154 for P141; then round-33 venture-slot P142; routines line unchanged coordinator-bound; inbox untouched), then flipped this card to release the landing workflow. `python3 bootstrap.py check --strict` before the flip failed only on the designed born-red HOLD (the session-card in-progress gate; claims-duplicate / claims-format / owner-action / seat-digest advisories are never exit-affecting; the check_ideas ↔ outbox gate passed — the P141 idea doc, verifier, and outbox block are consistent).

## ⟲ Previous-session review

The round-32 UNRELATED-slot CLOSER P140 → V153 (gambler's ruin edge asymmetry) landed on green — origin/main is HEAD c03594b, and the +13 offset held unbroken (P137→V150, P138→V151, P139→V152, P140→V153 landed on both sides). This slice OPENS round-33 on the proposal side at the FLEET slot (rotation fleet→venture→game→unrelated: P137 opened round-32 at the fleet slot, P140 closed it at the unrelated slot; P141 opens round-33 back at the fleet slot). It inherits the whole-dict / no-self-field / stdout-only digest posture verbatim from the P136+ fleet-lane exemplar family (pretty indent=2 stdout dump, compact canonical hashed preimage — the P127+ TWIST). Continuity carried forward: the verdict high-water is non-contiguous — V137 for P124, V132 for P119, and V126 for P113 (idea-engine mirror #527) remain open pulls below it, so the high-water must not be read as "all lower verdicts closed on both sides".

## 💡 Session idea

Two fleet-lane queueing heads now bracket the "isolate vs pool" decision from opposite failure modes: this P141 Erlang-B trunking efficiency (a LOSS system — M/M/c/c, no queue, the cost of fragmentation is BLOCKING that falls with pool size at fixed utilization) and the prior checkout-pooling-single-line Erlang-C head (a DELAY system — M/M/c queue, the cost of fragmentation is WAIT). Candidate follow-up: a single "pooling economics" playbook that unifies (a) loss-system fragmentation (Erlang-B blocking, economy-of-scale in c at fixed ρ), (b) delay-system fragmentation (Erlang-C wait, the same trunking gradient in the waiting metric), and (c) the loss↔delay boundary itself (when a bounded resource should shed load vs queue it) — so a fleet operator reads one thread from "should this resource block or wait when saturated" to "how large must the shared pool be before isolation's blocking/wait cost is dominated by its blast-radius benefit," pricing per-tenant/per-shard isolation against the trunking law in both regimes.

## GROUNDING

- Verifier (firsthand, ground truth): https://github.com/menno420/idea-engine/blob/main/ideas/fleet/erlang_b_trunking_efficiency.py — results-dict sha256 a9556fa032f268407b136f9aa0d93bdb936f26bd1eb2d609b019e9d60959173a, SEED=20260717, μ=1.0, SIGMA_GATE=3.0, ρ=0.8; CFG_SMALL (c=2,a=1.6,R=400,A=8000,W=1500), CFG_MED (c=10,a=8.0,R=400,A=8000,W=1500), CFG_HEAD (c=2,a=1.6,R=400,A=8000,W=1500), pooled anchor L=exact B(100,80)=0.003992029.
- Idea card: https://github.com/menno420/idea-engine/blob/main/ideas/fleet/erlang-b-trunking-efficiency-2026-07-18.md
- External reference (reachable): Erlang-B loss formula — the exact blocking probability B(c,a)=(a^c/c!)/Σ_{k=0}^{c}(a^k/k!) for an M/M/c/c blocked-calls-cleared loss system; https://en.wikipedia.org/wiki/Erlang_(unit)#Erlang_B_formula — verified reachable 2026-07-18.
- Grounding commit: https://github.com/menno420/idea-engine@c03594b0a4b828c8c241bfad8ef38fc5cc81bd86 · HEAD at start c03594b0a4b828c8c241bfad8ef38fc5cc81bd86 · fetched 2026-07-18T17:09:26Z · origin/main unchanged during the slice (no merge needed). PR: #585.
