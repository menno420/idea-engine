# VERDICT 238 — balls-into-bins expected collision count: throwing m balls independently and uniformly into n bins (hashing m keys into n buckets), the expected number of COLLISIONS counted as unordered PAIRS sharing a bin, X = Σ_bins C(load,2), is exactly E[X] = C(m,2)/n = m(m−1)/(2n) by linearity of expectation over the C(m,2) pairs (each colliding with probability exactly 1/n, no independence needed) — quadratic in m and NOT the naive m²/(2n) shortcut which overstates by exactly m/(2n) — reproduce PROPOSAL 225

> **Status:** `in-progress`

- **📊 Model:** Claude Opus · high · review/verify

started: 2026-07-20T18:52:00Z

💓 Heartbeat: round-54 FLEET slot P225 → V238 (+13, lane fleet); reproduction verifier already landed byte-identical in sim-lab via the P225 verifier-staging PR #319 (MERGED, sim-lab main 91aeac4) — this slice mirrors the verdict block into control/outbox.md (V238 ruled QUALIFIED, verdict high-water ADVANCES V237→V238, union-max no regress) and updates the coordinator heartbeat. Digest target results_sha256 `874a5b611f327149714d07b841bb4285481bbb223086823a5980b5ae6a31a57a` (printed AND literal 64-char string compared, no truncation); stdout sha256 `df886c2d1102774119b971fe56c55edc62c282f6c1dba7ebf512ad95d9e48279`, reproduced byte-identical across an in-process double-run AND a separate re-invocation process. Determinism CONFIRMED (in-process determinism_double_run=True AND separate cross-invocation byte-identical). SEED=20260717. Four pre-registered gates each read in its own direction. Grounding sha1-pinned to Birthday problem oldid 1357361405 (`d876b4f46b64278277ad0cf4b4bdf2ea0f271be1`) — caveat DEFECTIVE (basis for QUALIFIED). VERDICT 238 = QUALIFIED. Born-red HOLD armed on this first card commit (in-progress); this card flips to `complete` in the LAST commit to release merge-on-green.

⏳ Flip note (born-red): this card ships `> **Status:** `in-progress`` on its FIRST commit so the substrate born-red gate holds the idea-engine PR RED until the slice is genuinely done; it flips to `complete` as the deliberate LAST commit — only after the outbox block + status heartbeat are written and `python3 bootstrap.py check --strict` exits 0. That flip clears the HOLD and releases merge-on-green. NO merge API calls from this session; CI substrate-gate + the landing automation squash-merge the green PR.

## What this verdict does

Reproduces PROPOSAL 225 (P225 → V238, +13 offset, lane FLEET, opening the round-54 fleet→venture→game→unrelated cycle): throwing **m** balls independently and uniformly into **n** bins (equivalently hashing m keys into n buckets), the expected number of **collisions** — counted as unordered **pairs** of balls sharing a bin, X = Σ_bins C(load,2) — is exactly **E[X] = C(m,2)/n = m(m−1)/(2n)** by linearity of expectation over the C(m,2) pairs (each colliding with probability exactly **1/n**, no independence needed), quadratic in m and **NOT** the naive **m²/(2n)** shortcut, which overstates it by exactly **m/(2n)**. The verifier `balls-into-bins-collision-count.py` (byte-identical copy already landed in `sims/verdict-238-balls-into-bins-collisions/` via the P225 verifier-staging PR #319, MERGED) reproduces `results_sha256`, confirms determinism, and evaluates four pre-registered gates each in its own direction; the probe checks the byte-pinned grounding against the pinned Wikipedia revision.

## Method

- Independent byte-identical re-copy of the verifier (`diff` source↔copy exit 0, no differences; logic untouched), stdlib-only (`hashlib`, `json`, `math`, `random`, `fractions.Fraction`).
- Digest posture: WHOLE-DICT — the compact-canonical results dict's own sha256 IS the digest. Target `results_sha256` full-64 `874a5b611f327149714d07b841bb4285481bbb223086823a5980b5ae6a31a57a` (literal 64-char compare, no truncation); stdout sha256 `df886c2d1102774119b971fe56c55edc62c282f6c1dba7ebf512ad95d9e48279`; reproduced byte-identical across an in-process double-run AND a separate re-invocation; SEED=20260717.
- Gates (each read in ITS OWN direction, against the proposal's OWN criteria) — see `sims/verdict-238-balls-into-bins-collisions/probe-report.md` for observed values:
  - **G1 EXACT identity** (EQUALITY, `fractions.Fraction`) — three routes (literal Σ `Fraction(1,n)` over all C(m,2) pairs, per-bin linearity n·C(m,2)/n², closed form m(m−1)/(2n)) all equal: (365,23)→253/365, (256,64)→63/8, (1000,100)→99/20; any mismatch FAILs. PASS (teeth real).
  - **G2 MC AGREEMENT** (AGREEMENT, |z|<3) — 200000 trials/config, z = −0.745423 / −0.988399 / −1.509722 all within 3σ. PASS.
  - **G3 ROBUSTNESS** (AGREEMENT + exact algebra) — sweep (365,30)/(128,40)/(500,50)/(64,16) all |z|<3 (max 1.261990) AND exact-scaling `Fraction` identity E(2m)/E(m) n-independent (45/11, 254/63). PASS.
  - **G4 FALSIFIABILITY** (REJECTION) — true model agrees z=−0.576503; naive m²/(2n)=8.0 REJECTED at z=−20.566187 (overstatement exactly m/(2n)=1/8); ordered-pair m(m−1)/n=15.75 REJECTED at z=−1259.926656 — opposite polarity to G2/G3, genuine teeth. PASS.
  - all_pass=true, first_failing_gate=null.
- Grounding byte-pinned to the `action=raw` wikitext of the revision; local raw bytes sha1 matches.

## Grounding — DEFECT (basis for QUALIFIED)

External, sha1-pinned: Wikipedia "Birthday problem" oldid 1357361405, raw-wikitext sha1 `d876b4f46b64278277ad0cf4b4bdf2ea0f271be1` (MATCH). The DEFECT: P225's grounding caveat labels the general closed form E = m(m−1)/(2n) as "derived here, not quoted." This is inaccurate — the pinned revision displays that exact general symbolic form as a block equation (`<math display="block">`, lines 621–626): E[X] = ΣΣ E[X_ij] = C(k,2)·(1/n) = k(k−1)/(2n), with X defined verbatim as "a random variable counting the pairs of individuals with the same birthday" (P225's exact object) and instance 28×27/(2×365) ≈ 1.0356 (line 628), via the identical pair-indicator linearity method. The headline formula is QUOTED; only the numeric MC extensions and the falsification gates are firsthand. Same defect class as V235 (QUALIFIED over the quoted-vs-derived distinction); P225 did not get it right. The computation, digest, determinism, and all four gates are sound and land exactly as claimed — hence QUALIFIED (approve with a grounding-attribution caveat), not a clean APPROVE and not a reject.

## Merge posture

CI `substrate-gate` + the `enable-auto-merge` landing automation squash-merge the green PR; **NO merge API calls are made from this session**. The sim-lab verifier + run-stdout.txt + probe-report.md already landed byte-identical via the P225 verifier-staging PR #319 (MERGED, sim-lab main 91aeac4). The sim-lab verdict mirror (verdict session card + claim) lands via a separate READY sim-lab PR (convention R21). This idea-engine slice ships the outbox verdict block + the coordinator heartbeat + this `.sessions/` verdict card only.

## ⟲ Previous-session review

Previous-session review: VERDICT 237 (Cayley's formula via the Prüfer bijection; P224 → V237) landed APPROVE with a full-64 digest match and six gates PASS via the born-red HOLD choreography, and it explicitly cleared the V235 grounding-attribution discipline (count = quoted, random-tree statistics = derived firsthand — "P224 got it right"). V238 is the counter-case on that same discipline: P225's caveat OVERSTATES novelty by claiming the general m(m−1)/(2n) closed form is derived-not-quoted when the pinned Birthday-problem revision displays that exact general form as a block equation via the identical pair-indicator linearity argument — the same defect class V235 was QUALIFIED over. The carry-forward is GATE-POLARITY plus GROUNDING-ATTRIBUTION discipline: read each gate in ITS OWN direction (G1 exact three-route `fractions.Fraction` identity = self-certifying theorem, G2/G3 Monte-Carlo AGREEMENT gates |z|<3, G4 FALSIFIABILITY gate that REJECTS the naive m²/(2n) shortcut at z≈−20.57 and the ordered-pair m(m−1)/n at z≈−1259.93 in the opposite polarity), and hold the grounding caveat to the pinned bytes — a caveat that oversells firsthand novelty is a real (non-blocking) defect, exactly the QUALIFIED basis. Standing non-contiguity persists: landing V238 does not imply every lower verdict below the high-water is closed (V225, V137, V132, V126 remain open pulls).

## 💡 Session idea

The verifier pins the expected collision COUNT E[X]=m(m−1)/(2n) (a first-moment, pair-linearity result that needs no independence). A cheap orthogonal follow-on (P-next) would pin the SECOND moment / concentration: Var(X) for the same balls-into-bins collision count, which is NOT (E[X])-scaled naively because the C(m,2) pair-indicators are pairwise-dependent (two pairs sharing a ball are positively correlated). Gate: G1 an EXACT identity — an `fractions.Fraction` computation of Var(X) = Σ Cov(1_ij, 1_kl) over pair-of-pairs, decomposed by overlap class (disjoint pairs → covariance 0 under uniformity; pairs sharing one ball → covariance (1/n² − 1/n²)=... computed exactly; the two-shared-ball case is the pair itself), yielding a closed form; plus a FALSIFIABILITY leg that the independence-assumed variance E[X](1−1/n) is REJECTED (the dependence makes the true variance differ), distinguishing the correlated truth from the naive-independent impostor. Grounding stays honestly split: the pair count and linearity mean are on the Birthday-problem page (as this verdict's DEFECT finding establishes), but the exact variance decomposition by pair-overlap class is off-page firsthand.
