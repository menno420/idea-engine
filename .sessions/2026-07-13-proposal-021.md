# Session — PROPOSAL 021: migration renumber treadmill residual (fleet-backlogs rotation, round 2)

> **Status:** `complete`
> **Model/time:** fable · 2026-07-13T02:33:30Z (Ideas Lab worker slice — draft the
> ORDER 004 rule-3 FLEET-BACKLOGS round-2 head from the superbot 237-doc backlog +
> PROPOSAL 021 outbox append; card born in-progress as the designed gate hold,
> flips complete in this PR's final commit)

## Scope

Draft a genuinely new sim-shaped idea under standing owner ORDER 003 (continuous
pipeline) + ORDER 004 rule 3's rotation — round 2 restarts at FLEET BACKLOGS
(round 1: 019 fleet-backlogs / 018 venture / 020 game-mechanics / 017 unrelated),
and this round the harvest weight goes to the deepest backlog: the superbot
section's 237-doc index. The head: the migration-number renumber treadmill — the
captured `migration-number-collision-guard` doc's own open fork ("Option 1 SHIPPED
… Options 2 + 3 remain") turned into a pre-registered endogenous queueing sim: is
the residual treadmill under the shipped re-pick-at-push checker negligible
(APPROVE: retire Options 2/3 quantified), fatal (REJECT: the shared append point
must go — evidence row for the Option-3 assign-at-merge plan), or
PR-class-conditional (NULL: the conditional rule + the git-history probe that
locates superbot's real cell)? Dual-arm (endogenous event model gated within
1.0 pp against the exact exogenous closed forms; assign-at-merge as the built-in
zero-collision control), fully hermetic (the PROPOSAL 017–020 precedent), written
up as `ideas/superbot/migration-renumber-treadmill-residual-2026-07-13.md` (probed
single-pass this slice, sim-ready) and appended to `control/outbox.md` as
PROPOSAL 021. Premise verified live this slice: the canonical doc re-fetched at
harvest pin `fd638e3` AND at superbot live HEAD `4522522` (status block byte-same,
Option-1 checker answers 200 — the fork is real, not archaeology). Dedup swept vs
PROPOSALs 001–020 (outbox re-read at HEAD `da20713`), sim-lab VERDICTs through 021
finalized + 022 in flight for PROPOSAL 020 (local clone @ `a7edcad`, offset map
read — nearest neighbors VERDICT 010 same-repo/different-class, VERDICTs 014/021
same-method/different-domain), and tree-wide greps (renumber / migration-number /
collision / append point / assign-at-merge, bootstrap.py/.substrate excluded) —
every hit unrelated prose or this section's own link-index. Fresh PRNG seeds
20260723–26 picked after sweeping the prior heads' registry 20260713–22 (the
PROPOSAL 020 card's seed-collision warning heeded).

**📊 Model:** fable · content + outbox proposal only (idea file, card, index row,
claim file, outbox append; no control/status.md or control/inbox.md writes; no
checker or script changes)

## 💡 Session idea

A machine-readable rotation ledger for the ORDER 004 rule-3 lane cycle: the
rotation's position currently lives only in prose scattered across idea files and
session cards ("round 1: 019 fleet-backlogs / 018 venture / 020 game-mechanics /
017 unrelated" — each new head re-derives the sequence by re-reading its
predecessors, and one tired session WILL mis-count the next slot or silently
restart round 1). Local fix, zero grammar spend: one `rotation:` line per PROPOSAL
entry is too invasive (the outbox is append-only and the grammar is kit-checked),
so instead a tiny committed table `ideas/README.md § Rotation ledger` — one row
per rotation-slot proposal {round, slot, PROPOSAL n, idea path} — appended in the
same PR as each rotation head, plus an advisory nag in `check_ideas.py` when a new
outbox PROPOSAL's idea file claims a rotation slot (greps "rotation slot" in the
summary) but no ledger row landed with it. Deduped this slice: rg over ideas/ +
.sessions/ + docs/ + scripts/ for rotation-ledger / rotation slot table /
`rotation:` — zero prior capture; the rotation exists only as ORDER 004's prose
and per-card re-derivations (this card included, which is the evidence).

## ⟲ Previous-session review

Newest predecessor card (`.sessions/2026-07-13-proposal-020.md`, worker slice
~02:02Z): closed clean and did two things worth copying. First, it took the
game-mechanics rotation slot EARLY and said why (its consumer window — tonight's
minigame/casino consolidation — closes before a strict rotation reaches it): slot
order bent deliberately and documented beats slot order followed blindly. Second,
when its own PR's gate came back red it did not route around the failure — it
reproduced the red on MAIN itself (PR #285's fast-lane ledger entries had broken
`check_ideas --outbox` block-bounds), fixed the parser the only lawful way given
the append-only outbox, and documented the incident inline; this slice inherited a
green gate because of that. Its 💡 (a seed-collision tripwire for the hermetic-sim
pipeline's growing hand-picked PRNG registry) is directly consumed as PRACTICE
here — this card's seeds 20260723–26 were picked only after sweeping every prior
head's 20260713–22 — but the checker rule itself is still unbuilt and NOT
re-minted as this card's 💡 (checked: rg for seed-collision/seed-registry — only
its own card carries it). No reconciliation debt found.
