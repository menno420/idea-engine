# VERDICT 135 mirror — the NRR composition mirage: Net Revenue Retention (net dollar retention) is DOLLAR-weighted, NRR=Σr_i·m_i/Σr_i = Σw_i·m_i (w_i=r_i/Σr the account's revenue share, m_i its retained-revenue multiple — 0 if churned, the expansion factor if it survived), so it is a revenue-share-weighted mean of the retention multiple. When revenue is concentrated in a few large accounts that EXPAND while a long tail of small accounts CHURNS, the whales' dollars dominate the numerator and the blended NRR reads ABOVE 100% ("negative net churn", best-in-class) even though the MAJORITY OF LOGOS were lost — Simpson's paradox in a weighted mean: the aggregate points the opposite way from the accounts. Two facts the "NRR>100% = we retain customers" instinct misses: (1) NRR and logo (count-weighted) retention DIVERGE — a dollar-NRR of 118.3% coexists with 63.5% logo retention (a third of customers gone); (2) the >100% is the WEIGHTING, not the retention — strip the revenue weights (equal-weight every account, NRR_equal=mean_i m_i) and the identical book reads BELOW 100% (66.3%, contracting per logo): NRR_equal<1<NRR_dollar, the headline manufactured by concentration and sign-flipped the moment every account counts equally. Round-28 VENTURE-slot verdict (SaaS retention / revenue-quality metrics), P122 → V135 (+13) — APPROVE

> **Status:** `in-progress`
> 📊 Model: opus-4.8 · high · review/verify

Born in-progress as this session's first content commit — this born-red HOLD is deliberate and holds the substrate-gate RED until the mirror is fully recorded. The flip to `complete` is the last step, taken once the outbox VERDICT 135 block + heartbeat are written and `python3 bootstrap.py check --strict` is green — it releases the auto-merge-enabler. This is the MIRROR slice — fan-in to the fleet-manager of sim-lab's VERDICT 135; the verification itself was performed and LANDED on the sim-lab side (branch `claude/verdict-135-nrr-composition-mirage`, sim dir `sims/verdict-135-nrr-composition-mirage/`, PR #209 squash-MERGED, sim-lab main HEAD @63bcb2f7e1e534960604a356c82f6a52203c8cb0) after the born-red card flip there.

## Objective
Mirror sim-lab's VERDICT 135 (APPROVE) into idea-engine as the lane→manager fan-in (Q-0264): record the V135 card, append the VERDICT 135 outbox block, and advance the verdict high-water V134 → V135. Verifies idea-engine PROPOSAL 122 (`## PROPOSAL 122 · 2026-07-18T06:53:36Z · status: sim-ready`, idea-engine control/outbox.md), offset +13 (P122 → V135), the round-28 VENTURE-slot verdict (rotation fleet→venture; P121 opened round-28 at the fleet slot, this takes the venture slot).

## GROUNDING
Fan-in to the fleet-manager (Q-0264) of sim-lab's VERDICT 135 verifying idea-engine PROPOSAL 122, offset +13 (P122 → V135). Domain: SaaS retention / revenue-quality metrics — the NRR composition mirage, a venture-domain head deliberately DISTINCT from the prior venture heads P118 (sales-ramp capacity drag), P114 (freemium support-cost inversion), P110 (discount-depth breakeven), P106 (retention-survivorship mirage), P102 (growth cash-trough trap), P098 (referral-bonus value trap) — a WEIGHTED-AGGREGATE / Simpson's-paradox object on a revenue-concentrated account book. Real grounding: sim-lab PR https://github.com/menno420/sim-lab/pull/209 (squash-MERGED, sim-lab main HEAD @63bcb2f7e1e534960604a356c82f6a52203c8cb0) ; sim probe-report (pinned permalink) https://github.com/menno420/sim-lab/blob/63bcb2f7e1e534960604a356c82f6a52203c8cb0/sims/verdict-135-nrr-composition-mirage/probe-report.md ; sim dir https://github.com/menno420/sim-lab/tree/63bcb2f7e1e534960604a356c82f6a52203c8cb0/sims/verdict-135-nrr-composition-mirage ; idea https://github.com/menno420/idea-engine/blob/main/ideas/venture-lab/nrr-composition-mirage-2026-07-18.md ; source verifier https://github.com/menno420/idea-engine/blob/main/ideas/venture-lab/nrr_composition_mirage.py . Verified at idea-engine HEAD `8b5432c` (origin/main). The committed idea-engine verifier `ideas/venture-lab/nrr_composition_mirage.py` (git blob `3285a22e9af9fb7ca46364384398bed54c64750c`, file sha256 `da6c1893b2c4c3255c08142790e74ea1e795f9b6a97400bf70fabfbfb6fe0bf2`, 250 lines / 10447 bytes) was reproduced byte-identically on the sim-lab side under the pinned SEED=20260717, two runs byte-identical, reproducing the results-dict sha256 `de67af4a73bb810deefa5a916bbf76a1bc7a8fa141ae3f26fdcd755eb582d3bb` (all 64 hex) EXACTLY against the proposal's disclosed expected digest. DIGEST POSTURE: WHOLE-DICT / NO-SELF-FIELD / stdout-only (like P105/P109/P110/P114/P118, UNLIKE the P104/P107/P108/…/P120/P121 SELF-FIELD family) — main() computes `canonical = json.dumps(results, sort_keys=True, separators=(",",":"))` then `digest = sha256(canonical)` (L229–230) and prints `canonical` (L231) + `sha256: <digest>` (L232); the results dict NEVER carries a `results_sha256` key and the script writes NO on-disk JSON, so the disclosed digest IS the sha256 of the compact canonical dict (the sim-lab probe's `nrr_composition_mirage_results.json` = that captured canonical, re-hashing to de67af4a…) — resolved against the script text, not assumed. Offset authority for +13 is this repo's control/outbox.md per-block offset ledger + control/status.md heartbeat; docs/current-state.md is a dated SEAT-DORMANT snapshot (ledger only through V059) and carries NO P122/V135 row. Pinned world: SEED=20260717, TRIALS=400, N_ACCOUNTS=20000, F_WHALE=0.10, R_BIG=100.0, R_SMALL=1.0, P_WHALE=0.95, EXP_WHALE=1.30, P_MINNOW=0.60, EXP_MINNOW=1.00, SIGMA_GATE=3.0, GAP_MIN=0.05; whale revenue share W_WHALE=0.917431, EM_WHALE=P_WHALE·EXP_WHALE=1.235, EM_MINNOW=P_MINNOW·EXP_MINNOW=0.60; closed-form anchors NRR_dollar_cf=W_WHALE·EM_WHALE+(1−W_WHALE)·EM_MINNOW=1.182569, NRR_equal_cf=F_WHALE·EM_WHALE+(1−F_WHALE)·EM_MINNOW=0.663500, logo_ret_cf=F_WHALE·P_WHALE+(1−F_WHALE)·P_MINNOW=0.635000.

## Verdict — APPROVE (first-failing-gate None)
Sim-lab reproduced the proposal's OWN committed stdlib verifier (`ideas/venture-lab/nrr_composition_mirage.py` — random, math, json, hashlib; per-trial Monte-Carlo of N_ACCOUNTS=20000 accounts, each a whale w.p. F_WHALE else minnow, surviving Bernoulli(P_seg) with retained multiple EXP_seg-or-0, computing NRR_dollar=Σr·m/Σr, logo_ret=(#survived)/N, NRR_equal=mean(m), over TRIALS=400 replications with z on the estimated mean via se=std/√TRIALS; closed forms from the pinned constants — no numpy/scipy) VERBATIM under SEED=20260717; all three gates PASS in order G1→G2→G3 on the /se margin, reproducing the disclosed dry-sim EXACTLY. The measured NRR_dollar 1.182695 (se 0.000302) reads ABOVE 100% ("negative net churn") while logo retention is only 0.634884 (se 0.000168) — a third of customers gone — and the equal-weighted NRR 0.663366 (se 0.000178) reads BELOW 100%: NRR_equal<1<NRR_dollar, so the headline is manufactured by revenue concentration, not customer retention. all_pass=true, exit 0.
- G1 NRR-vs-logo divergence: PASS — mean(NRR_dollar−logo_ret) **0.547811** (se 0.000307) ≥ GAP_MIN=0.05 by z=**1621.337σ** ≥3σ — a dollar-NRR above 100% substantially overstates the fraction of customers retained.
- G2 concentration control + sign flip: PASS — mean(NRR_dollar−NRR_equal) **0.519328** (se 0.000298) ≥ GAP_MIN=0.05 by z=**1575.1311σ** ≥3σ AND the sign flip holds: **sign_flip=True** (NRR_equal 0.663366 < 1.0 < 1.182695 NRR_dollar) — removing the revenue weighting collapses NRR below 100%, so the >100% is concentration, not retention.
- G3 closed-form anchor MATCH: PASS — the mean simulated NRR_dollar 1.182695 reproduces the revenue-share-weighted closed form 1.182569 by |z|=**0.4169** (<3) AND NRR_equal 0.663366 reproduces the logo-share-weighted closed form 0.663500 by |z|=**0.7515** (<3) — the simulator lands on the analytic weighted means.

The mechanism: NRR=Σw_i·m_i is a revenue-share-weighted mean of the retention multiple, so a few expanding whales (10% of logos owning 91.7% of revenue) push the aggregate above 100% while a churning tail is invisible to the dollars. Design correction (transferable): a revenue-weighted retention metric is a CONCENTRATION statistic, not a customer-retention one — always report NRR ALONGSIDE logo (count-weighted) retention and an equal-weighted NRR, read the gap as revenue concentration (fragility — one whale-departure from collapse), segment retention by revenue band, and drive retention where the LOGOS are; the metric structurally cannot see the bleeding tail. all_pass=true, SEED=20260717.

## Constraints honored
Born-red FIRST content commit (substrate-gate held RED pre-flip by design); mirror appends ONLY the V135 card + outbox block + heartbeat (no re-add of the P122 proposal block, which already lives at origin/main); outbox is append-only (dedupe-checked — no prior VERDICT 135 / V135 block); control/inbox.md untouched (manager's writer); routines line in the heartbeat left UNCHANGED (coordinator-bound); verdict high-water advanced by UNION (no regress — the higher proposal high-water is kept intact); offset authority = the outbox per-block ledger + control/status.md, not docs/current-state.md.

## Outcome
IN-PROGRESS (born-red) — the APPROVE outcome, digest, gates and metrics above are recorded; this section flips to the final APPROVE statement as the deliberate LAST step once the outbox block + heartbeat are landed and `python3 bootstrap.py check --strict` is green.

## ⟲ Previous-session review
Reviewing the immediately prior VERDICT mirror slice — the V133 mirror (idea-engine PR #542 @5c26a04, `.sessions/2026-07-18-verdict-133-german-tank-mirror.md` + its VERDICT 133 outbox block): V133 (the German-tank MVUE, round-27 UNRELATED-slot closer, P120 → V133, +13) landed APPROVE (sim-lab PR #207 @cdb94b5, digest 37cea2bf… EXACT, all three gates PASS on the /se margin), a reference-verifier reproduction whose card flags the DIGEST-POSTURE distinction as the load-bearing carry-forward: classify `main()` before recomputing any digest, because it decides WHICH sha is the disclosed digest. That distinction pays off directly here — V133 was SELF-FIELD (the on-disk pretty json's own sha 145db407… is NOT the digest; the digest is the stdout `Results-JSON sha256:` line / the file's `results_sha256` field), whereas THIS V135 is the OPPOSITE pole, WHOLE-DICT / NO-SELF-FIELD / stdout-only (the results dict never carries a `results_sha256` key and the script writes no json — the compact canonical dict's own sha256 de67af4a… IS the digest), so blindly applying V133's "the on-disk file's own sha is not the digest" rule to V135 would have been wrong: I resolved the posture against V135's script text (L229–232) rather than assuming it. NON-CONTIGUITY carry-forward: V135 advances the verdict high-water V134 → V135, but V132 for P119 and round-26 FLEET-slot P113 → V126 remain open pulls / IN-FLIGHT below it (idea-engine mirrors pending) — do not read "high-water V135" as "V132/V126 fully closed on both sides". The coordinator-bound routines (failsafe trig_01MzoLVHMDuL28UPkKnbmHCm, pacemaker trig_01R26BEgXj5KVZkHA4Ri6AMi) stay armed and are NOT re-armed by a fan-in seat.

## 💡 Session idea
This slice proves the mirage as an existence-and-sign result (dollar-NRR>100% while logo/equal-weighted retention <100%) but never PARAMETERIZES it by how concentrated the book is — the whale share is pinned (F_WHALE=0.10, R_BIG=100.0). A genuinely different NEXT venture slice, deduped against every prior venture head, indexes the mirage by revenue concentration via the exact decomposition the gap already hides: NRR_dollar − NRR_equal = Σ(w_i − 1/N)·m_i = N·Cov(w_i, m_i), the covariance between an account's revenue share and its retention multiple — the divergence is literally the concentration×retention covariance, and its natural scalar is the revenue Herfindahl HHI=Σw_i². Seed-pinnable head: sweep a whale-share / R_BIG grid (HHI from near-uniform to whale-dominated) and pre-register three ≥3σ gates — (G-a) MONOTONE divergence-vs-HHI (NRR_dollar−NRR_equal rises with HHI, so the headline is a dial on concentration), (G-b) DECOMPOSITION-EXACTNESS (the measured gap reproduces N·Cov(w_i,m_i) within 3σ, closing the identity numerically), and (G-c) an HHI* AUTO-FLAG calibration (the concentration threshold at which NRR crosses 100% purely from weighting while logo retention stays fixed <100%, so a book can be scored "mirage-fragile" from its HHI alone). This turns the operator lesson from "report NRR alongside logo retention" into "score the book's HHI and flag the covariance term — a high-HHI book's NRR is a concentration reading, one whale-departure from collapse." A seed-pinnable venture-metrics mechanism distinct from this slice's existence/sign head and from the prior venture closers.
