# Session — PROPOSAL 024: Braess's paradox added-edge frequency (unrelated-domain rotation, round 2)

> **Status:** `in-progress`
> **Model/time:** fable · 2026-07-13T04:12:54Z (Ideas Lab worker slice — draft the
> ORDER 004 rule-3 COMPLETELY-UNRELATED-domain round-2 head: the round-2 successor to
> PROPOSAL 017's social-choice head, rotating into a fresh fleet-external domain —
> transportation / congestion routing games (Wardrop selfish-user equilibrium) — and
> pricing how OFTEN and by HOW MUCH adding a shortcut edge RAISES equilibrium travel
> cost (Braess's paradox); card born in-progress as the designed gate hold, flips
> complete in this PR's final commit once the 💡 slot resolves)

## Scope

Draft a genuinely new sim-shaped idea under standing owner ORDER 003 (continuous
pipeline) + ORDER 004 rule 3's rotation — the round-2 COMPLETELY-UNRELATED-domain
slot. PROPOSAL 017 opened round 1's unrelated-domain lane with social choice
(instant-runoff monotonicity); this head rotates to a DIFFERENT fleet-external
domain — transportation / congestion routing games — so the rotation demonstrably
spans domains rather than re-mining one. The head: a pre-registered dual-arm sim
measuring **Braess's paradox** in small affine-latency networks. The classic Braess
diamond (nodes s,a,b,t; base routes s→a→t and s→b→t; an optional bridge edge a→b
enabling s→a→b→t) is routed by selfish users to the Wardrop equilibrium, which is
UNIQUE for affine non-decreasing latencies → deterministic and exactly computable
with `fractions.Fraction`. That adding the bridge CAN raise everyone's equilibrium
travel time is textbook-settled; what is unmeasured is its FREQUENCY and MAGNITUDE
across a natural constructible family — is the paradox a vanishing curiosity or
common/large enough that a designer adding a shortcut should audit for it first?
This mirrors the accepted PROPOSAL 017 pattern exactly (existence settled;
frequency-in-a-natural-family is the measured unknown; NULL is a live outcome).
Two arms: **Arm A** — an exhaustive hermetic integer census over affine
coefficients (a_e,b_e ∈ {0,1,2} on the four base edges, {0,1} on the bridge; no
PRNG), reporting paradox frequency f_A and the inflation ratio r = cost_with /
cost_without (median and max among paradox fixtures). **Arm S** — a seeded
Monte-Carlo robustness leg with continuous coefficients (a_e,b_e ~ U[0,2], seeds
20260740–43) guarding the integer-grid modeling artifact, gated to |f_S − f_A| ≤
1.0 pp AND the SAME APPROVE/REJECT/NULL call or the verdict is
NULL-by-arm-disagreement (the P017 dual-arm discipline). Bands registered BEFORE any
code: APPROVE (f_A ≥ 0.15 OR median-r ≥ 1.15), REJECT (f_A ≤ 0.03 AND max-r ≤ 1.05),
NULL (the straddle, or arm disagreement). Written up as
`ideas/fleet/braess-paradox-added-edge-2026-07-13.md` (probed single-pass this slice,
sim-ready; homed in `ideas/fleet/` per the `check_sections.py` non-lane carve-out
P017 used — no unrelated-domain roster section exists and inventing one reds the
sections checker, so fleet-external sim-shaped ideas ride `ideas/fleet/`, flagged in
the file) and appended to `control/outbox.md` as PROPOSAL 024. Grounding: ZERO
network reads — both arms are self-contained (Arm A a pure integer enumeration, Arm S
stdlib `random` under the pinned seeds; all Arm-A arithmetic exact via
`fractions.Fraction`). Dedup swept tree-wide (`rg` for
braess/wardrop/congestion/selfish-rout/routing-game/price-of-anarchy, bootstrap.py
and .substrate excluded) — ZERO prior hits; no proposal, idea file, or card touches
routing-game / congestion-equilibrium anywhere. Fresh PRNG seeds 20260740–43 picked
above the P023 registry's high-water 20260733 (the seed-registry hygiene the P020/P021/P022/P023
cards carried, now extended one more slice).

**📊 Model:** fable · content + outbox proposal only (idea file, card, index row,
outbox append; no control/status.md or control/inbox.md writes; no checker or script
changes)

## 💡 Session idea

[[fill: resolve last — a genuine grammar-extension idea distinct from P023's band
registry, P016's couples:, P014's authenticity gate; deduped by grep before filling]]

## ⟲ Previous-session review

Newest predecessor card (`.sessions/2026-07-13-proposal-023.md`, worker slice
~03:48Z): closed clean, and it is the round-2 GAME-MECHANICS head — the direct
successor to VERDICT 022, chaining a same-night parent verdict (casino fairness,
reject: the odds lever left the SAFE envelope empty in all 36 cells) into an
entry-fee/prize-table envelope sweep, inheriting the parent's FUN/SAFE/SINK bands
UNCHANGED so the two envelopes compare cell-for-cell. Two of its habits are consumed
here. First, its **seed-registry hygiene**: P023 drew 20260730–33 after sweeping the
prior heads' 20260713–29 registry, and THIS slice extends the discipline one step
further — 20260740–43, allocated strictly above P023's high-water 20260733, so the
four Arm-S seeds are collision-free by construction and the next head reads one file
to continue the sweep. Second, its **dual-arm + NULL discipline** (exact Arm A vs
seeded Arm S, a 1.0 pp agreement gate, arm-disagreement ruled NULL) is the exact
shape this Braess head reuses — the pattern is now stable enough across P017–023 to
port into a completely unrelated domain without re-derivation. One recurring
observation P023 carried and this slice must not let rot: the **rotation-position
treadmill** — P021's 💡 (a machine-readable rotation ledger) is now, by P023's own
count, three consecutive cards of measured argument and still unbuilt (n=3), because
each card re-derives its rotation position ("round 2: 021 fleet-backlogs → 022
venture → 023 game-mechanics → 024 unrelated-domain") from predecessor prose rather
than reading a ledger. This card makes it n=4: the unrelated-domain slot's position
is again re-derived by hand here. No reconciliation debt found; the treadmill is a
standing 💡, not a blocker for this slice.
