# Session — PROPOSAL 029: lease-renewal claim expiry (fleet-backlogs rotation, round 4)

> **Status:** `in-progress`
> **Model/time:** fable · 2026-07-13T06:48:08Z (Ideas Lab worker slice — draft the
> FLEET-BACKLOGS rotation round-4 head under standing owner ORDER 003: rounds 1–3
> harvested websites (P019 → V021 approve), superbot (P021 → V023 reject), and the
> kit claims doctrine (P025 → V027 reject); round 4 follows round 3's own
> verdict-opened thread — V027 routed the lease-renewal `renewed:` slice and its
> LIMITS line left the mechanism undesigned. Card born in-progress as the designed
> gate hold, flips complete in this PR's final commit once the 💡 slot resolves)

**📊 Model:** fable · content + outbox proposal only (idea file, card, index row,
claim file, outbox append; no control/status.md or control/inbox.md writes; no
checker or script changes)

## Scope

Draft a genuinely new sim-shaped idea under standing owner ORDER 003 (continuous
pipeline — "continue coming up with new ideas, that is your main purpose"; ORDER
004's night window ended at the ~06:00Z morning tally, TALLY 001). The slot does
NOT rotate to a fourth harvest source, deliberately: P025's own committed survey
found the untapped sections sim-dry ("no sim-lab proposal — no simulator question"
on all six substrate-kit heads; "nothing sim-shaped" across superbot-next;
mineverse SECTION COMPLETE; product-forge 4/4 disposed) — the slot's freshest
sim-shaped material is the thread its round-3 verdict opened. The head: VERDICT
027 rejected silence-keyed claim expiry (Feas = ∅ in 6/9 cells) and routed a
lease-renewal mechanism carrying THREE unpinned constants — renewal cadence,
expiry horizon on the renewal stream, and the compliance the mechanism silently
assumes (a lane that works but forgets to stamp reads as dead). This sim prices
all three on V027's inherited bands with worst-case-per-metric contender tempos,
chained anchors (the silence baseline must reproduce V027's committed Feas map
exactly; the C48 contender-side identity restated by citation, never re-run), and
the V027-named zero-tooling git-log probe RUN at drafting on this repo's own
history (11 claim lifetimes, 19 heartbeat re-stamp gaps — multisets quoted into
the fixture; the first fleet datapoint of its kind). Written up as
`ideas/substrate-kit/lease-renewal-claim-expiry-2026-07-13.md` (probed
single-pass this slice, sim-ready) and appended to `control/outbox.md` as
PROPOSAL 029. Seed-registry note: seeds 20260756–59, allocated strictly above the
P027 high-water 20260755 (P028 drew zero, per its own card — this slice reached
back past the seedless entry exactly as that card's 💡 predicted a seeded
successor would have to). Dedup swept tree-wide (`rg -i -g '!bootstrap.py' -g
'!.substrate' 'renew|lease|re-stamp|restamp'` over ideas/ + .sessions/ +
control/ + docs/) — hits are V027's own routing lines quoted in P025's file and
index row, the claims doctrine README prose, and unrelated "renewal" prose; no
proposal, idea file, card, or sim-lab verdict V001–V028 designs or prices a
renewal/lease mechanism. The successor move itself is committed precedent (P023
priced V022's routed lever, P027 priced V025's, P026 priced V024's power half) —
applied to the fleet slot's own parent for the first time.

## 💡 Session idea

*(placeholder — resolves in this PR's final commit)*

## ⟲ Previous-session review

Newest predecessor card (`.sessions/2026-07-13-proposal-028.md`, worker slice
~06:22Z): closed clean at `535c232` (PR #295) — the unrelated-domain round-3 head
(tournament seeding bracket optimality), notable as the rotation lane's first
FULLY EXACT entry: zero RNG/seeds/floats, dual independent algorithms gated on
exact rational equality — a genuinely stronger reproducibility class than the
seeded-MC precedent it inherited. Two of its habits are consumed here: the
explicit seed-registry note (its card stated "draws ZERO seeds … high-water
20260755 left unchanged for the next seeded head" — this slice is that next
seeded head, and the reach-back worked but proved its 💡's point: the high-water
lives only in predecessor prose, and a seedless slice makes the chain one hop
longer and one prose-gap more fragile), and the explicit-alternatives dedup (P028
named its dropped runners-up with reasons; this card names the round-4
alternatives passed on: a fourth harvest source — surveyed sim-dry by P025 hours
ago — and V023's assign-at-merge mechanism sim, a genuine lead deliberately left
for a later slot because V027's routed slice has the nearer build window and the
sharper new axis). One habit is NOT repeated: P028's card re-derived its rotation
slot from predecessor prose and TALLY 001 for the n=8th time (P021's
rotation-ledger 💡, measured and unbuilt) — this card does the same re-derivation
for n=9, and says so instead of pretending the treadmill is new.
